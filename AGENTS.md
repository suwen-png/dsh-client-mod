# dsh-client-mod — 项目级 AI 执行约束

> **本文件是 dsh-client-mod 项目的最高优先级执行规范。任何 AI 操作本项目时，必须先完整阅读本文件，再按其约束执行。**
> **创建日期**: 2026-08-27
> **项目定位**: DeepSeek Harness 原客户端修改工作区 — **总监驾驶舱插件（含设计图工作室）** + 备份-修改-复原 全链路
> **最后更新**: 2026-09-18（第三十七轮：**导图四条需求全部落地 + 抓到并修掉「拖拽只响应横向」的真缺陷** —— 用户原话「按照项目分一个组，不然全堆在一起看看太麻烦了」「按照项目分组，然后再按照分支同一个分支的一个维度分组」「比如根据 1 创建分支 2.3.4，那么 1 应该在中间」「**所有**导图的节点都要允许拖拽」）—— **R3 有设计文档背书**（`docs/10-架构设计/思维导图驱动的多分支对话管理系统-统一方案文档.md` §2.3「后序遍历，叶子分配纵向槽位，**父节点居中于子节点**」）⇒ 是"实现没跟上设计"，**不是新需求**（纪律 79）。**R4 查明拖拽早已实现**（`onPointerDown` 挂在每个节点上），真缺口是「分区背景可能吃指针事件」＋「**闸门从来没守过**」（15 个段里没有拖拽段）⇒ 本轮补齐。🔴 **本轮最贵的一条：真机抓到「拖动只响应横向」** —— `verify-flow` `C8` 读到 `{"l":48,"t":44}` → `{"l":548,"t":44}`（X 动 500 / **Y 动 0**），**症状与真因毫无表面关联**：X 与 Y 走两条完全不同的路径（X 分组不动、Y 被分组吸收）。真因 = `mindmap-group.js` 的 `shift = cursor - minY` —— 把用户位置**先并进 rows 再分区**时，分区内只有一行（或用户拖的正是 `minY` 那行）⇒ `ny = cursor` ⇒ **拖到哪都被拉回槽位**；多成员分区还会**拖一个动一群**（拖一行改变 `minY` ⇒ 整组平移）。⇒ 定下**顺序铁律**：`buildGroups(baseRows)` → `applyUserPos(gres.rows, posMap)`（**先按自动布局分区、再叠加用户位置**），把机理写进新导出 `applyUserPos()` 的头注，并用离线 **C 组 MM-G23~G30** 常驻守（含拿**旧顺序的真实复刻**当坏样本做植入缺陷校准，纪律 ⑥/⑫）。**修复后取证**：`C8` 画布位移 **500 / 320**（`{"l":48,"t":44}` → `{"l":548,"t":364}`）✅。⚠️ 另修两条**会过期/会骗人**的判据：`C8` 由屏幕位移改为**画布坐标**（分区让画布显著变高 ⇒ fit 后 zoom 变小 ⇒ 屏幕位移变小 ⇒ **产品正确却判红**，纪律 14/102）；C 组判据提成纯函数（`dragKeptOk`／`dragIsolatedOk`）才能被坏样本校准。**结果**：lint 七把全绿 · `test-generators` **4/4** · 离线批 **OK=29 / 非 0=1**（`verify-index` 漂移 ⇒ `refresh-index --safe` 后**三份一致**）· 构建 **2,046,149 B / 89 模块 / 268 import · 声明 756 = 绑定 756 / 指纹 `17920f730bb815b1`**、**连跑两次 md5 一致** · `verify-bundle` **101/101** · `check-stale-build` TRUE · 装机**逐字节一致** · 真机 `verify-mindmap` **113/113 · 零跳过** · `verify-flow` **80/0/0** · 新增 **20 号文**（导图领域首份规格）· 🔴 **1:1 截图取证后又补两处**：① **维度分区标题显示内部英文 key**（截图实测 `chars · 1` / `plot · 1`）⇒ 改由 `MindMap.js` 从 `SPLIT_DIMENSIONS ∪ GENERIC_DIMENSIONS` **注入**中文名（为什么不直接在 `grouping.js` 里 import：`split-dimensions.js` 与 `attribution.js` **互为循环**，且该模块签了「零 import」契约）⇒ 实测 `A4 人物 · 1`/`A3 剧情 · 1`/`A2 力量体系 · 1`/`A5 正文 · 1`/`A1 世界观 · 1`/`未分流 · 10`，项目 `墟海 15 + 灵能修仙 8 + 未归类 5 = 28` = 节点数（守恒真机实证）· ② 🔴 **`src/logic/grouping.js` 含真 NUL 字节 ⇒ `grep`/`rg` 报 "Binary file matches" 静默跳过整个文件**（成因：写入时把 `\u0000` **转义序列**落成了**真字节**；后果是所有基于 rg 的扫描**静默漏掉该文件**，与纪律 106 同族）⇒ 改回**字面转义**（**语义等价、零行为变化**，前缀仍是 NUL 故兜底桶仍不可能与真实标题撞车），全仓含 NUL 文件 **0**；新增离线 D 组 `MM-G31`~`MM-G34`（注入生效 / 不注入不崩 / 代码式维度不受影响 / 兜底桶不被覆盖）· 取证截图 `logs/audit-r37/3-mindmap-100.png`（1:1 细节）+ `shot-all` 四界面 + `style-report.json` · 构建复跑 **2,049,908 B / 89 模块 / 指纹 `c3add7304bf92e27`**（两次 md5 一致）· 真机三套 **`verify-flow` 80/0/0 · `verify-mindmap` 113/113 零跳过 · `verify-director-logic` TRUE**）
> **历史**: 2026-09-17（第三十六轮：**会话重复新建根治** —— 项目级复用兜底 + 真机端口单一真相源）
> **历史**: 2026-09-17（第三十五轮：**删除总监页「横向延迟控件」** —— 用户原话「对话 tap 的总监页面下面有一个延迟 5 秒的 一个横向的控制延迟得元素 去掉」）—— 🔍 **真身定位**：`src/bridge/host-director-column.js` 的 `syncMemoryBar()`（81 行）+ `MEM_BAR_ID = "dsh-mem-bar"`，DOM 形态 `[延迟][秒数输入 500][s][锁定]`，**横向**贴在**记忆表头正上方**。⚠️ 用户说的"延迟 5 秒"是对现象的感知（历史上 `RAIL_HOVER_MS` 曾是 5000），该控件**实际默认 500ms**（`memoryHoverDelayMs: 500`）—— 要删的是**这个 UI 控件**，不是把延迟改成 5 秒，也不是删掉延迟能力。**改动**：整函数 + 常量 + 调用点 + `memoryZoneContains()` 并集 + `removeChrome()` 清单 + `hostColumnState.memBar` 字段 + `api()` 导出 + `client-entry.js` 的 import/导出 **全删**（1190 → 1109 行 / 57890 → 54930 B）。🔴 **关键判据 = 只删工具条、能力全保留**（纪律 54「静默砍功能」的**双向**形态 —— 否则"删干净了"与"顺手把能力也砍了"**长得一模一样**）：**负向** `dp-mem-bar` / `dp-mem-delay` / `dp-mem-lock` 三者都不在 DOM（`E1`/`E1b`）· **正向** `memoryHoverDelayMs` 仍是 store 上可读的唯一真相源（`E2` 实测 `{"delay":500,"h":160,"locked":false}`）+ 锁定仍走 `toggleMemoryLock()`（`E2b`）+ `E3` 直写 store 延迟**立即生效 900ms**。闸门同步 **4 文件**（`verify-v22` 的 `A3` 3/3→**2/2** + 新增 **`A3b`** 负断言 + **E1/E1b/E2/E2b/E3** 重写 + **E6/E7 改走同一真相源** · `_probe-v22-gaps` · `lint-dom-style-units`）。🔴 **同轮第二件：`lint-dom-style-units` 的 L4a「改前就一直红」** —— 阈值 `>= 8` 是**照着手工计数钉的**，实测恰 **7** ⇒ **无余量 + 会过期**（纪律 14/103）⇒ **就地更正**为**与规模无关的存在性判据**（`wrapped+direct > 0` / `wrapped >= 1 && files.length >= 10`）⇒ **删代码不再假红，而"检测器坏了"仍必红**；**先证前提用 `git HEAD` 原版复算**（纪律 23，不去就地复刻正则）+ **植入缺陷负向校准**（`L2a` 精确命中 `host-composer-slot.js:129`，还原后**逐字节一致** 15417 B）。🔴 **同轮第三件（新增 §八 纪律 126）**：**同一语义两个标识符 = 隐式断链** —— `CDP_PORT` **47 处**（**启动器真正用的**）vs `DSH_CDP_PORT` **26 处**；`verify-v21`/`verify-v22` 原先只认后者 ⇒ 带 `CDP_PORT=9333` 跑时启动器在 9333、套件却连 9222 ⇒ 报「连不上 CDP」，**看起来像环境坏了**。⇒ 两套件均改 **`CDP_PORT` 优先 + 别名归一**。⚠️ 本轮另有一条**假红**已登记（纪律 119 形态）：`run-live.mjs` **硬编码 9222 且绕过 `_harness.mjs` 的端口顺移** ⇒ 9222 被幽灵 pid 占用时报 `INVALID（90×2s 预算用尽）`；本轮用 `CDP_PORT=9333` 绕开，**未改它**。🧹 删两个已用尽的一次性探针（`_tmp-v22full.mjs` / `_tmp-move-guard.mjs`，均 git 未跟踪 + 零命令引用，**先证前移已落地** `START@8792 < INS@11124`）⇒ `refresh-index --safe` 三份索引复归一致。**结果**：lint 四把全绿（`cdp-templates` **TRUE/284** · `undefined-symbols` **阻塞 0** · `platform-stub` **9/9** · `dom-style-units` **17/17**）· `test-generators` **4/4** · 构建 **2,005,289 B / 87 模块 / 740 = 740（265 import）/ 指纹 `0b9373e030dcaa04`**、**连跑两次逐字节一致** · `verify-bundle` **101/101** · `check-stale-build` **TRUE** · 离线全量 **PASS=59 / FAIL·ERR 0** · 装机 `[4]` **逐字节一致** · **真机 `verify-v22` 62/62 · 零跳过 · 连跑两次（13s / 12s）** · 基线**第十三锁 93 文件 / 3,904,037 B**、复验 **漂移=0**）
> **历史**: 2026-09-17（第三十三轮：**「闪退」真因定为第三方插件「参与宿主启停」** —— 用户原话「**控制软件打开和关闭的东西全删掉**」）—— 🔴 **定位手段 = 插件启停二分**（把"是哪个插件"从猜测变成读数）：全开 ⇒ **🔴 全灭 @11.3 s** · 全关 `[]` ⇒ ✅ 存活 · 只留 `dsh-director-plugin` ⇒ ✅ 存活 · **只留 `hermes-command-center` ⇒ 🔴 全灭 @10.2 s** ⇒ **凶手 = hcc（源码在 `D:\hermes-data\hermes-command-center\harness-plugin\`，非本项目），director 无罪**。**真因代码**：`lib/index.js` 的 `apply()` 注册 `setTimeout(…, 3000)`，回调里 `checkHCCServer()` / `startHCCServer()`（`spawn('node',[server.mjs],{detached:true})`）⇒ **回调抛错 = 宿主 `uncaughtException` ⇒ 宿主 CLI `installFailLoud` ⇒ `process.exit(1)` ⇒ 桌面壳 `onUnexpectedExit` ⇒ `requestAppQuit()` ⇒ 整壳退出**。**时刻指纹** `died ≈ appeared + 5 s` + `outBytes=188`（**无** `Checking for update`）严格配对。**修复四条**：删 `child_process` 全部用法 · 删**所有自主定时器** · 删 `process.exit`/`app.quit`/`.kill` · `ctx.provide('hcc',{…})` **原子化** + `log` 进 try/catch（**日志永不致命**）。**装机回读** `lib/index.js` / `lib/client.js` **源↔装逐字节一致**（`3521d975…` / `88e4ebbf…`）。**真机复验**：`stop-harness.mjs` 验证式停机 + **连跑 10 轮 → OK=10 / DIED=0**，`appeared` 3.4–8.3 s **分散**、`outBytes` 255–256、`peak=5` —— **上一轮那个"恒 `appeared+5s`"的时刻指纹已消失**。🧹 **清理**：本轮探针 **82 → 64**（保留的 64 个**全部**被 `docs/` 点名 ⇒ 纪律 114「文档引用即保留」）；删前**归档** `logs/_trash-r33-flash-crash-20260917/`（**29 件**）；删桌面调试副本 `_r33-A.cmd`；**红线闸门已按用户意见撤除**（结论已落地，不再常驻）。**新增 §八 纪律 124**（插件不得参与宿主启停）
> **历史**: 2026-09-17（第三十二轮：**「闪退」第二、三层真因：启动器一 `exit` 应用就被回收 + 先证「谁在杀」而非「谁被杀」** —— 🔴 **决定性对照**（同一脚本、同一条 exe，只换"谁在跑它"）：`node start-harness.mjs --hold N` 直跑 ⇒ **`OK`**、输出 255–256 B、**无**致命行；`explorer.exe` 真实双击 `Start Harness.cmd`（**用户路径**）⇒ **`DIED`**、输出 187–188 B、**有** `Host exited unexpectedly (code 1)`。**逐条排除**：`chcp 65001` · 批处理 `set` 变量 · 读端被关（EPIPE） · 宿主单跑任意输出通道（`keep`/`drop`/`ignore` 全不退）。⇒ 真因 = 宿主处在启动器**子进程链**上，**启动器一 `process.exit` 整链被回收** ⇒ 壳打 `Host exited unexpectedly` ⇒ `requestAppQuit()`。修：`start-harness.mjs` 成功后**默认 keepalive 守着不退**（`--no-keepalive` 给自动化）⇒ 实测 `held 26.2 s 未退出`；⚠️ **结论与退出码必须在"守"之前定好**（不许因"守着"把红说成绿）。🔴 **另修一条假红**：本会话会回收**任何**由它拉起的 GUI（无关第三方 `notepad.exe` 同命）⇒ 「**启动成功了吗**」与「**此刻进程还在吗**」是**两个问题**；新增 verdict `STARTED_THEN_REAPED` + **两条反例校准**（`HT-47b` 有致命行仍判 `DIED` / `HT-47c` 窗口从未出现仍判 `DIED`）+ **`!j.appeared` 守卫**（起过窗口的不重试）。新增 §八 纪律 **120–123**）
> **历史**: 2026-09-17（第二十九轮：**清空会话 + 清截图/历史日志 + 把「打开闪退」查到底** —— `src/**` 与产物**零改动**，**无需重建/重装/重封**） —— 🔴 **「打开就闪退」实测两条真因都在**启动上下文**、产品无病**（§八 纪律 **112**）：**(a) 环境变量** `ELECTRON_RUN_AS_NODE=1`（agent 的终端里就有）⇒ 那个 exe 退化成**纯 Node**：无参数 **216 ms 静默 exit 0**（无窗口）、带 `--remote-debugging-port` ⇒ `bad option` **exit 9**；**(b) 单实例接管** 已有实例在跑（含**托盘里看不见的**）⇒ 第二次启动 **1211 ms 静默 exit 0、输出全空**、窗口永不出现。对照：干净环境 + 无实例 ⇒ 窗口 **2.6–4.8 s** 出现 · 5 个进程 · 稳定 60 s。 ⇒ **交付一键打开** `scripts/start-harness.mjs` + `start-harness.cmd`（桌面同款）：清环境变量 → 验证式停机 → 启动 → **等窗口真出现** → 起后又死则**自动去参数重试** → 每次追加 `logs/harness-launch.log`（**连跑 4 次全 ✅**） / 🔴 **一次假红**：从**工具调用**里拉起的 GUI 会随该次调用**整棵回收**（同命令内 60 s 稳定；命令一返回进程表归零）⇒ 「调用返回后进程没了」**不是**产品崩溃（§八 纪律 **113**） / 🧹 **清理**：`clear-sessions.mjs` ⇒ `~/.dsh/sessions` **488 文件 / 49.99 MB → 0**、`.trash-*` **7 → 0**、`archivedSessionIds` **134 → 0**（⚠️ **归档失败 ⇒ 会话不可恢复**，已告知）；`clean-logs.mjs`（判据 = 文档是否引用）⇒ `logs/` **233 个 / 1.87 MB → 12 个 / 0.09 MB**、全仓截图 **11 → 0**（§八 纪律 **114**））
> **历史**: 2026-09-17（第二十六轮：**19 号文 P4–P10 连续执行到底 + 两个真因修复** —— 🔴 **U10 真正修好**：冷启动首次派发为何必然重建 ⇒ 真因是**判据与真值不同源**（`branchTitle()` 角括号 vs `briefOf` head 方头括号）⇒ 抽 `briefTitlePrefix()` 作**唯一真相源** + 复用侧改**前缀**匹配（自带边界，封死 `A1`↔`A10`）⇒ 真机实证**冷启动 `复用 8 · 新建 0`**、宿主会话 **170 → 170（净增 0）** / 🔴 **`NS-11h` 牵出真产品缺陷**：常驻读数 `data-alive` 接到**层级树**（`loadTree()` 无 `rows`）⇒ **恒空**、文案恒「分支树未建立」，而快照有 **170** 行 ⇒ 改取**分支树**三态 + 离线接线自证 `RU-T15`（§八 纪律 106）/ 🔴 **最贵的一次弯路**：同一份 src 两次 `build` 指纹不同（`2085f97c` vs `cfd2c7df`）而 `check-stale-build` 两次都报 TRUE ⇒ 真因是**两个生成器互相覆盖** `src/logic/key-files.js` 的 `@map` 头 + `--check` **无条件报绿**（纪律 31）⇒ 单一写者 + `--check` 改判「是否需要写盘」+ 新增常驻闸门 `test-generators.mjs` **4/4**（§八 纪律 107）/ `U8 兜底诊断通道 · U9 窗口遮挡` 维持上轮结论 / 新增 §八 纪律 **106–107**）
> **历史**: 2026-09-17（第二十五轮：**落实 §八 六条建议 + 项目收尾** —— U8 兜底降级为**诊断通道**（`verify-novel-split` 收尾「真实鼠标 0 ｜ 诊断通道 2」）/ U9 **分离**：驱动者是**窗口遮挡**（撤销两项后仍 `visible`）/ O2 扫描完成并**降级为加固项**（前提成立时派发 **58–76 ms**，纪律 91 数值**就地更正**）/ O3 语料 **8→13 条** + 负对照 8 条（`test-director-corpus` **51/51**）/ O4 **跨重启连跑打通**：**6/6 轮 29/29**（含 **2 次冷启动**，端口 9222→9223）/ 🔴 修掉 **15 轮 INVALID 的真因**（`taskkill` 报成功但端口被**幽灵**占 ⇒ 新实例 `bind()` 失败）⇒ `_harness.mjs` 重写（验证式停机 + 端口顺移 + **应用输出留痕**）/ 🔴 **冷启动起点根治**：`_cdp-startup.mjs` 统一自举 ⇒ `verify-director-logic` 冷启动 **8/29 → 29/29** / 新增离线契约 `test-harness-ctrl` **36 条** / 🔴 **新发现 U10**（冷启动后复用索引随 origin 丢失 ⇒ 首次派发必然重建 N 条；`indexN:0 / hostSessions:74`）与 **U11**（`DL-5a/5b/7a/7b` 间歇同红，已补 `UI_STATE` 诊断）/ 新增 §八 纪律 **97–100**）
> **历史**: 2026-09-12（V16 落死：主管道由「改宿主编译产物」切换为「官方 client 插件」）

---

## 〇、当前基线（2026-09-12 落死 · 先读这条）

> **项目现状的唯一权威描述在**：`docs/00-统筹入口/10-当前基线-落死锚点-V16.md`
>
> ### 三件必须知道的事
>
> 1. **主管道已切换**：现在的交付物是**独立插件包 `dsh-director-plugin/`**（走 Harness 官方 client 插件通道），
>    **不是**改宿主编译产物。§二 里描述的 `workspace/@deepseek-ai/... + apply.ps1` 流程属**历史通道**，
>    仅在排查旧问题时参考，**不要再据此施工**。
> 2. **动手前先跑基线校验**：`cd dsh-director-plugin && node scripts/baseline-check.mjs`
>    输出 `IS_PASS: TRUE（漂移=0）` 才能引用基线文档当"现状"；`FALSE` 说明代码已漂移，先对齐。
> 3. **不要新开版本号**。用户明确要求「把这一版落死，我不想再开一版本了」——
>    基线演进 = **就地更新** `10-当前基线-落死锚点-V16.md` + `node scripts/baseline-check.mjs --write` 重新封存，
>    **禁止**新建 V17/V18 之类的新文档。
> 4. 🔴 **测试范围 = 「受影响面 ∪ 新增/修改面」，不做无条件全量**（2026-09-16 第十六轮 · 用户原话修正）。
>    原话：「**我的描述也有问题, 不需要测试所有的交互, 是每次修改值测试能影响到的部分的交互包含新加新修改部分的交互**」。
>    ⇒ 锚点 §六 的表降级为「**封存 / 交付前全量体检**」；每轮迭代按 §八 纪律 52 取集：
>    ① `grep -rn "<被改符号/新 data-testid>" scripts/*.mjs` **命中者**必跑；② 新增面必须有**新套件或新断言**覆盖；
>    ③ 未命中的套件列入"不跑"并写明**可复现的理由**（禁止"应该没问题"这类不可证伪的收尾）。
>    **例外（仍走全量）**：删目录 / 改冻结契约 / 动宿主状态 / 曾假红假绿过的套件。
>    ⚠️ 取集时**必须读闸门的代码，不能只 grep 符号名** —— 本轮新增的 `dp-act-split` 会被 `verify-walk.mjs`
>    的"逐个点击所有按钮"点到并**真建 8 个会话**（闸门成为产品的破坏者），而符号 grep 完全不提示这一点。

| 一句话 | 内容 |
|:-------|:-----|
| 交付物 | `dsh-director-plugin/`（产物 `lib/client.js` **2,008,557 B / 87 模块** · 绑定对账 **741 = 741 / 265 条 import** · `inject = ["slots","sessions"]` · **内容指纹 `2085f97cd1b8003c`**（真机读 `window.__dshBuildStamp`；装机前由 `check-stale-build` 拦陈旧构建）· **连跑两次 build 逐字节一致**（md5 `0c5bd9ac54683825e2ecf7ab0ff360bc`）） |
| 界面 | 宿主三页签【总监】【对话】【轨迹】+ 三浮层（设计图工作室 / 思维导图 / 总监弹窗）；四处右上角共用同一个**个性化设定**面板（总监页 / 总监弹窗 / 分支导图 / 设计图工作室） |
| 设计稿 | `docs/50-信息中心/V16-设计图·需求图·交互逻辑.html`（A 高保真 / B 需求图 / C 交互逻辑 / **D 设计图工作室** / **E 保存·版本·窗口安全区** / **F 思维导图元素库 18 条总账** / **G 单框控件·拖动·右侧对话·四维流转·质感与个性化**） |
| 基线闸门 | 静态闸门 `TRUE`（阻塞 0 / 提示 17） · CDP 模板 `TRUE`（208 文件） · DOM 样式单位 17/17 · **平台桩唯一性 `lint-platform-stub` 9/9** · bundle 桩执行 **97/97** · 设计图纯函数 **49/49** · 版本层 **63/63** · 思维导图纯函数 **91/91** · 个性化+四维流转 **87/87** · 分支聚焦 **26/26** · 统筹编排 **33/33** · 设计稿卫生 **49/49** · 数据安全 **42/42** · 批次 1 **97/97** · 批次 6 **66/66** · 批次 7 **66/66** · 批次 8 **93/93** · **弹窗离线 `verify-dialog` 268/268** · 工作室逐交互 **94/94** · 导图逐交互 **103/103** · 弹窗逐交互 `cdp-click-dialog` **102/102** · 真机演练 **80/80** · 第 16 批验收 `verify-novel-split` **130/130**（**第二十六轮**：由 108 历次增至 130 —— 新增 `NS-11e/f/g`（U10 标题弱匹配三项读数）与 `NS-8l0`（靠边缩进前提）；🔴 并修掉当时唯一那条红 **`NS-11h`** 牵出的**真产品缺陷**「常驻读数接到层级树 ⇒ `data-alive` 恒空」，修后读数额 `{"alive":"170","archived":"134","rows":170}`）（**第二十二轮新增 `NS-6l2`/`NS-6p–s` 七条** —— 清除前**快照** → 清除 → **界面真实点击**「↩ 恢复」→ 幂等复点 → 哨兵收尾，含**正对照 `NS-6q`**「清除前快照里的真实消息恢复后按 messageId 读得回来」；**第二十一轮新增 L 段 `NS-12a–f` 会话档案六条** —— 含**可见文本含「总监」**与**负对照互不相同**；第十九轮曾新增 `NS-11a–11d` 复用/新建读数断言）（第十八轮 E7/E8 加固 69→90：新增派发/回收/清除断言 `NS-4a–4d2` / `NS-6h0·e·f2·h` / `NS-10a–e`，并以**跑程内作用域** + `GATE_SENTINEL_REWRITE=1` 负向校准解掉 5 类假红） · 第 5/6 批真机 `verify-v20/v21/v22` **43/43 · 22/22 · 59/59** · 编排 `verify-orchestrate` `fail=0` · 离线全量 51 套 **PASS=39 / FAIL=0**（第十八轮 +4 套 / 53 条：`test-director-collect` · `test-director-dispatch` · `test-dispatch-log` · `test-session-io`；**当轮抓出并修掉 2 个真实产品缺陷**） · **第十九轮：离线全量 54 套 PASS=42 / FAIL=0** · **第二十一轮：离线全量 55 套 PASS=43 / FAIL=0**（+`test-branch-tree-archive` **13/13**） · **第二十二轮：离线全量 56 套 PASS=44 / FAIL=0**（+`test-message-backup` **13/13**：备份挑选/写入/超限/读回/清除 + 反例校准）（+`test-director-reuse` 17/17 · `test-session-dossier` 17/17 · `verify-build-stamp` 8/8；`test-director-collect` 16→**22/22** · `test-session-io` 12→**15/15**） · **第二十三轮：离线全量 57 套 PASS=44 / FAIL=0**（`test-split-dimensions` 45→**87**（+SD-13/14/15）· `test-dispatch-log` 14→**16**（+DL-14/14b）· `test-director-collect` **24**（含**重号自检**）；真机新建 `verify-director-logic` **25/25 · 四连绿**（`reload` 后**立刻** ×1 + 连跑 ×3）；**产品 4 缺口 + 调试机制 5 缺口**） · **第二十四轮：离线全量 58 套 PASS=45 / FAIL=0**（+`test-director-corpus` **33/33**：语料完整性含**真实项目路径磁盘存在** · 噪声类别 · 分线口径 · 整理去噪 · **CP-5a 反例校准** · **CP-5b/5c 负对照**；真机 `verify-director-logic` **25→29/29 · 连跑 40 轮**） · **第二十五轮：离线全量 59 套 PASS=46 / FAIL=0**（+`test-harness-ctrl` **33 PASS / 0 FAIL / 3 可分辨跳过** —— `_harness` **验证式停机 / 端口顺移 / stdout 留痕** + `ensureDirectorPage` **四类可分辨归因**，以**虚拟时钟**驱动；`test-split-dimensions` 87→**92**（+`SD-16a–e` **纯数字/符号边界**，含负对照 SD-16b）；真机 `verify-director-logic` 冷启动 **8/29 → 29/29**、稳态 **28/29**（唯一红 = U10）；`verify-novel-split` **108/108** · `verify-mindmap` **103/103 零跳过**） · 逐交互 `cdp-click` **74/74**（含职责配置快照/复原 · 第十六轮 72/73 假红已修：等待预算 60s→180s + 校验返回值 + 超预算判 INVALID + 补「树至少两层」前提断言与 I7 自建前提）· 反证 `prove-dup-gate` **红/绿双证** · 构建 **2,008,557 B / 87 模块 · 741 = 741（265 条 import）/ 内容指纹 `2085f97cd1b8003c`**（**连跑两次逐字节一致** = 确定性自证） · 生成器收敛 `test-generators` **4/4**（两个 `--check` **同时** TRUE · 植入缺陷校准精确命中） · 映射索引幂等（三份 `verify-index` **✅ 全部一致**） · 指纹 **93 文件 / 3,910,289 B · `漂移=0`** · **第二十六轮：离线全量 PASS=56 / FAIL=0 / INVALID=0**（真机套跳过 14 / 总计 70；+`test-generators` 4 条；`test-director-reuse` 17→**35/35**） · 退出码约定 **0 通过 / 1 FAIL / 2 INVALID**（↑真机五套**各连跑 3 次**，且工作室那套要求**载荷逐字一致**） |
| 真机演练 | `scripts/verify-flow.mjs` **80/80 · 零跳过 · 连跑 3 次全绿**（2026-09-16 第十五轮：修掉 A3「拖动收尾写在 `setState` 更新函数里 ⇒ 渲染循环」后由 76/76 提至 80/80；修复前首跑为 55/2/7）（五组原话逐条落点：原生对话框 / 个性化四处 / 单框控件+拖动+连线对账 / 右侧对话首位 / 四维流转跟随切换；**F 段**：总监页同源 + 浮动组 F8 不吃点击 · F9 配色随主题 · F10 位置不漂 · F11 跟随性正负对照）+ CDP 健康度断言 E2 |
| 导图逐交互 | `scripts/verify-mindmap.mjs` **103/103 · 零跳过**（**2026-09-17 第二十四轮**：+`C-M1c` 血缘前提**实体化**（复用优先、缺才 fork）/ `C-M1d` 三层树前提；并修 3 类闸门缺陷 —— 等待预算只有实测延迟的 **1/3** · `toastGone` **语义反转**自造红 · `data-children` **空真绿**。2026-09-16 第十五轮复跑确认；A3 修的就是本套件覆盖的拖动手势）（2026-09-12 由 81/0/5 提升：跳过 5→0 —— 修掉"可见集取错"与"用常数坐标代表空白点"两类假跳过，见下表「闸门纠错」） |
| 构建→装机 7 步 | 闸门 → `gen-source-map.mjs` → `build/build.mjs` → **`check-stale-build.mjs`（新增，装机脚本已内置）** → `plugin-install.mjs --apply` → **重启 Harness**（不可重启时用 `reload-client.mjs`）→ `verify-design-studio.mjs` + `verify-mindmap.mjs` + **`verify-flow.mjs`**（各连跑 3 次） |
| **第十九轮**（会话复用 / 会话档案 / 清理） | 用户原话：「重复创建了一百多个会话…总监确认完需求之后**先考虑目前存在的会话,然后没有才是新建会话**」<br>· **R1 复用**：真机实证 `{"made":"8","reused":"8","created":"0"}`，**宿主会话总数 131 → 131（净增 0）** —— 一条新会话都没建（`NS-11c` 核心验收）<br>· **R2/R3**：`store/session-dossier.js` 给**每个会话**写自己的总监角色（`A1 世界观 总监`…）与自己的总结（`dsh.director.dossier`）<br>· **R4 清理**：新工具 `clean-sessions.mjs`（dry-run 默认 / 移入 `.trash-<ts>` 可回滚 / 三口径复核）——会话 **131 → 0**；`scripts/` 下 32 个历史 `_tmp-*` 退役（40 件移入 `.trash-20260916-202933`）<br>· **R5 记录**：`docs/00-统筹入口/14-会话复用与会话档案-需求与实施文档-20260916.md`（含 §八 六条优化建议）<br>· 🔴 **两个新机制**：`build-stamp.mjs` 内容指纹（解决"产物陈旧却逐字节一致"，mtime 判据在本仓会假阳性）+ `check-stale-build.mjs`（装机前置，未过即拒装）；`reload-client.mjs` **升格为正式工具**（主进程不可结束是长期事实）<br>· 🔴 **真机数据反推出的第三个真实产品缺陷**：宿主 `turn/end.reason.error` 里的失败详情（`Insufficient Balance / QUOTA / 402`）过去被丢弃 ⇒ "环境阻塞"与"投完就静默"**同形**。已修：`endFailure`/`runFailure`/`hostFailed` 三级落地，裁定文案变为「宿主侧报错 8 条（Insufficient Balance（QUOTA））：简报**已送达**，是模型侧没出结果」 |
| **第二十一轮**（「一百多个会话」真因修复 / 会话档案可见 / 四层落点清理 / 连跑 10 次） | 用户原话：「重复创建了一百多个会话…**先考虑目前存在的会话,然后没有才是新建会话**…不会话都有自己的总监,存在自己的会话总结文档…清理无效的多余的文件和所有会话…反复测试…把整个流程记下来,思考如何优化…**需要持续重复起码十次**」<br>· 🔴 **「一百多个会话」的真因 = 树把已归档幽灵也画出来了**（不是"在建新会话"）：`refreshBranchTree()` 的**唯一摄取点**直接吃宿主快照**全部 126 条**（含 125 条已归档）⇒ **导图 126 个框**；第 19/20 批的归档过滤只加在 `director-dispatch.js`（**派发侧**）⇒ **用户看到的现象与代码里"已修好的东西"完全对不上**。修复：过滤点前移到唯一摄取点 + 三处报数，**对账 `rows + excluded = rawCount`**（实测 `rows: 126 → 1`）<br>· 🔴 **`applyDossiers` 全仓零调用** —— `store/session-dossier.js` 单测 **17/17 全绿**却**没人调它** ⇒ 档案写进 `localStorage` 却**从不显示**、`data-dossier-role` 在产物里 **0 处**。修复：接入 `refreshBranchTree()` + MindMap 挂 **5 个 `data-dossier-*`** + 节点第 2 行**可见**「· 总监 A1 世界观」<br>· 🔴 **「没有总结」与「读不到」不可分** ⇒ 两条「读不到」分支补写 `{source:"none",ok:false,reason}`<br>· **清理 = 四层落点全 0**：磁盘 **130→0** · `workspace.json` **130/125→0/0** · `session_projcache.json` **150→0** · `localStorage` 孤儿 **6→0** · 宿主 `archiveSession` **134 成功 / 0 失败**（侧栏 6→**0 行**）· 无效文件 **36 个 / 2,729 KB** 入 `.trash-20260916-2140/`<br>· **连跑 10 次全部达标**：run1 `复用 0 · 新建 8`；**run2–run10 恒 `复用 8 · 新建 0 · 净增 0`** ⇒ 判据 **RU-1/RU-2** 双向成立<br>· **闸门 94 → 100**：新增 **L 段 `NS-12a–f`**（档案六条，含**可见文本含「总监」**）· **连跑 2 次均 100/100**<br>· **新工具 4 件**：`test-branch-tree-archive.mjs`（13/13）· `clean-session-ui-state.mjs`（第四落点）· `repeat-novel-split.mjs`（收敛表）· `clean-all-sessions.mjs`（**一条命令 + 默认 dry-run**）<br>· **新增 §八 纪律 74–81**（74–77 = 环境/操作类：宿主以**管理员**运行 ⇒ 非提权杀不动 · 绝不把 `window.close()` 当关闭应用 · 「清盘」≠「清干净」（`archivedSessionIds` 被回写复原）· 本会话 PowerShell stdout 恒空但命令确实执行；78–81 = 工程类：存活集**单一真相源** · 「模块写好了」≠「接进去了」 · 全清后起点**实体化** · 清理**一条命令 + 默认 dry-run**）<br>· ⚠️ **未结项**：主进程内存持 125 条归档（磁盘 0）⇒ **退出时回写磁盘**，**须重启 Harness 才彻底归零** |
| **第二十三轮**（总监逻辑链全链路测试 · 产品侧 4 缺口 + **调试机制自身 5 缺口** · 真机 **25/25 四连绿**） | 用户原话：「整个的**逻辑测试**…总监的**分配机制**也是好用的，**整个调试的机制**也是好用的。回复包括总监对语言的**整理**、对于**文字的描述**、对于**整个项目把控**…**拿具体实际项目**去发送，然后可以发送一些**无意义的东西**让总监去**分辨**…遇到问题就改问题…同时判断**哪里还可以优化就优化哪里**」<br>· ✅ **A 路：分配机制经真机验证是好的** —— 真实项目 `D:\workspace\novels\墟海` ⇒ `kind=novel` · `made=8` · `sent=8` · `confirmed=8` · dims **A1–A8 全齐** · 会话净增 8；回收链**如实报出** 8 条 `Insufficient Balance（QUOTA 402）`（降级路径正确）<br>· **B 路 · 产品 4 缺口**：**D7** 不分辨噪声（投「今天天气不错 哈哈哈哈」⇒ 修复前 `made=3`、会话 +3）· **D8** 无"语言整理"（回收 8 条逐条重复同一句，822 B/8 行同构）· **D9** 不做"项目把控"（把"读清单"推给分支）· **D10** 审计消息写入后界面滞后一条（库 3 条 vs 页签 2 条，缺的正是最新那条）<br>· 🔴 **C 路 · 调试机制 5 缺口（比产品缺陷更危险 —— 会把自己的失效伪装成产品红）**：**D11** 台账 `items[]` **不含 `brief`**（白名单 map 丢弃）⇒ D8/D9 改完**没有观测面**，闸门只读到 `""`；**D12** 起点缺「**宿主已消费注册**」前提（`registered:true` ≠ 可交互）⇒ `reload` 后**立刻**跑 **13/25**，失败形态与产品坏**完全一样**；**D13** 点击**只认几何命中、不认后果** ⇒ CDP 合成鼠标丢事件被算成产品红（同坐标同序列**连丢 6 轮**，**同一时刻** `el.click()` 一次生效）；**D14** 取证日志**被下一次跑覆盖** ⇒ 假红证据当场丢失、白跑一轮；**D15** 消息**只走 DOM**（`dp-dir-msg` 仅消息页签激活时渲染）⇒ 把"没切视图"误判成"消息没写"<br>· **修复**：`split-dimensions.js`（`ACTION_HINTS` + `noiseReasonOf()` 七级 + `organize()`/`organizeSummary()` + `plan()` 噪声短路，`kind` 扩 `noise`）· `briefOf` 第 5 参 + 「项目把控 · 先做这四步再动手」指派段 · `director-collect.js` 归纳式 `digestMessage()`（合**原因**不合**条目**）+ `projectVerdict()` · `DirectorPage.js` 6 处（含 **D10 刷新顺序两处**）· `dispatch-log.js` 台账补 `brief` · 闸门 `verify-director-logic.mjs` 新建并加固（DL-0 前提 / 起点 **40s·8 轮可重试** / **`clickUntil` + `clickJs` 保底送达** / **双通道读消息** / **带戳留痕**）<br>· **判据**：真机 **25/25 · `IS_PASS TRUE`** 且**四连绿**（`reload` 后立刻 ×1 + 连跑 ×3）· 离线全量 **57 套 / PASS 44 / FAIL 0 / 跳过 13** · `test-split-dimensions` **87**（45→87）· `test-dispatch-log` **16**（14→16）· `test-director-collect` **24** · **破坏性校准 3 次全部精确命中**（含去掉 `brief` ⇒ 恰红 `DL-14` 一条、负对照仍绿）· 构建 **1,854,200 B / 711 = 711（252 import）/ 指纹 `0c43c1d001267747`** · 装机**逐字节一致** · `Page.reload` 后回读 `__dshBuildStamp` **一致** · 基线**第九锁 90 文件 / 3,610,207 B**（新增 0 / 修改 7），复验**漂移=0**<br>· 🔴 **环境**：`taskkill /F /IM "DeepSeek Harness.exe"` **沙箱内外均拒绝访问**（Harness 以**管理员**运行）⇒ 主进程无法自动重启；本轮以 **`Page.reload` + 构建指纹逐字符比对**装载新 bundle（判据不靠猜）<br>· **新增 §八 纪律 85–89**（85 已注册≠已可交互 · 86 点了≠生效了 · 87 台账要记"我们说了什么" · 88 取证产物不许被覆盖 · 89 读最新消息走落点）· **新增 backlog `T-PLUG-051/052/053`**（其中 **051 = 其余真机闸门"点击送达"统一加固 —— 本轮「哪里还可以优化」的首要答案**）<br>· **交付物**：`docs/00-统筹入口/17-总监逻辑链全链路测试与优化-20260916.md` |
| **第二十二轮**（总监消息「一个都没有」诊断与修复） | 用户原话：「总监消息没有持久化么 为什么一个消息都没有」<br>· 🔴 **先证通道完好，再找真凶**：真机往返实测 —— 点一次「统筹」⇒ 页签「总监消息」计数 **0 → 1** 且插件库 `directorConversations` **0 → 1**（**2.5 s 内**）⇒ **写入 / 计数 / 界面刷新三段全通，持久化通道本身没坏**<br>· 🔴 **真因 D4（主因）= 验收闸门把用户数据当耗材**：`verify-novel-split.mjs` **D 段**测「清除全部总监消息」时调 `clearDirectorMessages()`（**无参 = 清全表**），**只验哨兵消失、跑完不还原** ⇒ 第二十一轮**连跑 10 次验收 = 把库清零 10 次**（实测 `directorConversations` **0 条** / `directorNodes` **458 条**）<br>· **D5 = OPFS 单文件缓存串桶**：`store/persist.js` 的 OPFS 通道是**单文件** `director-store.json`（**不含桶名**），而 `loadDirectorStore(sessionId)` **先读这份缓存、命中即返回** ⇒ 多会话互相覆盖；实测 8 个 A1–A8 桶**全空**，而 `dsh.director.store.director-d1yi2z` 是**唯一还有 2 条消息**的桶 —— 但它属于**已不在树上**的会话（**孤儿桶**）<br>· **D6 = 写消息触点只有 4 个**（统筹 / 派发 / 回收 / 运行总监链），「登记为流转」**只写 flow、不写消息** ⇒ 若只做"登记"操作，消息数恒 0（**语义问题，仅记录未改**）<br>· **五层落点体检 M1–M10**：① IDB 插件库 `dsh-director-plugin-db@v2`（`directorConversations` **0** / `directorNodes` **458**）· ② 宿主库 `dsh-director-db@v3`（`memoryCore: 459`）· ③ OPFS 单文件 **2016 B** · ④ `localStorage` 桶（8 个空 + 1 孤儿桶 2 条）· ⑤ cookie **17 个**（**不含 A1–A8 任何一个**）。桶键 `safeDirectorKey()` 实测映射 A1=`director-hvycae` … A8=`director-f0oyl6` ⇒ **切作用域就是切桶**，用户"逐个点开都是空"是**必然**<br>· **修复 F1–F4**：**F1 闸门** → 清除前**快照**（`{n,realN,sample}`）→ 清除 → **界面真实点击**「↩ 恢复」→ 幂等复点 → 哨兵收尾；**F2 安全网** → `plugin-db.js` 新增 `backupDirectorMessages`/`readMessageBackup`/`pickRestorableRows`（纯函数）/`restoreDirectorMessages`/`clearMessageBackup`，`clearDirectorMessages` 改为**先备份再删除**（顺序不可颠倒），超 **3 MB** 备份失败即 `ok:false` 且**保留旧备份**；**F3 串桶** → `store/persist.js` payload 记 `sid`，读时校验不符即视为**未命中**并打印 SKIP；**F4 界面退路** → `DirectorPage.js` 新增「↩ 恢复」`dp-maint-restore` + 读数 `dp-restore-result`，toast **必须报退路**<br>· **判据**：离线新增 `test-message-backup.mjs` **13/13**（含 `MB-8` 超限**旧备份原样保留** · `MB-10b` `rows` 非数组**不抛** · `MB-12` **反例校准**）· 真机 `verify-novel-split` **106/106 TRUE**（连跑 2 次），还原链完整读数：`快照 {n:8,realN:7}` → 清除 `8/8` → 备份槽 `{exists:1,count:8}` → **真实点击** `{restored:8,total:8}` → 再点 `{restored:0,skipped:8}` → 哨兵收尾 `{del:true,left:0}` → **消息数 8 → 8**<br>· **结果**：离线全量 **56 套 / PASS 44 / FAIL 0** · 构建 **`1,830,903 B` / 84 模块 / 710 = 710（252 import）/ 指纹 `af7111e0b0064627`** · `check-stale-build` TRUE · 装机 `[4]` 逐字节一致 · 基线**第八锁 3,564,745 B**（新增 0 / 修改 5），复验**漂移=0**<br>· **新增 §八 纪律 82–84**（82 闸门不许把用户数据当耗材 · 83 破坏性动作必须自带退路且写进结果句 · 84 单文件缓存必须带归属标记）<br>· ⚠️ **未结项**：U1 已被清掉的历史消息**找不回**（修复**从当前时刻起生效**）· U2 孤儿桶无处可查 · U3 触点补全需用户确认语义 · U4 桶数量线性增长的配额预检 |

> 🔴 **两条最容易踩的测试陷阱**（详见 `docs/00-统筹入口/10-…V16.md` §七 7.5 / §八 13–19）：
> ① 用真实鼠标驱动 e2e 时，坐标必须**视口内且命中测试为自己** —— 画布滚动后第一个节点可能是 **x=-934**，
> 事件打空却像「功能坏了」（改用 `focusVisibleNode()`）；
> ② **不要无条件按 Esc** —— 无菜单时它关闭**整张导图**，会让其后所有断言都在量一个已卸载的 DOM。

### 闸门纠错台账（2026-09-12 · 「断言比产品更容易过期」）

> 本轮同一天内抓到 **5 处"闸门本身写错"**。共性：**旧断言把"现状变了"读成"功能坏了"**，
> 而且全部以**假红或静默跳过**的形态出现 —— 跳过比红更危险（红有人看，跳过没人看）。

| # | 闸门 | 旧写法 | 错在哪 | 新判据 |
|:-:|:-----|:-------|:-------|:-------|
| 1 | `verify-batch1.mjs` | 宿主 `filteredMessages` 命中数 **== 1** | 该断言写于"宿主还是坏的"时期，用**越界引用存在**当健康凭据；宿主补丁落地后命中数变 0 ⇒ 假红 | 两侧**都归零** + 都能找到同一处修正（`state.messages.map((msg) =>`）⇒ 双向一致性 |
| 2 | `verify-batch6.mjs` | `/0\.[6-9]\.0-batch[6-9]/` | `[6-9]` 是**单字符**类，产物到 `0.15.0-batch15` 后 minor/batch 都是两位数 ⇒ 假红 | 解析 `(major,minor,batch)` 做**序比较** + 与源码 `PLUGIN_VERSION` 同源交叉校验 |
| 3 | `verify-bundle.mjs` | `/batch15/` 写死 | 下次升批次必假红 | 「不低于批次 15」单调性闸门 |
| 4 | `verify-batch8.mjs` | ④⑤职责默认**禁用** | `duties.js` 已按文档 §1.2 五步规格改为启用（留痕在先）⇒ 闸门过期 | 五步**都为 true** 且每步**显式带 `enabled`** |
| 5 | `verify-mindmap.mjs` | ①折叠探针不退出聚焦；②"移开鼠标"目标写死 (18,320) | ①聚焦态可见集只剩祖先链（非根有子节点全被排除）⇒ 探针 `usable=0`；②该点在画布滚动后**正好压在节点内**（探针实测 `[-10,291,98,31]`）⇒「工具条应消失」被一个"其实还悬在节点上"的点否掉 | ①段首先**退出聚焦**；②空白点**按当前几何算**（`blankPoint()`），并补「移开前工具条确实在」防平凡真 |

> 跳过 5 → 0 的收益：`verify-mindmap.mjs` **81/0/5 → 100/0/0**（多出 19 条真断言，
> 含 C-M14a–f 右键菜单、C-M15a–c 悬浮工具条、C-M16a Esc 逐层退、C-M8a–d/C-M8q 折叠正负对照）。

### 闸门纠错台账（二）（2026-09-13 · 批次 16 · 又 8 处）

> 同一天隔日再抓 **8 处**。共性升级为三条：**① 尺子量错容器**（量的是兄弟节点）；
> **② 属性名语义撞车**（一个选择器跨两类语义，一个根因连爆数条红）；
> **③ 桩/尺寸写死**（同一份桩抄两份 ⇒ 必漂移）。另外首次抓到**闸门把整个脚本打哑**
> —— 它不再报红，而是**静默崩溃**，比跳过更隐蔽。

| # | 闸门 | 旧写法 | 错在哪 | 新判据 |
|:-:|:-----|:-------|:-------|:-------|
| 1 | `verify-dialog.mjs` | `mounted.launcher.textContent.length >= 1` | `makeLauncher()` 已退役（批次 13 移交 FloatDock）⇒ `mounted.launcher === null`，**null 解引用在 600 行把整个脚本打哑**，其后 100+ 断言（H10b–H12 / I 段 / J 段）**全部丢失且无人知晓** | 契约式断言（`launcher` 显式 `null`、入口改判 `document.getElementById(LAUNCHER_ID) === null`）+ 文件头补写正确用法 `node --import ./scripts/_platform-stub.mjs` |
| 2 | `verify-dialog.mjs` | 插件库版本写死 `=== 1`、store 数写死 `=== 6` | 插件库 v1→v2、store 6→7（**独立编号，不参与宿主 v3 协商**）⇒ 假红 | 关系判据：版本 `>= 1`；store 断言改「含 **6 个必需** store，**允许扩展**」 |
| 3 | `verify-dialog.mjs` | 「head 内样式节点**唯一**」 | 现为 2 个 `<style>`（分屏 + 个性化令牌）⇒ 假红 | 改**按 id 计数**（关心的是「同一 id 不重复注入」，不是标签总数） |
| 4 | `verify-install.mjs` | 内联 react 桩 `{ useCallback, useSyncExternalStore }` | 产物含 `class SafeLayer extends react.Component` ⇒ 桩缺 `Component` 令 factory 抛 `Class extends value undefined is not a constructor or null`，**32/33 NO** 且报错与真实缺陷无关 | 抽 `scripts/_platform-modules.mjs` 作**平台桩唯一真相源**（16 导出），本文件只 `import` ⇒ **60/60 YES** |
| 5 | `verify-bundle.mjs` | 本地第二份内联 react 桩 | **同一个坑的第 2 次**（09-12 已修过一次）⇒ 两份桩必漂移 | 同上：改引用共享桩（97/97 TRUE） |
| 6 | `cdp-click.mjs` | 「五步过程已展示」扫 `director-messages` 内的行 | **尺子量错容器** —— 五步区与 `director-messages` 是**兄弟节点** ⇒ 恒 false | 产品补 `data-testid="director-steps"` 锚点 + 结构断言「**恰好 5 条**」 |
| 7 | `cdp-click.mjs` | 「默认启用项数 **== 3**」 | `duties.js` 已按 03 号文 §1.2 **五步规格**全部启用（同 §台账一 第 4 条，上轮**漏改这一处**）⇒ 假红 | 「五步**全部启用** = 5」 |
| 8 | `cdp-click-dialog.mjs` | 选择器 `[data-dim]` | `data-dim` 被**审核维度**与**四维流转徽标**共用 ⇒ 扫到 10 个元素，**一个根因连爆 3 条红** | 拆命名空间：`data-review-dim`（审核维度）/ `data-flow-dim`（四维流转徽标），产品 3 处 + 闸门 2 处同步 |

> **同批附带修正（不单列行）**：
> ① `cdp-click-dialog` 入口文案判据 —— 入口本是**图标 + 文案**（`◆` + `总监`），与同组
> `🖌 设计图` / `🧠 思维导图` 同构 ⇒ 改语义判据（含「总监」且 ≤ 6 字）；
> ② 同文件 R5 白名单漏登记新键 —— R5 冻结的是「既有键**不得改名**」，**新增是允许的**
> ⇒ 白名单补 `design|personalize|flow`，断言改「冻结键未改名 + 新键已登记」；
> ③ 同文件覆盖审计漏点 `d-personalize`（批次 11/12 新增但**从未被点过**）⇒ 新增 D15b 段
> **真点开 → 回读面板选项 → 关闭**，不靠白名单假称点过；
> ④ `verify-design-doc.mjs` 新增**目标版本自检**（其判据按 V14.1 正文逐字写死，喂 V16 只会一路 ❌
> 却读起来像「新稿退化」）⇒ 非 V14 族判 **INVALID（exit 2，与 FAIL 的 exit 1 区分）** 并指路仓库根
> `verify-design-html.mjs`；⑤ `lint-platform-stub.mjs` **新增闸门**（9/9）并在自检中发现
> **判据污染**（坏样本字面量写在源码、注释里也留了一份）⇒ 样本改**拼接构造**；
> ⑥ `lint-cdp-templates.mjs` 又拦下一次**模板内注释带反引号**（**本坑第 5 次**，见 §踩坑索引 坑28）。

> 本批收益：`verify-dialog` **崩 → 268/268**（找回 100+ 条被吞断言）· `verify-install`
> **32/33 NO → 60/60 YES** · `cdp-click` **22/22 → 70/70** · `cdp-click-dialog` **94/100 → 102/102**。
> 离线 22 套全绿；真机五套仍各连跑 3 次全绿。

#### 台账（二）续 · 同日再抓 3 处（**闸门会持久化改坏产品状态** —— 本批最重的一处）

> 前三处都是"量错/写死/撞车"，这三处的**危害等级更高**：它们**不是报错，而是改坏**。

| # | 闸门 | 现象 | 真因 | 处置 |
|:-:|:-----|:-----|:-----|:-----|
| 9 | `cdp-click.mjs`（I 段 4/6/7/8） | `verify-flow` 的 `G3b`「助手消息含五步结论」报红，正文是`（全部职责已关闭，原文直转）` | I 段是一条**真实写入链**：翻转 5 个职责开关 → 保存本层（写选中节点）→ 向上提交（写父节点）→ 恢复继承**只清会话那一层** ⇒ 「五项全关」被**永久**写进 `__global__` 与 `ws_*` **两级**，且**跨脚本、跨运行活着**（脚本不重载页面），连**用户真实使用总监**都被改坏 | 新增 **I0 快照 / I8r 逐节点还原 + 回读自证**：写回快照里有的、**清掉快照里没有的**（那必然是本段写脏的）、再对账全树 ⇒ `cdp-click` **71/71**。**I8r 独立清掉的 2 个节点与我手工排查完全一致**（= 反证） |
| 10 | `cdp-click.mjs`（I9b/I9c）· `verify-flow.mjs`（G3a/G3b） | 同一条断言**时红时绿**；G3a/G3b 甚至**一红一绿** | 判据是**页面级**的（对整段 `innerText` 做正则），而消息 store **按 sessionId 持久化**、R5 只显示 `slice(-14)` ⇒ 问的是「**历史上**出现过这几个字吗」。`I9b` 的 `done: /总监分析/` **立即**为真 ⇒ 轮询提前退出 ⇒ `I9c` 在五步没算完时读到 `lastSteps === null` ⇒ **rows=0** | 一律改**跑程内判据**：读 `dp-dir-msg` 的**最后一条**；轮询条件加「本轮才会出现」的信号（消息**行数净增 ≥ 2** + `director-steps` 恰好 5 行）⇒ `verify-flow` **68/68 零跳过** |
| 11 | `verify-dialog.mjs` | 裸跑崩在 `Cannot find package 'react'`，栈里看不出"只是少了 `--import`" | `react` 按 **ADR-001** 是平台冻结模块、本仓库**不装 `node_modules`** ⇒ 离线脚本必须带解析钩子 | 动态导入包 `try/catch` ⇒ 命中即打印**可复制的正确命令**并 `exit 2`（**INVALID ≠ FAIL**）。顺手钉死退出码约定：**0 通过 / 1 FAIL / 2 INVALID**（`verify-design-doc` 的目标版本自检同此约定） |

> **本轮新增的两条通则**（已写入 `dsh-director-plugin` 侧的纪律 36–40 与 §八）：
> ① **凡"点了会写盘/落库"的闸门段，都要快照 + 还原 + 还原断言** —— 否则闸门就是产品的**破坏者**，
> 而且它改坏的东西会伪装成**下一个闸门的产品缺陷**（红在 `verify-flow`，根在 `cdp-click`）。
> ② **判据一律"跑程内"** —— 只要被测面是**持久化/累积**的（消息、日志、历史），
> "页面上有没有这几个字"就既可能假绿也可能假红，且**两个方向会同时出现在相邻两条断言上**。

### 闸门纠错台账（三）（2026-09-14 · 编排面板 43 条新闸门 + 两套既有真机闸门）

> 本轮对象：新写的 `scripts/verify-orchestrate.mjs`（编排面板真机 e2e，**43 条 · 零跳过**），
> 外加顺手补 `verify-flow` / `verify-design-studio` 的异常兜底。新共性两条：
> **④ "等多久"被写死成判据的一部分**（固定 `sleep` + 单次读取 ⇒ 抖动，且**是闸门自造的**）；
> **⑤ 把"成功的文案"当成唯一通过条件** ⇒ 把一次**正确的显式降级**读成红。

| # | 闸门 | 旧写法 | 错在哪 | 新判据 |
|:-:|:-----|:-------|:-------|:-------|
| 1 | `verify-orchestrate`（OB/OH 等 5 处） | 点击后 `await sleep(182)` 再单次回读 | **闸门自造抖动**：为"跑快点"把等待由 260ms 砍到 182ms，OB4 随即偶发红（点击已派发、读回还是旧值）。固定 sleep 把"等多久"写死成判据的一部分 | 全部改**条件轮询** `untilRead(fn, want, budget)`；实测状态 **3ms** 就到（说明旧等待**过度且仍然脆弱**）。预算提到 **8s**：失败形态是 `耗时ms≈3100` 而状态不动 = 主线程被后台装载**阻塞**，输入事件在排队 |
| 2 | 同上（`OD4`） | `波内合计 === 节点总数` | **平凡真**：面板没渲染时 `0 === 0` **通过** —— 那不是"没有孤儿"，是"根本没有图"。产品全坏时它反而变绿 | 先钉前提：`节点总数 > 0 && 波内合计 === 节点总数` |
| 3 | 同上（中途整段 null） | 只报红，无法区分成因 | **"产品坏了"与"页面重载了"读数完全一样**（都取 null）⇒ 29 条红看起来像产品崩了 | 埋 9 处**环境心跳**（`dp-root` 在不在 / `timeOrigin` / 节点数），跑程中 `origin` 一变 ⇒ 判 **INVALID exit 2** 并说明"前后段不同源，本次结果整体不可采信"。判据必须**无条件**（只在 `fail>0` 时判会放过"零红但页面已换"的**假绿**） |
| 4 | 同上（`OG4`） | `toast.indexOf("简报") >= 0` | **把"成功文案"当唯一通过条件**。真机里 Chrome 的**瞬时用户激活会被第一次剪贴板写入消耗** ⇒ 第二次复制**合法地**走失败分支，文案「复制失败（委派简报）：…」**含标签不含「简报」二字** ⇒ **一次正确的显式降级被判红**（`反馈:null` 是 toast 2.2s 自动清空后轮询读到的末值） | 改「标签必须在**两种结局里都出现**」：成功含「已复制：委派简报」/ 失败含「（委派简报）」。**两头都钉**，既不假红也不会因"永远失败"蒙过 |
| 5 | 同上（`OC4`） | `点击前 false → 点击后 true` | 起点假设不成立：面板 `open=false` 时只 `return null`、**组件仍挂载** ⇒ `roleId` **跨"关→开"甚至跨运行**存活（连跑第 2/3 次进来卡片已是 `true`）。且角色按钮是**开关**，点错一个会变成"换成另一个"而不是"取消" | 产品补 `data-role` 锚点 + 闸门**显式归零**：读卡片上的 `data-role`，点回**同一个**角色取消选中，再断言选中。实测连跑命中「点了 director 取消选中」= 修的是真污染 |
| 6 | `verify-orchestrate`（新增 `OG5`） | （原无此断言） | 上一版剪贴板失败时只发一句「请手动复制」，而界面上**没有任何东西可复制** ⇒ 一句话把人指到死路。实测 `navigator.clipboard` 必拒 `NotAllowedError`、旧兜底 `document.execCommand("copy")` **也返回 false** ⇒ 两条全封死 | 产品补**第三级**（照 `DesignStudio` 已有约定）：把文本塞进**只读可选中** textarea 摆出来（用 `readOnly` 而非 `disabled` —— disabled 无法选中，"手动复制"又落空）。闸门加**双向对照**：成功⇒文本域**不该**在；失败⇒**必须**在且非空 >50 字 |
| 7 | `verify-flow` · `verify-design-studio` · `verify-orchestrate` | 顶层无异常兜底 | **本项目登记过的同类事故第 2 次**：渲染卡顿时 `ev()` 不抛错而是返回 `{__err:"CDP_TIMEOUT …"}`，调用方当数组用 ⇒ 深层 `TypeError: arr.filter is not a function` ⇒ 顶层无兜底 ⇒ **其后断言一条不跑且无人知晓**（本轮 `verify-flow` 崩在 L1143，D6/D7 + 收尾段全丢） | 照 `verify-mindmap`/`verify-v20` **同一套约定**补 `uncaughtException`/`unhandledRejection` ⇒ 打印"已跑出 N 条 + 是否到达收尾段 + 其后未跑"，**exit 2（INVALID）**。「脚本自己死了 ≠ 产品不合格」（纪律 17）。**反例校准**：注入 `throw` ⇒ 正确输出 `已跑出：通过 18` 并 exit 2；正例 exit 0 |

> **本批另立两个可复用件**：
> ① `scripts/probe-reload-period.mjs` —— **空载对照**探针：全程不交互观测 `performance.timeOrigin`。
>    用它把"app 自重载 ~25–38s 一次"证成**环境**行为（空载 70s 也重载、**0 条 error/warning**），
>    而不是"新插件把渲染进程搞崩"（两者读数一样，**只能靠空载对照分开**）。
> ② `awaitForFreshOrigin()` —— 按**页面年龄**决策的起点：`Date.now() - performance.timeOrigin < 10s` 就直接开测，
>    否则等它自重载一次再开测。避免白等把 app 寿命耗光（上一版无条件等 55s，导致后续 3 次连不上）。
>
> **未修、已登记**：宿主重挂载会冲掉 `DirectorPage` 的组件内 `useState`，把浮层"顺手关掉"。
> 实测 `pOpen`（个性化，L303）与 `oOpen`（编排，L307）**同为局部 state** ⇒ 属**既有架构属性**、
> 非本面板引入；只改一个会造成四个浮层行为不一致 ⇒ 本轮**不动**，登记为待办。


---

### 闸门纠错台账（四）（2026-09-16 · 第十六–十八轮 · 总监枢纽闭环 `verify-novel-split`）

> 本轮对象：`scripts/verify-novel-split.mjs` 由 **69/69** 加固到 **90/90 零跳过**（新增派发 / 回收 / 清除断言）。
> 过程中解掉 **5 类假红 + 1 类假绿**，另**当轮抓出并修掉 2 个真实产品缺陷**（由新模块单测发现，非闸门发现）。
> 新共性两条：**⑥ 「跨批次累积面」上的绝对计数判据必然假红**（被测面持久化、宿主无 delete 契约）；
> **⑦ 闸门自己的读取时机早于产品异步落盘 ⇒ 读到上一批数据**，表现为「树没变 + 台账还是上一批」。

| # | 闸门 | 旧写法 | 错在哪 | 新判据 |
|:-:|:-----|:-------|:-------|:-------|
| 1 | `NS-4a` | `afterIds.length - beforeIds.length === 8` | 🔴 **绝对计数用在累积面上**：`dsh.director.split` 与派发台账**持久化累积**、宿主**无 delete** ⇒ 老会话删不掉、标签必留。实测 `delta=6`、`delta=2` **两次假红**；而同刻 `rows.filter(titleOrigin==='plugin:split')` **恰 8 条全在** ⇒ **自相矛盾即假红指纹** | 作用域改「**本次新增的 8 个 key**」：`thisRun = split 新增 ⊂ 本次`；断言 `thisRun.length===8 && addedRun.length===8`。新增 `NS-4c2` **交叉存储**断言（`split` 新增集合 === 台账新增集合） |
| 2 | `NS-4f` / `NS-4d2` / `NS-10c` | 全量 `rows` 里数派发节点 | 同一根因：读到的 8 条是**上一批《灵能修仙》**留下的 | 由「跑程内作用域」一并修好；`NS-4d2` 改**包含式**（`addedRows` 含 `thisRun`） |
| 3 | B 段整段 | 点完分流 `await sleep(500)` 即读树 | 🔴 **闸门读早了**：派发（落盘 + 建会话 + 刷树）未跑完 ⇒ 树没更新、`__dshDispatchLog` 仍是上一批。纪律 55 第二次实锤 | **两阶段有界等待**：① 等索引落盘（本次新增恰 8 key，预算 90s）② 等树能看到这 8 条（预算 20s）；**超预算判 INVALID exit 2**（绝不降级成产品 FALSE） |
| 4 | `NS-6h` | 「清除后消息数必须下降」（`1→1`、`3→3` 假红） | 🔴 **产品有后台写入者**：回收裁定落库后、清除**完成之后**又产生一条**新**裁定（`at=18:44:03` 落在 W 段期间）⇒ "数下去"这一判据本身不成立 | 改**哨兵法**：清除前写入 `gate-sentinel-<ts>`（`appendDirectorMessage`）→ 断言它**确实进过库** → 完成信号 = **哨兵从库中消失**（跑程内） |
| 5 | `NS-6e` / `NS-6f` | 等 `dp-maint-result` 出现 | `dp-maint-result` 是**持久 DOM** ⇒ `waitFor` **立刻命中上一轮遗留读数** `{before:1,removed:1}`，而本次清除还在逐条删（表 `3→2→0`） | 门控改「**哨兵已从库中消失**」+ 新增 `NS-6f2`（`data-before` ≥ 闸门独立实测条数 —— 旧值必然偏小） |
| 6 | `NS-6h`（假绿） | 「清除后哨兵必须消失」 | 🔴 **反例上也通过**：哨兵**从未写进去**时它当然"消失了" ⇒ 空真 | 判据加前提 `sentIn === 1`（`NS-6h0` 先证"写入后能按 messageId 读回来"） |
| 7 | 产品缺陷 ①  | （单测 `DL-9` 抓到）`applyDispatchLabels` 的 `applied`/`states` 数了 `rows` + `byId` **两遍** | `diag.dispatchApplied` **翻倍**（真机 `16` 而台账只有 8）⇒ `applySplitLabels` 与它**并非同口径**（前者只数 rows）。**两条"同口径"的声明当时并不成立** | 产品修：`patch(row, countIt)` —— 只 `rows` 计数；新增 `NS-10e` 断言 `diag.dispatchApplied === 8`（防回归） |
| 8 | 产品缺陷 ②  | （单测 `DD-1` 抓到）`projectOf` 的**终止字符类不含中文标点** | 「项目根 `D:\workspace\novels\墟海，先搭世界观`」把「**，先搭世界观**」一起吞进 `root`。真机原话恰好带 `"…墟海"` 引号才没踩到 ⇒ **属运气不是正确性** | 产品修：`STOP` 字符类补中文标点 |
| 9 | 取证截图 | `captureBeyondViewport: true` + 看文件体积 | 🔴 **取证产物本身也要当真**：拍出过**纯黑 PNG 却有 200 KB** ⇒「看体积」完全无效；`captureBeyondViewport:true` 让 CDP 按**页面坐标**解释 `clip`（`getBoundingClientRect()` 是**视口坐标**）⇒ 偏移到空白区；读数是**组件 state 渲染**的，跑完重挂载 ⇒ 读数回落 | `captureBeyondViewport:false` + **`pngInkRatio()`**（`zlib.inflateSync` 解 IDAT 看非零字节比例，纯色图几乎全 0，阈值 0.005）+ **在闸门内部、读数有效时**拍 |
| 10 | 临时诊断脚本 | `taskkill /F /IM "DeepSeek Harness.exe"`（吞输出）· `Runtime.evaluate` 挂起时"轮询+超时" | 🔴 **幽灵重启**：对 `7412/10312/5656` **一律"拒绝访问"**，只杀掉自己拉起的两个 ⇒ 连续"重启 Harness"其实**一次没重启**（app 端口恒 15117、`page.url` 逐字相同）。🔴 **渲染进程 wedged**：`Runtime.enable` 永不回 ⇒ 脚本在任何输出前**永久阻塞**、日志恒 0 字节 | 收进程**绝不吞输出** + 回读 PID 集合比对；wedged 判据 = `Target.getTargets`/`Page.getNavigationHistory` **有响应** 而 `Page.enable`/`DOM.getDocument`/`Runtime.*` **全超时** ⇒ 用 `Page.reload`（**浏览器进程处理**）3s 无损恢复、**无需杀进程**；`send()` **必须自带期限**（reload 期间 `evaluate` 挂起**且不回复**，"等一个永不 resolve 的 promise"= 保护形同虚设） |

> **新模块单测的直接收益（纪律 52「测试范围 = 受影响面 ∪ 新增面」）**：4 个新模块
> （`logic/director-collect.js` · `logic/director-dispatch.js` · `store/dispatch-log.js` · `bridge/session-io.js`）
> 此前**无任何单测** ⇒ 补 53 条后，**当轮（不是事后）**抓出上表第 7、8 两个真实缺陷。
> 负向校准均已做：改产品一行 ⇒ **恰红一条**；`revert` 后 md5 **逐字节复原**（`e6208d475bfd2cfb69660d03db11282c`）并复跑全绿。

> **本批新增纪律 59–62**（全文见锚点 §八）：59 分清「应用数据」与「应用身份」（清对话 ≠ 清 `profiles/`+cookie；不清 `archivedSessionIds` ⇒ 幽灵会话）；60 **「数出 0」≠「没有」**（"会话条数"探针**连错三次** ⇒ 任何"为空/为 0"的断言先要有"非空时能读出"的证据）；61 `taskkill` 静默失败 ⇒ 幽灵重启；62 渲染进程 wedged 判据与 `Page.reload` 无损恢复。


### 闸门纠错台账（五）（2026-09-17 · 第二十四轮 · 四十轮连跑 + 🔴 **U6 真因纠正**）

> 本轮对象：`verify-novel-split` **95–99/107 → 108/108** · `verify-mindmap` **101 → 103/103 零跳过** · `verify-design-studio` 保持 **94/94** · `verify-director-logic` **25 → 29/29 并连跑 40 轮**（29/29 × 40 · 累计新建会话 **0** · 活会话恒定 28）。
> 🔴 **本轮最重要的结论**：上一轮登记的 **U6「CDP 合成鼠标在 Electron 下静默丢事件」是误诊** —— 真因是**窗口可见性前提缺失**。
> 新共性 **⑧**：**一类红/绿的根子在"没等到"或"没拿到前提"，而不在产品** —— 下表 7 条里有 6 条是这个形态。

| # | 套件 / 断言 | 旧写法 | 错在哪 | 新判据 |
|:-:|:-----|:-------|:-------|:-------|
| 1 | `NS-0v` + **全部真机套件** | 靠 `el.click()` 兜底让红消失（看起来"修好了"） | 🔴 **真因被盖住**：不是"事件丢了"，而是 `visibilityState === "hidden"` 时 CDP 的 `press`/`release` **被整条吞掉**。🔴 `hasFocus()` 在 hidden 时**仍为 true** ⇒ **骗人的代理指标**，上一轮正是靠它反推才得出错误结论（纪律 86 原写"窗口不可见已被否定"） | `_cdp-focus.mjs` 判据换成 `visibilityState`；**11 套统一加装前提**；不成立判 **INVALID + 早退零消耗**（纪律 90）。效果：兜底依赖 **8 → 0**、耗时 **97s → 16s** |
| 2 | `C-M15a` / `C-M16b` | `mouseMoved` 后 `sleep(300)` / `sleep(150)` 就读状态 | 🔴 **等待预算只有真实耗时的 1/3** —— 实测单次派发 **974–1093 ms**（且每次派发在页面产生 **3 个** mousemove）⇒「工具条没出现 / toast 没弹」全是**没等到**，却被记成产品缺陷 | 改**有界轮询**（悬停 12s / toast 8s），超预算才判红**且打印实读值**（纪律 91） |
| 3 | `C-M16c` | `toastGone === false` | 🔴 **同名变量语义被改过**（改成有界轮询时顺手把"当前还在"变成"**已消失**"）而判据照抄 ⇒ **把"正确消失"判成红**，自造一条 bug 级红 | 修正为 `=== true` + 代码里**写死语义记号**（纪律 93） |
| 4 | `C-M1d` | `getAttribute('data-children') !== '0'` | 🔴 **产品根本没有 `data-children` 这个属性**（全仓 grep 零命中）⇒ `getAttribute` 返回 `null`、`null !== '0'` **恒真** ⇒ **空真绿** —— 比红更坏：**红会被查，空真绿会被当成"已验证"** | 改用**与目标段完全相同**的产品口径 `mm-node-toggle[data-enabled="1"]` + `data-depth !== "0"`（纪律 92） |
| 5 | `C-M2c` / `C-M7a` | 血缘连线必须 ≥ 1 条 | 🔴 **前提缺失被报成 FAIL** —— 宿主 28 条会话**全是平级**（无 `parentSessionId`，第二十一轮批量归档的后遗症）⇒ 连线 0 条，**读起来是"产品坏了"**（纪律 31 反面） | **前提实体化**（宿主有 `sessions.fork` ⇒ **复用优先、缺才建**）+ 确实建不出来改**可分辨跳过**（写明"未检验，不是产品坏"）→ 低于总数下限判 **INVALID**（纪律 94） |
| 6 | `C2.2`（工作室） | `sleep(1000)` 后量 rect | 🔴 **动画没播完就量几何** —— `dp-overlay-in` 的 CSS `duration` 只有 `.14s`，但实测 **1950 ms** 才归位（帧节流；t=1.2s/2.7s 时 rect 仍是 `11,6,1420,804`）⇒「动画没播完」被读成「工作室没铺满」，**"恰好播完"那次就绿** = 时绿时红 | 量几何前**轮询到动画归位**（`transform === 'none'` 且四边贴合），预算 15s（纪律 95） |
| 7 | `NS-6l2` | 备份条数 **==** 被删条数 | 🟠 **安全方向被记成红** —— 清除期间有后台写入者（快照 123 / `realN` 122 / before 121 / 备份 123 **四数互不相同**）⇒「多备份」是**安全方向**，判红属纪律 31 反面 | 放宽为「**备份 ⊇ 被删**」+ 新增 `NS-6l3`「**差额不无声**」；产品侧补 `hit`/`noId` 读数与差额说明（纪律 19/54） |

> **本批新增纪律 90–96**（全文见锚点 §八）：90 **鼠标失效第一查窗口可见性**（只认 `visibilityState`；`hasFocus()` 会骗人）· 91 **输入派发延迟是可测常数**（~1s ⇒ 禁止固定 `sleep` 后读状态）· 92 **判据必须用产品自己的口径**（自编属性 ⇒ **空真绿**）· 93 **改判据先问反例**（尤其同名变量语义被改过）· 94 **前提缺失 ≠ 产品坏**（实体化 → 可分辨跳过 → INVALID）· 95 **动画未播完不量几何**（`duration` ≠ 墙上时钟）· 96 **兜底会盖住真因**（兜底命中要计数；优先修前提）。
> 破坏性校准 2 次**精确命中**：① `data-reason` 写死 ⇒ 恰红 `DL-2e`；② `DSH_FOCUS_CALIBRATE=hidden` ⇒ 恰红 `NS-0v` + 早退零消耗（日志里 `hasFocus: true` 与 `visibility: hidden` **并存**，完美复现那个骗人指标）。

### 闸门纠错台账（六）（2026-09-17 · 第二十五轮 · 落实建议 + 跨重启连跑 + 项目收尾）

> 本轮对象：落实 `18-…-20260917.md` §八 的 **U8/U9/O1/O2/O3/O4** 六条 + 项目收尾。
> **产品源码改动 3 件**（本轮**并非零改动** —— 收尾取证时更正）：`src/logic/split-dimensions.js` **26,338 → 27,331 B**（`noiseReasonOf()` 新增 **⑤·5「纯数字/符号串」边界规则**，由 **O3 语料新增 C12** 驱动）· `src/logic/key-files.js`（`gen-key-files` 重生成，**字节同、内容变**）· 连带产物 `lib/client.js` **1,856,801 → 1,857,821 B** / 指纹 **`135a575c1fc6ea43`**。**取证**：`baseline-check` 漂移 3 文件 + mtime（09:35:58 → 09:36:13）+ `test-split-dimensions` **87 → 92**。⚠️ 遗留 nit（本轮不修）：⑤·5 的**代码注释**把归因写成「第二十四轮真机驱动新增」，实为**本轮 O3 驱动**；改注释会连带重建/重装/重封（使已验产物失效）⇒ **不改代码**，以锚点 §7.20 更正为准。
> 结果：跨重启连跑 **6/6 轮 29/29**（含 **2 次冷启动**）· 离线全量 **59 套 / PASS 46 / FAIL 0** · `verify-novel-split` **108/108** · `verify-mindmap` **103/103 零跳过** · `verify-director-logic` **28/29**（唯一红 = **U10**，见下）。
> 🔴 **新共性 ⑨**：**"环境失败"与"产品失败"同形** —— 本轮 15 轮 INVALID 的真因是 `taskkill` 之后端口被**幽灵**占着；而"判据假红"的真因是**判据的生命周期与产品数据的生命周期不一致**。两者都不是产品坏。

| # | 套件 / 断言 | 旧写法 | 错在哪 | 新判据 |
|:-:|:-----|:-------|:-------|:-------|
| 1 | 跨重启停机（工具侧） | 以 `taskkill` 的**文案**当结论 | 🔴 报"成功"后**端口没放**（幽灵）⇒ 新实例 `bind()` 失败（`0x2740` / `Cannot start http server for devtools`）⇒ **进程活着、界面也起、唯独 CDP 永连不上** ⇒ 15 轮全 INVALID。中文输出 utf8 解码是**乱码**，看文案会假成功（纪律 61/76） | `stopHarness()` **验证式停机**（进程表为空 **∩** 端口可 `bind`，轮询到干净；`taskkill` 退出码 **128** = 本来就干净）+ `ensureHarness()` **端口顺移** + 🔴 **新实例 stdout 必须留痕**（`stdio:"ignore"` 等于扔证词）（纪律 97） |
| 2 | `verify-director-logic` 起点 | 把"元素还没出现"当"点击失败"（8 轮 ≈5.7s） | 🔴 冷启动实测 **58–120s** 才有 `[role="tab"]` 环 ⇒ 真正的可点窗口**一次都没等到**（首轮 **8/29**、21 条红全由这一个起点问题级联）。且用内部口"凑"出的会话视图里「总监」**已 `aria-selected`** 但宿主**始终不渲染视图内容**（点它/切走切回/`Page.reload` 全无效，**0 console、0 异常**） | 起点自举抽成**唯一一份** `_cdp-startup.mjs`（`ensureDirectorPage()`）· **无环时只等不点** · 真实 UI 侧栏自举 · 只认 `dp-root`；失败判 **INVALID**（纪律 98） |
| 3 | `RU-4b`（连跑判据） | 「全程累计新建 ≤ 15」 | 🔴 **判据的生命周期 ≠ 产品数据的生命周期** —— 复用索引在 `localStorage`，app 端口每次变 ⇒ 按**含端口 origin** 隔离 ⇒ 冷启动后索引为空 ⇒ **新实例内首次派发必然重建** ⇒ 跨重启跑程**必然假红**（实测 19 > 15） | 改按**段**取（段 = 两次重启之间）+ **打印前提**（段数/端口/每段预算）；**改口径必须配植入缺陷校准**（S2/S4 精红、S3 正对照绿）（纪律 99） |
| 4 | `RU-4a`（连跑判据） | 按"用例 id 是否首见"过滤 | 🔴 **空真**：`N ≤ 语料条数` 时每个用例只出现一次 ⇒ `lateCreate` 恒空 ⇒ 判据**恒真**（纪律 23） | 改按"**段内该性质是否首见**"过滤 ⇒ `N=6` 下 3 轮受约束（[2,3,6]） |
| 5 | `DL-5a` / `DL-5b` / `DL-7a` | 失败只写「超预算未达成」 | 🔴 **读数没落地**（纪律 54）：无法区分"产品没更新读数"与"**整块 UI 已不在 DOM**"—— 实测真形态正是后者（`[D] 回收点击 … {"why":"不在 DOM"}`，「📥 回收」与 `dp-r5-msg` 一起消失） | 新增 `UI_STATE` 诊断（`root` / `r5view` / `msgTab` / `collect` / `ring` / `sel` / `deliver-mode`）并进 **C/D 两段失败证据**；健康基线实测 `{"root":true,"r5view":"director","msgTab":true,"collect":true,"sel":["总监"]}` |
| 6 | `HT-7`（新套件） | 段内多占一个端口却仍期望 `start+1` | 🟠 **判据期望写错**（自造红）—— 证据行 `start 被占 ⇒ 19322 期望 19321` 一眼可见"19321 当时确实被占"，**先查测试**（纪律 98 的姊妹条：报红先问"我的期望对不对"） | 按证据改正；B 组 **4/4** 绿 |
| 7 | `repeat-director-logic` **自造** | `let port = BASE_PORT` 写成**轮内局部变量** | 🔴 非重启轮被重置回基准端口 ⇒ 去连没人监听的 9222 ⇒ **306s INVALID**（"没跑成"伪装成"跑不动"） | 提到循环外做**段级共享** + 开跑前 `cdpAlive` **有界 20s 守卫**（超时记 INVALID + **可分辨原因**，不再空等） |
| 8 | `RU-2` / `RU-3` **自造** | INVALID 轮混进产品判据 | 🔴 "前提不成立"的轮次把产品判据一起染红 ⇒ **真失败被淹掉**（纪律 58） | 改用 `validRows = rows.filter(x => !x.invalid)` + 加 `validRows.length > 0` 守卫（防"全 INVALID 却报绿"） |
| 9 | `gen-project-index.mjs`（**收尾时踩到**） | 传 `--check` 当**只读对账**用 | 🔴 该脚本**完全不处理 `process.argv`** ⇒ `--check` 被**静默忽略并照常写盘**，而 stdout 全是「✅ 索引已生成」⇒ **"以为在做只读检查、实际写了"且不可分辨**（纪律 34「脚本不认参数 `exit 2`」的实例） | 加**参数契约守卫**：`ARGV.length` 非 0 ⇒ 打印用法 + 未知参数 ⇒ **`exit 2`**。**正负对照**：负例 `--check` ⇒ **exit 2 且 mtime 不变（未写盘）** · 正例无参数 ⇒ exit 0 且写盘 ✅ |

> **本批新增纪律 97–100**（全文见锚点 §八）：97 **`taskkill` 成功 ≠ 端口已释放**（停机判据是"能不能 `bind`"；新实例输出必须留痕）· 98 **「环还没出现」≠「点击失败」**（起点自举只允许一份实现，无环时**只等不点**）· 99 **判据的生命周期必须与产品数据的生命周期一致**（否则是口径错而非产品错；改口径必配植入缺陷校准）· 100 **起点失败必须可分辨归因，执行链上的模块一律不许抛穿**（离线可用**虚拟时钟**把长预算压到毫秒级）。
> 🔴 **纪律 91 就地更正**：旧记「派发延迟 974–1093 ms」是**在 `hidden` 前提下测的**；前提成立时实测 **58–76 ms**（中位 74，相差 **~15×**）⇒ 数值必须**按前提分开记**。
> 破坏性校准 **4 场景**：S1 原样绿（重见轮 [2,3,6] **非空真**）· S2 植入「段内重见仍新建」**精红** · S3 抹掉冷启动重建 → **仍绿**（正对照）· S4 植入「每轮都新建」→ **三判据全红**。

### 闸门纠错台账（七）（2026-09-17 · 第四轮 · **测试流程与增量测试矩阵**）

> 本轮对象：用户裁定「**测试太费 token —— 很多测试重复跑了很多遍，有些是反复打开关闭软件**」⇒
> 建「增量取集判据 + 批量化 + 免重跑台账」三件套（`_test-ledger.mjs` / `test-plan.mjs` / `run-live.mjs`），
> 流程文档 `docs/40-测试质量/22-…-20260917.md`，技能 `incremental-test-planning`，`execution-standards` **V3.18.0**。
> 结果：离线批次 `node scripts/test-plan.mjs --run --record` ⇒ **OK=40 / 非 0=0 全绿**。

| # | 套件 / 判据 | 旧写法 | 错在哪 | 新判据 |
|:-:|:-----|:-------|:-------|:-------|
| 1 | `lint-syntax` + `lint-cdp-templates`（**同一根因连爆两红**） | `verify-novel-split.mjs` 第 703 行**注释**里写：`禁止反引号与 ${}` | 🔴 **坑 28 的新变种**：该注释位于一个**模板字符串**内部 —— 反引号会**提前终止**模板串（已知），而**字面的 `${` 会在模板串内开启插值** ⇒ `node --check` 报 `Unexpected token '}'`，**且报错行号指向这条"提醒别人别犯"的注释本身**（自我指涉） | 改写为「美元花括号」。🔴 教训升级：模板串内注释的**禁用集 = 反引号 ∪ `${`**（`lint-cdp-templates` 已能拦，本轮即是它抓到的） |
| 2 | `verify-dialog` exit **2（INVALID）** | 批量执行器判据 `if (f === "verify-dialog.mjs") args.push("--import", …)`（**手工特例**）→ 换我写的正则 `from\s+["']\.\.\/src\/` | 🔴 **判据写窄**：只认一种 import 写法 ⇒ 该套件**漏加平台桩钩子** ⇒ 裸跑崩在 `Cannot find package 'react'`，**读起来像产品坏了**（纪律 94「前提缺失 ≠ 产品坏」） | 收敛到**共享唯一真相源** `_test-ledger.needsPlatformStub()`（**过包含**：任何位置出现 `src/` 引用即给桩 —— 多给只是空钩子，**漏给才造假红**） |
| 3 | `test-plan.mjs` 桩判据（**修第 2 条的代码本身又错**） | `needsPlatformStub(suitesByName.get(p.name) \|\| { text: text })` | 🔴 **"有兜底"是假象**：`suites` 在 `map` 时已**丢弃 `text` 字段**，而 Map 里**永远有**该键 ⇒ `\|\|` 右支**永不生效** ⇒ 判据恒 `false` ⇒ INVALID 复发（同一根因的第 2 次，**修法本身是新缺陷**） | 直接用循环里刚读到的 `text`；删掉无用的 `suitesByName`。**通则**：兜底分支若依赖"取不到值"，必须先证明它**真的会取不到**（此处它永远取得到，只是值是 `undefined`） |

> 🔴 **本轮新共性 ⑩**：**判据的"过包含"与"写窄"不对称** —— 写窄 ⇒ **假红**（有人看，成本高但可发现）；
> 而**假跳过/空真** ⇒ **静默**（没人看）。所以**判据一律偏保守侧**（宁可多跑、多给环境钩子）。
> 配套写进 22 号文 §四 反模式 3 / §十 第 4 条（假跳过比假红危险）。
> 🔴 **附带实证**：`verify-novel-split.mjs` 的那处注释是我**修它的时候自己第二次踩**（第一次写了反引号）——
> **同一个坑在同一个位置上连踩两次**，说明"记住坑"不够，得让**闸门**去拦（`lint-cdp-templates` 做到了）。

### 闸门纠错台账（八）（2026-09-17 · 第五轮 · **索引体系重构 + 工作区清理**）

> 本轮对象：用户裁定「**更新去处理好所有的文档和脚本的索引，清理多余的内容**」。
> 结果：5 份索引全部对齐（新增 `refresh-index.mjs` / `verify-index.mjs`），手工索引瘦身为入口，
> 清理 **12 项 / 227 文件 / 6.41 MB**（移入根 `.trash-20260917-1330/`）。
> 🔴 **根因不是"忘了更新"，是"手工清单在结构上必然过期"** —— 见下第 1 条。

| # | 对象 | 旧写法 / 旧状态 | 错在哪 | 新做法 |
|:-:|:-----|:---------------|:-------|:-------|
| 1 | `05-项目大索引.md` · `INDEX.md`（**手工索引**） | 逐条列举全部文档（`05` 声称 docs **96 篇**；`INDEX.md` 停在 2026-09-11） | 🔴 **手工清单必然过期**：实测已是 **115 篇**，`05` 的编号表只排到 `20-30`、`INDEX.md` 的状态停在"V9.4/V9.5 待重启验证"。**过期的手工索引比没有索引更坏** —— 它会被当成现状引用 | 两份手工索引**瘦身为入口**：只放「接手路径 / 目标包 / 脚本地图（分类而非清单）/ 人工标注的关键文档」，全量交给生成器；并写明"数字以生成器输出为准，本表不追" |
| 2 | `gen-project-index.mjs` 的自指 | 索引把**自己**也收录进「文档索引」（记录自己的行数 / 字节） | 🔴 **自指 ⇒ 永远滞后一版**：本次生成写入的是**上次读到的自己**的行数；且"改一次要跑两次才收敛"，`--check` 随之可能假红（无信息价值，读者不需要知道索引自己多少行） | 新增 `EXCLUDE_FILES` 排除自身；文档数 150 → **149** |
| 3 | `gen-project-index.mjs` 的参数契约 | 第 25 轮已发现它**不处理 `process.argv`**（传 `--check` 被静默忽略并照常写盘），当时只做了"拒绝未知参数" | **只有"拒绝"没有"支持"** ⇒ 无法纳入自动化对账 ⇒ 它成为**三份索引里唯一没有闸门的** | 补 `--check`；🔴 比对口径不能用逐字节（头部**生成时间**每次必不同，纪律 63 的反面） ⇒ 比**去掉时间戳行后的正文哈希** |
| 4 | `gen-source-map.mjs` 的**未文档化副作用** | 被当作"只读的索引生成器" | 🔴 它**会向 `src/**` 每个文件顶部注入 `@map` 血缘块** ⇒ **重建索引会改动源码**。不知情者跑完只重建了索引、没重建产物 ⇒ 产物与 src 不一致 | 写进 `refresh-index.mjs` 头注与 `05`；提供 **`--safe`**（跳过它，并行开发时用）；并在跑完后主动提示"必须接 `gen-key-files → build → plugin-install`" |
| 5 | 根目录 3 个 0 字节文件 | `任何对话、任何` / `本文件是` / `落死日期：2026-09-12` | 🔴 文件名里是 **私用区字符 U+F02A**（×2 当作 `**`）—— 典型的 **markdown 粘贴 / 重定向事故**：把带 `**加粗**` 的文本当成了路径。它们从未被 git 跟踪，也无人引用 | 移入回收站；🔴 新增判据：**根目录出现「0 字节 + 私用区字符」文件即垃圾**（可脚本化识别，无需逐个猜名字） |
| 6 | 死产物 / 退役生成器 | `scripts/dsh_docs_index.txt`（**1.09 MB** / 2026-09-03）· `scripts/gen-docs-index.ps1` | 旧链路是「`gen-docs-index.ps1` 用 IndexOf 手术把 `const DSH_DOCS_INDEX` **注入宿主** `workspace/.../client.js`」——**既改不到宿主也不该改**（`workspace/**` 不可回滚）。A14 剥离后真相源已是外部资源 `assets/docs-index.json`（由 `gen-docs-index.mjs` 产出） | 双双移入回收站；在 `05` 与 `INDEX.md` 注明"已删除，旧文档中的提及属历史记录，不要去找" |

| 7 | **删除引入的行为变化**（差点漏掉） | 删 `gen-docs-index.ps1` 时，`grep` 只搜了 `*.mjs` / `*.md` ⇒ 判为"无活引用" | 🔴 **`scripts/apply.ps1` 第 16 行仍在调用它**（`$genScript = Join-Path $PSScriptRoot "gen-docs-index.ps1"`）—— 好在有 `if (Test-Path $genScript)` 兜着**不会崩**，但**文件缺失即静默跳过** ⇒ **无声降级**（纪律 18/19「跳过比红更危险」「降级可以，无声不行」）。**更糟的是它连 WARN 都不打** —— 调用者会以为索引已生成 | 把该段改为**显式 WARN**（写明"已退役 + 替代物 + 为什么"），并留历史注释；`apply.ps1` 语法经 `[Parser]::ParseFile` 复验 `SYNTAX_OK`。🔴 **通则**：**删文件前查引用者要覆盖全部脚本类型**（`.ps1` / `.py` / `.json` / `.yml`，不只是 `.mjs`/`.md`）；且 **`Test-Path` 包裹的调用 = 天然的静默跳过**，删被调物时必须回头看它 |

> 🔴 **本轮新共性 ⑪**：**"手工维护"与"必然过期"是同一件事** —— 凡是机器能扫出来的事实（有多少文件、
> 叫什么名字、多少行），**不要写进人工文档**；人工文档只留机器**推不出**的东西（判断、坐标、纪律、为什么）。
> 判据：**改一次代码需要人工同步几处文档？** 答案 > 0 的地方，就是下次必然过期的地方。
>
> 🔴 **自证 —— 写下这条纪律后 5 分钟内我又犯了**：重写 `05` 时我在索引表里写死了
> 「源码 **89** / 脚本 **214** / 文档 **149**」，紧接着重建索引就变成 **215**（并行会话新增了脚本）。
> **处置不是"下次记得改"，而是根本不写** —— `05` §〇 / §四 / §五 与 `INDEX.md` 里
> **所有会变的计数已全部去掉**，改为「覆盖范围描述 + 指路生成器」。
> **这才是纪律 103 的正确落地**（"写上去但记得更新"还是会忘 —— 因为遗忘是系统性的，不是偶发的）。
> 🔴 🔴 **新共性 ⑫**：**重建索引会改源码**（第 4 条）—— "生成物"与"输入"的边界，
> **必须读生成器代码确认，不能按名字猜**（`gen-` 前缀不等于只读）。

---

## 一、启动会话必须加载的必读集合

在执行任何任务之前，必须按顺序读取以下文件：

| 顺序 | 文件 | 路径 | 说明 |
|:----:|------|------|------|
| 1 | 项目 AGENTS.md（本文件） | `AGENTS.md` | 最高优先级，强制全读 |
| 2 | **★ 当前基线 · 落死锚点** | `docs/00-统筹入口/10-当前基线-落死锚点-V16.md` | **项目现状唯一权威描述**：代码地图 / 三方映射 / 冻结契约 / 闸门 / 缺陷台账 |
| 3 | workspace 全局 AGENTS.md | `D:\workspace\AGENTS.md` | 全局约束+行为逻辑框架V1.2 |
| 4 | 总统筹入口文档 | `docs/00-统筹入口/00-总统筹入口文档.md` | 项目定位、特性、进度 |
| 5 | AI对话执行约束规范 | `docs/00-统筹入口/AI对话执行约束规范.md` | 10步链路+6条约束+25条行为逻辑V1.2 |
| 6 | AI规范执行保障机制 | `docs/00-统筹入口/AI规范执行保障机制.md` | 四层保障：加载协议+检查点+触发表+违规自检 |
| 7 | AI任务审核标准通用模板 | `docs/00-统筹入口/AI任务审核标准通用模板.md` | 审核标准（只读检查清单章节） |
| 8 | 项目大索引 | `docs/00-统筹入口/05-项目大索引.md` | 文档定位指南 |
| 9 | 项目记忆 | `_memory/MEMORY.md` | 历史决策、ADR、风险注意事项 |

**未加载必读集合之前，禁止执行任何写操作。**

**最小读取豁免**：仅"看一眼现状"类任务，可只读 #2 + 跑一次基线校验，不必全量加载。

---

## 二、项目特殊性（Harness 客户端修改的特殊约束）

> 🔴 **2026-09-12 起，主管道是「官方 client 插件」**（`dsh-director-plugin/`）。
> 下列 §2.2 描述的 `workspace/@deepseek-ai/* + apply.ps1` 属**历史通道**，仅排查旧问题时参考。
> 插件通道的三处安装点、重启陷阱、冻结契约见 `docs/00-统筹入口/10-当前基线-落死锚点-V16.md` §二 与 §五。

### 2.1 关键铁律（不可违反）

1. **主管道** — 功能改动落在 `dsh-director-plugin/src/**`，构建产物 `lib/client.js` 由 `build/build.mjs` 生成
2. **不直接修改原安装目录** — 装机走 `scripts/plugin-install.mjs --apply`（幂等 + 写后逐文件回读校验）
3. **改完必须重启 Harness** — 插件在 boot 时装载，不重启不生效（启动命令与 `ELECTRON_RUN_AS_NODE` 陷阱见锚点文档 §二）
4. **`original/` 是只读复原源** — 除非重新备份，否则不要修改 `original/` 目录
5. **所有操作有日志** — `logs/dsh-mod-YYYYMMDD.log`，重要操作必须记录
6. **契约不可改名** — localStorage key / DB 版本与 store 名 / cookie 前缀 / DOM 锚点 / view order，
   改名即老用户数据全丢（R5 冻结，见锚点文档 §五）
7. **视觉改动必须截图复验** — 逻辑断言全绿 ≠ 界面可用；锚点文档 §七 7.2 的 5 个缺陷全是在"全绿"下靠截图发现的
8. **允许新开版本号，禁止的是「架构重写」** — 版本可增量新建（V17 / V18 各成文档没问题）；真正禁止的是把既有设计推倒重来（用户原话：「主要是相当于三个版本我白做一样」）。基线指纹的演进仍以就地更新锚点文档 + `--write` 重封为准

### 2.2 插件通道（现行）与历史通道对照

| 项 | **现行：官方 client 插件** | 历史：改宿主编译产物 |
|:---|:--------------------------|:---------------------|
| 改哪里 | `dsh-director-plugin/src/**` | `workspace/@deepseek-ai/<包名>/lib/*.js` |
| 怎么装机 | `node scripts/plugin-install.mjs --apply` | `.\apply.ps1`（快照 + Junction 替换 + 清缓存） |
| 回滚 | `--uninstall` / `--purge`（外科式只删本插件 entry） | `.\restore.ps1` |
| 三处安装点 | ① 实体包 `<Harness>/resources/host/node_modules/@deepseek-ai/dsh-director-plugin/` ② `~/.dsh/profiles/node_modules/...`（junction）③ `~/.dsh/profiles/web/cordis.patch.yml`（entry `deepseek-ai.director`） | Junction 替换目标包 lib/ |
| 生效条件 | 重启 Harness | 清 Cache/Code Cache/GPUCache/**blob_storage**/Network 后重启 |
| ⚠️ 清缓存禁忌 | **不得删 `Network`** — 那是 Chromium 的 cookie 存储，内含 `dsh_director_*` 持久化 cookie，删即总监状态丢失 | — |

**技术栈**：JavaScript（ESM）+ Node 22 / Electron / react（平台外置）/ IndexedDB + localStorage + Cookie。

### 2.3 标准工作流（现行）

```
改一处功能的完整闭环：
1. node scripts/baseline-check.mjs              # 先确认没在读一份过期的"现状"
2. node scripts/refresh-index.mjs --check       # 索引三份对账（漂移 = 读到的是错的事实）
3. （界面改动）先改设计稿 docs/50-信息中心/V16-设计图·需求图·交互逻辑.html
4. 改 dsh-director-plugin/src/**
5. node scripts/gen-key-files.mjs               # 改 src 必跑（它会改 src/logic/key-files.js ⇒ 需重建）
6. node scripts/lint-syntax.mjs && node scripts/lint-undefined-symbols.mjs
   node scripts/lint-cdp-templates.mjs && node scripts/lint-platform-stub.mjs
   node scripts/verify-bundle.mjs
7. node scripts/test-plan.mjs --explain         # 先规划：该跑哪些 / 为什么（只读，先看再跑）
   node scripts/test-plan.mjs --run --record    # 离线批：一条命令跑完（免逐套件重复启停）
8. node build/build.mjs && node scripts/check-stale-build.mjs && node scripts/plugin-install.mjs --apply
9. 重启 Harness（后台方式 + env -u ELECTRON_RUN_AS_NODE）
10. node scripts/run-live.mjs <真机套件…>       # 真机整批**只冷启动一次**（免反复开关软件）
11. （视觉改动）node scripts/shot-studio.mjs logs/x.png   # 截图复验
    （设计稿改动）node scripts/verify-design-html.mjs "docs/50-信息中心/V16-设计图·需求图·交互逻辑.html"   # 卫生五项 5/5
12. node scripts/refresh-index.mjs --safe        # 索引重建（--safe：不碰 src；并行开发时必用）
13. 更新 10-当前基线-落死锚点-V16.md → node scripts/baseline-check.mjs --write
```

> 🔴 **索引必须与代码同批交付**：`assets/docs-index.json` 是**运行时注入给前端**的真相源，
> 它漂移 ⇒ 总监看到的文档不是最新的，而界面上**看不出来**（不报错、不缺页，只是内容旧）。
> 故第 2 步对账、第 12 步重建都不可省；改脚本 / 改文档 / 改 `src` 文件头 JSDoc 首行都会使其漂移。
> 🔴 **索引重建可能改动源码**：`gen-source-map.mjs` 会向 `src/**` 顶部注入 `@map` 血缘块 ⇒
> 跑完（非 `--safe`）**必须**接 `gen-key-files → build → plugin-install`，否则产物与 src 不一致。

### 2.4 风险提示

- 宿主**无 React 错误边界** ⇒ 单层崩溃卸载整棵 Shell 根 ⇒ 四个浮层全灭；故每层裹 `SafeLayer`，新增浮层必须照做
- 持久化的"打开态"是**自锁死**放大器 ⇒ 凡 `open && 数据为 null` 的组合必须判空
- 构建模板字符串内**禁用反引号**（构建期被当模板字面量求值）
- 写盘必须显式 LF（Windows 文本模式会转 CRLF，导致假重写 diff）
- Harness 端口每次启动变化 ⇒ localStorage 按 origin 分区 ⇒ 主存有丢失风险，设计图有 IndexedDB 冷备
- 修改编译产物风险高，必须仔细测试；三 tab 共存（总监/对话/轨迹），修改时需确保不破坏原功能

---

## 三、10步强制执行链路（引用全局）

每次任务必须按以下链路执行，不得跳过：

1. **解表达** — 拆解用户原始表达，区分核心诉求/附加约束/隐含需求
2. **提炼诉求** — 用一句话总结用户真正要什么
3. **检索标准** — 检查项目规范文档、历史方案、踩坑记录
4. **推导方案** — 基于规范和历史，推导最优执行方案
5. **匹配规范** — 对照项目规范文档，确认方案不违反任何约束（含本文件§二的特殊约束）
6. **生成任务清单** — 输出明确的、可追踪的任务步骤列表
7. **文档先行 + 前置审核** — 输出方案文档，等待用户确认（涉及代码修改时强制）
8. **落地执行** — 按审核通过的方案执行
9. **后置审核** — 执行完成后，对照审核标准自检
10. **输出交付 + 更新台账** — 交付产物，同时更新索引/任务清单/工作快照/记忆

**详细链路定义、检查点规则、行为逻辑触发表见**：`docs/00-统筹入口/AI对话执行约束规范.md` §二 和 `docs/00-统筹入口/AI规范执行保障机制.md` §三

---

## 四、6条强制约束（引用全局）

1. **举例/同类/等价/可选范围拓展规则** — 用户给出举例时必须合理拓展
2. **提示词库使用规则** — 理解意图后强制执行检索提示词库
3. **文档先行与前置审核规则** — 涉及代码修改第一步输出文档，禁止直接写代码
4. **后置审核机制** — 交付物必须对照审核标准校验
5. **文档三类分类与复用规则** — 执行中/正在使用/使用完成，禁止反复新建删除
6. **最小读取规则** — 按需求类型只读对应必读文档集，禁止全量加载

**详细定义见**：`docs/00-统筹入口/AI对话执行约束规范.md` §三

---

## 五、AI 行为逻辑框架（7维度25条 · V1.2，引用全局）

本项目遵循 workspace 全局 AGENTS.md 中定义的 AI 行为逻辑框架 V1.2，与"10步执行链路"（流程）和"6条强制约束"（红线）互补，构成完整行为体系。

### 7个维度概览

| 维度 | 核心 | 条数 |
|------|------|:----:|
| **思维方式 Think** | 第一性原理/能力前置评估/最优解平衡/元认知监控 | 4条 |
| **问题处理 Solve** | 三次失败换方案/根因优先/风险前置评估/多方案对比 | 4条 |
| **执行效率 Execute** | 任务原子化拆解/并行串行判断/进度及时反馈/资源最小消耗 | 4条 |
| **沟通协作 Communicate** | 先问不猜/先确认不擅决/反馈即承认即改/期望透明管理 | 4条 |
| **质量验证 Verify** | 交付前自检/数据事实验证/关键信息交叉验证 | 3条 |
| **环境与安全 Environment** | 网络访问规范(VPN)/安全边界守住/环境问题先诊断 | 3条 |
| **学习进化 Learn** | 经验双向沉淀/反馈驱动进化/能力边界自知 | 3条 |

**完整25条准则（含示例+例外/豁免+操作步骤）详见**：
- `D:\workspace\AGENTS.md` §三（全局完整版本）
- `docs/00-统筹入口/AI对话执行约束规范.md` §四（项目同步版本）
- `docs/00-统筹入口/AI行为逻辑框架审核文档.md`（审核记录）

---

## 六、规范执行保障机制（四层保障，引用全局）

本项目遵循 `AI规范执行保障机制.md` 定义的四层保障机制，确保 AI "只读该读的、执行该执行的、按标准执行"：

| 保障层 | 名称 | 解决的问题 |
|:------:|------|----------|
| 第一层 | 启动加载协议 | "只读应该读的部分" |
| 第二层 | 执行检查点机制 | "执行应该执行的逻辑"（CP1-CP4） |
| 第三层 | 行为逻辑触发与优先级 | "25条准则何时触发" |
| 第四层 | 违规自检与纠正机制 | "如何确保按标准执行" |

**完整机制详见**：`docs/00-统筹入口/AI规范执行保障机制.md`

**本项目特殊检查点**：
- CP2（前置审核）：涉及修改 `lib/client.js` 等编译产物时，必须额外说明修改位置、修改内容、测试方法、回滚方案
- CP4（后置审核）：修改完成后必须验证：①diff 正确 ②apply 成功 ③Harness 重启后功能正常 ④原功能未破坏

---

## 七、项目操作前检查清单

操作本项目之前，必须检查：

| 检查项 | 要求 |
|--------|------|
| 是否已加载必读集合 | 是（见§一，**含 §0 当前基线锚点**） |
| 是否跑过基线校验 | 是（`node scripts/baseline-check.mjs`） |
| 是否理解项目特殊性 | 是（见§二，**主管道=插件通道**） |
| 修改是否在 `dsh-director-plugin/src/` 中进行 | 是，禁止直接修改原安装目录 |
| 装机是否走 plugin-install.mjs | 是（幂等 + 写后逐文件回读校验） |
| 修改后是否重启 Harness | 是（后台方式 + `env -u ELECTRON_RUN_AS_NODE`） |
| 是否触碰冻结契约 | 无（localStorage key / DB 版本 / 锚点 / order 一律不改名） |
| 视觉改动是否截图复验 | 是 |
| 是否准备了回滚方案 | 是（`--uninstall` / `--purge` / git） |
| 用户说的"标准"是否有明确定义 | 没有则必须先询问，禁止自行编造 |

---

## 八、标准目录结构

```
dsh-client-mod/
├── AGENTS.md                 ← 本文件（最高优先级）
├── README.md                 ← 项目说明
├── config.json               ← 配置（原路径、目标包列表、规则）
├── dsh-director-plugin/      ← ★ 主管道：总监驾驶舱插件（现行交付物）
│   ├── src/                  ←   源码（47 个 js / 10,488 行）
│   ├── build/build.mjs       ←   构建（产物 lib/client.js，React 平台外置）
│   ├── lib/client.js         ←   构建产物
│   ├── scripts/              ←   闸门 / 安装器 / 真机验证 / 截图 / 基线指纹
│   ├── docs/                 ←   插件级设计说明（01 迁移明细 … 11 设计稿审核）
│   ├── INSTALL.md            ←   安装权威说明
│   └── BASELINE.lock.json    ←   ★ 落死基线指纹（60 文件 md5 / 1,730,783 B）
├── docs/
│   ├── 00-统筹入口/          ← 核心文档（必读集合）
│   │   └── 10-当前基线-落死锚点-V16.md   ← ★ 项目现状唯一权威描述
│   ├── 10-架构设计/
│   ├── 20-任务文档/
│   ├── 30-开发链路/
│   ├── 40-测试质量/
│   ├── 50-信息中心/          ← 设计稿本体（V16 …含板块 D）
│   ├── 90-历史归档/
│   └── 99-个人沟通/
├── original/@deepseek-ai/    ← 原客户端代码备份（只读，复原源）
├── workspace/@deepseek-ai/   ← 历史通道工作区（现行已不用）
├── patches/  snapshots/      ← 历史通道差异与快照
├── scripts/                  ← 历史通道自动化脚本（backup/apply/restore/…）
├── logs/                     ← 操作日志（按天）
└── _memory/                  ← 项目记忆（MEMORY.md）
```

---

## 九、违规处理

如果发现 AI 违反本约束：
1. 用户可直接指出违规内容
2. AI 必须立即停止当前操作，承认违规
3. 重新按10步链路执行
4. 将违规记录写入 `_memory/MEMORY.md` 或本文件的违规记录 section

### 违规记录

| 日期 | 违规内容 | 纠正措施 |
|------|---------|---------|
| 2026-08-27 | 项目缺少 AGENTS.md，AI 操作时未加载项目级约束 | 创建本 AGENTS.md，后续必须先加载必读集合再执行 |

---

## 十、参考文档

- `D:\workspace\AGENTS.md` — 全局执行约束+行为逻辑框架V1.2
- `docs/00-统筹入口/AI对话执行约束规范.md` — 10步链路+6条约束+25条行为逻辑V1.2
- `docs/00-统筹入口/AI规范执行保障机制.md` — 四层保障机制
- `docs/00-统筹入口/AI行为逻辑框架审核文档.md` — 行为逻辑框架审核记录
- `docs/00-统筹入口/AI任务审核标准通用模板.md` — 审核标准
- `docs/00-统筹入口/AI需求驱动读取范围与最小权限规范.md` — 读取范围规范
- `docs/00-统筹入口/10-当前基线-落死锚点-V16.md` — **项目现状唯一权威描述（V16 落死锚点）**
- `docs/50-信息中心/V16-设计图·需求图·交互逻辑.html` — 设计稿本体（A 高保真 / B 需求图 / C 交互逻辑 / D 设计图工作室）
- `dsh-director-plugin/BASELINE.lock.json` — 落死基线指纹（60 文件 md5 / 1,730,783 B）
- `docs/50-信息中心/01-Harness客户端修改-踩坑记录与快速定位索引.md` — 项目踩坑记录

---

*本文件创建: 2026-08-27（项目初始化+标准整改+行为逻辑框架V1.2同步+保障机制集成）*
*本文件最后更新: 2026-09-13（批次 16 收敛 · 闸门纠错轮 ②）*
> *§〇 基线锚点表同步产物 **964,544 → 964,872 B / 58 模块 / 绑定 474 = 474**；
> 新增「闸门纠错台账（二）」**8 条 + 附带修正 6 项** —— 新共性三条：**尺子量错容器**（五步区是
> `director-messages` 的兄弟节点）、**属性名语义撞车**（`data-dim` 跨两类语义 ⇒ 一个根因连爆 3 红）、
> **桩/尺寸写死**（同一份平台桩抄了两份 ⇒ 必漂移）；并首次抓到**闸门把整个脚本打哑**
> （`verify-dialog` 的 `launcher` 退役 ⇒ null 解引用，其后 100+ 断言静默丢失，**比跳过更隐蔽**）。
> 收益：`verify-dialog` **崩 → 268/268**（找回被吞断言）· `verify-install` **32/33 NO → 60/60 YES**
> · `cdp-click` **22/22 → 70/70** · `cdp-click-dialog` **94/100 → 102/102**；
> 离线 **22 套全绿**，真机五套**仍各连跑 3 次全绿**（flow 67/67 零跳过 · mindmap 100/100 零跳过 ·
> studio 90/90 · gate 5/5 · cdp-verify 39/39）。另**登记未做**：T-PLUG-030「文档上下文」死链路
> （`docs` 是 map 却当数组用 ⇒ 文案恒「共undefined篇文档」+ TypeError 被 `catch {}` 静默吞掉）。*
