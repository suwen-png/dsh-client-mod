# dsh-client-mod — 项目级 AI 执行约束

> **本文件是 dsh-client-mod 项目的最高优先级执行规范。任何 AI 操作本项目时，必须先完整阅读本文件，再按其约束执行。**
> **创建日期**: 2026-08-27
> **项目定位**: DeepSeek Harness 原客户端修改工作区 — **总监驾驶舱插件（含设计图工作室）** + 备份-修改-复原 全链路
> **最后更新**: 2026-09-12（V16 落死：主管道由「改宿主编译产物」切换为「官方 client 插件」）

---

## 〇、当前基线（2026-09-12 落死 · 先读这条）

> **项目现状的唯一权威描述在**：`docs/00-统筹入口/10-当前基线-落死锚点-V16.md`
>
> ### 三件必须知道的事
>
> 1. **主管道已切换**：现在的交付物是**独立插件包 `dsh-director-plugin/`**（走 Harness 官方 client 插件通道），
>    **不是**改宿主编译产物。§二 里描述的 `workspace/@deepseek-ai/... + apply.ps1` 流程属**历史通道**，
>    仅在排查旧问题时参考，**不要再据此施工**。
> 2. **动手前先跑基线校验**：`cd dsh-director-plugin && node scripts/baseline-check.mjs`
>    输出 `IS_PASS: TRUE（漂移=0）` 才能引用基线文档当"现状"；`FALSE` 说明代码已漂移，先对齐。
> 3. **不要新开版本号**。用户明确要求「把这一版落死，我不想再开一版本了」——
>    基线演进 = **就地更新** `10-当前基线-落死锚点-V16.md` + `node scripts/baseline-check.mjs --write` 重新封存，
>    **禁止**新建 V17/V18 之类的新文档。

| 一句话 | 内容 |
|:-------|:-----|
| 交付物 | `dsh-director-plugin/`（产物 `lib/client.js` **964,872 B / 58 模块** · 绑定对账 **474 = 474 / 185 条 import** · `inject = ["slots","sessions"]`） |
| 界面 | 宿主三页签【总监】【对话】【轨迹】+ 三浮层（设计图工作室 / 思维导图 / 总监弹窗）；四处右上角共用同一个**个性化设定**面板（总监页 / 总监弹窗 / 分支导图 / 设计图工作室） |
| 设计稿 | `docs/50-信息中心/V16-设计图·需求图·交互逻辑.html`（A 高保真 / B 需求图 / C 交互逻辑 / **D 设计图工作室** / **E 保存·版本·窗口安全区** / **F 思维导图元素库 18 条总账** / **G 单框控件·拖动·右侧对话·四维流转·质感与个性化**） |
| 基线闸门 | 静态闸门 `TRUE`（阻塞 0 / 提示 16） · CDP 模板 `TRUE` · **平台桩唯一性 `lint-platform-stub` 9/9** · bundle 桩执行 **97/97** · 设计图纯函数 **49/49** · 版本层 **63/63** · 思维导图纯函数 **91/91** · 个性化+四维流转 **87/87** · 分支聚焦 **26/26** · 统筹编排 **33/33** · 设计稿卫生 **49/49** · 数据安全 **42/42** · 批次 1 **98/98** · 批次 6 **66/66** · 批次 7 **66/66** · 批次 8 **93/93** · **弹窗离线 `verify-dialog` 268/268** · 工作室逐交互 **90/90** · 导图逐交互 **100/100** · 弹窗逐交互 `cdp-click-dialog` **102/102** · 真机演练 **68/68** · 逐交互 `cdp-click` **71/71**（含职责配置快照/复原）· 反证 `prove-dup-gate` **红/绿双证** · 构建 **964,872 B / 58 模块 · 474 = 474** · 映射索引幂等 · 指纹 `漂移=0` · 退出码约定 **0 通过 / 1 FAIL / 2 INVALID**（↑真机五套**各连跑 3 次**，且工作室那套要求**载荷逐字一致**） |
| 真机演练 | `scripts/verify-flow.mjs` **67/67 · 零跳过 · 连跑 3 次全绿**（五组原话逐条落点：原生对话框 / 个性化四处 / 单框控件+拖动+连线对账 / 右侧对话首位 / 四维流转跟随切换；**F 段**：总监页同源 + 浮动组 F8 不吃点击 · F9 配色随主题 · F10 位置不漂 · F11 跟随性正负对照）+ CDP 健康度断言 E2 |
| 导图逐交互 | `scripts/verify-mindmap.mjs` **100/100 · 零跳过 · 连跑 3 次全绿**（2026-09-12 由 81/0/5 提升：跳过 5→0 —— 修掉"可见集取错"与"用常数坐标代表空白点"两类假跳过，见下表「闸门纠错」） |
| 构建→装机 7 步 | 闸门 → `gen-source-map.mjs` → `build/build.mjs` → `plugin-install.mjs --apply` → **重启 Harness** → `verify-design-studio.mjs` + `verify-mindmap.mjs` + **`verify-flow.mjs`**（各连跑 3 次） |

> 🔴 **两条最容易踩的测试陷阱**（详见 `docs/00-统筹入口/10-…V16.md` §七 7.5 / §八 13–19）：
> ① 用真实鼠标驱动 e2e 时，坐标必须**视口内且命中测试为自己** —— 画布滚动后第一个节点可能是 **x=-934**，
> 事件打空却像「功能坏了」（改用 `focusVisibleNode()`）；
> ② **不要无条件按 Esc** —— 无菜单时它关闭**整张导图**，会让其后所有断言都在量一个已卸载的 DOM。

### 闸门纠错台账（2026-09-12 · 「断言比产品更容易过期」）

> 本轮同一天内抓到 **5 处"闸门本身写错"**。共性：**旧断言把"现状变了"读成"功能坏了"**，
> 而且全部以**假红或静默跳过**的形态出现 —— 跳过比红更危险（红有人看，跳过没人看）。

| # | 闸门 | 旧写法 | 错在哪 | 新判据 |
|:-:|:-----|:-------|:-------|:-------|
| 1 | `verify-batch1.mjs` | 宿主 `filteredMessages` 命中数 **== 1** | 该断言写于"宿主还是坏的"时期，用**越界引用存在**当健康凭据；宿主补丁落地后命中数变 0 ⇒ 假红 | 两侧**都归零** + 都能找到同一处修正（`state.messages.map((msg) =>`）⇒ 双向一致性 |
| 2 | `verify-batch6.mjs` | `/0\.[6-9]\.0-batch[6-9]/` | `[6-9]` 是**单字符**类，产物到 `0.15.0-batch15` 后 minor/batch 都是两位数 ⇒ 假红 | 解析 `(major,minor,batch)` 做**序比较** + 与源码 `PLUGIN_VERSION` 同源交叉校验 |
| 3 | `verify-bundle.mjs` | `/batch15/` 写死 | 下次升批次必假红 | 「不低于批次 15」单调性闸门 |
| 4 | `verify-batch8.mjs` | ④⑤职责默认**禁用** | `duties.js` 已按文档 §1.2 五步规格改为启用（留痕在先）⇒ 闸门过期 | 五步**都为 true** 且每步**显式带 `enabled`** |
| 5 | `verify-mindmap.mjs` | ①折叠探针不退出聚焦；②"移开鼠标"目标写死 (18,320) | ①聚焦态可见集只剩祖先链（非根有子节点全被排除）⇒ 探针 `usable=0`；②该点在画布滚动后**正好压在节点内**（探针实测 `[-10,291,98,31]`）⇒「工具条应消失」被一个"其实还悬在节点上"的点否掉 | ①段首先**退出聚焦**；②空白点**按当前几何算**（`blankPoint()`），并补「移开前工具条确实在」防平凡真 |

> 跳过 5 → 0 的收益：`verify-mindmap.mjs` **81/0/5 → 100/0/0**（多出 19 条真断言，
> 含 C-M14a–f 右键菜单、C-M15a–c 悬浮工具条、C-M16a Esc 逐层退、C-M8a–d/C-M8q 折叠正负对照）。

### 闸门纠错台账（二）（2026-09-13 · 批次 16 · 又 8 处）

> 同一天隔日再抓 **8 处**。共性升级为三条：**① 尺子量错容器**（量的是兄弟节点）；
> **② 属性名语义撞车**（一个选择器跨两类语义，一个根因连爆数条红）；
> **③ 桩/尺寸写死**（同一份桩抄两份 ⇒ 必漂移）。另外首次抓到**闸门把整个脚本打哑**
> —— 它不再报红，而是**静默崩溃**，比跳过更隐蔽。

| # | 闸门 | 旧写法 | 错在哪 | 新判据 |
|:-:|:-----|:-------|:-------|:-------|
| 1 | `verify-dialog.mjs` | `mounted.launcher.textContent.length >= 1` | `makeLauncher()` 已退役（批次 13 移交 FloatDock）⇒ `mounted.launcher === null`，**null 解引用在 600 行把整个脚本打哑**，其后 100+ 断言（H10b–H12 / I 段 / J 段）**全部丢失且无人知晓** | 契约式断言（`launcher` 显式 `null`、入口改判 `document.getElementById(LAUNCHER_ID) === null`）+ 文件头补写正确用法 `node --import ./scripts/_platform-stub.mjs` |
| 2 | `verify-dialog.mjs` | 插件库版本写死 `=== 1`、store 数写死 `=== 6` | 插件库 v1→v2、store 6→7（**独立编号，不参与宿主 v3 协商**）⇒ 假红 | 关系判据：版本 `>= 1`；store 断言改「含 **6 个必需** store，**允许扩展**」 |
| 3 | `verify-dialog.mjs` | 「head 内样式节点**唯一**」 | 现为 2 个 `<style>`（分屏 + 个性化令牌）⇒ 假红 | 改**按 id 计数**（关心的是「同一 id 不重复注入」，不是标签总数） |
| 4 | `verify-install.mjs` | 内联 react 桩 `{ useCallback, useSyncExternalStore }` | 产物含 `class SafeLayer extends react.Component` ⇒ 桩缺 `Component` 令 factory 抛 `Class extends value undefined is not a constructor or null`，**32/33 NO** 且报错与真实缺陷无关 | 抽 `scripts/_platform-modules.mjs` 作**平台桩唯一真相源**（16 导出），本文件只 `import` ⇒ **60/60 YES** |
| 5 | `verify-bundle.mjs` | 本地第二份内联 react 桩 | **同一个坑的第 2 次**（09-12 已修过一次）⇒ 两份桩必漂移 | 同上：改引用共享桩（97/97 TRUE） |
| 6 | `cdp-click.mjs` | 「五步过程已展示」扫 `director-messages` 内的行 | **尺子量错容器** —— 五步区与 `director-messages` 是**兄弟节点** ⇒ 恒 false | 产品补 `data-testid="director-steps"` 锚点 + 结构断言「**恰好 5 条**」 |
| 7 | `cdp-click.mjs` | 「默认启用项数 **== 3**」 | `duties.js` 已按 03 号文 §1.2 **五步规格**全部启用（同 §台账一 第 4 条，上轮**漏改这一处**）⇒ 假红 | 「五步**全部启用** = 5」 |
| 8 | `cdp-click-dialog.mjs` | 选择器 `[data-dim]` | `data-dim` 被**审核维度**与**四维流转徽标**共用 ⇒ 扫到 10 个元素，**一个根因连爆 3 条红** | 拆命名空间：`data-review-dim`（审核维度）/ `data-flow-dim`（四维流转徽标），产品 3 处 + 闸门 2 处同步 |

> **同批附带修正（不单列行）**：
> ① `cdp-click-dialog` 入口文案判据 —— 入口本是**图标 + 文案**（`◆` + `总监`），与同组
> `🖌 设计图` / `🧠 思维导图` 同构 ⇒ 改语义判据（含「总监」且 ≤ 6 字）；
> ② 同文件 R5 白名单漏登记新键 —— R5 冻结的是「既有键**不得改名**」，**新增是允许的**
> ⇒ 白名单补 `design|personalize|flow`，断言改「冻结键未改名 + 新键已登记」；
> ③ 同文件覆盖审计漏点 `d-personalize`（批次 11/12 新增但**从未被点过**）⇒ 新增 D15b 段
> **真点开 → 回读面板选项 → 关闭**，不靠白名单假称点过；
> ④ `verify-design-doc.mjs` 新增**目标版本自检**（其判据按 V14.1 正文逐字写死，喂 V16 只会一路 ❌
> 却读起来像「新稿退化」）⇒ 非 V14 族判 **INVALID（exit 2，与 FAIL 的 exit 1 区分）** 并指路仓库根
> `verify-design-html.mjs`；⑤ `lint-platform-stub.mjs` **新增闸门**（9/9）并在自检中发现
> **判据污染**（坏样本字面量写在源码、注释里也留了一份）⇒ 样本改**拼接构造**；
> ⑥ `lint-cdp-templates.mjs` 又拦下一次**模板内注释带反引号**（**本坑第 5 次**，见 §踩坑索引 坑28）。

> 本批收益：`verify-dialog` **崩 → 268/268**（找回 100+ 条被吞断言）· `verify-install`
> **32/33 NO → 60/60 YES** · `cdp-click` **22/22 → 70/70** · `cdp-click-dialog` **94/100 → 102/102**。
> 离线 22 套全绿；真机五套仍各连跑 3 次全绿。

#### 台账（二）续 · 同日再抓 3 处（**闸门会持久化改坏产品状态** —— 本批最重的一处）

> 前三处都是"量错/写死/撞车"，这三处的**危害等级更高**：它们**不是报错，而是改坏**。

| # | 闸门 | 现象 | 真因 | 处置 |
|:-:|:-----|:-----|:-----|:-----|
| 9 | `cdp-click.mjs`（I 段 4/6/7/8） | `verify-flow` 的 `G3b`「助手消息含五步结论」报红，正文是`（全部职责已关闭，原文直转）` | I 段是一条**真实写入链**：翻转 5 个职责开关 → 保存本层（写选中节点）→ 向上提交（写父节点）→ 恢复继承**只清会话那一层** ⇒ 「五项全关」被**永久**写进 `__global__` 与 `ws_*` **两级**，且**跨脚本、跨运行活着**（脚本不重载页面），连**用户真实使用总监**都被改坏 | 新增 **I0 快照 / I8r 逐节点还原 + 回读自证**：写回快照里有的、**清掉快照里没有的**（那必然是本段写脏的）、再对账全树 ⇒ `cdp-click` **71/71**。**I8r 独立清掉的 2 个节点与我手工排查完全一致**（= 反证） |
| 10 | `cdp-click.mjs`（I9b/I9c）· `verify-flow.mjs`（G3a/G3b） | 同一条断言**时红时绿**；G3a/G3b 甚至**一红一绿** | 判据是**页面级**的（对整段 `innerText` 做正则），而消息 store **按 sessionId 持久化**、R5 只显示 `slice(-14)` ⇒ 问的是「**历史上**出现过这几个字吗」。`I9b` 的 `done: /总监分析/` **立即**为真 ⇒ 轮询提前退出 ⇒ `I9c` 在五步没算完时读到 `lastSteps === null` ⇒ **rows=0** | 一律改**跑程内判据**：读 `dp-dir-msg` 的**最后一条**；轮询条件加「本轮才会出现」的信号（消息**行数净增 ≥ 2** + `director-steps` 恰好 5 行）⇒ `verify-flow` **68/68 零跳过** |
| 11 | `verify-dialog.mjs` | 裸跑崩在 `Cannot find package 'react'`，栈里看不出"只是少了 `--import`" | `react` 按 **ADR-001** 是平台冻结模块、本仓库**不装 `node_modules`** ⇒ 离线脚本必须带解析钩子 | 动态导入包 `try/catch` ⇒ 命中即打印**可复制的正确命令**并 `exit 2`（**INVALID ≠ FAIL**）。顺手钉死退出码约定：**0 通过 / 1 FAIL / 2 INVALID**（`verify-design-doc` 的目标版本自检同此约定） |

> **本轮新增的两条通则**（已写入 `dsh-director-plugin` 侧的纪律 36–40 与 §八）：
> ① **凡"点了会写盘/落库"的闸门段，都要快照 + 还原 + 还原断言** —— 否则闸门就是产品的**破坏者**，
> 而且它改坏的东西会伪装成**下一个闸门的产品缺陷**（红在 `verify-flow`，根在 `cdp-click`）。
> ② **判据一律"跑程内"** —— 只要被测面是**持久化/累积**的（消息、日志、历史），
> "页面上有没有这几个字"就既可能假绿也可能假红，且**两个方向会同时出现在相邻两条断言上**。


---

## 一、启动会话必须加载的必读集合

在执行任何任务之前，必须按顺序读取以下文件：

| 顺序 | 文件 | 路径 | 说明 |
|:----:|------|------|------|
| 1 | 项目 AGENTS.md（本文件） | `AGENTS.md` | 最高优先级，强制全读 |
| 2 | **★ 当前基线 · 落死锚点** | `docs/00-统筹入口/10-当前基线-落死锚点-V16.md` | **项目现状唯一权威描述**：代码地图 / 三方映射 / 冻结契约 / 闸门 / 缺陷台账 |
| 3 | workspace 全局 AGENTS.md | `D:\workspace\AGENTS.md` | 全局约束+行为逻辑框架V1.2 |
| 4 | 总统筹入口文档 | `docs/00-统筹入口/00-总统筹入口文档.md` | 项目定位、特性、进度 |
| 5 | AI对话执行约束规范 | `docs/00-统筹入口/AI对话执行约束规范.md` | 10步链路+6条约束+25条行为逻辑V1.2 |
| 6 | AI规范执行保障机制 | `docs/00-统筹入口/AI规范执行保障机制.md` | 四层保障：加载协议+检查点+触发表+违规自检 |
| 7 | AI任务审核标准通用模板 | `docs/00-统筹入口/AI任务审核标准通用模板.md` | 审核标准（只读检查清单章节） |
| 8 | 项目大索引 | `docs/00-统筹入口/05-项目大索引.md` | 文档定位指南 |
| 9 | 项目记忆 | `_memory/MEMORY.md` | 历史决策、ADR、风险注意事项 |

**未加载必读集合之前，禁止执行任何写操作。**

**最小读取豁免**：仅"看一眼现状"类任务，可只读 #2 + 跑一次基线校验，不必全量加载。

---

## 二、项目特殊性（Harness 客户端修改的特殊约束）

> 🔴 **2026-09-12 起，主管道是「官方 client 插件」**（`dsh-director-plugin/`）。
> 下列 §2.2 描述的 `workspace/@deepseek-ai/* + apply.ps1` 属**历史通道**，仅排查旧问题时参考。
> 插件通道的三处安装点、重启陷阱、冻结契约见 `docs/00-统筹入口/10-当前基线-落死锚点-V16.md` §二 与 §五。

### 2.1 关键铁律（不可违反）

1. **主管道** — 功能改动落在 `dsh-director-plugin/src/**`，构建产物 `lib/client.js` 由 `build/build.mjs` 生成
2. **不直接修改原安装目录** — 装机走 `scripts/plugin-install.mjs --apply`（幂等 + 写后逐文件回读校验）
3. **改完必须重启 Harness** — 插件在 boot 时装载，不重启不生效（启动命令与 `ELECTRON_RUN_AS_NODE` 陷阱见锚点文档 §二）
4. **`original/` 是只读复原源** — 除非重新备份，否则不要修改 `original/` 目录
5. **所有操作有日志** — `logs/dsh-mod-YYYYMMDD.log`，重要操作必须记录
6. **契约不可改名** — localStorage key / DB 版本与 store 名 / cookie 前缀 / DOM 锚点 / view order，
   改名即老用户数据全丢（R5 冻结，见锚点文档 §五）
7. **视觉改动必须截图复验** — 逻辑断言全绿 ≠ 界面可用；锚点文档 §七 7.2 的 5 个缺陷全是在"全绿"下靠截图发现的
8. **不新开版本号** — 基线演进就地更新，禁止新建 V17+ 文档

### 2.2 插件通道（现行）与历史通道对照

| 项 | **现行：官方 client 插件** | 历史：改宿主编译产物 |
|:---|:--------------------------|:---------------------|
| 改哪里 | `dsh-director-plugin/src/**` | `workspace/@deepseek-ai/<包名>/lib/*.js` |
| 怎么装机 | `node scripts/plugin-install.mjs --apply` | `.\apply.ps1`（快照 + Junction 替换 + 清缓存） |
| 回滚 | `--uninstall` / `--purge`（外科式只删本插件 entry） | `.\restore.ps1` |
| 三处安装点 | ① 实体包 `<Harness>/resources/host/node_modules/@deepseek-ai/dsh-director-plugin/` ② `~/.dsh/profiles/node_modules/...`（junction）③ `~/.dsh/profiles/web/cordis.patch.yml`（entry `deepseek-ai.director`） | Junction 替换目标包 lib/ |
| 生效条件 | 重启 Harness | 清 Cache/Code Cache/GPUCache/**blob_storage**/Network 后重启 |
| ⚠️ 清缓存禁忌 | **不得删 `Network`** — 那是 Chromium 的 cookie 存储，内含 `dsh_director_*` 持久化 cookie，删即总监状态丢失 | — |

**技术栈**：JavaScript（ESM）+ Node 22 / Electron / react（平台外置）/ IndexedDB + localStorage + Cookie。

### 2.3 标准工作流（现行）

```
改一处功能的完整闭环：
1. node scripts/baseline-check.mjs              # 先确认没在读一份过期的"现状"
2. （界面改动）先改设计稿 docs/50-信息中心/V16-设计图·需求图·交互逻辑.html
3. 改 dsh-director-plugin/src/**，同步文件头映射注释
4. node scripts/lint-undefined-symbols.mjs && node scripts/lint-cdp-templates.mjs
   node scripts/test-design-logic.mjs && node scripts/test-design-version.mjs
   node scripts/test-mindmap-logic.mjs && node scripts/verify-bundle.mjs
5. node build/build.mjs && node scripts/plugin-install.mjs --apply   # 写后逐文件回读校验
6. 重启 Harness（后台方式 + env -u ELECTRON_RUN_AS_NODE）
7. node scripts/verify-design-studio.mjs && node scripts/verify-mindmap.mjs  # 真机逐交互（各连跑 3 次）
8. （视觉改动）node scripts/shot-studio.mjs logs/x.png   # 截图复验
   （设计稿改动）node scripts/verify-design-html.mjs "docs/50-信息中心/V16-设计图·需求图·交互逻辑.html"   # 卫生五项 5/5
9. 更新 10-当前基线-落死锚点-V16.md → node scripts/baseline-check.mjs --write
```

### 2.4 风险提示

- 宿主**无 React 错误边界** ⇒ 单层崩溃卸载整棵 Shell 根 ⇒ 四个浮层全灭；故每层裹 `SafeLayer`，新增浮层必须照做
- 持久化的"打开态"是**自锁死**放大器 ⇒ 凡 `open && 数据为 null` 的组合必须判空
- 构建模板字符串内**禁用反引号**（构建期被当模板字面量求值）
- 写盘必须显式 LF（Windows 文本模式会转 CRLF，导致假重写 diff）
- Harness 端口每次启动变化 ⇒ localStorage 按 origin 分区 ⇒ 主存有丢失风险，设计图有 IndexedDB 冷备
- 修改编译产物风险高，必须仔细测试；三 tab 共存（总监/对话/轨迹），修改时需确保不破坏原功能

---

## 三、10步强制执行链路（引用全局）

每次任务必须按以下链路执行，不得跳过：

1. **解表达** — 拆解用户原始表达，区分核心诉求/附加约束/隐含需求
2. **提炼诉求** — 用一句话总结用户真正要什么
3. **检索标准** — 检查项目规范文档、历史方案、踩坑记录
4. **推导方案** — 基于规范和历史，推导最优执行方案
5. **匹配规范** — 对照项目规范文档，确认方案不违反任何约束（含本文件§二的特殊约束）
6. **生成任务清单** — 输出明确的、可追踪的任务步骤列表
7. **文档先行 + 前置审核** — 输出方案文档，等待用户确认（涉及代码修改时强制）
8. **落地执行** — 按审核通过的方案执行
9. **后置审核** — 执行完成后，对照审核标准自检
10. **输出交付 + 更新台账** — 交付产物，同时更新索引/任务清单/工作快照/记忆

**详细链路定义、检查点规则、行为逻辑触发表见**：`docs/00-统筹入口/AI对话执行约束规范.md` §二 和 `docs/00-统筹入口/AI规范执行保障机制.md` §三

---

## 四、6条强制约束（引用全局）

1. **举例/同类/等价/可选范围拓展规则** — 用户给出举例时必须合理拓展
2. **提示词库使用规则** — 理解意图后强制执行检索提示词库
3. **文档先行与前置审核规则** — 涉及代码修改第一步输出文档，禁止直接写代码
4. **后置审核机制** — 交付物必须对照审核标准校验
5. **文档三类分类与复用规则** — 执行中/正在使用/使用完成，禁止反复新建删除
6. **最小读取规则** — 按需求类型只读对应必读文档集，禁止全量加载

**详细定义见**：`docs/00-统筹入口/AI对话执行约束规范.md` §三

---

## 五、AI 行为逻辑框架（7维度25条 · V1.2，引用全局）

本项目遵循 workspace 全局 AGENTS.md 中定义的 AI 行为逻辑框架 V1.2，与"10步执行链路"（流程）和"6条强制约束"（红线）互补，构成完整行为体系。

### 7个维度概览

| 维度 | 核心 | 条数 |
|------|------|:----:|
| **思维方式 Think** | 第一性原理/能力前置评估/最优解平衡/元认知监控 | 4条 |
| **问题处理 Solve** | 三次失败换方案/根因优先/风险前置评估/多方案对比 | 4条 |
| **执行效率 Execute** | 任务原子化拆解/并行串行判断/进度及时反馈/资源最小消耗 | 4条 |
| **沟通协作 Communicate** | 先问不猜/先确认不擅决/反馈即承认即改/期望透明管理 | 4条 |
| **质量验证 Verify** | 交付前自检/数据事实验证/关键信息交叉验证 | 3条 |
| **环境与安全 Environment** | 网络访问规范(VPN)/安全边界守住/环境问题先诊断 | 3条 |
| **学习进化 Learn** | 经验双向沉淀/反馈驱动进化/能力边界自知 | 3条 |

**完整25条准则（含示例+例外/豁免+操作步骤）详见**：
- `D:\workspace\AGENTS.md` §三（全局完整版本）
- `docs/00-统筹入口/AI对话执行约束规范.md` §四（项目同步版本）
- `docs/00-统筹入口/AI行为逻辑框架审核文档.md`（审核记录）

---

## 六、规范执行保障机制（四层保障，引用全局）

本项目遵循 `AI规范执行保障机制.md` 定义的四层保障机制，确保 AI "只读该读的、执行该执行的、按标准执行"：

| 保障层 | 名称 | 解决的问题 |
|:------:|------|----------|
| 第一层 | 启动加载协议 | "只读应该读的部分" |
| 第二层 | 执行检查点机制 | "执行应该执行的逻辑"（CP1-CP4） |
| 第三层 | 行为逻辑触发与优先级 | "25条准则何时触发" |
| 第四层 | 违规自检与纠正机制 | "如何确保按标准执行" |

**完整机制详见**：`docs/00-统筹入口/AI规范执行保障机制.md`

**本项目特殊检查点**：
- CP2（前置审核）：涉及修改 `lib/client.js` 等编译产物时，必须额外说明修改位置、修改内容、测试方法、回滚方案
- CP4（后置审核）：修改完成后必须验证：①diff 正确 ②apply 成功 ③Harness 重启后功能正常 ④原功能未破坏

---

## 七、项目操作前检查清单

操作本项目之前，必须检查：

| 检查项 | 要求 |
|--------|------|
| 是否已加载必读集合 | 是（见§一，**含 §0 当前基线锚点**） |
| 是否跑过基线校验 | 是（`node scripts/baseline-check.mjs`） |
| 是否理解项目特殊性 | 是（见§二，**主管道=插件通道**） |
| 修改是否在 `dsh-director-plugin/src/` 中进行 | 是，禁止直接修改原安装目录 |
| 装机是否走 plugin-install.mjs | 是（幂等 + 写后逐文件回读校验） |
| 修改后是否重启 Harness | 是（后台方式 + `env -u ELECTRON_RUN_AS_NODE`） |
| 是否触碰冻结契约 | 无（localStorage key / DB 版本 / 锚点 / order 一律不改名） |
| 视觉改动是否截图复验 | 是 |
| 是否准备了回滚方案 | 是（`--uninstall` / `--purge` / git） |
| 用户说的"标准"是否有明确定义 | 没有则必须先询问，禁止自行编造 |

---

## 八、标准目录结构

```
dsh-client-mod/
├── AGENTS.md                 ← 本文件（最高优先级）
├── README.md                 ← 项目说明
├── config.json               ← 配置（原路径、目标包列表、规则）
├── dsh-director-plugin/      ← ★ 主管道：总监驾驶舱插件（现行交付物）
│   ├── src/                  ←   源码（47 个 js / 10,488 行）
│   ├── build/build.mjs       ←   构建（产物 lib/client.js，React 平台外置）
│   ├── lib/client.js         ←   构建产物
│   ├── scripts/              ←   闸门 / 安装器 / 真机验证 / 截图 / 基线指纹
│   ├── docs/                 ←   插件级设计说明（01 迁移明细 … 11 设计稿审核）
│   ├── INSTALL.md            ←   安装权威说明
│   └── BASELINE.lock.json    ←   ★ 落死基线指纹（60 文件 md5 / 1,730,783 B）
├── docs/
│   ├── 00-统筹入口/          ← 核心文档（必读集合）
│   │   └── 10-当前基线-落死锚点-V16.md   ← ★ 项目现状唯一权威描述
│   ├── 10-架构设计/
│   ├── 20-任务文档/
│   ├── 30-开发链路/
│   ├── 40-测试质量/
│   ├── 50-信息中心/          ← 设计稿本体（V16 …含板块 D）
│   ├── 90-历史归档/
│   └── 99-个人沟通/
├── original/@deepseek-ai/    ← 原客户端代码备份（只读，复原源）
├── workspace/@deepseek-ai/   ← 历史通道工作区（现行已不用）
├── patches/  snapshots/      ← 历史通道差异与快照
├── scripts/                  ← 历史通道自动化脚本（backup/apply/restore/…）
├── logs/                     ← 操作日志（按天）
└── _memory/                  ← 项目记忆（MEMORY.md）
```

---

## 九、违规处理

如果发现 AI 违反本约束：
1. 用户可直接指出违规内容
2. AI 必须立即停止当前操作，承认违规
3. 重新按10步链路执行
4. 将违规记录写入 `_memory/MEMORY.md` 或本文件的违规记录 section

### 违规记录

| 日期 | 违规内容 | 纠正措施 |
|------|---------|---------|
| 2026-08-27 | 项目缺少 AGENTS.md，AI 操作时未加载项目级约束 | 创建本 AGENTS.md，后续必须先加载必读集合再执行 |

---

## 十、参考文档

- `D:\workspace\AGENTS.md` — 全局执行约束+行为逻辑框架V1.2
- `docs/00-统筹入口/AI对话执行约束规范.md` — 10步链路+6条约束+25条行为逻辑V1.2
- `docs/00-统筹入口/AI规范执行保障机制.md` — 四层保障机制
- `docs/00-统筹入口/AI行为逻辑框架审核文档.md` — 行为逻辑框架审核记录
- `docs/00-统筹入口/AI任务审核标准通用模板.md` — 审核标准
- `docs/00-统筹入口/AI需求驱动读取范围与最小权限规范.md` — 读取范围规范
- `docs/00-统筹入口/10-当前基线-落死锚点-V16.md` — **项目现状唯一权威描述（V16 落死锚点）**
- `docs/50-信息中心/V16-设计图·需求图·交互逻辑.html` — 设计稿本体（A 高保真 / B 需求图 / C 交互逻辑 / D 设计图工作室）
- `dsh-director-plugin/BASELINE.lock.json` — 落死基线指纹（60 文件 md5 / 1,730,783 B）
- `docs/50-信息中心/01-Harness客户端修改-踩坑记录与快速定位索引.md` — 项目踩坑记录

---

*本文件创建: 2026-08-27（项目初始化+标准整改+行为逻辑框架V1.2同步+保障机制集成）*
*本文件最后更新: 2026-09-13（批次 16 收敛 · 闸门纠错轮 ②）*
> *§〇 基线锚点表同步产物 **964,544 → 964,872 B / 58 模块 / 绑定 474 = 474**；
> 新增「闸门纠错台账（二）」**8 条 + 附带修正 6 项** —— 新共性三条：**尺子量错容器**（五步区是
> `director-messages` 的兄弟节点）、**属性名语义撞车**（`data-dim` 跨两类语义 ⇒ 一个根因连爆 3 红）、
> **桩/尺寸写死**（同一份平台桩抄了两份 ⇒ 必漂移）；并首次抓到**闸门把整个脚本打哑**
> （`verify-dialog` 的 `launcher` 退役 ⇒ null 解引用，其后 100+ 断言静默丢失，**比跳过更隐蔽**）。
> 收益：`verify-dialog` **崩 → 268/268**（找回被吞断言）· `verify-install` **32/33 NO → 60/60 YES**
> · `cdp-click` **22/22 → 70/70** · `cdp-click-dialog` **94/100 → 102/102**；
> 离线 **22 套全绿**，真机五套**仍各连跑 3 次全绿**（flow 67/67 零跳过 · mindmap 100/100 零跳过 ·
> studio 90/90 · gate 5/5 · cdp-verify 39/39）。另**登记未做**：T-PLUG-030「文档上下文」死链路
> （`docs` 是 map 却当数组用 ⇒ 文案恒「共undefined篇文档」+ TypeError 被 `catch {}` 静默吞掉）。*
