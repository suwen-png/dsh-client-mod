# 06 · 回滚点与交接说明（草案 v1）

> 本回合（resume-20260916）的交界状态与可回滚性全景。**回滚演练要求**：交付前应在静态层面逐条确认下列路径真实可用。

---

## 一、本回合的提交层（git）

| 提交 | 时点 | 内容 | 回滚方式 |
|:-----|:-----|:-----|:---------|
| `2b8286a` | 09-16 16:45 | **修正前基点**：第 15/16 轮成果全量落盘（39 文件 / +6445 −566），工作区清零 | `git revert 2b8286a`（如需）——但该提交是干净基点，通常**保留** |
| `1ec845a` | 09-16 17:56 | 第 17 批 E1 探针 + 13 号文落盘（**由并行会话 b10b34e6 提交**，内容为共享基线） | 与第 17 批一并处置 |
| `390a71d` | 09-16 18:09 | **本会话修正**：T-PLUG-037 闸门加固（`verify-design-studio.mjs` C14.5 段，+2,749 B） | `git revert 390a71d`（单文件、无依赖，冲突面=0） |

## 二、未提交层（第 17 批进行中，**非本会话产物**）

- 15 个脏文件（`git status --porcelain`）：`docs/00-统筹入口/13-*.md` + 插件 `src/` 10 处 + `lib/client.js` + `scripts/verify-novel-split.mjs` + `docs/12-映射索引.md`
- 新增未跟踪：`src/bridge/session-io.js` / `src/logic/director-dispatch.js` / `director-collect.js` / `src/store/dispatch-log.js`
- **保留**：等 17 批收口（E9）后按其自身流程提交（或由收口方裁定）
- **丢弃**（仅在中止时）：`git checkout -- <脏文件>` + 删除 4 个未跟踪文件 + `git checkout -- lib/client.js`；⚠️ 丢弃前必须先跑 `baseline-check` 留档

## 三、应用/环境层

| 层 | 当前状态 | 回滚/复原 |
|:---|:---------|:----------|
| 插件装载 | 已装（3 处安装点，IS_PASS TRUE）；安装副本 = 仓库产物（md5 `ba9ffec9…`，18:35） | `node scripts/plugin-install.mjs --uninstall`（外科式，只删本插件 entry） |
| Harness | 运行中（CDP 9222，page `http://127.0.0.1:15117/...`，**本回合与并行作业者共用实例**） | 重启即得新 origin（localStorage/IDB 按端口隔离；cookie 跨启动持久） |
| 宿主文件（conversation） | **本会话零改动**；现状：workspace 565,158 B（剥离版）/ 安装 1,479,909 B（未剥离） | 备份在 `snapshots/host-b6-20260911-before-patch`（1,479,577 B，**滞后**——B6 执行前须重取） |
| 用户数据 | 15/16 轮测试所建宿主会话已累积（宿主无删除契约）；17 批 e2e 又 +8 | 已知代价，无法程序化回收（登记项） |

## 四、从零启动 / 恢复到基线（交接演练脚本）

```bash
# ① 校验基线一致性（应报 漂移=0；17 批在飞期间会报漂移，属预期）
cd dsh-director-plugin && node scripts/baseline-check.mjs

# ② 从零启动（干净目录自检）
node scripts/verify-install-clean.mjs          # 沙箱内跑全生命周期（52/52 期望）

# ③ 构建 → 装机 → 重启 → 健康
node build/build.mjs                            # 看两行：语法自检 + 绑定对账 666=666
node scripts/plugin-install.mjs --apply         # 逐文件回读校验
# 重启（必须清 ELECTRON_RUN_AS_NODE，且确认无旧进程）：
#   MSYS_NO_PATHCONV=1 taskkill /F /IM "DeepSeek Harness.exe"
#   env -u ELECTRON_RUN_AS_NODE -u NODE_OPTIONS "./DeepSeek Harness.exe" --remote-debugging-port=9222
node scripts/cdp-verify.mjs                     # 39/39 期望

# ④ 回到修正前基点（只在必要时）
# git reset --hard 2b8286a    # ⚠️ 会同时丢弃 1ec845a/390a71d 与全部未提交 17 批改动
# 或最小回滚：git revert 390a71d   # 只撤本会话的闸门加固
```

## 五、交接注意事项（给下一位接手者）

1. **先读锚点**（`10-当前基线-落死锚点-V16.md`）+ 本回合 `resume-20260916/` 工作台 + 17 批 `13-*.md`。
2. **确认没有并行写入者再动手**：扫 `_tmp-*` / `.wb-*` / 产品文件 mtime（本回合的教训：三方并行）。
3. 冻结契约（锚点 §五）**一律只增不改**；`data-*` 新名不得与旧名同义。
4. 17 批若已收口：先 `baseline-check`（应漂移 0）再继续；若未收口：先收口（E9）再继续。
5. 本会话新增工作台文件全部在 `.cluster/resume-20260916/`（已入 `.git/info/exclude`，不污染仓库）。
