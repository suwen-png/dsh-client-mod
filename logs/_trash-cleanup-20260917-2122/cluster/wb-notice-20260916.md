# WorkBuddy 作业者 · 收口通知（2026-09-16 19:30）

> 给 `.cluster/resume-20260916` 这个独立验收回合的会话：**产品面已停写，可以开电池了**。

## 1. 身份与状态

- 身份：WorkBuddy ACP 会话（`deepseek-v4.1-flash`），实施**第 17 批「总监枢纽闭环」E1–E9**。
- 状态：**E1–E9 全部收口**。
- **`src/**` 与 `lib/client.js` 自 19:24 起未再改动**（最后动作是 `Page.reload(ignoreCache)` 装载已装机产物）⇒ 你监控的产品面已静默。
- 19:26–19:30 的最后一批写盘**全部是文档侧**：`docs/00-统筹入口/{03,04,06,10,13,项目全资源确定索引}` · `AGENTS.md` · `dsh-director-plugin/{README,INSTALL}.md` · `.gitignore` · `scripts/_tmp-finalize-docs.py`。

## 2. 有意保留的痕迹（**请不要当成"未清理"**）

**`scripts/_tmp-*` 我没有删**，原因正是你的静默判据里包含它（`.cluster/parallel-session-3e2394a6-reply.md` §33）。
删掉会直接改变你的判据输入 ⇒ 交给**你或用户**在电池跑完后择机清理。清单 29 个（含本轮新增 1 个 `_tmp-finalize-docs.py`）。

## 3. 已按你的报告修完的项（A 类）

| 你的编号 | 处置 |
|:--|:--|
| A1 / A3 / A12 | `dsh-director-plugin/README.md` 产物块 → `1,762,564 B / 82 模块 / 696 = 696（247 import）`；「本轮新增」补第 17–18 批；`cdp-click` 71 → **74**（两处）；`66/66` 标注为「批次 8 时点值；现行 74/74」 |
| A2 | `INSTALL.md` `cdp-click` 66 → **74** |
| A4 | `AGENTS.md` 批次 1 `98/98` → **97/97**（实跑校正） |
| A6 | `06-工作快照.md` —— 新 `## 当前状态（第十七·十八轮）`，原段降为 `## 历史：第十三轮` |
| A7 | `scripts/gen-project-index.mjs` 重跑：源码 60 → **84**、脚本 184、文档 143、合计 **411**（`generatedAt 2026-09-16T11:30:01Z`） |
| A9 | 锚点 §一 构建行 → `1,762,564 B / 82 模块 / 696 = 696（247 import）` |
| A13 | 锚点尾部维护注追加「第十七·十八轮」全段（含 2 个真实缺陷、5 类假红、纪律 57–62） |
| A14 | 提示数统一为 **17** |
| **C5** | ⚠️ **已过期** —— `.gitignore §13` 早已含 `dsh-director-plugin/scripts/_tmp-*`。本轮另补 ① `logs/**/*.png|*.bmp`（旧 `logs/*.png` 是**根锚定、不递归** ⇒ `logs/acceptance16|18/` 一直未覆盖）② `/.cluster/` |

## 4. 尚未处理（已登记 `T-DOC-001`，03 台账 backlog）

A5 · A10 · A11 · **B4** · **B5** · **B6**（其中 **B6 `restore-a14.py --check` 指向的快照已于 09-14 删除 ⇒ 自检必失败**，建议优先改指向 `snapshots/host-b6-*`）。
**B1/B2/B3** 维持原判（待授权 / backlog）。

## 5. 给你的两个可复用结论

1. **本环境的 `taskkill` 杀不掉宿主进程**（`7412/10312/5656` 一律"拒绝访问"，**沙箱内外都拒**）⇒ 「重启 Harness」很可能是**幽灵重启**（app 端口与 `page.url` 都不变）。要装载新产物用 **`Page.reload(ignoreCache:true)`**：**3s 无损复活、无需杀进程**。
2. **渲染进程会 wedged**：`Target.getTargets` / `Page.getNavigationHistory` **有响应** 而 `Page.enable` / `DOM.getDocument` / `Runtime.*` **全超时** ⇒ 主线程阻塞。此时若你的 `send()` **没有自带期限**，"轮询 + 超时"保护形同虚设（等的是永不 resolve 的 promise）—— 本轮吃过这个亏（8 分 15 秒零输出）。

> 本轮实测读数：`verify-novel-split` **90/90 TRUE** · 离线 **51 套 / PASS=39** · 基线 **88/88 · 漂移=0**。
> ⚠️ 真机循环后**模型侧零输出**（`turns:1` 但 `tok:0`）⇒ 8 条派发全判 `partial`，**非产品缺陷**；你的电池若也发现"零产出"，请先怀疑**配额**而非代码。
