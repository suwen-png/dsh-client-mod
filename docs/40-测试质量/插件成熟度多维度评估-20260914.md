# 总监插件成熟度多维度评估 — 2026-09-14

> **审计性质**：只读评估（未修改 `src/**`、未改台账、未重封基线）
> **审计时点**：2026-09-14 16:08 – 16:20（GMT+8）
> **审计对象**：`dsh-director-plugin`（交付物 `lib/client.js`）
> **取证方式**：实跑 22 套离线闸门 + 逐模块 grep 接线 + 字节级规模统计 + 锚点/台账对账。**不采信任何自述状态**。
> **视角**：高位开发 / 技术负责人 —— 回答"距离一个成熟好用的产品还差什么、还差多少"。

---

## 〇、审计前置声明（两条必须知道的事实）

| # | 事实 | 影响 |
|:-:|:-----|:-----|
| 1 | **审计期间有另一会话正在实时写入** —— `src/components/DirectorPage.js` 在 16:06:18 → 16:08:02 之间从 102,711 B 变为 103,723 B，二次采样仍在变 | 本报告对"页面交互"类结论标注 **in-flight**；本会话全程不动 `src/**`（纪律 E：同一文件不得并行编辑） |
| 2 | **Harness 未运行、CDP 9222 未监听**（`tasklist` 无 `DeepSeek Harness.exe`，无 `node.exe`，9222 未 LISTENING） | **11 套真机 e2e 全部无法执行** ⇒ 本报告的"验证覆盖"维度只能给区间值，不能给现值 |

### 审计自身副作用（诚实记录）

> 执行 `node build/build.mjs --check` 时发现：**`build.mjs` 不认 `--check`**（仅第 575 行认 `--selftest-syntax`）
> ⇒ 该命令**直接执行了完整构建并写盘**，产物由 **1,171,906 B → 1,357,216 B**。
>
> - 产物当时**本就滞后于源码**（产物 mtime 16:06:24 vs 源码 16:08:02）⇒ 属"追平"而非"倒退"；
> - 构建自带两道闸门均通过：`✅ 产物语法自检通过` + `✅ import 绑定对账通过（211 条 import · 573 = 573）`；
> - 但**这是本会话造成的写盘**，须记录。
> - **建议**：给 `build.mjs` 补 `--check` 只读模式（现在唯一只读入口是 `--selftest-syntax`），否则任何人想"只看不写"都会误触构建。
> > 这本身是一条**易踩坑**：脚本没有 `--check` 时应**报错退出（exit 2）**而不是静默执行默认动作。

---

## 一、结论摘要（先行）

**综合成熟度：5.75 / 10 —— 距"成熟好用"还差约 4.5–5 个工作批次。**

一句话诊断：
> **界面层已经像一个成品，工程层已经像一支正规军，但「总监」这个产品的智能内核还没有真正通电，而且它当前的电闸（真机验证）是拉下的。**

三条正在进行的调整线，完成度**严重不均衡**：

| 线 | 内容 | 完成度 | 判定 |
|:--|:-----|:------:|:-----|
| **线 1** | 页面交互（第 6 批 V20 方案 9 条） | **2 / 9 落地 + 1 超纲** | 🔴 **最大缺口**，用户亲口提的 6 条界面问题仍未解决 |
| **线 2** | 设计图（分层 + AI 可读规格） | **源码已建 / 闸门未跟进** | 🟡 半成品，缺专项闸门 |
| **线 3** | 执行逻辑（7 个新架构模块） | **建模完成 / 接管为零** | 🔴 **悬空层**：写好了框架，但实际执行链一行都没用上 |

---

## 二、现状基线（客观数字）

### 2.1 版本与规模

| 项 | 现值 | 锚点声明 | 漂移 |
|:---|:-----|:---------|:-----|
| git HEAD | `bf1f8c5` | — | 未提交 **60 项** |
| 基线指纹 | — | 封存于 2026-09-14T04:46:23Z | **FALSE（漂移 = 14）** |
| 源码 | **71 文件 / 1,259,011 B** | — | +7 新增 / 7 修改 |
| 产物 | **1,357,216 B / 69 模块 / 573 = 573** | 1,089,309 B / 59 模块 / 516 = 516 | **+267,907 B（+24.6%）· +10 模块 · +57 绑定** |
| 文档 | 104 `.md` + 21 `.html`（`docs/` 3.0 MB） | — | 锚点正文未同步 |
| 闸门脚本 | **60 个 `.mjs`** | 22 套离线全绿 | 新增 6 套单测 + verify-v20 + 3 个生成器 |

**漂移清单**（`baseline-check` 实读）：
- ➕ 新增 7：`logic/{checkpoint,dag,delegate,policy,roles,task-state,verify}.js`
- ✏️ 修改 7：`lib/client.js` · `components/{DesignStudio,DirectorPage}.js` · `logic/key-files.js` · `store/{design-schema,design,layout}.js`

### 2.2 离线闸门实跑（22 套，本会话亲跑）

| 结果 | 数量 | 明细 |
|:-----|:----:|:-----|
| ✅ 绿 | **21** | `lint-undefined-symbols`（阻塞 0 / 提示 18）· `lint-cdp-templates` · `lint-platform-stub` · `verify-bundle` · `verify-design-doc` · `verify-dialog` **268/268**（须 `--import ./scripts/_platform-stub.mjs`）· `verify-install` · `verify-install-clean` · `verify-batch6/7/8` · `verify-v19` · `prove-dup-gate`（反证成立）· **13 套纯函数单测全绿**（含新增 6 套：`test-{checkpoint,dag,delegate,roles,task-state,verify}`） |
| ❌ 红 | **1** | **`verify-batch1` FAIL（exit 1）· 2 条断言红** —— 见 §四 B3 |
| ⛔ 不可执行 | **11** | 真机套（Harness 未运行）：`verify-flow` · `verify-mindmap` · `verify-design-studio` · `verify-v17-sync` · `verify-walk` · `verify-data-safe` · `verify-v20` · `cdp-click` · `cdp-click-dialog` · `shot-studio` · `probe-render-errors` |

> **闸门之所以值钱**：13 套纯函数单测在源码大改（+24.6% 产物）后仍**全绿**，
> 说明线 2/线 3 的新增是**加法而非破坏** —— 这是本项目最扎实的资产。

---

## 三、三条在途线的逐条核实

### 3.1 线 1 · 页面交互调整（`V20-第6批界面调整方案.md` 9 条）

方案文档质量很高（26 KB，逐条"原话 → 取证 → 方案 → 判据编号 → 批次划分"），
但**代码落地率低**。逐条取证（截至 16:15）：

| # | 需求 | 落地 | 取证 | 闸门 |
|:-:|:-----|:----:|:-----|:-----|
| 1 | 对话页两条删除 + 折叠列宽归零 + 最小化按钮注入 | ❌ | `TRIM_TARGETS` 仍**只有 3 条**（`host-agent-row` / `host-director-chat-title` / `host-chat-titlebar`）；`host-panel-min` **零命中** | 无 |
| 2a | R4/R7 hover 延迟（5000→500 / 600→300） | ✅ | `DirectorPage.js:137-138` 常量 + `data-rail-hover-ms` 已暴露 | `verify-v20` V-A |
| 2b | 总监记忆（延迟 / 锁定语义 / 拖高 / 按作用域分档） | ⚠️ **部分** | `setBottomPanelHeight` ✅；**`MEM_HOVER_MS` 零命中**、锁定语义未接管 | 无 |
| 3 | R4 条目两行靠右 + 点开详情占用 R5 + 未完成项补说明落库 | ❌ | `dp-r4-desc` / `dp-r4-id` / `dp-r5-detail` / `todoNotes` **全零命中** | 无 |
| 4 | R7 关键文件三行 | ❌ | `dp-r7-line` **零命中** | 无 |
| 5 | R4/R7 宽度自由拖拽 + 三栏无缝 | ✅ | `layout.js:138/158/304-313` `railWidth` + `setRailWidth`（clamp 140..420） | `verify-v20` V-B/V-C |
| 6 | 执行状态窗口（把记账下沉到执行链） | ❌ | `recordAgentRun()` 的调用点**仍全部在 `DirectorDialog.js`**（436/535/550/560/603 行）；`logic/**` 零引用 ⇒ 窗口数据源恒空（方案 §六定性正确，但未实施） | 无 |
| 7 | 「总监/对话」合成单按钮 + 注入宿主 composer 统计行 | ❌ | `bridge/` **无 `host-composer-slot.js`**；`dp-scope-toggle` / `dp-host-scope-toggle` 零命中 | 无 |
| 8 | 本地模型配到「标准模型选择」 | ⛔ | 方案已判定**超纲·如实上报**（渲染进程无 `require`/`electron`/`fs`，写不了 `~/.dsh/settings.yaml`） | — |
| 9 | 去掉 R8 整行 | ❌ | `dp-r8` 仍渲染于 `DirectorPage.js:1439`，`dp-route-director` / `dp-route-chat` / `dp-register-flow` / `dp-r8-note` 全在 | 无 |
| + | 技能/智能体补 `path` 与「未指向」可见 | ❌ | 零命中 | 无 |

**落地率：2 / 9（22%），另有 1 条部分、1 条超纲。**

> ⚠️ **闸门覆盖严重不足**：`verify-v20.mjs`（34,655 B）只覆盖 **V-A / V-B / V-C 三段**
> （hover 延迟、railWidth 拖拽、无缝），即**只覆盖已落地的那 2 条**。
> 其余 6 条**既无代码也无判据** —— 方案里写好的 H1–H6 / M3–M4 / R4-1–6 / R7-1–4 / G1–G4 / S1–S6
> 共 **20+ 条判据**目前**一条都没有载体**。

### 3.2 线 2 · 设计图调整（分层 + AI 可读规格）

| 文件 | 变化 | 新能力 |
|:-----|:-----|:-------|
| `store/design.js` | 33,839 → **63,926 B（+89%）** | 文档/版本/元素操作全量导出外露 |
| `store/design-schema.js` | 29,493 → **42,225 B（+43%）** | **`SCREENS`（director/chat 两屏）· `LAYERS` · `LAYER_ORDER` / `layerOf` / `paintOrderKey` / `sortForPaint` · `layerCollisions` / `intersectArea` · `LOGIC_FIELDS`** |
| `components/DesignStudio.js` | 73,323 → **92,332 B（+24%）** | 分层编辑器 UI |
| `docs/50-信息中心/V20-设计图分层与AI可读规格.html` | 新建 60,111 B | 规格正文 |

**判定：源码层已建、规格稿已有，但闸门未跟进。**
`test-design-logic` 仍是**旧的 49 项**，没有针对 `SCREENS` / `LAYERS` / `layerCollisions` /
`sortForPaint` 的专项判据 ⇒ 新能力目前**只有"存在性"，没有"正确性"**。

### 3.3 线 3 · 总监执行逻辑调整（7 个新架构模块）

**规模 140,834 B / 7 模块**，设计意图清晰（每个文件头部都有"为什么需要它"+ 开源依据）：

| 模块 | 字节 | 职责 | 来源依据 |
|:-----|-----:|:-----|:---------|
| `roles.js` | 36,048 | 角色注册表（Agent Card） | OrgAgent / agency-orchestrator |
| `dag.js` | 21,373 | 声明式步骤图（可并行、可声明） | agency-orchestrator `steps:` |
| `policy.js` | 20,245 | 执行模式与策略（默认 `direct`，升档需理由） | Anthropic「multi-agent 约 15× token」 |
| `verify.js` | 20,144 | 双层验收（机械断言 + 去偏语义评审） | 本项目纪律 27「同源自评」 |
| `checkpoint.js` | 14,956 | 不可变快照链（断点续跑 / 时间旅行 / Fork） | LangGraph / agency-orchestrator `--resume` |
| `task-state.js` | 14,369 | 九态任务状态机（拆「等输入」与「等批准」） | A2A 协议 |
| `delegate.js` | 13,699 | 委派模板 + 上下文装配（防 lost-in-the-middle） | Anthropic 四要素 |

**🔴 核心发现：这是一个悬空层。**

| 检查项 | 结果 |
|:-------|:-----|
| 被 `client-entry.js` 引用（安装链装载） | ✅ 7/7 |
| 被 UI 组件引用 | ⚠️ 5/7（`OrchestratorPanel.js` 用了 `roles/dag/policy/delegate/verify` —— 且是**只读展示**：列角色、校验图、估成本、生成简报、读标签） |
| `task-state` / `checkpoint` 被 UI 引用 | ❌ 零 |
| **被实际执行链 `logic/director-run.js` 引用** | ❌ **7/7 全部为 0** |
| 设计稿板块 | ❌ 全部标注 **「板块 —」** |
| 台账条目（03/04） | ❌ 零 |
| 设计/方案文档 | ❌ 零（仅 `12-源码映射索引.md` 这个**生成物**自动收录） |

⇒ **用户实际体验到的执行链，与新建的架构，是两套互不相干的东西。**
`director-run.js:70` 仍是那句 `const steps = [];` + 写死的 5 个顺序块（整理语言 → 切分支 → 切模型 → 上下文筛选 → 审核），
——正是 `dag.js` 头部注释里说的"改之前"的形态。

**另一个必须点破的事实（影响对"智能"的期望）**：
`config/model.js:40` 的 `localModel: { enabled: true, ... }` **默认是开**的（03 清单 `T-P2-001` 写"默认关"，**台账已过期**）；
但本机 `window.__dshCheckOllama()` 实测返回 `connection_failed`（无 Ollama 服务）
⇒ `callLocalModel()` 全部返回 `null` ⇒ **五个步骤全部降级为规则路径（G0 档）**。
⇒ **总监当前是一个纯规则引擎，没有任何真实模型参与。** 这是"是不是真总监"这个产品问题的直接答案。

---

## 四、缺口清单 —— 除三条线之外，「还缺什么」

> 这是本次审计的**主问题**。按"是否阻塞成熟好用"分三级。

### A 级 · 阻塞"成熟好用"的定义（4 项）

| # | 缺口 | 证据 | 为什么是 A 级 |
|:-:|:-----|:-----|:--------------|
| **A1** | **执行智能未闭环** —— 总监无真实模型；新架构未接管执行链 | `callLocalModel` 全降级；`director-run.js` 对新架构引用 0 | 产品的名字叫"总监驾驶舱"，但"驾驶"目前是**规则表**。这一条不解决，"好用"只能到"顺手" |
| **A2** | **第 6 批 6 条未落地** | §3.1 表 | 用户**亲口提过**的界面问题（两条删不掉、R4 竖排换行、R7 只有两行、切换按钮位置、R8 还占一行、执行状态窗口不出现）一个都没解决 |
| **A3** | **真机验证全面停摆** | 11 套 e2e 无法执行；最后一次真机绿（09-14 午）之后 `src` 又前进 14 文件 | 所有"已修/已好"的结论**当前都不可验证**。本项目的历史教训（纪律 30：注册成功 ≠ 渲染成功）正是发生在这个盲区里 |
| **A4** | **台账与锚点未同步** | 漂移 14；锚点正文仍写 `1,089,309 B / 59 模块`；新架构 **零台账条目**（`03` 未新增 `T-PLUG-037+`） | 违反本项目纪律 B「能力单元连带同步下游文档」⇒ 下一位接手者读到的"现状"是**旧世界** |

### B 级 · 成熟度硬指标缺失（7 项）

| # | 缺口 | 证据 |
|:-:|:-----|:-----|
| **B1** | **4 个模块零闸门覆盖** | `components/NodeDetailPanel.js` · `components/OverviewDialog.js` · `components/PersonalizePanel.js` · `store/file-adapter.js` —— 被任何 `scripts/*.mjs` 引用数 = **0** |
| **B2** | **新批无闸门** | 第 6 批 20+ 条判据无载体；`design` 分层无专项闸门（`test-design-logic` 仍 49 项）；`OrchestratorPanel.js` 仅 1 个脚本提及 |
| **B3** | **`verify-batch1` 已失效（本次实跑唯一红灯）** | 两条红：`剥离前快照 0 B` / `实减 -565158 B`。真因：`SNAP = snapshots/snapshot-20260908-131412-before-apply/...` **已被 09-14 备份清理删除** ⇒ `existsSync=false` ⇒ `snapSize=0` ⇒ 断言恒假。**属闸门连带损伤，非产品缺陷**。⚠️ 它同时暴露**清理流程漏洞**：清理时只 `grep` 了 `original/` 与 `workspace/` 的引用者，**漏了 `snapshots/` 的引用者** |
| **B4** | **无性能预算判据** | 产物 **1,357,216 B**（69 模块）注入宿主三页签，**无**注入耗时 / 首帧 / 交互延迟 / 内存断言。产物增速 **+24.6% / 批**，没有红线 |
| **B5** | **无 a11y 系统断言** | `aria-label` 散落在 10 个文件（有意识），但**零**键盘全导航 / 焦点陷阱 / 对比度 e2e |
| **B6** | **设计图数据单向** | `DesignStudio.js:934` 导出走**剪贴板**（理由充分：桌面壳拦 `<a download>`），但**无导入通道**（`importFile` 零命中）⇒ 导出即终点，无法回流 |
| **B7** | **四处 ⚙ 个性化面板仍是单例** | 已在闸门侧用 `panelShut()`「起点归零」打补丁，但**产品侧的单例 + 开合语义**未改 ⇒ 历史 flaky 的根仍在（曾致 `verify-flow` 74/74 与 73/74 交替） |

### C 级 · 产品化与长期风险（6 项）

| # | 缺口 | 说明 |
|:-:|:-----|:-----|
| **C1** | **无 onboarding / 帮助** | 界面有 3 页签 + 3 浮层 + 设计图工作室 + 思维导图 + 4 处 ⚙，**没有任何引导**。新用户不知道"从哪点第一步" |
| **C2** | **无用户侧回滚入口** | 回滚只在开发者侧（`snapshots/` + `plugin-install --uninstall`）⇒ 用户遇到坏版本**无自救路径** |
| **C3** | **无错误上报通道** | `__dshDebug` 只写内存；用户遇到白屏/红块时**拿不到任何线索**（历史上"打不开软件"就是靠人肉 CDP 才定位到 Cookie 431） |
| **C4** | **产物无拆分** | 单文件 69 模块 1.36 MB，无按需加载。增长无约束 ⇒ 迟早触及注入/解析开销 |
| **C5** | **插件能力边界未书面固化** | 纯渲染进程：`require`/`global`/`electron`/`process` 全 `undefined` ⇒ "打开文件/文件夹""写 `settings.yaml`"这类需求**结构性不可达**。已在方案里逐条如实上报，但**未沉淀为"插件能力边界"一份常置文档** ⇒ 每批都会重新勘察一遍 |
| **C6** | **产品定位未书面闭环** | 「总监」到底是**规则编排器**（现状）还是**AI 驾驶舱**（目标）？这条不拍板，A1 就无法收敛 —— 是要"接本地模型"，还是"改用宿主既有 LLM 通道"，还是"明确接受规则引擎" |

---

## 五、多维度评分（8 维度加权）

| 维度 | 权重 | 得分 | 打分依据（可核验） |
|:-----|:----:|:----:|:-------------------|
| 产品功能完整度 | 15% | **7.0** | 界面面宽（3 页签 / 3 浮层 / 工作室 / 导图 / 版本 / 个性化 / 权限配置）；但 `R-DIR-101..104`（本地模型）未接、`R-DIR-201..203` 未回归、`R-DIR-301..306` 部分未验证 |
| 交互成熟度 | 15% | **6.0** | V17/V18/V19 三轮设计迭代 + 大量真机 e2e 沉淀；但第 6 批 **6 条未落地**、2 条已知 bug（裁剪一闪而逝 / 最小化）仍在修 |
| **执行智能度** | **20%** | **3.0** | 🔴 规则引擎（无模型参与）；新架构 7 模块**接管率 0**；`verify.js` 的"双层验收"未接入 `director-run#reviewOutput` ⇒ **同源自评仍在**（纪律 27 所述病根） |
| 数据健壮性 | 10% | **7.5** | Cookie 预算层（`COOKIE_TOTAL_BUDGET=12288`）+ 真机三层判据（前提/判据/正负对照）· 三向负向校准；扣分项：跨启动数据按**含端口 origin** 隔离（结构性丢失） |
| 工程质量 | 15% | **8.0** | 60 脚本 / 22 套离线闸门 / `lint` 自校准 27 项 / 平台桩唯一真相源 / 闸门纠错台账 20+ 处 —— **本项目最强项**；扣分：`build.mjs` 无 `--check`、产物无拆分 |
| **验证覆盖** | **15%** | **4.5** | 🔴 真机 11 套**全部无法执行**；4 模块零闸门；新批 20+ 判据无载体；`verify-batch1` 已失效 |
| 文档一致性 | 5% | **5.0** | 体量极大（104 md / 3.0 MB）但**锚点漂移 14**、**新架构零文档零台账**、台账状态过期（`T-P2-001` 说"默认关"实为"默认开"） |
| 可交付性 | 5% | **6.5** | 装机链路完整（`plugin-install` 逐文件回读 + 唯一回滚点 + 干净目录验收）；扣分：**60 项未提交**、未推远程、宿主退坡待授权 |

**加权总分 = 5.75 / 10**

> 计算：`0.15×7.0 + 0.15×6.0 + 0.20×3.0 + 0.10×7.5 + 0.15×8.0 + 0.15×4.5 + 0.05×5.0 + 0.05×6.5 = 5.75`

### 分视角解读（同一个 5.75，三种视角差别很大）

| 视角 | 现值得分 | 说明 |
|:-----|:--------:|:-----|
| 「界面好不好用」 | **≈ 8.0 / 10** | 第 6 批落地即可到 9+。用户感知的"好用"主要在这一层 |
| **「它是不是真总监」** | **≈ 3.0 / 10** | 智能内核未通电。这是产品定位问题，不是工作量问题 |
| 「我敢不敢改它」 | **≈ 8.5 / 10**（有电闸时）→ **≈ 5.0**（电闸拉下时） | 闸门文化极强；但真机不停摆是前提 |

---

## 六、距离"成熟好用"还有多远（收敛路径）

### 6.1 剩余工作项（可枚举，非估计）

| 序 | 工作项 | 对应缺口 | 批次量 | 是否受阻 |
|:-:|:-------|:---------|:------:|:---------|
| 1 | 第 6 批补完 6 条（需求 1 / 2b / 3 / 4 / 6 / 7 / 9 + 技能指向） | A2 | **1.5 批**（6-B/6-C/6-D） | 需 1 次确认（用户"先出图再审"口径） |
| 2 | 执行逻辑接管：新架构接入 `director-run.js` | A1 | **1 批** | 需先定 C6 定位 |
| 3 | 本地模型接入 **或** 明确放弃（二选一决策） | A1 / C6 | **0.5 批** | 🔴 **需用户拍板** |
| 4 | 闸门补齐：4 模块 + 第 6 批判据 + design 分层 + 性能预算 | B1/B2/B4 | **1 批** | 无 |
| 5 | 锚点同步 + 台账登记 + 设计稿补板块 + 基线重封 | A4 | **0.5 批** | 须真机跑完 |
| 6 | 真机全量复验（8 套 × 3 次）+ 装机 + 重启 | A3 | **0.5 批** | 🔴 **需 Harness 运行** |
| 7 | `verify-batch1` 修复 + 清理流程补 `snapshots/` 引用检查 | B3 | **0.2 批** | 无 |
| 8 | B6 导入通道 / B7 单例治理 / C1 引导 / C2 用户侧回滚 / C3 错误上报 / C5 能力边界文档 | B/C 级 | **1.5 批** | 无（可排期） |

**合计 ≈ 6.7 批**（其中"距成熟好用"的**必做项 1–7 ≈ 5.2 批**，C 级产品化项 ≈ 1.5 批）。

> 按本项目近期节奏（第 4/5/6 批均在同一日内推进）折算：
> **必做项 1–7 约 5 个有效工作批次**；若把 C 级产品化一并收口，约 **6.5 个批次**。

### 6.2 三条收敛路径（按性价比排序）

| 路径 | 内容 | 收益 | 代价 |
|:-----|:-----|:-----|:-----|
| **P1 · 先通电闸门**（强烈推荐先做） | 启动 Harness → 跑 8 套真机 ×3 → 修 `verify-batch1` → 补第 6 批判据 → 同步锚点/台账/基线 | 把"验证覆盖"从 4.5 拉回 8+，总分 **5.75 → ≈ 7.0**；且**解锁后续所有改动的可信度** | 0.5–1 批，**零产品风险** |
| **P2 · 补完界面** | 第 6 批剩余 6 条 + 闸门同步 | 把"交互成熟度" 6.0 → 8.5，用户可感知收益最大；总分 **→ ≈ 7.5** | 1.5 批，含 1 处宿主桥（需求 7，中高风险） |
| **P3 · 接通智能**（决定产品上限） | 定 C6 定位 → 新架构接管 `director-run` → 接/弃本地模型 | 把"执行智能度" 3.0 → 7+，总分 **→ ≈ 8.5** | 1.5 批 + **1 次产品决策** |

> **P1 必须在 P2/P3 之前或同时做**：当前 14 文件漂移 + 真机停摆，
> 任何新增改动都在**没有安全网**的情况下进行 —— 这正是纪律 30 事故（整页空白而注册句柄报 true）的复现条件。

---

## 七、阻塞与待决策（须用户拍板）

| # | 项 | 类型 | 说明 |
|:-:|:---|:-----|:-----|
| 1 | `T-PLUG-005-B6` 宿主退坡（改 `workspace/**` 1 行） | 🔴 不可逆 | `workspace/**` 被 `.gitignore` 排除 ⇒ **git 无法回滚**，唯一回滚点 `snapshots/host-b6-20260911-before-patch`（已在清理中硬约束保留） |
| 2 | 本地模型：接入 / 放弃 / 改走宿主 LLM | 🟡 产品定位 | 决定 A1 的解法与整个"总监智能"的上限（见 C6） |
| 3 | 第 6 批其余条目开工确认 | 🟡 流程 | 按用户既有口径「先出设计图，我看看没问题再做」 |
| 4 | `T-AESTH-002` 是否作废 | 🟡 台账 | 遗留待决策项 |

**⚠️ 环境阻塞（非决策项）**：Harness 未运行 ⇒ 真机 0 覆盖；另一会话正在编辑 `DirectorPage.js` ⇒ 本会话不动 `src/**`。

---

## 八、审计方法与可复现性

| 项 | 命令 / 方式 |
|:---|:-----------|
| 基线校验 | `cd dsh-director-plugin && node scripts/baseline-check.mjs` |
| 离线闸门（22 套） | `for s in lint-* verify-* test-*; do node scripts/$s.mjs; done`（`verify-dialog` 须加 `--import ./scripts/_platform-stub.mjs`） |
| 接线取证 | `grep -rn "logic/<模块>" src/`（逐模块计引用数，区分 UI 引用与执行链引用） |
| 规模统计 | `find src -name '*.js' -printf '%s %p\n' \| sort -rn` + `wc -c lib/client.js` |
| 需求落地 | 按 `V20-第6批界面调整方案.md` 的 `data-testid` / 常量名逐条 grep（**不采信方案文档的自述**） |
| 并行写入检测 | `find ... -printf "%T@ %s %p\n"` 两次采样 diff |

**本报告未修改任何产品文件、台账、锚点或基线。** 唯一副作用见 §〇（`build.mjs` 缺 `--check` 导致的一次构建写盘，产物与源码一致）。

---

> **编制**：2026-09-14 16:20 · 只读审计
> **关联**：`docs/00-统筹入口/10-当前基线-落死锚点-V16.md`（基线）· `03-待完成任务清单.md`（台账）
> · `docs/50-信息中心/V20-第6批界面调整方案.md`（线 1 方案）· `dsh-director-plugin/docs/12-源码映射索引.md`（线 3 唯一收录处）
