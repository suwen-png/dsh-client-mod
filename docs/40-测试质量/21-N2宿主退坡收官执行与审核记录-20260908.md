# N-2 宿主退坡收官（P2 死代码删除）· 执行与审核记录（21号）

> **编号**: 40-测试质量/21 ｜ **时间**: 2026-09-08 13:20 ｜ **标准**: execution-standards V2.3.0 · 30号文清单 N-2（已确认）
> **结果**: ✅ DirectorView 死代码删除（1,519 行整段）+ apply + 重启 + 三段冒烟全绿；**运行时零回归迹象**

---

## 一、手术过程（含两次安全中止如实记录）

| # | 事件 | 结果 |
|:-:|------|------|
| 1 | 初次定位命中嵌入式文档字符串里的假目标（L6539）→ 修正为行首匹配 | 教训：编译产物内嵌文档含同名标识符 |
| 2 | 括号匹配被函数签名解构花括号干扰（提前闭合）→ 修正为括号感知匹配 | fnEnd 计算仍因正则字面量失同步未闭合（fnEnd=-1） |
| 3 | 改用 **//#region DirectorView.js 边界**作为权威删除范围（L8897–L10415，1,519 行整段）——该 region 由既有构建体系生成，边界可信 | span 外引用 4 处全为注释/字符串/文档索引数据（DSH_DOCS_INDEX 白名单） |
| 4 | 共享基础设施核查 | directorStores(L5496)/createDirectorStore(L6327)/useDirectorStore(L6404)/directorProcess(L6704) 均**在删除段之前**，按《锚点契约》保留 ✅ |
| 5 | node --check ✅ → apply（快照 snapshot-20260908-131412）→ 清缓存 7 类 → 重启 | numstat **+11,996 / −9,855**（净 −1,518） |

## 二、运行时冒烟（三段）

| 段 | 断言 | 结果 |
|---|---|---|
| BOOT | conversation rev 更新 + 无模块级错误 | rev **10afd797e46f** ✅ |
| 对话 tab | 插件透明视口 + 挂载 | ✅ |
| 会话打开（ChatView 渲染 → MOD-A 路径执行） | sessionChatView=true / textarea=1 / **零未定义引用** | ✅（唯一错误 = Ollama 11434 设计内降级） |

## 三、70% 目标的诚实裁定

numstat +11,996 vs 目标 ≤4,066 —— **未达标，且在保留 MOD-A 的前提下不可达**：剩余补丁主体 = MOD-A ChatView 左栏 + directorStores/directorProcess/IDB 共享基础设施（均为《锚点契约》保留物）。进一步下降的唯一路径 = MOD-A 退役（对话左栏由插件承接），属产品决策：当前对话 tab 的插件透明视口已提供等价入口，但会话内左栏的交互习惯是否迁移需用户定夺。**建议：保持现状（锚点面 ≈ 11,996 增行），把「MOD-A 退役」登记为远期候选而非本轮任务。**

## 四、5步自审（V2.3.0 §五）

1. 完整性：P2 删除 + 三段冒烟 + numstat 全达成 ✅
2. 准确性：region 边界为准 + span 外引用 4 处逐一分类 + 快照 131412 可回滚 ✅
3. 规范性：删除段含退坡标记（含恢复路径）✅
4. 可用性：重启后对话/会话/插件全链路可用 ✅
5. 交付：源码 + 部署产物 + 审核记录 + 台账 ✅

**结论：✅ 通过**。阶段 N-2 关闭。
