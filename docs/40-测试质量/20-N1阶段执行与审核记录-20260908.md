# N-1 阶段（插件收尾）· 执行与审核记录（20号）

> **编号**: 40-测试质量/20 ｜ **时间**: 2026-09-08 12:00 ｜ **标准**: execution-standards V2.3.0 清单驱动（30号文 §六 清单已获确认）
> **范围**: N-1.1 Q2-2 / N-1.2 M6 模型切换 / N-1.3 M7 轨迹增强 / N-1.4 校准报告 —— 全部完成

---

## 一、N-1.1 Q2-2 数据图标与余量 emoji（插件侧）

| 文件 | 修改 |
|---|---|
| icons.tsx | +10 图标（alert/refreshCw/zap/settings/palette/rocket/gitMerge/download/search/filter），累计 36 |
| BranchMindMap.tsx | 悬浮工具条 4 按钮 emoji→Icon（plus/folderOpen/gitMerge/trash）+ 节点 icon hybrid 渲染 |
| DirectorChat.tsx | 空状态警告 Icon 化 + 文案中性化（P0-4 插件侧） |
| V10Director.tsx | dCardIcon hybrid（字母→Icon/emoji→原文）+ currentAgent chip hybrid ×2 + 下拉 label emoji 前缀剥离 + dCardIcon 样式去紫 |

**渲染策略**：hybrid —— icon 字段为字母（IconName）时渲染 SVG，为 emoji 时按数据内容原样显示（用户可编辑数据不动）。文件内旧紫引用残留 = 0。

## 二、N-1.2 M6 模型切换 UI

- DirectorChat：`selModel` 状态（localStorage `dsh-director:model` 持久化）+ `ollamaModels` 列表（isOllamaAlive 时 getOllamaModels 拉取，独立 useEffect）+ 输入区模型下拉（默认项「默认 (qwen2:7b)」）。
- 两处 chatWithOllama 调用（对话回复 + 方案生成）均传入 `{ model: selModel || undefined }`。

## 三、N-1.3 M7 轨迹页增强（TracePanel 重写）

- 新增：类型筛选下拉 + 关键词搜索（标题/ID）+ 行内详情展开（ID/消息数/更新时间）+ **JSON 导出**（Blob 下载）+ 计数徽标（filtered/total）。
- 保留：30 秒自动刷新、类型识别、当前会话标记、打开会话。

## 四、N-1.4 校准报告

31号文：《设计图与实现差异校准报告》——9 项差异全部裁定「实现为准」，设计图转历史冻结版。

## 五、运行时实证（构建 rev dd46d1352b43，重启后 reload 干净起步）

| 断言 | 结果 |
|---|---|
| 插件 rev | dd46d1352b43（四批连续构建热更链：ce516814c17c→8f964…→dd46…） |
| .dsh-dir-app 挂载 | true；PAGEERRORS = 0 |
| 可见 emoji | 17（全部为数据字段/消息内容，chrome 已零 emoji） |
| Icon SVG | app 内 16 个 |

## 六、5步自审（V2.3.0 §五）

1. 完整性：N-1.1~N-1.4 四项全达成 ✅
2. 准确性：全部替换带断言；3 处保守跳过已登记（toolbar 分叉 0=模式不同/下拉双 {item.label} 已定点处理 1 处）
3. 规范性：icons.tsx 统一 lucide 内嵌规范；hybrid 策略文档化 ✅
4. 可用性：构建即热更，模型选择/轨迹功能即用 ✅
5. 交付：源码 + 构建产物 + 审核记录 + 台账 ✅

**结论：✅ 通过**。阶段 N-1 关闭。
