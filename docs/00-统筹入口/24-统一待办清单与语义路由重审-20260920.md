# 24 · 按用户想法重审 23 号方案 + 全量统一待办清单

> **编号**：DSH-需求-24
> **日期**：2026-09-20
> **触发**：用户原话「现在按照我的想法重新审核方案，然后结合现有代码实现情况、之前的需求文档等，做所有的待完成清单，包含所有的代办；然后再重新审核这个代办清单，我不理解为什么会有什么『之后做』，所有需要做的事情放在一起；然后按照清单进行修改」
> **最高优先级约束（需求对齐权重 3/3）**：**分流判断必须由模型按语义自行判断，不能写死关键词规则**。插件只做执行（建分支 / 投简报 / 画节点），不做判断。
> **硬约束（必须正面回应，不许假装没有）**：① 宿主渲染进程**无 fs 服务**、无 `window.require`；② `callLocalModel` 是死代码（`localModel.enabled` 默认关）；③ Harness 以管理员运行、本会话无法自动重启；④ 不改宿主编译产物，主管道 = `dsh-director-plugin/`；⑤ 不新开 V17/V18 基线号。

---

## 一、对 23 号方案的重审结论（用户想法 vs 原文）

### 1.1 用户真正要什么（逐字对齐）
| 用户点 | 23 号原文写法 | 重审结论 |
|:---|:---|:---|
| 分流由 AI 语义判断，不写死关键词 | §五 5.4 把「模型真路由」放到**阶段 4–5**，v1 先纯规则 | 🔴 **改判**：这是用户本轮唯一的 3 权重需求，**不许分阶段推迟**。但要换一种今天就能落地的实现路径（见 §二）。 |
| 所有事放一起，不要「之后做」 | §十二 把工作切成阶段 0–5，阶段 5「依赖本地模型」 | 🔴 **改判**：本清单**不设「以后再做」栏**。确实被外部硬阻塞的项，单独标 `[外部阻塞]` 并写清阻塞面与绕开方式，而不是笼统推迟。 |
| 结合现有代码实现 | 方案偏设计、未扣到函数级 | ✅ 本清单每条都落到具体模块/函数/文件。 |

### 1.2 关键架构判断（自问自答，CP0/CP1）
**问：宿主无 fs、`callLocalModel` 是死代码，凭什么说「模型语义判断」今天就能做？**
答：插件**早已在驱动带活模型的宿主会话**——`director-dispatch.js` 用 `sendToSession(sessionId, brief)` 给分支会话投消息，宿主自己的大模型就会回复；`findSummary()` / `stateOfSummary()` 读回复。也就是说「调模型」这件事**不需要插件自己有 fs 或本地模型**：
> **开/复用一个专门的「L0 路由会话」→ 把需求 + 一份路由提示词投给它 → 等它回复一段 JSON 决策 → 插件解析 JSON 后只做执行（建分支/投简报/画节点）。**

这正好满足用户「模型判断、插件执行」的分工：判断者是宿主会话里的模型（已可用），插件是执行器。
- **降级路径**：模型没回 / 返回非 JSON / QUOTA / 超时 → 回落到现有 `plan()` + `planAttribution()` 关键词规则。**规则版从「主路径」降级为「离线兜底」**，且完整保留（它是唯一可离线测、可离线对账的判据，纪律 126 同族：判断与执行分离后，规则版就是判断层的可替换降级实现）。
- **不许造假**：模型版路由必须把 `routingSource`（`"model"` / `"rule-fallback"`）如实写进台账与读数，规则降级时必须带原因（纪律 19）。

---

## 二、统一待办清单（全量，不打折扣，不分「以后做」）

> 图例：`[新]` 新建 ｜ `[复用]` 复用现有 ｜ `[外部阻塞]` 被宿主/模型外部条件卡住，**不是推迟**，附绕开方式 ｜ `[本回合落地]` 本轮已开工/已完成。
> 每条都带：落点（文件/函数）｜ 验收（离线断言 / 真机读数）｜ 来源。

### WS-A 🔴 语义路由判断层（用户 3 权重需求，**本回合开工**）
| # | 事项 | 落点 | 复用/新建 | 验收 | 来源 |
|:-:|:---|:---|:---:|:---|:---|
| A1 | 新建纯模块 `logic/router-decision.js`：路由决策 JSON 契约 + 提示词构造 `buildRoutingPrompt()` | `src/logic/router-decision.js` | [新] | 离线单测：提示词含 JSON schema、不写死任何用户数据 | 用户核心需求 |
| A2 | 纯函数 `parseRoutingDecision(rawText)`：剥代码围栏、取首个平衡 `{...}`、校验、归一；**解析失败返回 `{ok:false,reason}` 不抛** | 同上 | [新] | 离线单测：正常/围栏/带噪/垃圾 四类输入；垃圾**不抛**、走降级 | 同上 |
| A3 | `normalizeBranches(branches, knownPool)`：模型命名命中已知维度（A1–A8 / 通用三段）则映射到 key，否则保留为自由分支；输出与 `plan()` 同形 `{kind,dims,name,reason,routingSource}` | 同上 | [新] | 离线单测：命中已知→映射 key；自由分支→原样保留；空 branches→`kind:none` | 同上 |
| A4 | 离线闸门 `scripts/test-router-decision.mjs`（含植入缺陷校准：喂坏 JSON 必红） | `scripts/test-router-decision.mjs` | [新] | 纳入 `_offline-run` 自动发现；`ROUTER_NEG=1` 必红 | 纪律 ⑥/⑫ |
| A5 | 派发接线：`dispatchBranches` 加可插拔 `opts.decide` 决策点；默认仍走规则 `plan()`；接入模型决策后 `routingSource` 如实落台账 | `director-dispatch.js` | [复用] plan() 降级 | 既有 `test-split-dimensions` / `verify-novel-split` **不回归变红**（默认走规则路径） | 同上 |
| A6 | 异步「路由会话」驱动：复用/新建一个 L0 路由会话 → 投提示词 → 轮询回复 → 解析；失败/QUOTA/超时 → `rule-fallback` | `logic/router-live.js`（桥到 `session-io.js`） | [新] | 离线可测到「拼提示词/解析」；**真机跑通需 Harness 在线**（见 §四阻塞面） | 同上 |

### WS-B 23 号方案：外层单对话 + 左侧看板 + 嵌套总监
| # | 事项 | 落点 | 复用/新建 | 验收 |
|:-:|:---|:---|:---:|:---|
| B1 | 统一 id 语义：流转/看板/导图节点主键挂**会话 id**（解决 T-PLUG-074 的 `ws_…` 作用域 id 歧义） | `lineage.js` / `dispatch-log.js` / `T-PLUG-074` 判据 | [复用]改造 | 离线断言：流转记录 `sid` ∈ 会话 id 集合 |
| B2 | 扩展 `task-state`：`pinned/summary/projectRoot/state(draft→…→done/partial/blocked/aborted)`，单一写入点 | `store/task-state.js` | [复用]扩展 | 离线断言：看板只读 store、不自拼 |
| B3 | 左侧常驻看板（置顶/进行中/待完成/已完成折叠） | `host-director-column.js` / 新 `Board.js` | [新] | 真机：看板条目数 == 导图节点数对账；点条目定位节点 |
| B4 | 导图任务态语义（完成✓/待办灰/进行中/置顶⭐/阻塞红角标）与看板同源渲染 | `MindMap.js` | [复用]扩展 | 1:1 截图 + 真机 3 次数值一致 |
| B5 | 外层单对话收件箱（把总监操作台降级为后台面板） | `DirectorPage.js` / 新外层输入 | [复用]重组 | 真机：一句话多意图 → 正确拆分派发 |
| B6 | 三层职责边界纯函数 + 跨层消息信封（复用现有 env 协议） | 新 `logic/layers.js` | [新] | 离线：四层链纯函数可回放 |
| B7 | 思维链路视图：L0 每次决策留 `{输入,决策,理由,时间}` 落 `Decisions`（不新建 store） | 轨迹页签 + `dispatch-log` | [复用]扩展 | 用户能翻「上次为何派给 A4」 |
| B8 | 模型 tier 决策树（cheap/standard/strong 纯函数，映射表外置配置） | 新 `logic/model-tier.js` | [新] | 离线单测；映射不写死代码 |

### WS-C 22 号审计缺口（G1/G3/G4/G8/G9 + 03 号台账未结项）
| # | 事项 | 来源 | 落点 |
|:-:|:---|:---|:---|
| C1 | G1：`R21-05`「结果」采集通道缺失 | 22 号 §3 | L1 回收摘要主动写 task-state（与 B2 同源） |
| C2 | G3：`R21-01` 视觉重做无人验收入口 | 22 号 §3 | 补 1:1 截图取证段 |
| C3 | G4：「中键」vs 实现「Ctrl+滚轮」手势口径对齐用户原话 | 22 号 §3 | 核对交互 + 判据 |
| C4 | G8：N8 截图目录产出 | 22 号 §3 | 取证流程落盘 |
| C5 | T-PLUG-073：整批「起点等价」隐式依赖前序 reload → 改自证式起点 | 03 台账 | `verify-orchestrate` / `verify-register-gate` |
| C6 | T-PLUG-059：`DL-5a/b/7a/7b` 间歇同红，补幂等回总监页 | 03 台账 | `verify-director-logic` |
| C7 | T-PLUG-053 余两项：双通道读消息抽公共 + 其余套件 until() 推广 | 03 台账 | `_cdp-wait.mjs` 推广 |
| C8 | T-PLUG-075：`deliverImpl` 干跑分支可读性收紧（判据面变更须同批改） | 03 台账 | `chat-bridge.js` |
| C9 | T-PLUG-065：`run-live.mjs` 硬编码 9222 → 走端口顺移 | 03 台账 | `run-live.mjs` |
| C10 | T-PLUG-063/064/040/052/041/T-P2-001/T-V12 系列：外部/用户决策项 | 03 台账 | 见 §四外部阻塞表 |

### WS-D 用户没讲清的四块（设计图/思维链路/执行逻辑/审核机制）
| # | 事项 | 落点 |
|:-:|:---|:---|
| D1 | 设计图工作室定位为「L2 产出物编辑器」，产物带分支/版本元数据 | `DesignStudio.js` |
| D2 | 思维链路 = 决策记录可视化（与 B7 合并实现，不重复造） | 轨迹页签 |
| D3 | 执行逻辑：状态机幂等 + 可回放 + 失败不静默（blocked 必带因） | `task-state` + dispatch |
| D4 | 审核机制：5 层×2 阶段（23 号 §10.4）落到本仓库既有 `verify-*` 体系 | 闸门 |

---

## 三、三轮自审记录（execution-standards §3.9）

### 第 1 轮（初稿）打分
- 完整性 1/2：漏了 03 台账里 T-PLUG-073/075 与 22 号 G1/G3/G4/G8。
- 可执行性 1/2：WS-A 只说「接模型」没说清「无 fs 怎么接」。
- 风险覆盖 0.5/1：没写规则降级如何不回归。
- **合计 5.5/10 → 整改**。

### 第 2 轮（v2）整改后打分
- 补全 WS-C 全部台账项 + WS-D 四块；§1.2 讲清「用宿主会话跑模型」的绕开路径。
- 完整性 2/2；可执行性 2/2；风险覆盖 1/1；一致性 1/1；规范性 1/1；准确性 1.5/2；可验证性 1/1。
- **合计 9.5/10 → 仍有一处**：准确性 0.5 扣在「真机验证需要 Harness 在线」没单列阻塞面。

### 第 3 轮（v3）打分 = 10/10
- §四单列「外部阻塞表」，把所有被宿主/模型/用户决策卡住的项列清阻塞面与绕开方式，**不再是笼统推迟**。
- 完整性 2 · 准确性 2 · 可执行性 2 · 规范性 1 · 一致性 1 · 风险覆盖 1 · 可验证性 1 = **10/10**。

---

## 四、外部阻塞表（不是「以后做」，是写清卡在哪、怎么绕）

| 项 | 阻塞面 | 绕开方式（现在就能做的部分） |
|:---|:---|:---|
| A6/B5 真机路由跑通 | Harness 以管理员运行、本会话无法自动重启；QUOTA 402 | 先把**纯函数层 + 离线闸门 + 接线缝**做完（本轮）；真机联调待用户启动 Harness 后用 `reload-client.mjs` + 构建指纹比对验证 |
| T-PLUG-041 / T-P2-001 | 模型侧 QUOTA 402、本地模型未接入 | 插件侧已三级落地失败原因；规则降级保证断模型时仍能用 |
| T-PLUG-063 | Harness 官方源停在 rc.5、本机 rc.11 | 建议 A：不动（本机已更新）；B 升内核需整轮真机复验，待用户拍板 |
| T-PLUG-064 | 根 `.trash-20260917-1330` 软删 6.41MB 待确认真删 | 已软删可回退；真删不可逆，待用户确认 |
| T-PLUG-005-B6 | 改宿主编译产物 1 行，未授权、`workspace/**` 被 gitignore | **唯一硬阻塞授权项**；不碰 |
| T-V12-008 | git push 需 VPN/凭据，用户已决策暂不开 | 用户决策项，不单方面推进 |

---

## 变更记录
| 版本 | 日期 | 变更 |
|:---|:---|:---|
| V1.0 | 2026-09-20 | 初稿：按用户「语义判断、不分阶段」重审 23 号；合并 03 台账 + 22 审计 + 四块补齐 = 全量待办；三轮自审 10/10 |
