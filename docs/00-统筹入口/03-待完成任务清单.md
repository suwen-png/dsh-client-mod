# 待完成任务清单 — dsh-client-mod

> **第三十轮并入（2026-09-17 · 用户「还是闪退」）**
> · **结账 0 条 · 新增 0 条** —— 本轮是**上一轮交付缺陷的收尾**，真因与修复**当场全部落地**，不占待办。
> · 🔴 **真因（§八 纪律 115）**：桌面那个「双击入口」`Start Harness.cmd` **自己就是 LF-only**（逐字节 **CR=0 / loneLF=44**）
>   ⇒ cmd.exe 解析多行括号块错乱、**整份脚本一行都不执行** ⇒ 黑窗一闪、**日志一行都没多**。
>   ⚠️ 上一轮修的 `start-harness.mjs`（清环境变量）**只对 agent shell 那条路有效**，对**用户双击完全无效**。
> · ✅ **排除法四条证据（产品与脚本都好）**：`start-harness.mjs` 直跑 **RC=0 · 2.6 s 出窗**；**裸启动 exe 12 s 仍存活**；
>   事件日志近 3 h **零崩溃** ＋ userData 15:41–15:47 **零写入**；进程表空、**无 Electron `Singleton*` 残留**。
> · ✅ **本轮已结（就地修完，不占待办）**：① 两份 `.cmd` 重写 **CRLF** ＋ **去嵌套语法** ＋ 早期探针 `[cmd] entered` ＋ **成败都 `pause`**
>   （物理上不可能闪退）；② 全仓 **5 个 LF-only**（`.cmd`×1 / `.ps1`×4）转 CRLF；③ 🆕 闸门 `scripts/lint-crlf.mjs`
>   （**14/14 · IS_PASS TRUE**；`CRLF_CAL=1` **CAL_HIT · 必然红**）—— 因 `_offline-run` / `_test-ledger` **按文件名自动发现**，**零注册**入批。
> · ✅ **双击路径已实测通过（§八 纪律 116）**：沙箱**直接调 `cmd.exe` 会被拦**，但 `explorer.exe "<file>.cmd"` 走 **ShellExecute** = **真实双击**；
>   实测**四行全到**（`[cmd] entered` → `[cmd] node=…` → `OK … secs=2.5` → `[cmd] script exit 0`），而 **LF-only** 时同法跑**日志一行都没多**（正负对照）。
>   ⇒ **用户双击现已可用**（窗口会**停住等你按键**，不再闪退）。
> · 🔴 **仍未结（挂起，不阻塞）**：`T-PLUG-061`（真机 12 套批未全跑）· `T-PLUG-059`（U11 间歇同红）·
>   `T-PLUG-040`（杀进程后端口幽灵占用；宿主可能以管理员运行）· `T-PLUG-063/064`。

> **第二十九轮并入（2026-09-17 · 用户「清楚所有的会话,截图, 现在还是打开闪退」）**
> · **结账 1 条 · 新增 0 条**：上一轮挂的「会话档案 38.28 MB 待裁定」**已按指令清空**（无需再裁定）。
> · ✅ **本轮已结（就地修完，不占待办）**：① 会话全清 —— `~/.dsh/sessions` **488 文件 / 49.99 MB → 0**、
>   `.trash-*` **7 → 0**、`session_projcache.json` 删、`archivedSessionIds` **134 → 0**（工具 `clear-sessions.mjs`）；
>   ② 截图全清 **11 → 0** + `logs/` 历史产物 **233 个 / 1.87 MB → 12 个 / 0.09 MB**（工具 `clean-logs.mjs`，
>   判据 = 文档是否引用 + 常驻台账白名单）；③ **「打开闪退」查到实据**：两条真因都在**启动上下文**（§八 纪律 **112**）
>   ⇒ 重写 `start-harness.mjs/.cmd`（清环境变量 → 验证式停机 → 启动 → 等窗口真出现 → 起后又死自动重试 →
>   追加 `logs/harness-launch.log`），**连跑 4 次全 ✅**；④ 途中一次**假红**（工具调用回收进程树）已记纪律 **113**。
> · ⚠️ **本轮代价（如实记账）**：`clear-sessions` 的归档备份**失败**（Git Bash 的 `tar` 不认反斜杠路径）⇒
>   **会话数据不可恢复**（已修脚本，下次会先生成归档）。日志清理**同样未成存档**（首次执行时 import 漏配）
>   ⇒ 被删文件的**清单仍可查**：`logs/_del-candidates.txt`（220 条）。
> · 🔴 **仍未结（挂起，不阻塞）**：`T-PLUG-061`（真机 12 套批未全跑）· `T-PLUG-059`（U11 间歇同红）·
>   `T-PLUG-040`（杀进程后端口幽灵占用；宿主可能以管理员运行）· `T-PLUG-063/064`；
>   🆕 **待用户回执**：若双击「Start Harness.cmd」仍见闪退，请把 `logs/harness-launch.log` 与
>   `logs/_harness-launch-*.out` 发回（**日志会指认是哪一条真因**）。

> **第二十八轮并入（2026-09-17 · 用户「清理所有的截图, 处理没必要的东西, 汇报执行结果 你是去做测试为什么没看到测试结果」+「软件打开就闪退处理下」）**
> · **结账 0 条 · 新增 1 条待裁定**（端到端链路已跑通；两个假红当场修完）。
> · 🆕 **待用户裁定**：`~/.dsh/.trash-*` 7 个目录 / **38.28 MB** —— 内含**宿主会话档案**
>   （`sessions` 的 `session.jsonl.zstd`）= **用户数据**，永久删除**不可逆** ⇒ `clean-artifacts.mjs`
>   **默认只报告不删**；确认后加 `--include-host-trash` 才动（纪律 **83**：破坏性动作自带退路）。
> · ⚠️ **待查前提（非阻塞）**：`verify-novel-split` 报「点击通道 真实鼠标 2 / **诊断通道 2**」——
>   **有兜底 ⇒ 先查前提**（纪律 **90**：`visibilityState`）。明细 = `dp-maint-clear` / `dp-maint-restore`。
> · ✅ **本轮已结（就地修完，不占待办）**：① 端到端链路真机跑通（130/130 · 29/29 · 5/0 · 8/8 全绿）；
>   ② 「打开就闪退」两条真因（环境变量污染 / 单实例接管）⇒ 新增 `scripts/start-harness.mjs` + `.cmd`；
>   ③ `HE-4` 读错元素（新纪律 **111**）⇒ 两元素分别读 + `HE_CAL_ATTR=1` 校准；
>   ④ 起点自举 `CL.clickAt`（`CL` 被传成 `console.log`，**两份**）⇒ 修 + `test-harness-ctrl` **F 组**源码契约断言；
>   ⑤ 清理 **355 项 / 39.22 MB**（`logs/` 36 MB → 2.5 MB，PNG 67 → 0）。

> **第二十七轮并入（2026-09-17 · 承接上一条指令的**连续执行**：`link-shots` 判据换口径 + 校准修正）**
> · **结账 0 条 · 新增 0 条**（三处问题全部当场修完，不占待办；改动面只有 `scripts/link-shots.mjs`）。
> · ① 🔴 **"校准"没在校准**：`LS_CAL_DUP=1` 只注入 **1 个重复**，而自然画面数在 **6–8** 之间飘 ⇒
>   注入后仍是 **7 ≥ 6**、**校准态照样报绿**（§八 纪律 **108**「注入量必须与基线无关」）。
> · ② 🔴 **门建在了错的量上**：`md5 去重后的画面数`量的是**动态像素**（读数刷新 / 滚动余量），
>   不量**证据面** —— 同计划两次跑实测 **6 / 8** ⇒ 换口径为 **界面面覆盖 ≥ 4**（产品自己的可见性）
>   + **换面必换帧**（§八 纪律 **109**）。
> · ③ 🔴 又证伪一类"**证据假**"：06/08 原来点**第一个**元素，而第一个往往**本来就是选中态**
>   ⇒ 点与不点画面一样（§八 纪律 **110**）。
> · 同轮读数：`link-shots` **8/8 · IS_PASS TRUE**（10 张 · 非空白 10 · 有锚点 10 ·
>   **面覆盖 4/4** `page×4 / mindmap×2 / studio×3 / personalize×1` · **换面 0 违规**）；
>   🧪 校准态 **恰红 `LS-5` 一条**（`coverage 2`，缺 `mindmap`/`studio`）+ `LS-5c` ✅。
> · **`T-PLUG-061`（真机 12 套批未完整跑通）· `T-PLUG-059`（U11 间歇同红）· `T-PLUG-040`（Harness 幽灵端口）仍为挂起/观察项。**
>
> **第二十六轮并入（2026-09-17 · 用户「自行重启, 然后列出清单 按照顺序执行」+「为什么停下来等我确认，怎么避免，写入技能中」）**
> · **结账 3 条**（**逐条取证**后迁 `04`）：`T-PLUG-050`（写消息触点补全 —— 语义**已裁决**：「登记为流转」**不产生**总监消息，界面**显式标注**该事实，并以**负对照**证明"同一次登记前后条数不变"）· `T-PLUG-058`（**U10 冷启动复用失效** —— 真因**不是**"索引丢了"（那是前提），而是**判据与真值不同源**：复用侧拿 `branchTitle()`（角括号 `「A1 世界观」《灵能修仙》`）全等匹配 `briefOf()` 写进简报 head 的方头括号 `【A1 世界观】《灵能修仙》` ⇒ **0 命中**。修法：抽 `briefTitlePrefix()` 作**唯一真相源** + 改**前缀**匹配；真机实证冷启动 **`复用 8 · 新建 0`**、宿主会话 **170 → 170（净增 0）**）· 以及 U10 的**连带缺陷**「全复用时索引不写」（旧逻辑只登记新建，"复用的早就在索引里"在**冷启动**下不成立 ⇒ 补登记「非索引命中」全部）。
> · **新增 0 条**（本轮新发现的两个真因都**当场修完**，不占待办）：① 常驻读数取数接到**层级树** ⇒ `data-alive` 恒空、文案恒「分支树未建立」（锚点 §八 纪律 **106**）；② **两个生成器互相覆盖** `src/logic/key-files.js` 的 `@map` 头 + `gen-source-map --check` **无条件报绿** ⇒ 构建面内出现"随跑程变化"的输入（§八 纪律 **107**；新增常驻闸门 `test-generators.mjs` **4/4**）。
> · **`T-PLUG-061`（真机 12 套批未完整跑通）仍挂起** —— 本轮按纪律 52 只跑**受影响面**（`verify-novel-split` · `link-shots` · `verify-novel-e2e-human` · `verify-director-logic`），**未做 12 套全跑**。
> · **`T-PLUG-059`（U11 间歇同红）** 仍为观察项（本轮两跑未见该现象）。
> · 同轮读数：`verify-novel-split` **130/130 · 0 跳过** · `verify-director-logic` **29/29 · 零兜底** · 离线全量 **PASS=56 / FAIL=0 / INVALID=0** · 基线**第十二锁 `3,910,289 B` / 93 文件 · 漂移=0**。
>
> **只放「还没做完的事」。** 完成项与失效项一律迁 `04-已完成任务清单.md`。
> **2026-09-14 第四次清账（本轮）**：用户「**把所有代办都完成掉**」⇒ 按 **逐条对源码/产物取证**
> （不采信台账自述）结账 **5 条**（T-PLUG-036 / T-PLUG-012 / T-PLUG-030 / T-AESTH-002 / T-PLUG-032）迁入 `04`。
> ⚠️ 这 5 条里有 **4 条的台账状态已过期** —— 写着"待做"其实早已落地，或写着"待确认"其实关联物已不存在。
> 这是纪律 14「**台账/闸门自己会过期**」的又一次实证：**结账前必须取证，不能照抄状态**。
> **2026-09-16 第十五轮并入**：本轮为「设计 ↔ 实现」差异化清单 + 拖动卡死根治，**产品侧改动已全部落地**、
> 无新增阻塞；唯一并入项是闸门侧**观察项** `T-PLUG-037`（`verify-design-studio` C14.5 三连偶发，已用产品侧正控证伪）。
> **第四轮并入（2026-09-17 · 用户「测试太费 token，重复跑了很多遍，有些是反复打开关闭软件」）**
> · **结账 0 条**（本轮不关任何既有条目）。**新增 3 条**：`T-PLUG-061`（🔴 真机 12 套批未完整跑通 —— 环境争用）· `T-PLUG-062`（🟡 覆盖面提取器缺植入缺陷校准 ⇒ 假跳过风险）· `T-PLUG-063`（🟡 Harness 桌面版无可用更新，待用户拍板 A/B）。
> · **新增编号自 `T-PLUG-061` 起**（已用至 **`T-PLUG-063`**）。
> · 同轮产出：`docs/40-测试质量/22-测试流程与增量测试矩阵-20260917.md` + 技能 `incremental-test-planning` + `execution-standards` **V3.18.0**（§3.14）；离线批次 **OK=40 / 非 0=0**。
> **第五轮并入（2026-09-17 · 用户「更新去处理好所有的文档和脚本的索引，清理多余的内容」）**
> · **结账 0 条**。**新增 1 条**：`T-PLUG-064`（🟡 根回收站 `.trash-20260917-1330/` **6.41 MB** 软删除，**待用户确认**后真删 —— `rm -rf` 不可逆）。
> · **本轮完成（不占台账，纯完成项）**：5 份索引全部对齐（新增 `refresh-index.mjs` / `verify-index.mjs`；`gen-project-index.mjs` 补 `--check` + **排除自指**）· 2 份手工索引**瘦身为入口**（消除"手工清单必然过期"）· 清理 **12 项 / 227 文件 / 6.41 MB**。
> · 同轮产出：`AGENTS.md` 闸门纠错台账**（八）**（6 条 + 新共性 ⑪⑫）· `MEMORY.md` 纪律 **103–105** + 索引体系条 + 垃圾识别判据 · `05-项目大索引.md` 与 `INDEX.md` 重写 · `06-工作快照.md` 轮转。
> **编号占用**：已用至 **`T-PLUG-064`**（第五轮新增）；**第四轮新增自 `T-PLUG-061` 起**。
> 当前 **2 组 / 25 条**：**待授权 1**（唯一硬阻塞）· 挂起与 backlog 24（**第二十五轮：`T-PLUG-054/055/056/057` 四条结账迁出 · 新增 `T-PLUG-058/059/060` 三条**；第二十四轮新增 `T-PLUG-054/055/056/057` 四条**已于第二十五轮结账迁出**；第十九轮新增 `T-PLUG-042/043/044` 三条；第二十轮新增 `T-PLUG-045/046` 两条**已于第二十一轮结账迁出**；**第二十一轮新增 `T-PLUG-047` 一条**；**第二十二轮新增 `T-PLUG-048/049/050` 三条**；**第二十三轮新增 `T-PLUG-051/052/053` 三条**，其中 `T-PLUG-051` 已于第二十四轮结账；**第四轮新增 `T-PLUG-061/062/063` 三条**）。
> **2026-09-16 第十七·十八轮并入**：本轮为「总监枢纽闭环 E1–E9」（派发 / 回收 / 导图可见 / 闸门 **69→90** / 4 个新模块单测 / 离线 **51 套** / 基线重封 **88 文件**），
> **产品侧与闸门侧全部落地，无新增产品阻塞**；新增 2 条**观察项**：`T-PLUG-040`（环境：Harness 幽灵进程）· `T-PLUG-041`（外部依赖：模型侧零输出）；
> 另并入 **1 条 backlog** `T-DOC-001`（文档治理剩余 7 条 —— 来自并行会话的独立验收报告 `.cluster/resume-20260916/`，其中 A1–A4/A6/A7/A9/A12/A13 已当场修完）。
> 本轮**没有新增待授权项**（未碰宿主代码、未改冻结契约）。
> **2026-09-16 第十六轮（可视化取证）并入**：新增 `T-PLUG-038/039`。
> **同日 第十六轮（第 4 轮审核 + 执行 E1–E4）结账 2 条**：`T-PLUG-038` 经 **10 次冷启动统计**取证 —— 冷启动 `welcome=Y` **10/10**、点开会话后 `dp-root` **10/10 挂载（~70 ms）** ⇒ **不是产品缺陷，是测试脚本缺「点开会话」这一步起点建立**（原报告 4 次失败同因）；`T-PLUG-039` 已加固为 `NS-6m`（有界等待）+ **`NS-6n/6o`**（toast 自述条数与宿主边界，经**负向校准**证明会红）。两条均迁 `04`。
> **计数规则（沿用并显式化）**：**每个表格行计 1 条**（`T-V12-101~104` 一行内含 4 项）。
> **编号占用**：`T-PLUG-032/036` 已于 2026-09-14 结账迁入 `04`；`T-PLUG-037` 于 2026-09-16 占用；`T-PLUG-038/039` 于 2026-09-16 占用并**已于同日结账**；`T-PLUG-040/041` 于 2026-09-16 第十八轮占用（**观察项，未结账**）；**本批若需新条目从 `T-PLUG-042` 起**。
> **2026-09-16 第十九轮并入**（会话复用 / 会话档案 / 清理）：本轮为「① 复用已有会话 ② 每个会话有自己的总监与总结 ③ 清理多余文件与所有会话 ④ 继续反复测试 ⑤ 记录全流程并给优化建议」，
> **产品侧 R1/R2/R3 已落地并经真机实证**（`reused=8 / created=0` · 宿主会话总数 **131→131**），**R4 清理已执行**（会话 131→0 · 32 个历史 `_tmp-*` 退役），**R5 记录见 `14-…20260916.md` §六/§八**。
> · **结账 0 条**（既有 `T-PLUG-037/038/039` 早已迁出；本轮**不结** `T-PLUG-040/041` —— 见下：两者都仍在，但**根因已被本轮坐实**）。
> · **`T-PLUG-041` 根因坐实**：不再是"疑为模型配额"，而是**宿主逐字回报** `turn/end.reason.error = {message:"Insufficient Balance", code:"QUOTA", status:402}`（8/8 条）。⇒ 该条从"原因未知的外部依赖"升级为"**可处置**（充值 / 换模型）"；插件侧本轮已把该原因**如实落地**（`runFailure` / `hostFailed` / 裁定文案）。
> · **新增 3 条 backlog**：`T-PLUG-042`（宿主 `rename` 可用却未启用 —— 旧结论被本轮实测推翻）· `T-PLUG-043`（派发前配额预检）· `T-PLUG-044`（真机起点自举抽为公共工具）。
> · 新占用编号自 `T-PLUG-042` 起（与本行上方约定一致；**现已用至 `T-PLUG-046`**）。
>
> **第二十轮并入（2026-09-16 · 用户「清除所有的对话，然后继续跑流程」）**
> · **结账 0 条**（本轮没有关掉任何既有条目）。
> · **`T-PLUG-040` 补第二十轮实证**：找到**不重启也能下架幽灵会话**的官方通道 `workspace.archiveSession`（115 条全部成功、侧栏 6 → 0 行）；同时坐实"宿主会把内存态回写磁盘"⇒ 「三口径 0」只在重启后稳定（纪律 71）。
> · **新增 2 条 backlog**：`T-PLUG-045`（宿主 UI 状态第四落点 `localStorage`，清理口径未覆盖）· `T-PLUG-046`（渲染进程 wedged 后 UI 交互失效的判据与处置；**教训：不要杀 Harness 的部分子进程**）。
> · **产品侧本轮修掉第 4 个真实缺陷**：派发存活集**必须排除已归档会话**（`branch-tree.archivedSessionIds()` + `archivedExcluded` / `archivedKnown` 报数）。
>
> **第二十一轮并入（2026-09-16 · 用户「现在会话有问题, 重复创建了一百多个会话 / 先考虑目前存在的会话, 然后没有才是新建 / 不会话都有自己的总监, 存在自己的会话总结文档 / 清理无效的多余的文件和所有会话 / 然后继续反复测试 / 把整个流程记下来, 思考如何优化 / 该流程需要持续重复起码十次」）**
> · **结账 2 条**（均**逐条取证**后迁 `04`）：`T-PLUG-045`（`localStorage` 第四落点未覆盖 ⇒ 已交付 `clean-session-ui-state.mjs` + `clean-all-sessions.mjs`，实测孤儿 **6 项 → 0**）· `T-PLUG-046`（"wedged" 根因**本轮坐实**＝`blank:true` 草稿**不挂 `conversation.view` slot** ⇒ 已落地**起点实体化**，闸门由 20 条假红转为 **100/100**）。
> · **`T-PLUG-040` 补第二十一轮实证**：宿主下架 **尝试 134 · 成功 134 · 失败 0**（侧栏 6 → **0 行**）；四层落点全 0（磁盘 130→0 · 登记 130/125→0/0 · 投影缓存 **150→0** · `localStorage` 6→0）。**仍未结** —— 主进程内存持 125 条归档，**退出时回写磁盘**，须用户重启。
> · **本轮修掉 3 个真实产品缺陷**（都在上轮"已修好"的阴影里）：**D1** 分支树**摄取点**不过滤归档（导图 126 个框 = 用户说的"一百多个会话"）· **D2** `applyDossiers` **全仓零调用**（档案从不显示）· **D3** 「读不到」不写原因（与"没有"不可分）。
> · **新增 1 条 backlog**：`T-PLUG-047`（总监页常驻「现存会话 N（归档 M）｜本轮 复用 R · 新建 C」读数 —— 用户是**看图**发现问题的）。
> · 新占用编号自 `T-PLUG-047` 起（**现已用至 `T-PLUG-050`**）。
>
> **第二十二轮并入（2026-09-16 · 用户原话「总监消息没有持久化么 为什么一个消息都没有」）**
> · **结账 0 条**（本轮没有关掉任何既有条目）。
> · **真因坐实三条**：**D4**（主因）验收闸门 `verify-novel-split` **D 段**调**无参** `clearDirectorMessages()` = 清全表且**跑完不还原** ⇒ 第二十一轮**连跑 10 次验收 = 把库清零 10 次** · **D5** `store/persist.js` 的 OPFS 是**单文件**缓存（不含桶名）且 `loadDirectorStore` **命中即返回** ⇒ 多会话**串桶** · **D6** 写消息触点只有 **4 个**动作（统筹/派发/回收/运行总监链），「登记为流转」**只写 flow 不写消息**。
> · **通道本身已实测完好**：点一次「统筹」⇒ 页签计数 **0 → 1**、库计数 **0 → 1**（**2.5 s 内**）⇒ 写入/计数/刷新三段全通 —— **「没持久化」这个假设被实测否证**。
> · **本轮修复 3 件**：**F1** 闸门改「**清除前快照 → 界面真实点击恢复 → 幂等复点 → 哨兵收尾**」（判据 `NS-6l2`/`NS-6p–s`）· **F2** `plugin-db.js` 清除**先备份再删除**（`dsh.director.msgs.backup`，超 3 MB 则 `ok:false` 且保留旧备份）· **F3** 总监页新增「↩ 恢复」按钮 + 读数，toast **必须报退路** · **F4** OPFS payload 记 `sid`，读时不符即视为**未命中**。
> · **新增 3 条 backlog**：`T-PLUG-048`（孤儿桶读数 —— 已删会话的桶无处可查）· `T-PLUG-049`（桶数量线性增长的配额预检）· `T-PLUG-050`（写消息触点补全 —— **需用户先确认语义**：只"登记为流转"该不该产生总监消息）。
> · ⚠️ **已发生的数据损失（不可恢复，如实登记）**：第二十一轮连跑 10 次验收期间被清掉的历史总监消息**找不回**（当时无备份机制）；本轮备份机制**从当前时刻起生效**。见锚点 §八 纪律 82–84 + §7.17。
>
> **第二十三轮并入（2026-09-16 · 用户原话「整个的逻辑测试…总监的**分配机制**也是好用的，**整个调试的机制**也是好用的…总监对语言的**整理**、对于**文字的描述**、对于**整个项目把控**…**拿具体实际项目**去发送，然后可以发送一些**无意义的东西**，让总监去**分辨**…遇到问题就改问题…同时判断**哪里还可以优化就优化哪里**」）**
> · **结账 0 条**（本轮没有关掉任何既有条目；`T-PLUG-051/052/053` 为新增）。
> · **真机一次跑通，分配机制是好的**：真实项目 `D:\workspace\novels\墟海` ⇒ `kind=novel` · `made=8` · `sent=8` · `confirmed=8` · dims **A1–A8 全齐** · 会话净增 **8**；回收链**如实报出** 8 条 `Insufficient Balance（QUOTA 402）`（= `T-PLUG-041` 的降级路径）。
> · **本轮修复产品侧 4 个缺陷**：**D7** 总监不分辨噪声（投「今天天气不错 哈哈哈哈」⇒ 修复前 `made=3`、会话净增 3）；**D8** 没有"对语言的整理"（回收 8 条逐条重复同一句）；**D9** 不做"项目把控"；**D10** 审计消息写入后界面滞后一条。
> · 🔴 **本轮修复调试机制自身 5 个缺陷（比产品缺陷更危险 —— 会把闸门失效伪装成产品红）**：**D11** 台账 `items[]` 不含 `brief` ⇒ D8/D9 改完**无观测面**；**D12** 起点缺「宿主已消费注册」前提 ⇒ `reload` 后立刻跑 **13/25**（形态与产品坏**完全一样**）；**D13** 点击只认几何命中 ⇒ CDP 合成鼠标丢事件被算成产品红；**D14** 取证日志被下一次跑覆盖 ⇒ 假红证据当场丢失；**D15** 消息只走 DOM ⇒ 把"没切视图"误判成"消息没写"。
> · **结果**：真机 `verify-director-logic` **25/25 · `IS_PASS TRUE`**，且 **四连绿**（`reload` 后**立刻**跑 ×1 + 连跑 ×3）· 离线全量 **57 套 / PASS 44 / FAIL 0 / 真机跳过 13** · 单测 `test-split-dimensions` **87**（45→87）· `test-dispatch-log` **16**（14→16）· `test-director-collect` **24** · **破坏性校准 3 次全部精确命中** · 构建 **1,854,200 B / 711 = 711（252 import）/ 指纹 `0c43c1d001267747`** · 装机逐字节一致。
> · **新增 3 条 backlog**：`T-PLUG-051`（🔴 **其余真机闸门的"点击送达"统一加固** —— 本轮只修了一个套件）· `T-PLUG-052`（`reload` 后"可交互预热窗口"根因）· `T-PLUG-053`（把这批闸门能力抽为公共工具）。
> · **`T-PLUG-040` 补第二十三轮实证**：`taskkill /F /IM` **沙箱内外都拒绝访问**（三个 PID `7412/10312/5656` 逐一失败）；本轮**免重启通道已获得判据**——`Page.reload(ignoreCache:true)` + 回读 `window.__dshBuildStamp` 与构建指纹**逐字符比对**（`0c43c1d001267747` 一致）⇒ **确认新 client bundle 已生效且不靠猜**。**仍需用户提权重启一次**才能覆盖主进程侧变更。
> · 新占用编号自 `T-PLUG-051` 起。
>
> **第二十四轮并入（2026-09-17 · 用户原话「整个的**逻辑测试**…总监的**分配机制**也是好用的，**整个调试的机制**也是好用的…拿**具体实际项目**去发送，然后可以发送一些**无意义的东西**让总监去**分辨**…遇到问题就改问题…记下整个流程的情况…**重复该流程起码四十次**」）**
> · **结账 1 条**：`T-PLUG-051`（🔴 其余真机闸门的「点击送达」统一加固 —— 本轮**全量完成**）迁入 `04`。
> · 🔴 **`T-PLUG-051` 的真因（本轮证伪旧归因）**：不是「Electron 静默丢事件」，而是 **`visibilityState === "hidden"` 时 CDP 的 `mousePressed`/`mouseReleased` 被整条吞掉** —— 而 `document.hasFocus()` **仍为 true**（**骗人的代理指标**，第二十三轮就是用它反推才得出错误结论）。**处置**：`_cdp-focus.mjs` 判据换成 `visibilityState` + **11 个真机套件统一加装** + `DSH_FOCUS_CALIBRATE=hidden` 破坏性校准（**恰红 `NS-0v` 一条、早退零消耗**）。效果：`verify-novel-split` **95–99/107 → 108/108**、兜底依赖 **8 → 0**、耗时 **97s → 16s**。
> · **`T-PLUG-052` 结论更正**：原写「窗口不可见**已排除**」⇒ **被本轮受控对照推翻**，该条正文已改为"**这就是主因**"。
> · **重复 → 有分辨力的重复**：新增语料池 `_corpus-director.mjs`（**8 条 = 5 类噪声 × 4 个真实项目**，路径全部 `existsSync` 核实）+ 40 轮连跑编排 `repeat-director-logic.mjs` + 离线闸门 `test-director-corpus` **33/33** ⇒ **29/29 × 40 全 PASS · 累计新建会话 0 · 活会话恒定 28 · C1–C8 各跑 5 次 · 总 1154s**。
> · **本轮修掉 6 类闸门缺陷**（全是"把没等到读成产品坏"）：① 等待预算只有实测延迟的 **1/3**（实测 974–1093ms/次）⇒ 改**有界轮询**；② 判据**语义反转自造红**（`toastGone`）；③ 🔴 **判据用产品不存在的属性 ⇒ 空真绿**（`data-children`，比红更坏）；④ **前提缺失报 FAIL** ⇒ **实体化**（复用优先、缺才 fork）；⑤ **动画没播完量几何**（`.14s` 的动画实测 **1950ms** 才归位）⇒ 归位轮询；⑥ **差额判红** ⇒ 放宽为「**备份 ⊇ 被删**」+ 新增「差额不无声」。
> · **产品侧 3 处读数改动**：`data-reason`（让「分辨」**可断言** —— 此前 40 轮的核心验收项**没有观测面**）· `data-hit`/`data-noid`（三口径分开）· `clearDirectorMessages` 回传 `hit`/`noId` + 结果句写明差额原因。
> · **结果**：真机 `verify-novel-split` **108/108** · `verify-mindmap` **103/103 零跳过** · `verify-flow` **80/80** · `verify-design-studio` **94/94** · `verify-director-logic` **29/29**；离线全量 **58 套 / PASS 45 / FAIL 0**；基线重封 **90 文件 / 3,615,328 B / 漂移=0**；**破坏性校准 2 次精确命中**。
> · **新增 4 条 backlog**：`T-PLUG-054`（🔴 `el.click()` 兜底降级为**诊断路径** + 兜底命中计数）· `T-PLUG-055`（`visibilityState` 变更**归因未分离**）· `T-PLUG-056`（其余套件的"固定 `sleep` 后读状态"**全扫一遍**）· `T-PLUG-057`（语料池扩容 + **跨重启连跑**）。
> · 新占用编号自 `T-PLUG-054` 起。**（上述 4 条已于第二十五轮全部结账迁 `04`）**
>
> **第二十五轮并入（2026-09-17 · 用户原话「可以按照那得需求建议修改 / 修改完成之后对项目收尾」）**
> · **结账 4 条**（逐条取证后迁 `04`）：
>   · `T-PLUG-054` ⇒ 兜底已降级为**诊断通道** + `stats{mouse,diag,geomFail}` + 收尾打印「真实鼠标 N ｜ 诊断通道 M ｜ 几何未命中 K」。实测 `verify-novel-split` 收尾读数 **「真实鼠标 0 ｜ 诊断通道 2 ｜ 几何未命中 0」**（前提成立时主流路径已回到真实鼠标）。
>   · `T-PLUG-055` ⇒ **分离实验完成**：撤销 `setFocusEmulationEnabled`、再单独撤销 `bringToFront`，两次 **`visibilityState` 仍为 `visible`** ⇒ 驱动者是**窗口遮挡**，而非某一个 API。结论已写进锚点 §八 纪律 90 与 §7.20。
>   · `T-PLUG-056` ⇒ 两轮扫描（宽口径 **56 处** → 高危 **26 处**）+ 三组探针实测 ⇒ **降级为加固项**：**前提成立时派发延迟仅 58–76 ms（中位 74）**，旧记的 974–1093 ms 是 `hidden` 态测的（纪律 91 已就地更正）。
>   · `T-PLUG-057` ⇒ 语料 **8 → 13 条** + 负对照 **8 条**（边界 4 / 校准 4）⇒ `test-director-corpus` **51/51**；**跨重启连跑打通**：**6/6 轮 29/29**（含 **2 次冷启动**、端口 **9222 → 9223**、总 **163s**）。
> · ✅ **本轮"需求建议"6 条全部落地**：**U8** 兜底降级为诊断路径 · **U9** `visibilityState` 归因分离（结论 = 窗口遮挡）· **O1** 兜底命中计数并打印 · **O2** 全仓 `sleep(` 后读状态扫描 · **O3** 语料池扩容至 13 条 · **O4** 跨日（跨重启）连跑。
> · 🔴 **本轮修掉 8 条问题（G1–G8，全部记入 `AGENTS.md` 闸门纠错台账（六））**：**G1** 停机只看 `taskkill` 文案 ⇒ **假成功**（真因：幽灵仍占 9222 ⇒ 新实例**进程活着、界面也起，唯独 CDP 绑不上**；铁证 `bind() returned an error … (0x2740)` + `Cannot start http server for devtools`）· **G2** 连跑段间不换端口 ⇒ 非重启轮回连旧端口（**我自造，306s INVALID**）· **G3** `RU-2/RU-3` 被 INVALID 轮染红 ⇒ 改 `validRows` + 非空守卫 · **G4** `_harness.mjs` 端口顺移条件写反 · **G5** `test-harness-ctrl` 中文串内嵌 ASCII 双引号 ⇒ `SyntaxError` · **G6** `HT-7` 判据期望写错 · **G7** 🔴 **`RU-4b` 假红实为口径错**（判据隐含"全程同一实例"，而复用索引按 **origin** 隔离 ⇒ 跨重启必然重建，实测 19）· **G8** 冷启动**起点自举缺失**（`run01` **8/29** ⇒ 修后 **29/29**）。
> · **新增 4 条纪律 97–100**（跨重启真因 / 起点自举唯一实现 + 可分辨归因 / 判据生命周期 / 不许抛穿 + 虚拟时钟）。
> · **新增 1 套闸门**：`test-harness-ctrl.mjs` —— **33 PASS / 0 FAIL / 3 可分辨跳过**（含**虚拟时钟**驱动的 `ensureDirectorPage` 契约：HT-22 零点击正对照 / HT-23 四种归因互不相同 / HT-27 抛穿防护 / HT-28 发 Esc）。
> · **新增/重写工具 2 件**：`_cdp-startup.mjs`（`ensureDirectorPage` **统一起点自举**，四段式 + 四类可分辨归因）· `_harness.mjs`（**验证式停机 + 端口顺移 + stdout 留痕**）。
> · **结果**：离线全量 **59 套 / PASS 46 / FAIL 0 / 真机跳过 13**；真机 `verify-novel-split` **108/108** · `verify-mindmap` **103/103 零跳过** · `verify-director-logic` **28/29**（唯一红 = 下条 `T-PLUG-058`）。
> · **新增 3 条 backlog**（见下表）：`T-PLUG-058`（🔴 **冷启动后复用索引随 origin 丢失 ⇒ 首次派发必然重建 N 条**，🟠 待裁决）· `T-PLUG-059`（🟡 `DL-5a/5b/7a/7b` **间歇同红**约 **1/5**，观察项；**用户已明确"先收尾"，本轮不追**，但诊断面已补）· `T-PLUG-060`（🟡 **收尾审计发现：索引只到文件级，函数级覆盖率实测 0%** ⇒ 执行标准 §3.11 步骤 4 的未达标项，**不在收尾轮内新建**，已给建议方案）。
> · 新占用编号自 `T-PLUG-058` 起（**已用至 `T-PLUG-060`**）。
> · ⚠️ **本轮无待授权项**（未碰宿主代码、未改冻结契约；`lib/client.js` 字节变动仅因 `src/**` 与 `key-files.js` 重生成）。

---

## 待授权（🔴 唯一硬阻塞 —— 属"不可逆操作"，按铁律须用户先确认）

| 编号 | 任务 | 优先级 | 关联文档 | 状态 |
|:-----|:-----|:------:|:---------|:-----|
| T-PLUG-005-B6 | 🔴 **宿主退坡**：F3 需改宿主 `workspace/@deepseek-ai/dsh-client-ui-conversation/lib/client.js` **1 行** | P1 | `dsh-director-plugin/docs/03-批次6接线与退坡方案.md` | 🔴 **待授权**。F1/F2/F4/G1/G4 经实测**零改动** · F5 已由 MOD-B 退坡。⚠️ `workspace/**` 被 `.gitignore` 排除 ⇒ **git 无法回滚** —— 唯一回滚点是 `snapshots/host-b6-20260911-before-patch`（已在备份清理中**硬约束保留**）⇒ 依铁律须先确认。<br>**本批新并入**：`T-PLUG-030` 的**宿主侧**一处（`client.js:6757` 把 `docs`（map）当数组用 ⇒ 「共 undefined 篇文档」+ `TypeError` 被空 `catch` 吞掉）。**同源、同批、同一个文件** ⇒ 与 F3 一起改才不重复动宿主。<br>**下一步（用户点头即可执行）**：`git stash` 不可用（未纳管）⇒ 改前先 `cp` 一份带时间戳的副本到 `snapshots/`，改后跑 `verify-flow` + `verify-dialog` + 重启复验。 |

---

## 挂起 / backlog（低优先，不占主线）

| 编号 | 任务 | 状态 |
|:-----|:-----|:-----|
| T-V12-008 | git push 到 GitHub（含凭据 store 配置） | ⏸ **挂起**（2026-09-11 用户决策：仓库 public、远程历史直接废弃、VPN 暂不开启；方案见 `20-任务文档/32`）—— **属用户决策项，不由 AI 单方推进** |
| T-V12-101~104 | 后优化 4 项：`_memory`/`_snapshots` 自动归档脚本 · 项目级标准摘要版 · V10 V3 测试报告合并 · `patches/` 归档策略 | 🟡 backlog（P3）。**前置已满足但价值待排序**：现有 `06-工作快照.md` + `04` 台账已承担其大部分职能 ⇒ 建议下次容量治理时与"索引自动化"合并评估 |
| T-COG-004 | 07 号报告发现项复核（`40-测试质量` / `20-任务文档` 台账题名与实况一致性） | 🟡 backlog（P3）。本轮已顺带清掉其中一类（**台账过期**：`T-PLUG-012` 与 `T-PLUG-032` 的 4 条状态均与实况不符，已纠正并留证）⇒ 剩余为文档**题名**层面的核对，独立小批量 |
| T-P2-001 | 阶段2 本地模型真接入（Ollama 调用封装 + prompt 构建 + 输出解析 + 降级策略） | 🟡 backlog。插件已有配置面（`config/model.js` + `dp-model-menu` 候选与置灰），但 `callLocalModel` 在 `localModel.enabled` 默认关时为死代码 ⇒ **在真接入之前，R8 的模型切换只改"选择"不改"执行"**（界面已如实置灰不可用项） |
| T-PLUG-037 | `verify-design-studio` 的 **C14.5 / C14.5b / C14.5c（设计图版本回滚三连）偶发红**加固 | 🟡 **观察项**（2026-09-16 第十五轮登记）。**产品侧已用独立正控证伪"产品坏了"**：`scripts/_probe-studio-restore.mjs` 走用户路径（＋新建图 → 保存 → 加元素 → 开版本面板 → **真实鼠标**点回滚）实测元素 **21 → 20**、按钮文案 **「✓ 已保存 v1」**、面板**自动收起** —— 三条判据全中；闸门自身 45/45 点击落点亦在目标子树内 ⇒ 判为偶发（1 红 1 绿），复跑 `fail=0`。**处置方向**：再现时按"先补点击前几何 / 命中取证 + **一次有界重试**，再断言"加固，**优先怀疑起点等价性**（纪律 33：e2e 不重载页面 ⇒ 状态跨运行保留）。台账正文见锚点 §7.10 |
| T-PLUG-040 | **环境**：Harness 有 3 只 `taskkill` **杀不掉**的进程（`7412 / 10312 / 5656`，一律"拒绝访问"） | 🟡 **需用户手动处理**（2026-09-16 第十八轮登记 · **第十九轮补实测**）。**属性：环境，非产品** —— `MSYS_NO_PATHCONV=1 taskkill /F /IM "DeepSeek Harness.exe"` 只杀掉本会话自己拉起的进程 ⇒ 连续"重启 Harness"其实是**幽灵重启**（app 端口恒 `15117`、`page.url` 逐字相同）。**纪律 61**。绕行①`Page.reload(ignoreCache:true)`（**由浏览器进程处理**、3s 无损）—— 第十九轮已把它**升格为正式脚本 `scripts/reload-client.mjs`**（原 `_tmp-reload.mjs`，因为"主进程不可结束"是**长期**事实而非一次性），并新增指纹回读判据；绕行②新产物生效与否现在**可直接读 `window.__dshBuildStamp`** 对上 build 输出。<br>🔴 **第十九轮新增实证**：清理全部会话后，**磁盘/登记/缓存三处已 0**，但宿主仍列 **115 条幻影会话**（会话列表在主进程内存里）⇒ 用户会以为"没清干净"；**此状态不跑真机闸门**（只会测到半清理态）。**彻底解决需手动结束进程或重启机器**（重启后应补跑一次真机：首跑场景 `reused=0 ⇒ created=8`）。<br>🔴 **第二十轮补实测（好消息）**：**不重启也能让宿主列表变空** —— 走宿主官方通道 **`api.workspace.archiveSession({sessionId})`**（**不在** `sessions.*` 命名空间下，此前只扫过 `api.sessions` 所以漏掉；签名见宿主 `dsh-host-apiproxy/.../api/workspace.schema.js`）。新工具 `scripts/purge-host-sessions.mjs` 实测 **115 条全部下架成功（失败 0）**，**侧栏 `sessionRow` 6 → 0 行**。⚠️ 但 **`byId` 只增不减**，不能当判据；且宿主会把内存态**回写磁盘**（reload 后 `archivedSessionIds` 又变回 115）⇒ **「三口径 0」只在宿主重启后才稳定**（纪律 71） |
| T-PLUG-041 | **外部依赖**：真机循环下模型侧**零输出**（`turns:1` 投递成功但 `tok:0`）⇒ 8 条派发全判 `partial` | 🟡 **观察项**（第十八轮登记 · **第十九轮根因坐实**）。🔴 **真因已实测**：宿主逐字回报 `turn/end.reason.error = {message:"Insufficient Balance", code:"QUOTA", status:402}`（8/8 条，逐条相同）；同轮全环境对照 **131 条会话里 114 条有真实 token** ⇒ **环境有产出能力，唯独这 8 条被模型侧拒绝** —— 属**模型配额**，不是插件缺陷。**处置**：插件侧已**如实落地**该原因（`extractTurns.endFailure` → 台账 `runFailure` → `countStates.hostFailed` → 裁定文案「**简报已送达**，是模型侧没出结果」），用户据此可**直接处置**（充值 / 换模型）。**待用户侧确认额度**；UI 表现可用历史证据链验收 |
| T-PLUG-042 | **宿主 `rename` 可用却未启用**：`store/split-index.js` 走的是**插件侧标签覆盖**，而实测宿主 RPC `api.sessions.rename({sessionId,title})` **存在**、且插件**拿得到**会话实体（`scopedConversationOf(id).scopedSession()`） | 🟡 backlog（P2，第十九轮登记）。旧结论「宿主无 rename ⇒ 只能插件侧覆盖」**已被实测推翻**（两条事实见 `14-…20260916.md` §2.3.1）。**痛点**：只有插件视图里标题好看，宿主自己的会话列表/搜索里仍是默认标题 —— 而这正是"一百多个会话分不清"的一半原因。**建议**：派发时优先调 `rename`，失败再回落插件侧覆盖且**降级可见**（纪律 19）；改前**先读原标题**留档以便复原。**代码内已就地更正注释**（`split-index.js` 头部），未实现前不得读成"已按宿主能力做过了" |
| T-PLUG-043 | **派发前配额预检**：模型侧 QUOTA 时仍会投出 8 条简报、8 次运行全失败 | 🟡 backlog（P3，第十九轮登记）。见 `14-…20260916.md` §八 8.3。**建议**：派发前做**零成本**判据（读上一次 `turn/end` 的 failure），命中 QUOTA 类失败就**不派发**，直接在总监页说明"配额不足，先充值或换模型"。**代价**：若改用探针则可能自耗额度 ⇒ 优先选"读上次结局" |
| T-PLUG-044 | **真机闸门「起点自举」抽为公共工具**：零会话时 `verify-novel-split` 的 A1 原会级联假红 | 🟡 backlog（P3，第十九轮登记）。本轮已在 A1 内联修好（零会话 ⇒ 真实鼠标点「新会话」，判据与 `_probe-session-count.mjs --click-new` 一致）。**建议**：抽 `scripts/lib/ensure-session.mjs` 供所有真机闸门共用（清空会话后"零会话"是**常态起点**）。**代价**：一次小重构 |
| T-DOC-001 | **文档治理批次**（来源：并行会话的独立验收报告 `.cluster/resume-20260916/02-文档实现差异对照.md` A/B 类**剩余**项；同批的 A1–A4 / A6 / A7 / A9 / A12 / A13 已于 2026-09-16 第十八轮**当场修完**） | 🟡 backlog（P3，第十八轮登记）。剩 7 条：**A5** `05-项目大索引` 与锚点纪律 8 仍写「不新建 V17 文档」，而 09-14 已修正为「**允许**新开版本号」且 V17 文档族实际存在 · **A10** `docs/50-信息中心/01-踩坑记录` §二 以「改 workspace 宿主」为主语（主管道早已是插件通道）⇒ 待加横幅 · **A11** `最终版开发文档-V16` 自称「开发执行最高依据」与锚点「唯一权威」并列冲突 ⇒ 待降级为历史参考 · **A14** 各处「提示 16 / 提示 17」混杂 ⇒ 统一 · **B4** 死引用族（28/29/30/31 号文写插件路径 `D:\hermes-data\dsh-director`，**该路径不存在**）⇒ 待加「路径已迁移」横幅 · **B5** `AI对话执行约束规范` 等引用的 5+ 文件项目内不存在 ⇒ 待登记为历史残留或补建 · **B6** `restore-a14.py --check` 校验的快照目录已于 09-14 清理时删除 ⇒ **自检必失败**，待改指向 `snapshots/host-b6-*` 或退役 |
| T-PLUG-047 | **总监页缺「会话总数 / 复用·新建」常驻读数**：用户是从**导图上 126 个框**发现重复建会话的，而面板上没有任何地方写着现在到底几条会话 | 🟡 backlog（P2，第二十一轮登记）。**痛点**：没有这个数，用户只能靠**数框**判断系统是否在乱建会话 ⇒ 反馈天然滞后（本轮的一百多个会话就是这么来的）。**建议**：总监页常驻一行 `现存会话 N 条（归档 M 条）｜本轮 复用 R · 新建 C`。**数据源现成**：树的 `rows` + `diag.archivedExcluded` + 派发台账的 `reused/created`。**代价**：一处渲染 + 一条「读数与实测一致」的断言（否则读数本身会成为新的静默点，纪律 19） |
| T-PLUG-048 | **孤儿桶无处可查**：`localStorage` 里 `dsh.director.store.director-<key>` 桶，其**所属会话已不在树上**时**没有任何入口能读到它**（实测 `director-d1yi2z` 桶里留着 **2 条**消息，但用户在界面上**永远看不见**） | 🟡 backlog（P2，第二十二轮登记）。**痛点**：这类桶既不显示、又不清理 ⇒ 既是"消息丢了"的错觉来源，又是配额只增不减的静默来源。**建议**：维护页加一行「**存储体检**」读数：`桶总数 N（孤儿 M）`，并提供「查看孤儿桶」+ 一次性「清理孤儿桶」（走纪律 83 的**破坏性动作自带退路**：先备份再清）。**判据**：读数与实测一致 + 孤儿判定有正负对照（在树上的会话**不得**被判孤儿） |
| T-PLUG-049 | **桶数量线性增长的配额预检**：每切一个作用域就多一个 `localStorage` 桶，**没有任何上限 / 淘汰 / 用量读数** | 🟡 backlog（P3，第二十二轮登记）。**风险**：与 `T-PLUG-035`（Cookie 体积越预算 ⇒ 白屏）**同型**，但发生在 `localStorage` —— 配额打满时 `setItem` **抛异常**，而存储层当前只降级不报数（纪律 19）。**建议**：① 桶数 / 总字节数进诊断读数；② 超阈值时**最旧优先**淘汰（参照 `COOKIE_TOTAL_BUDGET` 的现成写法）且**每次淘汰必告警**；③ 离线闸门 `test-store-budget.mjs`（含**负向校准**：合成超限必须判红）。**代价**：中 |
| T-PLUG-050 | **写消息触点补全（需用户先确认语义）**：现在只有 4 个动作会往「总监消息」里写（统筹 / 派发 / 回收 / 运行总监链），而「**登记为流转**」**只写 flow、不写消息** | 🟡 backlog（P2，第二十二轮登记 · **阻塞在语义确认**）。**问题**：用户若只做"登记"操作，总监消息数**恒为 0** ⇒ 会被读成"又没持久化"。**待用户裁定**：登记为流转**该不该**产生一条总监消息？若**该** ⇒ 在 `split-index`/流转登记处补一次 `appendDirectorMessages`（含 `nodeId` 作用域，否则又会串桶）；若**不该** ⇒ 在总监页把「本动作不写消息」**显式写出来**（避免误解）。**两个方案都不改冻结契约** |

| T-PLUG-052 | **`Page.reload` 之后存在一段"可交互预热窗口"** —— 实测 `reload` 后**立刻**跑闸门，按钮点击会失效（13/25）；同一套**连跑**则 25/25。`el.click()` 保底后不再假红，但**真实鼠标**在该窗口内仍可能丢事件 | 🟡 **P3 观察项**（第二十三轮登记）。🔴 **第二十四轮更正**：本条原写「窗口不可见**已排除**」—— **该结论被本轮受控对照推翻**：`visibilityState="hidden"` 时真实鼠标序列**只送达 `pointermove`**，`pointerdown`/`mousedown`/`pointerup`/`click` **一条不到**；`Page.bringToFront` + `Emulation.setFocusEmulationEnabled` 之后 `visible` 且 **6 个事件全到**（详见锚点 §八 纪律 90 与 §7.19）。⇒ 「reload 后立刻跑 13/25」的主因**就是它**，不是独立的预热窗口。**已排除**：hover 位移（带 `mouseMoved` 同样 91ms 成功）。**仍未定位**：是 `setFocusEmulationEnabled` 改写了 `visibilityState`，还是 `bringToFront` 在特定窗口状态下才生效（改挂 `T-PLUG-055`）。**未定位**：宿主何时完成"事件系统就绪"（`__dshDirectorView.registered` 为真**不是**充分条件 —— 纪律 85）。**建议**：找一条**可观测的就绪判据**（而不是靠重试兜住），落地后写进锚点 §八 85 |
| T-PLUG-053 | **把这批闸门能力抽为公共工具**：`clickUntil` / `clickJs` / `clickJsByText` / 带戳留痕 / 双通道读消息（DOM + IndexedDB）目前都内联在 `verify-director-logic.mjs` 里 | 🟡 **P3 backlog**（第二十三轮登记）。**建议**：抽 `scripts/lib/verify-kit.mjs` 供所有真机闸门共用；与既有 `T-PLUG-044`（起点自举抽公共工具）**同族合并实施**更划算。**代价**：一次小重构（须保证各套件判据不因抽取而放松） |
| T-PLUG-058 | 🔴 **冷启动后「复用索引」随 origin 丢失 ⇒ 首次派发必然重建 N 条会话**：`dsh.director.split` 存在 `localStorage`，而 app **端口每次启动都变** ⇒ origin 变 ⇒ 索引为空 ⇒ 明明 8 个维度的会话**都还活着**，仍会**新建 8 条** | 🟠 **P2 · 待裁决**（第二十五轮登记）。**实证**：探针 `{"origin":"http://127.0.0.1:1233","indexN":0,"indexBytes":0,"hostSessions":74,"archived":134}`；判别实验 run05（同 port 9223、同 8 维、那 8 条会话都活着）仍 `created=8`。**已量化**：`run01` **+8**、`run04` **+3**，跨重启连跑共多建 **11 条**。**注意**：第二十四轮的 `RU-4b` 判据**已按此事实改口径**（段内而非全程）并配**植入缺陷校准** ⇒ **口径已正、产品行为仍未修**。**三条候选修法（推荐 A）**：**A** 复用候选集改为**纯读 `sessions.search` 弱匹配**（不依赖 origin 绑定的索引，成本最低、不动冻结契约）· **B** 索引**双写一份到 cookie**（cookie 跨启动持久，但头硬上限 16,384 B，须与 `COOKIE_TOTAL_BUDGET` 共用预算）· **C** 索引写入**宿主侧会话标题**（依赖 `T-PLUG-042` 的 `rename` 落地）。**判据**：冷启动后首次派发 `created=0`，或**如实打印**「索引未命中，本次重建 N 条」并给**可分辨原因**；**须配负对照**（真·新需求必须仍能新建）。**代价**：A 小 / B 中 / C 依赖链长 |
| T-PLUG-059 | 🟡 **`DL-5a/5b/7a/7b` 间歇同红（约 1/5）**：派发后「📥 回收」与 `dp-r5-msg` **一起从 DOM 消失**，旧判据**不打印任何读数**故无法归因 | 🟡 **P3 观察项**（第二十五轮登记 · **用户已明确"先收尾"，本轮不追**）。**现象**：`[D] 回收点击 结果=false · 未命中：{"ok":false,"why":"不在 DOM"}`；复跑后仅红 1 条（`DL-6c` = 即上条 `T-PLUG-058`），其余全绿 ⇒ 判为**间歇**。**健康基线对比**（已捕获）：`{"root":true,"r5view":"director","msgTab":true,"collect":true,"sel":["总监"]}`。**首嫌疑**：投递回退 `open-then-send` 时 `opener(sessionId)` 切会话 ⇒ 宿主切页签 ⇒ **总监页被卸载**。**本轮已补诊断面**：`UI_STATE` 表达式（`root/r5/r5view/msgTab/flowTab/r5body/detail/now/collect/split/deliver/ring/sel`）在 C 段派发后与 D 段回收前**各打印一次**，并进 `DL-5a/5b/7a` 的失败证据串 ⇒ **下次再现即有一手读数**（纪律 54）。**建议**：① 再现时比对两次 `UI_STATE`，判"页签被切走"还是"组件内分支渲染掉了"；② 若确为切页签 ⇒ 回收段前补**幂等回到总监页**（不是重试兜底，纪律 96）；③ 末位再议「投递回退是否必要」—— 若首选通道在正常态恒成功，回退本身就是噪声源。**代价**：小（诊断已就位） |
| T-PLUG-060 | 🟡 **缺「函数级索引」**：本仓索引只做到**文件级** —— `docs/12-源码映射索引.md` 是 **86 个文件**的总表（职责 / 引用 / 设计稿板块 / 上游 / 下游），`@map` 块覆盖 **86/86 文件**但**不含函数名**，`src/logic/key-files.js` 里函数名命中 **0 处** ⇒ 符号定位只能靠全仓 `grep`（实测 **≤2 步**达成，但**不经过索引**） | 🟡 **P3（第二十五轮收尾审计登记）**。**来源**：执行标准 §3.12「索引确认」要求做**定位测试** + §3.11 步骤 4 要求「**脚本函数索引**：提取函数名 / 参数 / 用途，覆盖率 脚本 **100%** / **函数 ≥90%**」⇒ 本仓函数级覆盖率实测 **0%**，**属未达标项**（注意：**不是索引漂移** —— `gen-key-files` / `gen-docs-index` / `gen-source-map` 三者 `--check` **全部一致**，是**粒度缺失**）。**为什么不本轮补**：新建函数级索引生成器（含 `--check` 与**正负校准**）属**新增交付物**，按 §3.2 暂停情形 ③「影响范围大的决策」**不在收尾轮内单方新建**。**建议方案**：扩 `gen-source-map.mjs` 顺带产出函数级表（它**已在逐文件解析并生成 `@map`**，加一张表是增量工作），输出到**新文件**（落在 `src/**` + `lib/client.js` **之外 ⇒ 不触发基线重封**），配 `--check` + 一条「符号 → 文件」的**定位断言**；**代价**：一次中小改动。**判据**：随机抽 **10 个导出符号**，**经索引** ≤2 步定位，覆盖率 **≥90%**（当前 **0%**） |
| T-PLUG-061 | 🔴 **真机批次（12 套）尚未完整跑通一次**：`run-live.mjs` 已具备"整批只冷启动一次 + 逐套标 OK/FAIL/INVALID"的能力，但本轮数次尝试都撞上**并行会话占用同一 Harness**（`dp-root` 未挂载 / 两次探测间宿主状态就变了） | 🟠 **P2**（2026-09-17 第四轮登记）。**归因**：**前提缺失 + 环境争用**，**非产品缺陷**（真机套读活宿主，`dp-root` 不在 DOM 时一切判据都无意义 —— 纪律 30）。**待办**：① 在**无并发占用**时跑一次 `node scripts/run-live.mjs <12 套>` 留全绿记录；② `shot-dialog-bg` 的 **follow / custom 两档复测**（改动后真机未跑通，同因）；③ 记录"整批耗时"与"首套自举耗时"两个数，作为后续真机批的预算基线。**判据**：单次命令内 12 套全 PASS + 冷启动**只发生一次**（日志里 `ensureDirectorPage` 自举计数 = 1） |
| T-PLUG-062 | 🟡 **覆盖面提取器缺"植入缺陷校准"**：`surfaceOf()` 是判据 C 的唯一依据，而它目前**没有任何坏样本验证**（比如"故意写一个引用不存在 `src/xx.js` 的套件，确认不计入覆盖面"） | 🟡 **P3**（2026-09-17 第四轮登记）。**风险**：与"闸门永远绿"同型 —— 提取器若因正则改动整体失效，会**静默**把所有套件判成"无交集"而全跳（**假跳过比假红危险**，已写进 22 号文 §十）。**建议**：给 `_test-ledger.mjs` 加 `--self-check`（4 组样本：真引用 / 注释里的路径 / 不存在的路径 / 变量拼路径），并把它接进 `test-plan.mjs --explain` 的前置。**判据**：4 组样本全部给出期望结论；把 `SRC_ANY` 正则改坏后 `--self-check` 必须变红 |
| T-PLUG-063 | 🟡 **Harness 桌面版无可用更新（待用户拍板处置）**：官方源**停在 `0.1.0-rc.5`**，而本机已 **`0.1.0-rc.11`** ⇒ 点「检查更新」只会得到 up-to-date | 🟡 **外部依赖 · 待裁定**（2026-09-17 第四轮登记）。**一手证据**（Harness 自己打印，不是推测）：`Checking for update` → `Update for version 0.1.0-rc.11 is not available (latest version: 0.1.0-rc.5, downgrade is disallowed).` **原因**：升级链 `app-update.yml`（provider generic · url `.../deepseek-harness-desktop/releases` · channel `rc`）指向的源**没有比本机更新的构建**。**两条候选**：**A（推荐）** 什么都不做（本机已比源新）；**B** 升级**内核** `@deepseek-ai/dsh`（走 npm，与桌面壳分离）—— 但会改变宿主行为，**必须整轮真机复验 + 重封基线**。**代价**：A 零 · B 中高 |
| T-PLUG-064 | 🟢 **清理回收站待确认真删**：本轮清理 **12 项 / 227 文件 / 6.41 MB** 已移入根 `.trash-20260917-1330/`（**软删除，可整体回退**） | 🟢 **P3**（2026-09-17 第五轮登记）。**为什么先软删**：这批里多为 **git 未跟踪**（3 个 0 字节垃圾文件名、`_probe/` 探针、**1.09 MB** 死产物 `dsh_docs_index.txt`、退役生成器 `gen-docs-index.ps1`、4 个旧回收站）⇒ `rm` 之后 **git 救不回**（纪律 83：破坏性动作自带退路）。**待办**：用户确认后 `rm -rf .trash-20260917-1330`（**不可逆**）。**判据**：目录不存在，且根目录不再出现「**0 字节 + 私用区字符（U+E000–U+F8FF）**」文件。**顺带建议**：把 `.trash-*` 加进 `.gitignore`（本轮**未加** —— 不擅自改忽略规则） |

---

> **归档去向**：已完成项与已失效项见 `04-已完成任务清单.md`（2026-09-14 第十三轮结账节）。
> **闸门纠错的逐条取证**见 `AGENTS.md` §闸门纠错台账。
> **落死锚点与冻结契约**见 `10-当前基线-落死锚点-V16.md`（§五 契约表 / §六 闸门清单 / §七 各轮记录）。
