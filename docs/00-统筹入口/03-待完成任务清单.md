# 待完成任务清单 — dsh-client-mod

> **只放「还没做完的事」。** 完成项与失效项一律迁 `04-已完成任务清单.md`。
> 2026-09-14 已做过两次清账：第一次（158 行 → 41 行）把 40+ 条 ✅/已失效行迁入 `04`；
> 第二次（同日晚，真机跑完）把 T-PLUG-033 / T-PLUG-034 结账迁出。当前 **4 组 / 9 条**。

## 进行中

| 编号 | 任务 | 优先级 | 关联文档 | 状态 |
|:-----|:-----|:------:|:---------|:-----|
| T-PLUG-032 | 🎨 **第 3 批界面调整（14 条）中尚未落地的 9 条** | P1 | `50-信息中心/V18-界面调整设计稿.html`（板块 A–H） | 🟡 **用户已审图并给出第 4 批 5 条修正（已落地 + 真机全绿 + 基线重封）**。剩余 9 条：对话列浮窗 · 折叠态单箭头 · R3 去除 + 模型前置 · 模型切换器 · R4/R7 左右缩回 + hover 5s + 固定 · R5 语义统一 · R7 执行功能区 · 对话框左右清单。**按用户「先出设计图，我看看没问题再做」的口径，开工前需一次确认** |

## 待排期

| 编号 | 任务 | 优先级 | 关联文档 | 状态 |
|:-----|:-----|:------:|:---------|:-----|
| T-PLUG-005-B6 | 🔴 **宿主退坡**：F3 需改宿主 `workspace/@deepseek-ai/dsh-client-ui-conversation/lib/client.js` **1 行** | P1 | `dsh-director-plugin/docs/03-批次6接线与退坡方案.md` | 🔴 **待授权**。F1/F2/F4/G1/G4 经实测**零改动** · F5 已由 MOD-B 退坡。⚠️ `workspace/**` 被 `.gitignore` 排除 ⇒ **git 无法回滚**，依铁律须先确认 |
| T-PLUG-012 | 📄 `assets/docs-index.json` 索引 90 篇 vs 磁盘 96 篇（**缺 6 篇，0 篇多余**） | P2 | `dsh-director-plugin/assets/docs-index.json` | ⏳ 待排期。**为何不现在修**：① 现生成器 `gen-docs-index.ps1` 是注入宿主 `client.js` 的旧 A14 前形态（PowerShell 通道无输出）；② 重新生成会改产物字节 ⇒ 尺寸锚点与整套验证基线需重跑；③ 与 A14 部署偏差同源，应一并处理 |
| T-PLUG-030 | 📄 **「文档上下文」死链路**（宿主与插件副本都把 `docs` map 当数组 + 五步根本没读文档） | P2 | `workspace/.../lib/client.js:6757` · `src/logic/process.js:104` · `src/logic/director-run.js` | ⏳ 待排期（**建议随 B6 一并做**）。三者同源：修①要改宿主（不可回滚）；修②在当前 `localModel.enabled` 默认关时是死代码；修③要重新生成外置资源 ⇒ 单做只是把死代码搬个位置 |
| T-PLUG-035 | 📄 **把「Cookie 体积」纳入常态闸门**（T-PLUG-034 的残余项） | P2 | `scripts/test-cookie-budget.mjs` · `scripts/verify-data-safe.mjs` | ⏳ 待排期。现状：预算层已有**离线**闸门（21/21，含负向校准），且真机实测 `document.cookie.length = 6,033`（修复前 16,794）**稳定**；但**没有一条真机闸门**在每轮断言"体积没越预算"。⇒ 建议并入 `verify-data-safe`（它本就在真机跑 localStorage 快照），加一条"当前 `dsh_director_*` 总量 ≤ 12,288 B"并配正负对照 |

## 待用户决策

| 编号 | 任务 | 优先级 | 关联文档 | 状态 |
|:-----|:-----|:------:|:---------|:-----|
| T-AESTH-002 | 审核结构项并入插件化批次（P0-1~P0-4 / P1-6 / P1-7） | P2 | `40-测试质量/19` §五 + 26号文 §八 | 🟡 **待用户确认是否作废**（对应组件已随批次 10–15 重写，可能已无对应物） |

## 挂起 / backlog（低优先，不占主线）

| 编号 | 任务 | 状态 |
|:-----|:-----|:-----|
| T-V12-008 | git push 到 GitHub（含凭据 store 配置） | ⏸ **挂起**（2026-09-11 用户决策：仓库 public、远程历史直接废弃、VPN 暂不开启；方案见 `20-任务文档/32`） |
| T-V12-101~104 | 后优化 4 项：`_memory`/`_snapshots` 自动归档脚本 · 项目级标准摘要版 · V10 V3 测试报告合并 · `patches/` 归档策略 | 🟡 backlog（P3） |
| T-COG-004 | 07 号报告发现项复核（`40-测试质量` / `20-任务文档` 台账题名与实况一致性） | 🟡 backlog（P3） |
| T-P2-001 | 阶段2 本地模型真接入（Ollama 调用封装 + prompt 构建 + 输出解析 + 降级策略） | 🟡 backlog。插件已有配置面（`config/model.js`），但 `callLocalModel` 在 `localModel.enabled` 默认关时为死代码 |

---

> **归档去向**：已完成项与已失效项见 `04-已完成任务清单.md`（2026-09-14 两次清账两节）。
> **闸门纠错的逐条取证**见 `AGENTS.md` §闸门纠错台账。
