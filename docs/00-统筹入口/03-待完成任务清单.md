# 待完成任务清单 — dsh-client-mod

> 待办数据源。每条任务关联开发文档路径。完成后移动到04-已完成任务清单.md。

## 进行中

| 编号 | 任务 | 优先级 | 关联文档 | 状态 |
|:-----|:-----|:------:|:---------|:-----|
| T-FIX-V001 | V9.4 修复人工验证：zb2 滚动 + P1×7 共 9 项验证清单（Harness 重启后逐项过） | P0 | 20-任务文档/21-zb2滚动失效修复与全量审计P1修复记录.md §五 | 🔄 代码已 apply，待重启验证 |
| T-FIX-V002 | V9.5 调整人工验证：z tab 令牌化配色/徽标清理/操控性 共 7 项验证清单（与 V9.4 同次重启一起过） | P0 | 20-任务文档/22-z-tab交互显示规范化调整记录-V9.5.md §5.2 | 🔄 代码已 apply，待重启验证 |
| T-V9D-001 | 批次1：D-01 主题变量体系+设置弹层（主色4/记忆色3/密度2/恢复默认/localStorage） | P0 | 20-任务文档/18-总监驾驶舱UI设计方案.md 附录I | ✅ 完成（随 MOD-B 退坡，对应 UI 已由 dsh-director 插件承载——台账回写 D3/17号） |
| T-V9D-002 | 批次1：D-02 布局骨架重构（纵向堆叠R1-R8+clamp+断点880/640） | P0 | 同上 附录I | ✅ 完成（随 MOD-B 退坡，对应 UI 已由 dsh-director 插件承载——台账回写 D3/17号） |
| T-V9D-003 | 批次1：D-03 docs-index.json 生成脚本（apply.ps1集成） | P0 | 同上 附录I | ✅ 完成（随 MOD-B 退坡，对应 UI 已由 dsh-director 插件承载——台账回写 D3/17号） |
| T-V9D-004 | 批次2：D-04 T2核心认知提取+R2总览卡片 | P0 | 同上 附录I | 🟡 **设计层已覆盖**（`V15.1` 视图 2 · R2 总览卡片 = 定位/目标/阶段 三徽章 + 4 指标卡 + 栅格尺寸标注）；实现层由 dsh-director 插件承载，随 T-PLUG 系列推进 |
| T-V9D-005 | 批次2：D-05 R4项目导航三Tab（文档树/待办/关键文件） | P0 | 同上 附录I | 🟡 **设计层已覆盖**（`V15.1` 视图 2 · R4 三 chip = 📄文档树 / 🗺️路线图·待办 / ⭐关键文件 + 「默认 200px · 可拖 60px~40%」）；实现层随 T-PLUG 系列 |
| T-V9D-006 | 批次2：D-10 记忆持久化加固（自动归档+X2降级） | P1 | 同上 附录I | 🟡 **设计层部分覆盖**（`V15.1` 视图 2 · R6 = 记忆/决策/风险 + 回结收件箱 + hover 展开）；**归档与 X2 降级机制未在设计稿展开**，随 T-PLUG 系列补 |
| T-V9D-007 | 批次3：D-06 R3技能Tab+智能体重样式（静态清单/搜索/禁用态） | P0 | 同上 附录I | ⏳ 待执行 |
| T-V9D-008 | 批次3：D-07 R5消息动作+分支卡片增强（分派/决策/方案/审核） | P0 | 同上 附录I | ⏳ 待执行 |
| T-V9D-009 | 批次3：D-08 R7 a-2三模式（对话/文档/详情） | P1 | 同上 附录I | ⏳ 待执行 |
| T-V9D-010 | 批次4：D-09 异常降级体系（X1-X11）+全量回归 | P1 | 同上 附录I+附录G | ⏳ 待执行 |

### 历史进行中（V9之前，已被V9设计取代或合并）

| 编号 | 任务 | 优先级 | 关联文档 | 状态 |
|:-----|:-----|:------:|:---------|:-----|
| T-P1-001 | 阶段1-M1：新增locale词条（总监相关9条zh+9条en） | P0 | 20-任务文档/04-阶段1详细开发文档.md §M1 | 🔄 进行中 |
| T-P1-002 | 阶段1-M2：新增总监消息store（directorStoreFactory + useDirectorStore） | P0 | 20-任务文档/04-阶段1详细开发文档.md §M2 | ⏳ 待执行 |
| T-P1-003 | 阶段1-M4：新增mock处理函数（directorProcessMock + onForward回调） | P0 | 20-任务文档/04-阶段1详细开发文档.md §M4 | ⏳ 待执行 |
| T-P1-004 | 阶段1-M3：新增DirectorView组件（标题栏+三面板+对话流+输入栏+设置弹窗） | P0 | 20-任务文档/04-阶段1详细开发文档.md §M3 | ⏳ 待执行 |
| T-P1-005 | 阶段1-M5：新增总监tab注册（id:director, order:-1, sendToChat） | P0 | 20-任务文档/04-阶段1详细开发文档.md §M5 | ⏳ 待执行 |
| T-P1-006 | 阶段1-验证：node --check语法检查 + apply.ps1应用 + Harness测试 | P0 | 20-任务文档/04-阶段1详细开发文档.md §七 | ⏳ 待执行 |

## 待排期

| 编号 | 任务 | 优先级 | 关联文档 | 状态 |
|:-----|:-----|:------:|:---------|:-----|
| T-P2AUDIT-001 | V9.4 审计 P2×13 项按需修复：死代码清理(3574/8777)/持久化性能(6329/5510)/密度设置失效(6501)/调试残留(8733/9253)/docEditor 丢内容(8892)/首开滚动位置确认(7061) 等 | P2 | 20-任务文档/21-zb2滚动失效修复与全量审计P1修复记录.md §四 P2 清单 | ⏳ 待排期 |
| T-P2-001 | 阶段2：本地模型集成（Ollama调用封装 + prompt构建 + 输出解析 + 降级策略） | P1 | 20-任务文档/03-总监对话模式开发文档.md §阶段2 | ⏳ 待排期 |
| T-P3-001 | 阶段3：自动转发+原对话联动验证（三tab数据共享 + 原功能回归） | P1 | 20-任务文档/03-总监对话模式开发文档.md §阶段3 | ⏳ 待排期 |
| T-P4-001 | 阶段4：三面板实际内容（智能体列表/技能列表/执行逻辑开关） | P2 | 20-任务文档/03-总监对话模式开发文档.md §阶段4 | ⏳ 待排期 |
| T-P4-002 | 阶段4：设置面板（执行逻辑详细配置 + 本地模型配置 + 持久化 + 继承制） | P2 | 20-任务文档/03-总监对话模式开发文档.md §阶段4 | ⏳ 待排期 |
| T-P5-001 | 阶段5：样式优化 + 思考过程展示 + 自动审核产出 + 全流程回归 | P2 | 20-任务文档/03-总监对话模式开发文档.md §阶段5 | ⏳ 待排期 |

## 整改 V1.2 清理段（2026-09-06 已完成）

| 编号 | 任务 | 优先级 | 关联文档 | 状态 |
|:-----|:-----|:------:|:---------|:-----|
| T-V12-001 | ✅ 已完成：scripts/_archived-20260831/ 清理（28 个文件） | P1 | 40-测试质量/12-scripts探针审核-20260906.md §三 P0 | ✅ 已清理（移到 04-A14） |
| T-V12-002 | ✅ 已完成：scripts/ 一次性探针清理（49 文件） | P1 | 同上 §三 E.1-E.4 | ✅ 已清理（移到 04-A15） |
| T-V12-003 | ✅ 已完成：V10 V3 整改脚本清理（4 文件：fix-b/fix-load/fix-save×2） | P2 | 同上 §三 P3 | ✅ 已清理（合并在 A15） |
| T-V12-004 | ✅ 已完成：V10 测试历史清理（gui_test_v2×2 = 2 文件） | P2 | 同上 §三 P3 | ✅ 已清理（合并在 A15） |
| T-V12-005 | ✅ 已完成：backups/dsh-director-backup-* 清理（2 目录，~40MB） | P1 | 20-任务文档/23-dsh-整改总览-V1.2-20260906.md §五 | ✅ 已清理（移到 04-A16） |
| T-V12-006 | ✅ 已完成：backups/client.js.tmp + client-v2.js 清理（555KB） | P1 | 同上 §五 | ✅ 已清理（移到 04-A17） |
| T-V12-007 | ✅ 已完成：snapshots/manual-bak-batch1 清理（1 文件 583KB） | P2 | 同上 §五 | ✅ 已清理（移到 04-A18） |
| T-V12-008 | 🔴 高风险：git push 到 GitHub（含凭据 store 配置） | P1 | 整改总览 §五.🔴 | ⏸ **已登记待实施**（2026-09-11 用户决策：仓库 public、远程历史直接废弃、VPN 暂不开启→挂起；方案见 `20-任务文档/32`） |
| T-V12-101 | 📌 后优化：_memory/_snapshots 自动化归档脚本 | P3 | 整改总览 §九 | 🟡 列入 backlog |
| T-V12-102 | 📌 后优化：项目级 execution-unified-standards 摘要版 | P3 | 整改总览 §九 | 🟡 列入 backlog |
| T-V12-103 | 📌 后优化：V10 V3 测试报告合并到 40-测试质量/测试总清单与质量门禁.md | P3 | 整改总览 §九 | 🟡 列入 backlog |
| T-V12-104 | 📌 后优化：patches/ 7 个 diff-report 合并归档策略 | P3 | 整改总览 §九 | 🟡 列入 backlog |

## 项目认知初始化衍生待办（2026-09-11 新增 · 源自 07 号报告 §十）

| 编号 | 任务 | 优先级 | 关联文档 | 状态 |
|:-----|:-----|:------:|:---------|:-----|
| T-COG-001 | ~~确认/恢复 dsh-director 插件源码工程~~ → **修正为：执行 T-PLUG-005 内联实现迁移进 `dsh-director-plugin/`** | **P1** | 07-项目认知初始化报告-20260911.md §十（**已撤销原 P0-1 误判**） | ✅ **已解除阻断**（2026-09-11 用户澄清：源码就在宿主体内内联，无需恢复）。施工图见 `dsh-director-plugin/docs/01-插件迁移明细清单.md` v2 |
| T-COG-002 | 🟡 台账漂移整改（6 处） | P1 | 同上 §十 P1-4~P1-10 | ✅ **已完成**（deploy.py 引用 8 处清理 / README 包表补行 / INDEX 头部 / agents.json 清 7 条测试残留 / project_overview 里程碑 / 05-大索引统计） |
| T-COG-003 | 🟡 git 工作区纳管（9 改 + 6 未跟踪，含 V11/V12 设计稿 162KB） | P1 | 同上 §十 P1-10 | ✅ **已完成**（commit `2327912`，工作区 clean，7 commits） |
| T-COG-004 | 🟢 07 号报告发现项复核：`docs/40-测试质量` 与 `docs/20-任务文档` 部分台账题名与实况一致性 | P3 | 07 号报告 §14.4 | 🟡 backlog（本轮未展开） |

## 插件迁移（T-PLUG-005 · 2026-09-11 启动）

> 施工图：`dsh-director-plugin/docs/01-插件迁移明细清单.md`（v3）｜ 施工方式：6 批次拓扑（见清单 §三）

| 编号 | 任务 | 批次 | 状态 |
|:-----|:-----|:----:|:-----|
| T-PLUG-005-B1 | 批次 1 零依赖层：A14 剥离 + A3+D3 日志 + A11 布局 store + A12 主题 store + C1 探针 + C2 模型配置 | 1 | ✅ **已完成**（宿主 -914,751 B / -61.8%） |
| T-PLUG-005-B1b | C1 布局探针（批次 1 剩余项） | 1 | ✅ **已完成**（并入 B1，`dev/layout-probe.js`） |
| T-PLUG-005-B2 | 批次 2 数据层：A1 消息 store → A4 分支 → A2 记忆 → A5 docs store + cookie + idb | 2 | ✅ **已完成 2026-09-11**（6 模块；verify 66/66；4 契约 + XSS + R5 key 兼容全通过） |
| T-PLUG-005-B2b | **插件构建与安装**（新增）：零依赖打包器 → host node_modules → profile junction → cordis.patch insert | 2 | ✅ **已完成 2026-09-11**（真机装载实证：`__DSH_BOOT__` 42 entries 含本包；`__dshDocsIndex` docCount=**90**；四层验证全绿） |
| T-PLUG-005-B2c | **打包器「平台模块外置」扩展**（新增，批次 3 引入）：ESM 静态 import → `require("<spec>")`，支持 4 种 import 形态 | 3 | ✅ **已完成 2026-09-11**（`react` 外置，React 零打包，规避 ADR-001 双实例崩溃） |
| T-PLUG-005-B3 | 批次 3 持久化：A6 文件通道 → A7~A10 | 3 | ✅ **已完成 2026-09-11**（5 模块；产物 134,346 B / 18 模块；**四层验证全绿**：源码 66/66 · bundle 44/44 · 安装链路 46/46 · 真机 24/24） |
| T-PLUG-005-B4 | 批次 4 逻辑层：D1 directorProcess → D2 审核 | 4 | ✅ **已完成 2026-09-11**（2 模块；产物 150,593 B / 20 模块；**四层验证全绿**：源码 82/82 · bundle 54/54 · 安装链路 52/52 · 真机 31/31；D1/D2 真机实际调用成功） |
| T-PLUG-005-B5 | 批次 5 组件层：E1 DirectorFlow（E2 已废弃） | 5 | ✅ **已完成 2026-09-11**（1 模块；产物 162,285 B / 21 模块；**四层验证全绿**：源码 96/96 · bundle 63/63 · 安装链路 60/60 · 真机 39/39） |
| T-PLUG-005-B6 | 批次 6 接线与退坡：F3 / F1+F2+F4+G1+G4 / F5 | 6 | 🔴 **方案就绪，待授权**（`docs/03-批次6接线与退坡方案.md`）。实测重定级：**F1/F2/F4/G1/G4 零改动** · **F5 ⛔ 已由 MOD-B 退坡**（2026-09-07）· **F3 需改 1 行**。⚠️ 须改宿主 `workspace/` `client.js`（`.gitignore` 排除，git 无法回滚）→ 依铁律须先确认 |
| T-PLUG-005-S5 | 🟡 **遗留架构问题：双份实例**（宿主内联与插件各持独立 `directorStores` Map；持久化 key 共享） | 缺陷 | ✅ **已登记 2026-09-11**：内存态不共享 → 同页可能临时不同步；持久化层共享 → **刷新后一致、不丢数据**。属**可接受的过渡态**；彻底消除需宿主内联硬退坡（高风险，建议独立任务） |
| T-PLUG-005-S4 | 🔴 **宿主缺陷登记**：E1 `filteredMessages` 跨作用域越界引用（宿主 `client.js:7050` 引用零定义标识符，必然 ReferenceError） | 缺陷 | ✅ **插件侧已修复 2026-09-11**（改用 `state.messages` + 四层防回退反证）；⚠️ **宿主侧未改动**，属 B6 退坡范围，届时一并处理 |
| ~~T-PLUG-005-B6~~ | ~~批次 6 接线与退坡：F3+F4 / F1+F2+G1+G4 / F5~~ | 6 | ⛔ **过期重复行（2026-09-12 清理）**：与第 84 行同 ID 且内容更旧（标"待开工"），以第 84 行为准 |
| T-PLUG-005-B7 | 🔴 **多层级总监结构**（对话级 / 文件夹级 / 全局级）+ 分层总结 + 分梯度调用 | 7 | ✅ **已完成 2026-09-11**。依据：03号文 §1.3 三层体系 / §3.2 继承制 / §4.3 降级；17号文 §2.1 MemoryNode / §2.3 继承 / §1A.13 分层汇总。三方案评分选型 → **方案 C（层级树+内容区，42/50）**。产出 `store/hierarchy.js` `logic/summarize.js` `components/DirectorHierarchy.js` `mount.js`。**真机：三层 6 节点建成、分层总结跑通、入口已可见**。验证 `scripts/verify-batch6.mjs` **65/65**。⚠️ 修复实测缺陷：memoryCore keyPath=`projectId` 而节点主键为 `id` → `put()` 缺 key 静默失败 |
| T-PLUG-005-B8 | 🔴 **自动同步 + 覆盖度自检**：让**每一个**对话 / 文件夹都真的拥有总监 | 7+ | ✅ **已完成 2026-09-12**。批次 7 只交付了三层**结构**，节点全靠手工创建 → **8 个真实会话一个都没有总监**。本批次接入数据源：`localStorage["dsh.workspace.view.*"]`（**workspace=文件夹级 / session=对话级**）。新增 `logic/discover.js`（数据源发现，key 前缀模糊匹配不写死 v5）+ `logic/sync.js`（**稳定 id `ws_`/`se_` 派生 ⇒ 幂等**，实测二次同步 `created=0`）+ `auditCoverage()`（覆盖度自检）+ `util/bus.js`（变更事件总线）。**真机实测：会话 8/8 · 文件夹 2/2 · 全局 1/1 = 100% 覆盖**；分层总结逐层汇总（会话 94 字 → 文件夹 784 字 → 全局 563 字）。修复 2 个实测缺陷：① `shortId` 取前 8 位得到常量前缀 `session-` → 8 个会话同名，新增 `sessionLabel()` 剥离前缀；② 面板首帧早于同步完成 → 显示陈旧快照「0/8」，用事件总线修复。验证 `scripts/verify-batch7.mjs` **66/66**（产物 233,045 B / 29 模块）。详见 `docs/05-自动同步与覆盖度自检.md` |
| T-PLUG-005-B9 | 🔴 **总监逻辑完善**（§3.1 五项职责 + §3.2 三级继承 + §1.2 五步执行 + 自动转发/审核）+ **真机逐交互点击验证** | 8 | ✅ **已完成 2026-09-12**。依据：03号文 §3.1 五项职责 / §3.2 继承制 / §1.2 五步执行逻辑 / §2.3 消息流与自动转发 / §4.3 降级。方案列举 A~E 五方案五维评分 → **方案 E（层级面板内嵌总监工作台，42/50）**。新增 `logic/duties.js`（五项职责 + prompt 模板）、`store/duty-config.js`（继承解析/覆盖/向上提交，写后回读）、`logic/director-run.js`（五步 + 自动转发 + 自动审核，中文 bigram 分词）、`components/DirectorWorkbench.js`（执行逻辑面板 + 对话流）。**真机逐交互验证 `scripts/cdp-click.mjs` 66/66**（22 小节 / 30 个可交互元素，每项「点击 → 回读实际状态 → 比对预期」，I21 全量覆盖审计 0 遗漏）。🔴 修复 5 个只有真机暴露的缺陷：① 面板空白（漏 import → 给打包器加构建期静态检查 `lintUndefinedComponents`，未绑定 JSX 组件直接构建失败）；② 发送无反应（用户消息改立即上屏 + catch 写【总监异常】）；③ 树节点点击选中失效（加 `data-node-id`/`data-selected`）；④ 「向上提交」同名取错（全部交互元素补 `data-testid`）；⑤ 禁用按钮点击静默 no-op（测试改 `waitIdle()` 轮询）。验证 `scripts/verify-batch8.mjs` **92/92**；产物 278,301 B / 32 模块（React 零打包）。**宿主零改动**。详见 `docs/06-总监逻辑完善：方案选型与缺失清单.md` §七。📌 **编号澄清**：本行正式编号 `T-PLUG-005-B9`，文档内简称「批次 8 总监逻辑完善」，与 `B8`（自动同步，提交 `815d4d7`）**是两个不同批次**；产物名 `verify-batch8.mjs` / `cdp-click.mjs` 为历史既成命名。🔄 **后续（2026-09-12 同日）**：本批次交付物经 T-PLUG-009 审计后再加固（`isHierarchyNode` schema 白名单），并重新下发运行时复验 —— 八层全绿 **523 项** + 真机逐交互 **66/66**，产物 **279,841 B / 32 模块** |
| T-PLUG-005-S1 | **T5** R3 spike：插件通道下 fs 可达性验证 | 前置 | ✅ **已完成 2026-09-11**（真机 CDP 实测 `ALL_UNREACHABLE`；三法皆 undefined，**宿主 A6 三法为死代码**；同时发现 FSA API + OPFS + dshDesktop.workspace.pickDirectory 可用） |
| T-PLUG-005-S2 | **T6** G 区宿主注入点清单化 | 前置 | ✅ **已完成 2026-09-11**（`docs/02-G区宿主注入点清单.md`；6 写入锚点 + 6 读取点 + 5 调用点；结论「不迁，只保」） |
| T-PLUG-005-S3 | **启动踩坑登记**（新增）：`ELECTRON_RUN_AS_NODE=1` 致 `--remote-debugging-port` 被拒 | 经验 | ✅ **已登记**（README §启动 + 清单 §8.4；正解 `env -u ELECTRON_RUN_AS_NODE` + 后台常驻启动） |


## 插件化整改 + 审美审核（2026-09-07 新增）

| 编号 | 任务 | 优先级 | 关联文档 | 状态 |
|:-----|:-----|:------:|:---------|:-----|
| T-PLUG-001 | P0 勘察插件加载通道（dsh-client-web「插件物化」+ runtime __ModuleLoader__，读 original/ 只读源码） | P0 | 50-信息中心/插件加载通道勘察-20260907.md | ✅ 已完成（13号审核通过） |
| T-PLUG-002 | P0 开源调研：Cordis 内核溯源（Koishi 开源内核 + 官方 cordis-tutorial） | P0 | 同上（勘察记录 §三） | ✅ 已完成（13号审核通过） |
| T-PLUG-003 | P0 插件通道验证（原 spike，随方案①重定义关闭） | P0 | 14号失败记录 + 16号成功实证 | ✅ 关闭：通道已由 dsh-director 活样本端到端实证；自建骨架注册不再必要 |
| T-PLUG-004 | P0 插件包骨架（6 文件）+ patch 基线测量（+13,553/−9,855 行，70% 目标 ≤4,066 增行） | P0 | 20-任务文档/28-插件化整改执行总清单.md §四 | ✅ 已完成（13号审核通过） |
| T-PLUG-005 | P1 MOD-B 迁移：DirectorView/stores/styles/IDB → 插件包，client.js 留 shim | P0 | 同上 §四 | ⏳ 待 P0 完成 |
| T-PLUG-006 | P1 MOD-A 可迁移部分迁移，ChatView 留最小锚点 | P0 | 同上 §四 | ⏳ 待 P0 完成 |
| T-PLUG-007 | P2 补丁减薄 ≥70% + 《锚点契约》文档 | P1 | 同上 §四 | ⏳ 待排期 |
| T-PLUG-008 | P3 apply/restore/deploy 支持插件部署单元 + 安装 README | P1 | 同上 §四 | ✅ **已完成 2026-09-12**。产出 ① `dsh-director-plugin/scripts/plugin-install.mjs` —— 三处安装点（实体包 / profile junction / 用户补丁层 entry）一把梭，模式 `verify`（**默认零写入**）/ `--apply`（**幂等**，写后**逐文件回读校验**）/ `--uninstall`（**外科式**只删本插件 entry，不碰第三方内容）/ `--uninstall --purge`；② `dsh-director-plugin/INSTALL.md` —— 安装权威说明（三处安装点为何缺一不可、命令面、启动陷阱、验证矩阵、故障排查、开发者改动清单、回滚）；③ `scripts/verify-install-clean.mjs`（**52/52**）—— 临时沙箱「双机模拟」全生命周期。🔴 **该测试捕获真实缺陷**：`PAYLOAD_FILES` **漏 `package.json`**（本机正常只因早先手工拷入；而 host Loader **靠它发现本包** ⇒ 干净机「目录存在但插件永不被装载且不报错」），反证实测移除后 8 项转红、安装器**自身 `IS_PASS` 仍 TRUE**（对自己的缺件是盲的）；④ `scripts/deploy.ps1` **退役**为硬失败垫片 |
| T-PLUG-011 | 🔴 **`Network` 缓存误删缺陷族（3 处）** | P0 | `dsh-director-plugin/INSTALL.md` §四 + `50-信息中心/01` 坑5 | ✅ **已修 2026-09-12**。`scripts/deploy.ps1` / `clear-cache.ps1` / `restart-harness.ps1` 与「修改前必读」文档 `50-信息中心/01` §坑5 的清缓存列表**均含 `Network`** —— 那是 Chromium 的 **cookie/网络状态存储**，内含 `dsh_director_*` 持久化 cookie（**R5 冻结契约**）⇒ 执行一次即**总监状态丢失**（真实用户数据丢失路径）。处置：三脚本移除该项并加红线注释（`clear-cache.ps1` / `restart-harness.ps1` 同时把 `Network` 列入 `$dirsToKeep`）；`deploy.ps1` 整体退役；`50-信息中心/01` 三处更正（含可执行片段） |
| T-PLUG-009 | P3 IDB 数据兼容验证（三 store 条数抽查） | P1 | 同上 §四 | ✅ **已完成 2026-09-12**。产出 `dsh-director-plugin/scripts/verify-data-safe.mjs`（**42/42**）+ `docs/07-数据兼容与边界安全验证.md`。证据：IDB `dsh-director-db` **版本恒为 3**、store 集合**恰好 6 个（未新增）**、六 store 条数**零增零减**；宿主对 `memoryCore` **零 `getAll()` / 零 `delete()`**（读取精确按 `get(projectId)` ⇒ 不受插件记录影响）；插件记录 `projectId === id`（无静默写失败）、**未越界写** `memoryDecisions` / `memoryRisks`；**动态反证**：注入宿主形态记录 → 插件 `listAllNodes()` 不泄露且该记录确实在 IDB → 删除后回基线；宿主数据源（`dsh.workspace.view.v5` / `dsh.sessions.current`）分毫未动；R5 key 契约（`dsh.director.store.` / `dsh_director_` / `director-main`）源码静态断言未改名。🔴 **顺带修掉「约定失效即静默」缺陷**：层级过滤原为 `n.level` 真值判断 → 一条带任意 `level` 的宿主记录即混入（实测泄露 2 条、树节点 13 vs 11，**不报错**）⇒ 收紧为 schema 白名单 `isHierarchyNode`（附改前/改后反证） |
| T-PLUG-010 | 🟢 **F-DATA-01 结案**：冷启动后 `dsh.director.store.director-e0w2f3` 与 IDB `directorStores` 1 条「消失」现象 | P2 | `dsh-director-plugin/docs/07` §六 | ✅ **已结案 2026-09-12 · 判为假阳性（观测口径缺陷，非数据丢失）**。① 3 次冷启动只读实验：`dsh.workspace.view.v5` **恒为 1118 B**、`memoryCore` 11 条节点完好、`dsh.sessions.current` 存在 ⇒ 宿主核心数据零损伤；② 全仓 grep 取证：宿主与插件**不存在** `removeItem`(该 key) / `localStorage.clear()` / `deleteDatabase` 任何分支；③ **根因（两条叠加，均在探测侧）**：`dsh.workspace.view.*` **不是** dsh-client-ui-conversation 写的（该 bundle 中 **0 处命中**），由**另一个 bundle 开机后异步写入** ⇒ 早探测必然读空，在「重启前后对比」中被误读为丢数据；且 `dsh.director.store.*` 只在**总监活动**时由 `create-store.js#notify→saveDirectorStore` 写入 ⇒ 冷启动无活动**本就该无**。④ **修复**：`verify-data-safe.mjs` 新增 **[0] 就绪等待**（取基线前轮询等 `dsh.workspace.view.*`，超时 60s）+ 第 6 节输出字节数锚点。脚本 **42/42** |
| T-PLUG-012 | 📄 **`assets/docs-index.json` 已漂移 6 篇**（索引 90 / 实际 96） | P2 | `dsh-director-plugin/assets/docs-index.json` + A14 | ⏳ **待排期（归入 A14 边界，不单独抢修）**。实测缺失 6 篇（0 篇多余）：`00-统筹入口/07-项目认知初始化报告-20260911.md`、`10-架构设计/思维导图驱动的多分支对话管理系统-统一方案文档.md`、`20-任务文档/32-GitHub推送执行方案-T-V12-008.md`、`20-任务文档/P0阶段详细实现方案-思维导图驱动多分支对话系统.md`、`20-任务文档/开发交接包-P0思维导图多分支对话系统.md`、`40-测试质量/21-N2宿主退坡收官执行与审核记录-20260908.md`。**为何不现在修**：① 现有生成器 `scripts/gen-docs-index.ps1` 是**注入宿主 `client.js`** 的旧 A14 前形态（PowerShell 通道无输出，不可靠）；② 重新生成会改变产物字节 ⇒ 现有「279,841 B」尺寸锚点与九层验证基线需整体重跑；③ 与 A14 部署偏差（下方第 4 项）同源，应一并处理 |
| T-PLUG-013 | 🎨 **页面交互重设计：原生 Tab 集成**（把「总监」从插件浮层改回宿主原生 tab 环） | P0 | `dsh-director-plugin/docs/09-页面交互重设计·原生Tab集成方案.md` + `50-信息中心/V13-原生Tab集成设计稿.html` | 🟡 **设计稿已出，待评审**（按用户要求「先出设计图文档，**不要直接重画实现**」，本轮**零实现改动**）。**核心结论**：需求侧 **一直是原生 tab** —— `03号文 §1.3` 原文「在 Harness 的每个会话中，**总监 tab** 提供预处理能力」、`18号文` 标题即「**总监 tab 完整界面**」（R1–R8 + E.9 跨区联动）、`V12 设计稿` tab 栏实绘「**总监 ｜ 对话 ｜ 轨迹**」；而现状是 **882×562 独立浮层**（真机实测 `x=536,y=190` vs 原生 tab `y=48`；导航需 2 步）⇒ 用户「看起来像完全重画」的观感**属实**，差异在**形态层级**而非配色。**技术门已打通**：① tab 环**纯 slot 驱动**（宿主 `dsh-client-ui-conversation/lib/client.js:9172`：`renderSlot("conversation.view", props, { only: active.id })`）② 插件 `apply(ctx)` **本就收到 ctx**（产物尾部 `void ctx;` 将其丢弃、`inject=[]` 未声明依赖）③ 官方契约 `inject:['slots']` + `ctx.slots.register`（`dsh-client-modules/lib/index.js:2146` 示例 + 未声明依赖的报错模板）④ **同族先例**：`dsh-client-ui-trajectory` 正是用 `ctx.slots.inject("conversation.view", …)` 注册「轨迹」tab ⇒ 注册 `{id:"director", order:-1, label:()=>"总监"}` 即被接管，**宿主零改动**；⑤ 当初探测的 `window.__DSH_SLOTS__` **从来不存在**（`mount.js` 猜错名字 ⇒ 恒走 DOM 兜底）。**已有设计图文档处理方式**（文档 §二）：D1 `03号文`/D2 `18号文`/D3 `V12`/D4 导图设计图/D5 统一方案/D6 17号文 → **全部沿用**（D2 升为设计基线）；V11/V10布局/V10美化/V9框架稿 → **降级历史参考（不删）**；`31号文` → ⚠️ **分层适用**（细节层仍「以实现为准」，**信息架构层反转为「以设计稿为准」**）；`docs/04` 方案选型 → 🔴 **需修订**（方案 A「实现成本 4 分」的评分前提「插件侧能否拿到 slots 实例未经实测」**已被证伪**）。**待确认 1 项**：文档 §7.2「双向联动」口径 = ①同源视图（默认，两 tab 共用一个会话、零搬运）还是 ②跨会话投递 · ⚠️ **2026-09-12 形态订正**：用户明示「**两者均为弹窗形式，并允许最小化**」⇒ 本行原「总监 = 原生 tab」形态**作废**；其 §五 技术证据链**保留**（降级为"入口备选通道"）。业务形态真相源移交 `T-PLUG-014` / `docs/10` |
| T-PLUG-014 | 📐 **总监与对话架构总纲（业务不变量唯一真相源）+ 逻辑丢失归因** | P0 | `dsh-director-plugin/docs/10-总监与对话架构总纲.md` + `50-信息中心/V14-弹窗式总监架构设计稿.html` | ✅ **文档 + 设计图已交付并修订 2026-09-12**。**用户 11 条要求逐条落地**：① **独立数据元** → 新建插件自有库 `dsh-director-plugin-db` v1（6 store，对齐 17号文 §2.2）；**关键论证**：新库**不参与宿主的 IndexedDB 版本协商** ⇒ 隔离与「R5 冻结 key 不变更」**同时成立**（订正 T-PLUG-009「复用 memoryCore」的过度约束）② 总监有自己对话、**不执行**（17号文 §1A.1 原文）③ **六维审核** + 纠偏留痕（§1A.9）④ 对话逻辑不变（**红线**，18号文 §1.3）⑤ **左总监 / 右对话数据**（05号文 §4.1/§4.3/§4.4）——「与对话 tab **完全一致**」的判据 = **订阅同一 store + 同一渲染器** ⇒ **结构上不可能不一致**（06号文 §4）⑥ **弹窗 + 三态最小化**（05号文 §4.1 两 collapsed 字段 + §4.7 M8 + §4.8 拖拽）⑦ 点击文件夹/项目展示对应总监 ⑧ **单总监页 + 智能路由**五步（17号文 §三）⑨ 项目级同构总监（§一 / §6.2）⑩ **智能体与技能**可查看调用 + 主动选择（18号文 R3 + §1A.8 五类）⑪ R1–R8 + V12 八视图。**丢失归因（五级）**：① **入口被主动注销**（批 C-2 移除 director tab 注册 ⇒ 宿主 MOD-A 布局 `:6477-7420` **28 处代码活着但不可达**，判据 `client.js:9150` 永假）② **迁移拆成两个世界**（A11 布局 store + E1 DirectorFlow **已迁入插件却零渲染消费方**；渲染位置留宿主，"桥"没人修）③ **契约反向固化"共用"**（设计独立库 6 store → 落地共享库 3 store，《锚点契约》再冻结为不可动）④ **需求真相源缺失**（11 条散在 7 份文档，**无一份声明自己是业务真相源** ⇒ 被最新文档自动覆盖）⑤ **版本控制面仅 6.8%**（磁盘 3593 / git 纳管 245；`workspace/` 宿主全部改造 与 `snapshots/` 唯一回滚点 **均 0 纳管**；`fe6e651` 另删 78 文件）—— 即「**此前新建内容未被保留**」的答案。**防复发 6 条**已立于 `docs/10` §八。**待裁 3 项已全部就地裁定**（design-doc 修订时落定）：Q1「两者」＝**左总监面板 + 右对话区（分屏两栏）** ｜ Q2 总监页与对话 tab ＝**共存不互斥，右区即对话 tab 内容区本身** ｜ Q3 右栏渲染通道 ＝ ~~官方 `renderSlot`~~ → 🔴 **方案 C · 布局分屏**（docs/11 §三 评分 57 居首）。设计稿已修订为 `50-信息中心/V14.1-弹窗式总监架构设计稿.html`（91,445 B）。 |
| T-PLUG-015 | 🏗️ **V14.1 设计稿修订 + 总监弹窗实现交付（T1–T10）** | P0 | `dsh-director-plugin/docs/11-设计稿审核与实现方案.md` + `50-信息中心/V14.1-弹窗式总监架构设计稿.html` + `src/bridge/split.js` + `src/components/DirectorDialog.js` | ✅ **已完成 2026-09-12**。**① T8 设计稿修订 V14 → V14.1**：落实 `docs/11` §1.1 审美修订 **10 项**（scrim / 单一主色紫 `#8957e5` + 11 卡中性细线 / `--faint #6a7078→#8b9199`（3.0:1→4.6:1）+ `.sidebar .lbl #4e545c→#7c828a` / 全稿最小字号 **≥10.5px**（清 9px·9.5px·10px）/ `.g3→repeat(auto-fill,minmax(248px,1fr))` + `.winbody→min-height` / 四态 `:hover`·`:active`·`:focus-visible`·`:disabled` + **状态一览 7 态** / `role`+`aria-label`+`tabindex` 真实 `<button>` 化 + 键盘路径 / `min-width:640px` + `.tscroll` / 统一 **140ms** + `prefers-reduced-motion`）＋ §二 交互缺口 **I1–I12** 全部落实，并把本轮**真机实测值**写入（`.composerSeat x 280→580`、`w 1154→854`、`railCtl [true,true,true,true]`、拖拽 `300→390`、上限 `793=55%×1442`）。新增校验脚本 `scripts/verify-design-doc.mjs` **49/49**（**含反向证伪**：对 V14.0 旧稿跑出 **31 项失败** ⇒ 证明防线非空转）。**② docs/10 §四 同步**：4.1 表格改「非模态覆盖层 + 右栏通道=方案 C」，新增 **4.2.1 焦点判定粒度**、**4.6 键盘路径**、**4.7 折叠态控制可达性**、**4.8 分屏锚点判据（六条 + 真机实测表）**；订正 §五 / §九阶段3 / §十 Q3 / 附录A 中已被否决的 `renderSlot`+`ChatFlow` 通道（保留订正记录：**优先重定义通道，而非降低目标**）。**③ docs/11 增补**：§八 **反模式 12 条**（E-SPLIT-001~004 / E-TPL-001 / E-DOM-001~002 / E-TEST-001~003 / E-DOC-001，逐条「现象→原因→正确做法+防线+来源」）+ §九 **交付记录与测试总账**。**④ 本轮修复 6 个真缺陷**：① E-SPLIT-001 分屏锚点命中**插件自身 DOM**（"右栏完全一致"全程是假的）② E-SPLIT-002 兜底用 `input[type=text]` 命中侧栏搜索框 ③ E-SPLIT-003 `padding` 挂到**滚动容器**产生横向溢出 ④ **折叠态下四个控制按钮全部不可达**（无法还原）⑤ 拖拽 `down→move→up` 同 task 内位移**静默丢失** ⑥ `listAgentRuns()` 静默截断致断言误判。**⑤ 新增独立 linter** `scripts/lint-cdp-templates.mjs`（模板内注释含反引号截断模板，**同一坑连踩 4 次后产品化**，含坏/好样本自检防空转）。**⑥ 测试全绿**：真机 `cdp-click-dialog.mjs` **100/100 ×2 轮**（归一化后可反复运行已证明）、`cdp-click.mjs` **70/70**、`cdp-verify.mjs` **39/39**；离线 **269/269**（verify-dialog 244→269）、**60/60**（verify-install 37→60）等 9 套 + `verify-design-doc.mjs` **49/49**。**⑦ 构建下发**：`lib/client.js` **413,359 B**（字符 345,123）· 模块图 38 · 平台外置 `react, react/jsx-runtime, react-dom/client` · `plugin-install.mjs --apply` 逐文件回读通过。**⑧ 数据隔离复核**：插件库 `dsh-director-plugin-db@v1`（`nodes:12 conversations:36 reviews:3 decisions:3`）；宿主库 `dsh-director-db@v3` **未动**；R5 冻结键**零改名/零新增变体**。**残留**：`_probe-*.mjs` 临时探针 9 个（**untracked，删除不可逆 ⇒ 已列候选待确认，不自行删除**） |
| T-PLUG-016 | 🔍 **V15.1 画面与交互审核（范围：只看画面 / 交互）** | P0 | `50-信息中心/V15.1-画面与交互审核版设计稿.html`（**115,872 B / 19 视图**） | ✅ **已完成 2026-09-12**（**零实现改动**）。审核对象 `V15-三页签·弹窗·分支导图设计稿.html`（62,193 B / 13 视图）。**结论：画面 16 项待修（6 项 P0）· 交互 13 项待补（5 项 P0）**，已全部修订进本稿，**V15 十视图骨架未推翻**。**P0 五项**：① **A1 说一套画一套** —— V15 文字写"浮层不挤占 R 区"，画面却用 `padding:10px 262px 10px 10px` 硬挤底层 262px = **正是用户第 5 条骂的"留占位"**；② **A4 V13 联动图整张丢失**（L1–L9，L8 对话→总监 / L9 总监→对话 为 ★核心）⇒ 恢复为**视图 10**，并按"独立数据元"重定义为**显式投递 / 回传**（不再"同源共享"）；③ **A5 结构漂移**（V13 `R4 / R5 / R7` 三栏 → V15 两栏 + R6/R7 对调）⇒ 已回归 `200px 1fr 190px`；④ **A2/A3 缺缩起态与关闭态画面**（用户第 3、5 条的正题）⇒ 新增视图 4 **三态并排 + 真实尺寸缩起态舞台**；⑤ **B1/B2/B3/B4/B5 交互留白**（切 tab 弹窗存续 / 四输入框路由 / 未画回结卡 / 未画六维三态 / 六动作无入口位置）⇒ 全部补齐。**新增画面**：回结卡 + 六维审核三态（视图 9）· 创建分支三维定义（视图 11）· 上下文压缩+模型切换（视图 12）· 空/加载/错误六格（视图 15）· 交互规格总表（视图 14）。**审核中新发现 A16**：V15 退掉 V14.1 的可访问性（实测 `aria-label 0 / role 0`）⇒ 视图 14 ⑤ 增八条契约。**B7 定案**：`Alt+1/2/3` 让位给**切三页签**，弹窗折叠改 `Alt+\`（V14.1 旧语义**显式作废并记录**）。**校验 10/10 `IS_PASS TRUE`**（正反对照：V15 原稿在同一判据下报 `挤占 2 / 越界 top:-4 / .pop 250px / rail34 0`，V15.1 全部归零；卫生：字号 min **10.5** · 标签全平衡 `div 453/453` · **LF 零 CRLF** · 零反引号）。**下一步（已登记未执行）**：`docs/10 §4.1/4.4/4.6-4.8` 由"分屏"改回"浮层"并补 fork 血缘与三态；`docs/11 §三` 选型作废 + 加 A/B 反模式；`src/bridge/split.js` 由 padding 注入改为浮层挂载器 |
| T-PLUG-017 | 🎨 **V16 三件交付物：设计图 + 需求图 + 交互逻辑** | P0 | `50-信息中心/V16-设计图·需求图·交互逻辑.html`（**90,975 B / 1,214 行 / 13 视图 / 10 表**） | ✅ **已完成 2026-09-12**（**零实现改动**）。**动因**：用户「出一个设计图 需求图和交互逻辑 这部分都没有做好」⇒ 前稿全是「虚线框 + chip」的**标注图，不是设计图**。**① 板块 A 设计图（5 视图 · 高保真真界面）**：A1 总监页全界面（侧栏 230 + tab 环 + R1 / R2.5 / R2 / R3 / [R4 / R5 / R7] / R6 / R8 + 浮动按钮组「🧠 思维导图」在「◆ 总监」**之前**）· A2 弹窗三态并排（展开 322 / 缩起 36 / 关闭 0，**三格底层一致**、真浮层不占位）· A3 对话页 + 绿色边框总监弹窗 · A4 分支导图态（原生 fork 血缘 + 悬浮工具条 + 右键菜单 + 底部待路由输入条）· A5 六控件细节放大（tab 环 / 气泡 / 四输入条徽章 / 节点 4 态 / 回结卡 / 六维审核卡）。**② 板块 B 需求图（3 视图）**：B1 需求 → 界面落点矩阵 17 条 × 落点 × 设计图编号 × 判定 · B2 分区承载热力（**R2.5 五处 / 弹窗带四处 / 导图态四处**居前，合计 30 处落点）· B3 覆盖度总账（**已落点 16/17 · 部分 1/17（#11 标注模式待并入）· 未落点 0**）。**③ 板块 C 交互逻辑（5 视图）**：C1 全局导航图（三页签 × 两弹窗 × 导图态，**7 行可达关系矩阵**，每行都带回退路径；`Alt+1/2/3` 切页签 · `Alt+\` 折弹窗 · `Alt+M` 导图 · `Esc` 只退最上层浮出物；**V14.1 旧 Alt 语义显式作废**）· C2 分支生命周期状态机（S0–S5 + SX，**三选一** 继续 / 再分支 / 归档；含每跳留痕字段表与血缘树示意）· C3 消息投递时序（**双向两套独立 store**：outbox 显式投递 / inbox 自动回结，6 行对比表说明为何不能合一）· C4 输入路由决策树（四输入框去向唯一化 + **多义 / 零命中两支补齐** + 免确认与必确认 5 条判据）· C5 总监治理闭环（五步一转 + 纠偏回路，**证据不足不许自决**）。**新增防线** `scripts/verify-design-html.mjs`（卫生五项 + **正负对照自检 7/7**；判据污染防护 = 先剥离 `<code>`；`--unused` 降级为 INFO 不计入 IS_PASS）。**校验 5/5 `IS_PASS TRUE`**：LF 零 CRLF · 零反引号 · 字号 min **10.5**（80 样本 0 违规）· 标签平衡（未闭合 0 / 错配 0）· class 全部有定义（161 个全部命中）。**反证**：占位型 padding **0 处**（V15 为 2 处）· 三浮层均 `position:absolute` · 三栏 `186px 1fr 168px`。**清理**：删 `.vrail` 死样式组（与 `.mrail` 重复真相源）+ `.lifeline`。**与其它稿关系**：本稿定位为**真界面基线**；V13 结构基线 / V15.1 画面交互审核 / V12 八视图素材 仍为配套。**下一步**：见本稿 R2 表 6 项 |
| T-PLUG-018 | 🧩 **设计图插件（DesignStudio）实现 + 顶栏补齐 / 保存·版本 / 窗口安全区** | P0 | `50-信息中心/V16-...html` 板块 D · E + `src/components/DesignStudio.js` · `VersionPanel.js` · `store/design.js` · `store/design-schema.js` · `util/safe-area.js` | ✅ **已完成 2026-09-12**。⚠️ **本行属补登记**：原稿在 `V16` 板块 D/E 与 `docs/12-源码映射索引` 中均已引用 `T-PLUG-018`，但**从未写进本清单**（台账漂移，本轮一并补上）。交付：设计图插件（顶栏四区 / 五区布局 / 18 类元素 / 七元组逻辑 / 标准框架 20 元素 / 底部修订对话）+ 保存与 `versions[]` 版本模型 + 版本面板 + 窗口控件安全区（真机 `inset = 138`）。**闸门**：`verify-design-studio.mjs` **79/79 连跑 3 次**、`test-design-logic.mjs` 49/49、`test-design-version.mjs` 63/63；并新增 `verify-design-html.mjs`（设计稿卫生五项 + 正负对照自检 7/7） |
| T-PLUG-019 | 🧠 **思维导图元素库完善（18 条总账）+ ✕ 与原生窗口按钮「同位」修正** | P0 | `50-信息中心/V16-...html` **板块 F** + `src/components/MindMap.js` · `store/mindmap-schema.js` · `logic/branch-tree.js` · `logic/mindmap-render.js` | ✅ **已完成 2026-09-12**（真机 80/80 · 连跑 3 次全绿）。**用户原话两条**：①「按照可能用到的思维导图元素 完善思维导图」→ 导图此前**只有节点方块和一条直线**，设计稿 A4 画过的元素**一个都没实现**；本轮建立 **18 条元素总账**（14 已落 / 1 待办 / 3 明确不做，每条带取证）并逐族落地：节点四型、状态四态（**「出错」取证后不画** —— 宿主摘要无错误字段，第四态改「待审」= 对设计稿的显式修正）、五条有据徽标、四类连线语义（主干实线 / 分支虚线 / 选中链高亮 / 折叠点线）、悬浮工具条 6 动作、右键菜单（**有接口才可点**，无接口项禁用并写明缺什么）、缩放组 / 搜索 / 小地图 / 图例 / 覆盖度徽标。②「思维导图的 X 和原软件也是一个位置」→ 顶栏与工具条 `paddingRight = max(10, inset + 10)`，e2e **C-M13a–f** 六条断言（含「防平凡真」前置与 ✕ 中心命中测试）。**修掉 3 个真根因**：`inject` 漏声明 `sessions` 致血缘**静默降级**（cordis Proxy 陷阱 + try/catch 吞错）· 订阅时序反了致首次打开空树 · 状态靠猜（宿主其实有 `running`/`completed`）。**测试侧新增 4 条纪律**：真实鼠标必须打在「视口内且命中自己」的坐标（否则 x=-934 打空却像功能坏）· 不许无条件按 Esc（会关掉整张图，致 4 段量空 DOM）· 断言防平凡真（`allCollapsed >= 0` 恒真、空集上的「toast 已消失」恒真）· e2e 每段带 `state(tag)` 现场勘察行。**存量问题一并修复**：`verify-bundle.mjs` 因**测试桩两份漂移**（缺 `Component`）静默红成 12/57 ⇒ 统一引用共享桩后恢复 **63/63**。**闸门**：`verify-mindmap.mjs` **80/80 ×3** · `test-mindmap-logic.mjs` 69/69 · `verify-bundle.mjs` 63/63 · 设计稿卫生 5/5。**产物** `lib/client.js` **743,318 B / 52 模块** · 绑定对账 **389 = 389** · `inject = ["slots","sessions"]` |
| T-PLUG-020 | 🎛️ **批次 11：单框控件 / 框可拖动 / 点击框右侧展开对话 / 四维流转打通 / 四处个性化与质感** | P0 | `50-信息中心/V16-...html` **板块 G** + `src/components/MindMap.js` · `NodeDetailPanel.js` · `PersonalizePanel.js` · `DirectorPage.js` · `DirectorDialog.js` · `DesignStudio.js` · `store/personalize.js` · `logic/flow.js` · `logic/mindmap-render.js` | ✅ **已完成 2026-09-12 · 真机 46/46 连跑 3 次全绿**。**用户七条原话逐条落地**：①「单个框没有展开和折叠的选项」→ 12 个框**每个都有**折叠钮（无子框也存在，`data-enabled=0` 并写明原因，符号由 `controlsOfRow().alt/icon` 单一真相源提供）②「框不能动需要可以移动」→ 框体与 `✥` 手柄都可拖，松手才写 `layout.mmPos`（拖动中只改本地 state），右键「归位」+ 顶栏「▦ 自动布局」可还原 ③「点击框在右侧展开对话，最上面是现在正在做的事情」→ `NodeDetailPanel` 三段顺序固定（现在在做的事 → 跨维度流转 → 输入条），后跟「总监对这个对话说过的话」与「读不到原生内容」的显式说明 ④「我发给或总监发给对话的信息，整理出目前在做的事情」→ `currentTaskOf()` 每个分支都带 `source`（`host.running` / `host.pendingInteraction` / `flow.trail` / `plugin-db` / `none`）并在 UI 显示来源 ⑤「总监 tab 下面你加了一个对话框，不要这个，用原本的对话框」→ R8 焦点条内**零** input/textarea（仅五个动作键），输入一律落宿主原生 composer ⑥「都在右上角加自定义个性化设定」→ 总监页 / 总监弹窗 / 分支导图 / 设计图工作室**四处**共用同一个面板（主色 4 档 / 质感 4 档 / 密度 / 字号 / 圆角 / 动效 / 连线形状，`:root` 变量 + `html[data-dp-*]` 属性即时生效）⑦「打通总监和正常对话还有思维导图还有设计图之间的关联…点击左侧点入不同对话切进去就是和当前对话有关的流转信息」→ `logic/flow.js` 四维流转（一条消息带 `trail` 足迹，`director/chat/mindmap/design` 四枚徽标），总监页 R5 按**宿主当前会话**过滤并轮询跟随（点左侧切会话 ⇒ 列表跟着换）。**修掉 1 个真缺陷 + 捕获 1 个渲染进程顶死**：① 三处浮层根节点 `background` **简写**重置 `background-image`，而内联优先级高于类规则 ⇒ 纹理规则**永不生效**（静默失效，读属性一切正常；判据只能用 `getComputedStyle().backgroundImage`）② 第 7 次真机跑中 `Input.dispatchMouseEvent` 挂 8s 超时**抛穿顶层 await**，整轮 45 条断言结果全丢 ⇒ 所有指针派发统一记账 + 收尾 `E2` 断言"零超时"、非零判 `INVALID`、退出码 2。**测试侧三次假红全部归因"挑错断言对象"**（台账 #50/#51/#53）：只比第一条 path（未必连着被拖框）· 扫直接子节点找 `nd-now`（它在 `bd` 里）· 用"DOM 第一个会话 id"登记（R5 按宿主当前会话过滤）。**闸门**：`verify-flow.mjs` **46/46 ×3**（`logs/verify-flow-8/9/10.log`）· `test-mindmap-logic.mjs` **91/91**（新增 G 段 22 条）· `test-personalize.mjs` **81/81**（新建）· `verify-bundle.mjs` **80/80**（新增批次 11 段 16 条）· 设计稿卫生 5/5。**产物** `lib/client.js` **879,837 B / 54 模块** · 绑定对账 **450 = 450 / 170 条 import**。**清理**：删 `_probe-*.mjs` 15 个 + `.tmpcheck/` 9 项（均 gitignore 且未跟踪）；`_clip.mjs` / `_preview-board.mjs` / `_preview-sec.mjs` 来源不明**只列候选未删** |
| T-PLUG-021 | 🎨 **批次 12：总监页背景采用原软件的背景 · 风格统一** | P0 | `50-信息中心/V16-...html` **板块 H** + `store/personalize.js` · `components/DirectorPage.js` · `client-entry.js` | ✅ **已完成 2026-09-12 · 真机 53/53 连跑 3 次全绿**。**用户原话**：「总监tap页面的背景采用原软件的背景 保持风格统一」。**改前事实**（真机回读）：宿主是**半透明玻璃 + 云猫背景图**（`body` 背景 = 横向渐变 + `cloud-cat-background.webp`，底色 `rgb(7,17,29)`；原生页签容器 `.RWZidW_root` 用 `var(--dsw-alias-bg-base)` = `rgba(246,250,255,0.40)`），而总监页是**一整块死黑 `rgb(11,12,14)`**，把背景图整片盖住 ⇒ 与宿主完全割裂。**修法**：在总监页根节点 `[data-testid="dp-root"]` 上把六个令牌桥接到宿主 —— `--dp-bg-0 → --dsw-alias-bg-base`、`-bg-1/-bg-2 → layer-1/layer-2`、`--dp-line → border-l2`、`--dp-t1/t2/t3 → label-primary/secondary/tertiary`（另加 `--dp-shadow*`）；总监页 133 处颜色里只有 3 处硬编码 ⇒ **一处改动覆盖全页**。作用域刻意只限总监页：导图 / 设计图工作室 / 总监弹窗是浮层，仍走原深色底。**顺带修**：① 纹理色变量化（`--dp-tex`）—— 原来写死白色，浅色底上「选了网格像没选」；② 根节点背景改长写（`background` 简写会重置 `background-image`）。**捕获 4 个测试/工具缺陷**（台账 #57–#60）：① 🔴 **CSS `var()` 在「定义它的元素」上求值** —— 桥接写成 `:root{...}` 时背景压根不跟随宿主却**零报错**（宿主把 `--dsw-alias-*` 定义在 `body`，`:root` 上回读是空串）⇒ 离线 C15/C16 一对正反断言钉死；② 🔴 **三个 CDP 工具（`cdp-eval` / `cdp-mouse` / `cdp-shot`）没有硬超时** ⇒ 渲染进程挂死时表现成「空输出 + exit 0」，读起来像"表达式写错"，完全指不到真因（已补 8s 硬超时 + 自检提示行）；③ 🔴 **断言扫源码不剥注释** ⇒ 新写的说明注释把负断言 C6 弄红（危害是逼人删注释而不是修代码）⇒ 加 `stripComments()` + C6c 自证；④ **`test-personalize` 偶发红** ⇒ B36 用下标 `ofSession()[0]` 取"刚 push 的那条"，而数组按 `lastTouchOf` 升序排（最近碰过的排末尾）⇒ 改为按 `flowId` 精确取 + 补 B36b 排序不变式断言。**事故（已修）**：F5 正负对照收尾用 `removeProperty` 一刀切，把**宿主自己的内联值**也删掉 ⇒ 宿主毛玻璃背景被改坏（`after = rgb(255,255,255)` ≠ `before`）⇒ 重载宿主复原，并改为「照原样恢复」（有内联值就写回原值）+ 把 `after === before` 写进断言。**闸门**：`verify-flow.mjs` **53/53 ×3**（`logs/verify-flow-14/15/16.log`，新增 F 段 7 条含正负对照与亮度扫描 130 元素深色 0 个）· `test-personalize.mjs` **87/87**（新增 C15–C18 + C6c；连跑 12 次验证偶发已消）· `verify-bundle.mjs` **85/85**（新增批次 12 段 5 条）· `test-mindmap-logic.mjs` **91/91** · 设计稿卫生 5/5 · 基线指纹 60 文件 / 1,730,783 B 复验漂移 0。**产物** `lib/client.js` **883,339 B / 54 模块** · 绑定对账 **450 = 450 / 170 条 import** · `PLUGIN_VERSION = 0.12.0-batch12`。**证据**：`logs/bg-before-director.png`（改前整块死黑）↔ `logs/bg-final-director.png`（改后浅色玻璃，与原生一致） |
| T-PLUG-022 | 🎈 **批次 13：浮动入口三件套（点击穿透 / 配色随主题 / 定位不漂）** | P0 | `src/components/FloatDock.js` · `components/DirectorPage.js` · `client-entry.js` | ✅ **已完成 2026-09-12 · 真机 57/57 连跑 3 次全绿**。**动因**：上一轮把总监页背景从「整块死黑」改成「宿主浅色玻璃」，**深色底把三个缺陷全掩盖了**。**① 透明容器吃点击**：浮动组容器 98×103、三颗药丸长 81/98/63 + 6px 间隙 ⇒ 盒内大片**看不见的空隙**照样吃鼠标事件（真机 `dp-send` 89×24 被吃 **88×19 px**，`elementFromPoint` 最上层是 `d-floatdock`）⇒ 容器 `pointer-events:none` + 药丸 `auto`；并导出 `FLOAT_DOCK_RESERVE = 108` 由 R6/R8 消费（光穿透不够，"交给总监整理"在**视觉上**仍被压）。**② 浅底浅字**：药丸写死给深色底配的浅色（`#7fe3e8`/`#9fc2ff`/`#b794f6`），实测对比度 **1.35/1.63/2.21 : 1**（AA 线 4.5）⇒ 字色改走宿主 `--dsw-alias-label-primary`（自动随主题），强调色只留在边框 + 淡底。⚠️ 该组件挂在 `dsh-director-dialog-host`（**不在** `dp-root` 内，实测 `closest('[data-testid=dp-root]')===null`）⇒ 读不到 `--dp-*`，**必须直接用宿主令牌**。**③ 定位漂移**：composer 空会话居中（top 287）/ 有消息靠底（top 690），这一变既无 `resize`、也不改根元素盒尺寸 ⇒ `measure()` 只在 resize + 3 个固定定时器里跑，接不到 ⇒ `bottom` 永久过期（漂到 y=172 压住 R2.5/R2）⇒ 加 **500ms 轮询**（只在值真变时 `setState`）。**测试侧**：F8 第一版把"命中容器"与"命中药丸"混为一谈（假红 3 处）⇒ 拆成 5×5 网格采样 + 带文字叶子节点计数 + 药丸仍可点三条；F10 判据第一版在"居中/靠底"两形态下都成立（抓不到真问题）⇒ 改成与产品**同一条公式**算期望差 ≤2px，并补 F11 跟随性正负对照；F 段产物负断言被**自己写的注释**弄红 ⇒ `stripJsComments` 改**行级过滤**（第一版逐字符状态机在 888KB 产物上被正则字面量**静默失步**）+ 负断言**按模块取块**。**闸门**：`verify-flow.mjs` **57/57 ×3**（`logs/verify-flow-23/24/25.log`）· `verify-bundle.mjs` **90/90**（新增批次 13 段 5 条）· `test-personalize` 87/87 · `test-mindmap-logic` 91/91 · 设计稿卫生 5/5。**产物** `lib/client.js` **890,104 B / 54 模块** · 绑定对账 **452 = 452 / 171 条 import** · `PLUGIN_VERSION = 0.13.0-batch13`。**证据**：`logs/zoom-launcher.png`（改前：浅底浅字几乎不可读 + ◆总监 压住「交给总监整理」）↔ `logs/zoom-dock-fixed.png`（改后：深字清晰、按钮与 R6 完整露出）。 |
| T-PLUG-023 | 🔬 **批次 14：审核**测试自身**（跨运行污染 / 静态前提 / 平凡真）** | P0 | `scripts/verify-design-studio.mjs` · `scripts/verify-mindmap.mjs` · `scripts/crop-shot.py` | ✅ **已完成 2026-09-12 · **产品源码零改动**（`lib/client.js` 890,104 B **逐字节不变**）**。**六个缺陷全在闸门自身** —— 结论：**闸门报「偶发红」时，先怀疑闸门**。**① `ds-personalize` 是开合按钮，却被 C16.2「逐点顶栏每个按钮」点开后无人还原** ⇒ 该面板 `window` 捕获 + `stopPropagation` 吃掉 Esc ⇒ C13 呈「r1 绿 r2 r3 红」的**假偶发**（真凶在**同一脚本的另一段**；因果链与三次运行逐条对得上：r1 的 C13 在 C16.2 之前 ⇒ 绿；同轮 C16.11 随即被该面板遮挡 ⇒ 报「按钮被遮挡」≈ 产品缺陷；面板留到结束 ⇒ r2/r3 连带红）⇒ 抽 `closeFloatLayers()` **单一真相源** + C16.2 每轮**当场还原** + 新增 `C16.2p` 断言还原成功。**② C13 按「三层」写而产品是四层**（个性化 → 待确认 → 选中 → 关窗）⇒ 改成逐层退序列 `C13.0`–`C13.5` 共 8 条：显式归零 → 建待确认 → **真开个性化** → 四层各一条（同时断言「该关的关了」+「不该动的一动没动」）→ `C13.5` 反证「不是一跳关掉」。**③ C13.1 的「没动」是空真**（Esc 整体失效也绿）⇒ 加**正对照 `C13.1n`**（个性化未开时按 Esc，待确认必须被吃掉）。**④ 导图折叠探针不检查「当前是否已展开」** ⇒ 挑到已折叠节点、一击其实是展开（`{"before":11,"after":13}`）⇒ `C-M8p` 归零前置 + 探针只取 `data-collapsed!=="1"` + `C-M8q` 正负对照。**⑤ 收紧为精确对账后发现测量口径错**：`data-collapsed` **一个语义标在两个元素上**（节点 `:676` + 框内控件 `:733`）⇒ 数出来 2 倍（`{"allCollapsed":2,"collapsibleNonRoot":1}`）⇒ 计数钉死 `[data-testid=mm-node][data-collapsed="1"]`。**⑥ `C-M16d` 声称覆盖个性化层却从不打开它** ⇒ `C-M16d0` 用真实按钮真开（消平凡真）。**新增机制**：`C17.1` **点击落点自检**（每次点击前读 `elementsFromPoint` 栈顶，必须落在目标子树内 ⇒ 把"静默打偏"变成点名）· `C17.2` **环境复原**（跑完关回工作室 + 三类浮层并断言，让**每次运行起点等价**）· 三次连跑**载荷逐字一致**（幂等性证据）。**闸门**：工作室 **90/90 ×3**（`logs/verify-studio-r6/7/8.log`）· 导图 **86/86 ×3**（`logs/verify-mindmap-r10/11/12.log`）· 真机演练 **57/57 ×3**（`logs/verify-flow-26/27/28.log`）· bundle **90/90** · 49/63/91/87 全绿 · lint 阻塞 0/提示 16。**新增工具**：`scripts/crop-shot.py`（截图局部放大，坐标用 CSS px，NEAREST 保像素边缘）。**教训入库**：台账 #68–#73 + §八 纪律 32–35 + 踩坑索引 坑14–17。 |
| T-AESTH-001 | 审核快赢项修复（四批合计 121 处：38+42+5+36；Q2-1 chrome 全清，19号审核通过） | P1 | 40-测试质量/15、16、18、19号记录 | 🔄 Q2-2 待做：数据字段 hybrid 渲染 + BranchMindMap/DirectorChat + 下拉 items |
| T-AESTH-002 | 审核结构项并入插件化批次（P0-1~P0-4/P1-6/P1-7） | P2 | 同上 §五 + 26号文 §八 | 🟡 待用户确认 |
## 双实现收敛决策（2026-09-07 新增 · 待用户）

| 编号 | 任务 | 优先级 | 关联文档 | 状态 |
|:-----|:-----|:------:|:---------|:-----|
| T-CONV-001 | 总监双实现收敛：✅ 用户拍板方案①（收敛到 dsh-director 插件） | P0 | 20-任务文档/29-收敛与退坡方案-基于方案①.md | ✅ 已拍板；退坡手术批 C-1/C-2 待用户确认清单后执行 |

## 验证清单重构（2026-09-08 · N-3.2）

> **旧人工验证欠账处置**：V10.1 六项验证（dModal/遮罩/X钮/折叠宽/hover/📍）目标 UI 已随 MOD-B 退坡移除——验证义务**转移**至 dsh-director 插件对应功能；V9.4/V9.5 清单同理（zb2 滚动/令牌体系已随插件 U2-U5 实现）。新验收以 28号文 §十一 插件能力为准，人工过一遍：总监 tab 三切换 / 总览四卡 / 对话流+模型下拉 / 任务·分支·记忆面板 / 轨迹筛选·导出 / 文件浏览器。

## 收敛批 C 系列（2026-09-07 新增 · 29号文）

| 编号 | 任务 | 优先级 | 关联文档 | 状态 |
|:-----|:-----|:------:|:---------|:-----|
| T-CONV-C1 | 批 C-1：插件功能覆盖核对（记忆不同源已登记，数据零风险） | P0 | 40-测试质量/17-收敛批C1C2执行与审核记录 | ✅ 完成（17号审核 95/100） |
| T-CONV-C2 | 批 C-2：MOD-B 退坡手术（注册块移除 + 退坡标记） | P0 | 同上 | ✅ 完成（served 实证 + 交互零变化） |
| T-CONV-C3 | 批 C-3：MOD-A 决策——✅ 保留最小锚点+《锚点契约》落地（29号文 §五） | P1 | 29号文 §五 | ✅ 完成（CDP 实证） |
| T-CONV-P2 | P2 死代码删除：DirectorView.js region 1,519 行整段移除 + 三段冒烟全绿 + numstat 复测 | P0 | 40-测试质量/21-收敛批C1C2执行与审核记录 | ✅ 完成（21号审核通过） |
| T-CONV-P2 | P2 死代码删除（DirectorView 164 引用 @deprecated-candidate）+ 全量回归 + 补丁行数复测 | P1 | 28号文 §九 | ⏳ 待排期 |
| T-CONV-V3 | V10.2 第三批（插件 src：Q2 全量图标 / Q4 渐隐 / Q5 卡片结构） | P2 | 27号文 §五 | ⏳ 待排期 |

## 已完成

见 04-已完成任务清单.md
