# 已完成任务清单 — dsh-client-mod

> 已归档任务。完成时间倒序。

## 2026-09-17 — 第三十一轮（**「还是闪退」真因查到底**：单实例静默接管 × 关窗不退（设计）+ **报绿口径修正** + 可复现回归验收 `verify-flash-crash` 11/11 + 桌面双入口闭环）

> 用户原话：「**还是不行 闪退!! 你块给我查 查所有的**」—— 承接上一轮的 CRLF 修复，说明**上一轮的结论仍不完整**。
> 本轮取舍：**读应用整本行为（asar）而不是猜**；`src/**` 与产物**零改动**。

**A · 真因（两条，都有 asar 原文 + 实测双证 ⇒ 新增 §八 纪律 117）**

| # | 机制 | 出处（`resources/app.asar`） | 实测读数 |
|:--|:-----|:-----------------------------|:---------|
| a | **单实例静默接管** —— 已有实例在跑时再启动 ⇒ 抢不到锁 ⇒ **静默 `exit 0`、零输出、窗口永不出现** | `/lib/main.js`：`if (!app.requestSingleInstanceLock()) app.quit();` | 首次 **1.40 s** `exit 0` · 输出 **2 字节 `\r\n`**；复现 **1.93 / 1.76 s**；**第一个实例不受影响**（进程在 + 窗口标题 `DeepSeek Harness` 在） |
| b | **制造机 = 关窗不退（设计如此）** —— 点 × 只 `hide()` 到托盘，进程常驻 ⇒ 用户以为关了 ⇒ 下次双击必然命中 (a) | `/lib/types/window-lifecycle.js`：`onWindowClose(event){ if (quitting) return; event.preventDefault(); options.getWindow()?.hide(); }` + `/lib/main.js`：`app.on("window-all-closed", () => {})` | 唯一正常退出 = 托盘右键「退出」→ `releaseAppQuit()`（`quitReleased = true; tray?.destroy(); app.quit();`） |
| b′ | **boot 期唤窗是空操作** —— `lifecycle` 尚未赋值（排在 `await currentHost.start()` 之后，就绪上限 **90 s**）⇒ `lifecycle?.showWindow()` 什么也不做 | `/lib/main.js`：`app.on("second-instance", (_e, cmd) => { …; lifecycle?.showWindow(); })` | 那段时间再双击，旧实例**连窗口都不唤** |
| — | **已排除**：普通双击**不带**安装器私有参数 `--dsh-installer-quit`（`isInstallerQuitRequest` 不会误命中） | `/lib/main.js` | — |

**B · 🔴 本轮真正的病根在判据侧（我自己的）⇒ 新增 §八 纪律 118**

`start-harness.mjs` v2 的通过判据 = 「CDP 出现 `page` 目标」（实测 2.5–4.3 s），而单实例接管发生在**其后**（a 段 1.4–2.0 s 退出）⇒ **连续 4 次 ✅，用户每次都闪退**。修：加**观察窗** `--hold`（默认 **15 s**），窗口出现后**继续盯** `imageProcs()` 里自己的 pid；结论段打印 verdict + 处置建议 + 应用输出 + 4 条排查顺序，「窗口出现了」**不算终点**。

**C · 新增交付物**

| 项 | 说明 | 验证读数 |
|:---|:-----|:---------|
| `scripts/verify-flash-crash.mjs`（新建） | **可复现「闪退」的回归验收**，真机 11 条含正负对照 | **PASS 11 / FAIL 0 · IS_PASS TRUE**（复跑两次均绿）：`FC-5` 第二个进程 ≤15 s 消失（**1.93 s**）· `FC-6` `exit 0` · `FC-7` 输出 **2 B** · `FC-8` 首实例不受影响 · `FC-9` verdict **`TAKEN_OVER`** · `FC-10` `OK ≠ TAKEN_OVER` · `FC-11` 收尾停机 |
| `scripts/stop-harness.mjs` + 桌面 `Stop Harness.cmd`（新建） | **真退出入口**（用户级"关机"）；打印停机前清单 | 桌面副本 **rc=0**；同调用 Start→Stop：准确认出主窗口 `pid 6448「DeepSeek Harness」`+4 个托盘进程，**进程表 0** |
| `scripts/_asar-read.py`（新建，可复用） | `--list` / `--dump` / `--grep`；**凡应用整体行为级故障先读 asar** | 908 条目 · `--grep` 匹配 120 · dump 2388 B |
| `scripts/_harness.mjs`（增强） | 纯函数判据 `classifyLaunchOutcome()`（5 verdict + `BAD_OPTION` 最高优先）· `verdictAdvice()` · **`spawnHarnessEx()`（留句柄 ⇒ 退出码可读）** · `harnessWindows()` / `mainWindowPids()` · **`stopHarness()` 主判据改为进程表空** |
| `scripts/start-harness.mjs`（v3）· `start-harness.cmd`（行尾 TIP）· `lint-crlf.mjs`（`DESKTOP_COPIES` 认两个桌面副本） | 判据 + 入口面同步 |

**D · 闸门与基线（全绿）**

`test-harness-ctrl` **52 PASS / 0 FAIL / 跳 3 · IS_PASS TRUE**（新增 `HT-34~HT-46`，含 `HT-40` 植入缺陷校准、`HT-44` 源码接线自证、`HT-45/45c` 可校准谓词、`HT-46` stop 入口）· `lint-crlf` **16 个批处理（含桌面副本 2）** + `CRLF_CAL=1` `CAL_HIT` · `lint-syntax` / `lint-undefined-symbols`（阻塞 0 / 提示 17）/ `lint-cdp-templates` / `lint-platform-stub` / `lint-dom-style-units` 全 TRUE · `baseline-check` **IS_PASS TRUE（93/93 · 漂移=0）** · `check-stale-build` TRUE（指纹 `2085f97cd1b8003c`）· `refresh-index --check` **三份一致**。

**E · 给用户的操作口径（关键）**

1. 打开：双击桌面 **`Start Harness.cmd`**（窗口 3.6–7.3 s 出现）。
2. 🔴 **关掉窗口 ≠ 关掉应用**：点 × 只是**缩到托盘**（应用设计如此）。要真退出：**右键托盘图标 → 退出**，或双击桌面 **`Stop Harness.cmd`**。
3. **否则以后每次双击都会"静默闪退"** —— 因为旧实例还在，新启动抢不到单实例锁（这正是本次报的"闪退"）。

**F · 清理与台账**

本轮一次性探针 8 个（`_r25-asar-scan.py` / `_r25-asar-dump.py` / `_r25-close-probe.py` / `_r25-si-probe.py` / `_r25-lnk-scan.py` / `_r25-soak.py` / `_r25-write-cmds.py` / `_r25-run-cmd.py`）已删；其中 asar 能力**升格为正式工具** `_asar-read.py`。取证日志保留：`logs/_r25-*.log/.txt` + `logs/_asar/lib__types__window-lifecycle.js`（纪律 114）。

---

## 2026-09-17 — 第三十轮（**「还是闪退」真因：交付入口自己就是 LF-only 批处理** + 四条排除法证据 + 新闸门 `lint-crlf`）

> 用户原话：「**还是闪退**」—— 只有一句，但它**推翻了上一轮「已修好」的结论**。
> 本轮取舍：**不碰闸门层、只查真因**；`src/**` 与产物**零改动**。

**A · 真因（新增 §八 纪律 115）**

| 检查项 | 读数 |
|:---|:---|
| 双击后 `logs/harness-launch.log`（**脚本第一行就写的探针**） | **一行都没增加** ⇒ **脚本一行都没执行** |
| `Start Harness.cmd` 行尾（**逐字节**统计） | 🔴 **CR=0 / loneLF=44** ⇒ LF-only |
| 同机同时刻 `start-harness.mjs` 直跑 | ✅ **RC=0 · 2.6 s 出窗 · 进程 5 个 · CDP 通** |
| **裸启动 exe**（= 双击 exe / 开始菜单 lnk 的等价路径） | ✅ **12 s 仍存活** · 输出 `dsh web: http://127.0.0.1:24436` |
| Windows 事件日志（近 3 h）· userData 15:41–15:47 | **零崩溃记录** · **零文件写入** |
| 进程表 · Electron `Singleton*` 锁文件 | **空** · **无残留** |

⇒ **产品好、脚本好，坏的只有「双击」这一层**。根因链：本仓写盘默认 LF（纪律 ⑨ 只针对 `src/**`）→ 上一轮**新写的** `.cmd` 必然是 LF-only → cmd.exe 解析纯 LF 时 `if ... (`…`)` 多行括号块与 `for /f` 复合行错乱 → **整份脚本不执行就退出**。
⚠️ 为什么上一轮没发现：上一轮修的 `start-harness.mjs`（清 `ELECTRON_RUN_AS_NODE`）**只对 agent shell 那条路有效**，对**用户双击**这条路**完全无效**。

**B · 修复（治本 + 结构性防复发）**

| 项 | 处置 |
|:---|:---|
| `start-harness.cmd`（项目内 + 桌面副本） | **CRLF** 重写 ＋ **去掉全部嵌套语法**（无 `for /f`、无多行括号块，`goto` 替代）＋ **早期探针** `[cmd] entered`（以后再坏必留痕）＋ **成败都 `pause`** ⇒ **物理上不可能"闪退"** |
| 全仓同类扫查 | **5 个 LF-only**（`.cmd` ×1 ＋ `.ps1` ×4）⇒ 全部转 CRLF |
| 🆕 闸门 `scripts/lint-crlf.mjs` | `.cmd/.bat/.ps1` 一律 CRLF；扫描面**含桌面交付副本**。常规 **14/14 · IS_PASS TRUE**；`CRLF_CAL=1` 注入 LF-only ⇒ **CAL_HIT · 必然红**（纪律 ㉜） |
| 入批方式 | `_offline-run.mjs` / `_test-ledger.mjs` **按文件名模式自动发现** `lint-*.mjs` ⇒ **零注册**即入离线批 |

**C · 元教训（本轮最值钱）**：上一轮把「启动器**连跑 4 次全 ✅**」当成交付完成 —— 但那 4 次**全是直跑 `.mjs`**，从未跑过「**双击 `.cmd`**」这条**用户真实路径**。⇒ **验证路径必须与用户的使用路径一致**，否则验的是"我的世界"而非"用户的世界"（纪律 ㊶ 同类）。

**D · 双击路径实测通过（纪律 116）**：沙箱**直接调 `cmd.exe` 会被拦**，但 `explorer.exe "C:\Users\15142\Desktop\Start Harness.cmd"` 走的是 **ShellExecute** = **真实双击**。实测**四行全到**：`[cmd] entered 周四 2026/09/17 15:55:11` → `[cmd] node="C:\Program Files\nodejs\node.exe"` → `OK env-polluted=[] before=0 pid=18932 port=9222 secs=2.5` → `[cmd] script exit 0`。🔴 **正负对照（决定性）**：同一入口在 **LF-only** 时 ⇒ 日志**一行都没多**。⇒ 用户双击**现已可用**（窗口会停住等按键，不再闪退）。

**E · 未结项**：`T-PLUG-061` · `T-PLUG-059` · `T-PLUG-040` · `T-PLUG-063/064`。

## 2026-09-17 — 第二十九轮（**清空会话 + 清截图/历史日志 + 把「打开闪退」查到底**；途中一次假红）

> 用户原话：「**清楚所有的会话,截图, 现在还是打开闪退**」
> 本轮定位：**只做两件事** —— 清理（会话 / 截图 / 历史日志）与「打开闪退」的实据；`src/**` 与产物**零改动**。

**A · 「打开闪退」—— 两条真因都在**启动上下文**，产品无病（§八 纪律 112）**

| 条件（本机实测，逐秒看进程表） | 结果 |
|:---|:---|
| 干净环境 + 无实例（有/无控制台、任意 cwd） | ✅ 窗口 **2.6–4.8 s** 出现 · 进程 5 个 · 稳定 60 s · 输出 `dsh web: http://127.0.0.1:<port>` |
| 终端带 `ELECTRON_RUN_AS_NODE=1` | 🔴 **216 ms 静默 exit 0**（无窗口）；带 `--remote-debugging-port` ⇒ `bad option` **exit 9**（exe 退化成**纯 Node**） |
| 已有实例在跑（含**托盘里看不见的**）⇒ 再启动一次 | 🔴 **1211 ms 静默 exit 0、输出全空** —— 窗口永不出现，用户看到的就是「闪退」 |

**B · 交付「一键打开」**（`scripts/start-harness.mjs` 重写 + `start-harness.cmd` 重写，桌面同款）
清 `ELECTRON_RUN_AS_NODE`/`NODE_OPTIONS` → **验证式停机**（进程表空 ∩ 端口可 bind）→ 启动 → **等窗口真出现**（判据 = CDP `page` 目标，不是「进程还在」）→ 起后又死则**去掉调试参数自动重试一次** → 失败时给 4 条排查顺序 → 每次追加 `logs/harness-launch.log`（环境警告 / 残留数 / pid / 端口 / 结论 / 耗时 / 输出字节）。**连跑 4 次全 ✅**。

**C · 清理（工具化，dry-run 默认 + 复验）**

| 目标 | 工具 | 结果 |
|:---|:---|:---|
| 宿主会话档案 + 回收站 + 会话缓存 + 会话索引 | `scripts/clear-sessions.mjs`（新） | `~/.dsh/sessions` **488 文件 / 49.99 MB → 0**；`.trash-*` **7 → 0**；`session_projcache.json` 删；`archivedSessionIds` **134 → 0**；应用在跑则**拒执行**（退出码 2） |
| 截图 + `logs/` 历史产物 | `scripts/clean-logs.mjs`（新） | 截图 **11 → 0**；`logs/` **233 个 / 1.87 MB → 12 个 / 0.09 MB**；判据 = **文档是否引用**（引用即保留）+ 常驻台账白名单；清单落 `_del-candidates.txt` |

**D · 一次假红（§八 纪律 113）**：从**工具调用**里拉起的 GUI 会随该次调用**整棵回收** —— 同一条命令内观察 60 s 稳定存活，命令一返回进程表**立刻归零**。一度被我读成「应用起来又死」，实为**调用结束**（纪律 94 形态：前提缺失 ≠ 产品坏）。

**E · 代价（如实记账）**：会话清理的**归档备份失败**（Git Bash 的 `tar` 不认反斜杠路径）⇒ **会话不可恢复**；
日志清理首次执行时 import 漏配，**同样未成存档**（清单保留）。两处脚本均已修正，下次会先生成归档。

**F · 未结项**：`T-PLUG-061` · `T-PLUG-059` · `T-PLUG-040` · `T-PLUG-063/064`；🆕 待用户回执：双击仍闪退则回传 `logs/harness-launch.log`。

## 2026-09-17 — 第二十八轮（**端到端链路真机跑通** + 「打开就闪退」两条真因 + 两个假红 + 清理 39.22 MB）

> 用户原话：「**清理所有的截图 ,处理没必要的东西, 汇报执行结果 你是去做测试为什么没看到测试结果**」+「**软件打开就闪退处理下**」
> 本轮的取舍：用户指出一直在做闸门/判据、**没跑 19 号文那条链路、也没给结果** ⇒ **先跑链路，再谈别的**。`src/**` 与产物**零改动**。

| 段 | 内容 | 读数 / 证据 |
|:---|:---|:---|
| **A · 端到端链路（19 号文 §1.1）** | 一条命令内冷启动一次：链路闸门 + 逻辑链 + 人工验收路径（真派发）+ 取证截图 | `verify-novel-split` **130/130 · 0 失败 · 0 跳过 · TRUE**（派发 8 → **复用 8 · 新建 0**｜宿主 **170 → 170 净增 0**｜索引孤儿清理 0）· `verify-director-logic` **29/29 · 零兜底 · TRUE** · `verify-novel-e2e-human`（`E2E_RUN=1`）**5 通过 / 0 未通过 · TRUE** · `link-shots` **8/8 · 10 张 · 面覆盖 4/4** |
| **A1 · 用户可感知读数（实测）** | 常驻读数四段齐 | `现存会话 170 条（归档 134）｜本轮 复用 5 · 新建 0 ｜归属 A3 剧情 + A1 世界观 + A2 力量体系 + A5 正文 + A4 人物 · 置信 0.90 ｜通道 —` ｜ 属性 `attr=plot,world,power,prose,chars` / `conf=0.90` ｜ `data-alive=170 === 树 rows=170`（**对账一致**）｜ 派发后 `{attr:"plot",conf:"0.90",reused:"1",created:"0",made:"1",kind:"novel"}` |
| **B · 「打开就闪退」真因** | **都不是产品坏**，是启动路径问题 | (a) `ELECTRON_RUN_AS_NODE=1`（**agent 的 shell 里就有**）⇒ exe 退化成**纯 Node**：无参数 **216 ms 静默 exit 0（无窗口）**、带 `--remote-debugging-port` ⇒ `bad option` **exit 9**。(b) 已有实例在跑 ⇒ 第二次启动 **1274 ms 静默 exit 0**、窗口**永不出现**。**反证**：干净环境无参数无实例 ⇒ 存活 20 s、`dsh web` 正常 |
| **B1 · 处置** | 🆕 `scripts/start-harness.mjs` + `start-harness.cmd`（+ 桌面 `Start Harness.cmd` 纯包装） | 流程 = 清环境变量 → **验证式停机**（进程表空 ∩ 端口可 bind）→ 启动 → **等窗口真出现**（判据 = CDP 有 `page` 目标，预算 120 s）→ 打印结论；`--keep` / `--no-cdp` |
| **C · 假红 ①（判据读错元素）** | `HE-4` 恒红，产品无病 ⇒ 新纪律 **111** | `data-attr`/`data-conf`（归属）只在 **`dp-live-readout`**；`data-reused/created/made`（派发）在 **`dp-flow-split`** ⇒ 从后者读 `data-attr` **恒 `null`**。修：两元素**分别读再合并** + `HE_CAL_ATTR=1` **复现错元素读法**（校准态**恰红 HE-4 一条**，`attr:null`） |
| **C · 假红 ②（起点自举抛穿）** | `CL.clickAt is not a function` ⇒ `dp-root` 永不出现 ⇒ `HE-0/1/2/4` **整片假红** | 真因：`verify-novel-e2e-human.mjs` 与 `link-shots.mjs` **两份**都把 `ensureDirectorPage({CL})` 传成 **`console.log`**（复制粘贴）。⚠️ **平时看不出来** —— 只有"宿主冷启动没渲染出页签环、必须走真实 UI 自举"那条路径才命中 |
| **C1 · 防复发（源码级契约断言）** | `test-harness-ctrl.mjs` 新增 **F 组** | `HT-33`（扫 **8 个**引用起点的脚本，`CL` 不得是 `console.*`）+ `HT-33b`（**自指豁免恰好 1 个** —— 豁免名单不许悄悄变长）+ `HT-33c`（合成缺陷源校准**必须报出**）⇒ **36 PASS / 0 FAIL / 跳 3 · IS_PASS TRUE** |
| **D · 清理** | 🆕 `scripts/clean-artifacts.mjs`（**默认 dry-run**，逐条落清单） | **删 355 项 / 回收 39.22 MB**：截图 **67 张 / 25.66 MB** · `_` 前缀一次性中间件 **286 个 / 7.14 MB** · `scripts/__pycache__` + 项目根 `.trash-20260917-1330` / **6.42 MB**。`logs/` **36 MB → 2.5 MB**；全仓 PNG **67 → 0** |
| **D1 · 未删（只报告）** | `~/.dsh/.trash-*` 7 个 / **38.28 MB** | 内含**宿主会话档案**（`sessions` 的 `session.jsonl.zstd`）= **用户数据**，永久删除**不可逆** ⇒ 默认只报告；需 `--include-host-trash` 显式授权（纪律 **83**） |
| **E · 记忆** | `MEMORY.md` **超限被截断** ⇒ 压缩重写 | 16.7 KB → 约 11 KB（纪律 1–100 收敛为索引行、详情仍指向 `MEMORY-DETAIL.md`）+ 新增**环境事实 5/6**（闪退两条真因）+ 纪律 **111**；`2026-09-17.md` 追加第二十八轮整段 |
| **F · 仍挂起（不阻塞）** | 见 `03` | ⚠️ **诊断通道 2 次**（兜底被走到 ⇒ 先查前提，纪律 90）· `T-PLUG-061` · `T-PLUG-059` · `T-PLUG-040` · `T-PLUG-063/064` · 🆕 会话档案待裁定 |

> ⚠️ **本轮过程失误（如实记录）**：我在验收正在跑时误跑了刚写好的启动器（`--help`），它把验收用的 Harness 杀了 ⇒ 那一轮作废、重跑。**教训**：会**停/起宿主进程**的脚本，在真机套件运行期间**一律不许调**（`test-harness-ctrl` 的 G 组跳过理由里写的就是这条 —— 纪律 83）。

> 说明：**2026-09-07 – 09-13 各批次的逐条记录**已由 `03-待完成任务清单.md` 的 ✅ 行
> **于 2026-09-14 一次性迁入本文件**（见下节）。`03` 从此只保留「还没做完的事」。
> 逐条闸门纠错的取证细节仍以 `AGENTS.md` §闸门纠错台账为准。

## 2026-09-17 — 第二十七轮（承接同一条指令的**连续执行**：`link-shots` 判据**换口径** · 校准修正 · 清除"证据假"）

> 无新指令 —— 用户上一条仍是「**自行重启, 然后列出清单 按照顺序执行**」+「**为什么停下来等我确认，怎么避免**」。
> 改动面：**只有 `scripts/link-shots.mjs` 一个文件**（`src/**` 与产物**零改动** ⇒ 无需重建/重装/重封）。

**A · 修掉的三条（全部当场修完）**

| # | 现象 | 真因 | 修法 + 证据 |
|:--|:-----|:-----|:-----------|
| 1 | 🧪 校准跑**照样报绿**（"校准等于没校准"） | `LS_CAL_DUP=1` 只把**第 2 张**换成第 1 张的锚点 ⇒ 注入量恒 = **1 个重复**；而自然画面数在 **6–8** 之间飘 ⇒ `8−1=7` 仍 ≥6 | 校准改成**复现修前形态**（第 02–09 张全部同锚点、不开浮层 / 不点击 / 不关闭）⇒ **与基线无关地必红**：`coverage 2`（缺 `mindmap`/`studio`）；并加 **`LS-5c`** 用**同一个谓词** `ls5Ok()` 判缺陷数据 ⇒ 机器可判（§八 纪律 108） |
| 2 | 🔴 门**建在了错的量上** | `md5 去重后的画面数`量的是**动态像素**（读数刷新 / 滚动余量），不量**证据面**（纪律 31/93）—— 同计划两次跑实测 **6 / 8** | 换口径：**`LS-5` 界面面覆盖 ≥ 4**（快门那一刻**实时观察**的面，用产品自己的可见性）+ **`LS-5b` 换面必换帧**（不同面 md5 必须不同）；`不同画面数` 降级为**报告项**（照打不设门）（§八 纪律 109） |
| 3 | 🔴 一类**"证据假"**（切换成功没被看见） | 06/08 原都点**第一个**元素，而第一个往往**本来就是选中态** ⇒ 点与不点**画面一样**（`mindmap ≡ mindmap-node`、`studio-screen ≡ canvas`） | 新增 **`clickIdx`**（点**第 2 个**，不存在则回落第 1 个）⇒ 本跑 `mindmap ≢ mindmap-node`、`studio ≢ studio-screen`（§八 纪律 110） |

**B · 判据（本轮封存值）**

| 项 | 值 |
|:---|:---|
| 正常态 | `link-shots` **PASS 8 / FAIL 0 · IS_PASS TRUE** · 10 张（非空白 10 · 有锚点 10）· **面覆盖 4/4**（`page×4 / mindmap×2 / studio×3 / personalize×1`）· **换面 0 违规** · 报告项 不同画面 **6/10**（同帧 4 条**全在同一面内**：`console/attribution/collect≡before`（四处读数本就同屏）、`canvas≡studio-screen`） |
| 🧪 校准态 | **恰红 `LS-5` 一条**（`coverage 2` · `missingFaces: mindmap, studio`）+ **`LS-5c` ✅** ⇒ "红/绿"确实由那一行谓词决定 · 不同画面 2/10（8 张同帧） |
| 真机（同批） | `verify-novel-split` **IS_PASS TRUE**（`复用 8 · 新建 0`）· 静态 lint 四把全绿（`undefined-symbols` 阻塞 0 / 提示 17 · `cdp-templates` · `platform-stub` · `dom-style-units`） |
| 实现细节 | `VIS`（浮层**可见性**判据）提为**唯一实现**，face 观察与 `LS-6` 共用（各写一份迟早不同源 —— 纪律 78/「同一事实只留一份真相源」）· 🧹 每次运行**清掉旧计划残留帧**（编号模式 `NN-*.png` 但不在本次计划里）并**逐个打印** —— 实测清掉 **10 张**上一版计划的截图，理由：人翻这个目录时分不出哪 10 张是当前的（**残留会被当成当前证据**，纪律 83 留痕） |

**C · 结账**：**结账 0 条 · 新增 0 条**（`03` 只加了本轮并入说明；另修掉 `03` 里一处**过期引用**「纪律 101/102」→ **106/107** —— 上一轮改号只改了锚点，属纪律 59「记号撞车」形态）。

## 2026-09-17 — 第二十六轮（**19 号文 P4–P10 连续执行到底**：U10 真因修复 · `NS-11h` 真产品缺陷 · 🔴 生成器振荡 · 结账 3 条）

> 用户原话：「**自行重启, 然后列出清单 按照顺序执行**」+「**在此前考虑下为什么要停下来等我确认, 怎么避免, 写入技能中**」
> ⇒ 本轮是**自主连续执行**的样板：从「构建 → 装机 7 步」第 6 步接着跑到底，**不再中途索要确认**。

**A · 修掉的真因（三条，全部当场修完）**

| # | 现象 | 真因 | 修法 + 证据 |
|:-:|:-----|:-----|:-----------|
| 1 | 🔴 **冷启动后"复用"必然失效**（`T-PLUG-058` / U10） | **不是**"索引丢了"（那是**前提**）——是**判据与真值不同源**：复用侧拿 `branchTitle()`（角括号 `「A1 世界观」《灵能修仙》`）去**全等**匹配 `briefOf()` 写进简报 head 的方头括号 `【A1 世界观】《灵能修仙》` ⇒ 字形都不同 ⇒ **0 命中**（纪律 27） | 抽 `briefTitlePrefix()` 作 head 与匹配判据的**唯一真相源** + 匹配改**前缀**（自带边界 `】`/`》`，封死 `A1`↔`A10`、`《灵能修仙》`↔`《灵能修仙传》`）+ `titlePool` 按 `at` 降序取最新。**真机实证**：冷启动 `复用 8 · 新建 0`、宿主会话 **170 → 170（净增 0）**、`title-pool=153 / hit=1` |
| 2 | 🔴 **全复用时索引不写 ⇒ 索引永远空**（U10 连带） | `recordSplits` 只登记"新建的"，理由是"复用的早就在索引里" —— **冷启动时索引是空的** | 补登记「**非索引命中**」全部（含标题命中）+ `titleHits` 报**条数**（不是布尔）；`RU-T12/T13` 自证 |
| 3 | 🔴 **常驻读数 `data-alive` 恒空**（`NS-11h` 唯一那条红牵出） | `data-alive` 接到**层级树** `tree`（`loadTree()` 返回的是**根节点**，字段 `childNodes`，**没有 `rows`**）⇒ `tree` 恒 truthy 而 `tree.rows` **恒 `undefined`**、文案**恒显示**「分支树未建立」，而同一刻 `getBranchSnapshot().tree.rows` 有 **170** 行 | 改取**分支树**三态（`liveRows`：`null` / `[]` / 非空）+ 离线接线自证 **`RU-T15`**（正则断言取 `liveRows`、**不得**取 `tree.rows`）。修后 `{"alive":"170","archived":"134","rows":170}`；**§八 纪律 106** |

**B · 🔴 本轮最贵的一次弯路（已固化为纪律 107）**

同一份 `src/**` **连续两次** `build` 得到**不同**产物（`2008557 B / 2085f97c…` 与 `2008511 B / cfd2c7df…`），而两次 `check-stale-build.mjs` **都报 TRUE** ⇒ 看起来像"构建不稳定"。

- **真因**：`src/logic/key-files.js` 是**生成物**，它的 `@map` 头有**两个写者**且口径不同 —— `gen-key-files.mjs`（第 97 行）写 `设计稿：…V19…（板块 H / I）`，`gen-source-map.mjs` 按人工小表回写 `…V16…（板块 —）` ⇒ **谁最后跑谁赢**；而两个生成器**都在构建面（`src/**`）内**、且**离线套件会跑它们** ⇒「我构建的是哪一份 src」在**跑程内会变**。
- **第二层缺陷**：`gen-source-map --check` **无条件**打印 `IS_PASS: TRUE`，而 `verify-index.mjs` **只看退出码** ⇒ 振荡在闸门层面**完全不可见**（纪律 31「报绿先审口径」）。
- **修法**：① **一个文件的头只允许一个生成器写**（`OWNED_BY_OTHER` 跳过生成物）② `--check` 改判「**是否需要写盘**」（`刷新>0 ⇒ IS_PASS FALSE + exit 1`）③ 新增常驻闸门 **`test-generators.mjs`（4/4）** 守"两个 `--check` 必须**同时** TRUE"（它们曾经**互斥**）。
- **植入缺陷校准（留证）**：去掉 skip ⇒ `刷新 1` · `IS_PASS: FALSE` · **exit 1**，`verify-index` 由**全绿**转 `合计 3 份：❌ 漂移 1`；还原后 `刷新 0` 且脚本**逐字节一致**（17,831 B）。
- **硬证据**：修后**连跑两次 `build` 逐字节一致**（`2,008,557 B` / 指纹 `2085f97cd1b8003c` / md5 `0c5bd9ac54683825e2ecf7ab0ff360bc`）。

**C · 闸门侧（修 + 加固）**

- **W 段 5 条假红**（`NS-8l/m/n/o/q`）：由新建的**逐帧几何对账**探针 `_probe-win-drag-geom.mjs` 钉死为**闸门等待不足** —— 步间 `sleep(18 ms)` < 实测派发延迟 **58–76 ms**（纪律 91）⇒ 松手先到、排队中的 move 被整条丢弃。修法：`settleDragMove()` **松手前排空**（不靠固定 sleep）+ 前提断言 **`NS-8l0`**（`w3.x === 0`）。修后 **126/126**。
- **读数口径更正 4 处**（纪律 99，**改口径不掰产品**）：`NS-4c2`（允许部分复用）· `NS-11b`（冷启动**必须**补登记）· `NS-11c`（净增 == `created`）· 索引键不存在 == **0 条**（原读 `null` ⇒ `Array.isArray` 恒假 ⇒ **永远等不到**，而全复用时该键本来就不写）。
- **统一起点等待**：新增 `waitCdpPage()` 进 `_cdp-startup.mjs`（纪律 98 唯一实现）—— 四个真机脚本原来各自写等待，一次连跑**三个脚本同时**死在 `INVALID：CDP 无 page 目标`。
- **新增断言**：`NS-11e/f/g`（U10 标题弱匹配三项读数）· `NS-11h`（读数对账）· `NS-8l0`（前提）· `RU-T15`（接线自证）；`test-director-reuse` **17 → 35/35**。
- **N8 三件产出全绿**：常驻读数 `dp-live-readout` · `link-shots` **10 张截图（非空白 10 / 有锚点 10 / 面覆盖 4-4）** · 一键人工验收 `verify-novel-e2e-human`。⏭️ 截图计划与判据在**第二十七轮**换过口径（见上节）。

**D · 结账 3 条**

| 条目 | 内容 | 结论 |
|:-----|:-----|:-----|
| **T-PLUG-050** | 写消息触点补全（**需用户先确认语义**：只"登记为流转"该不该产生总监消息） | ✅ **语义已裁决**：「登记为流转」**不产生**总监消息，界面**显式标注**该事实，并以**负对照**证明"同一次登记前后条数不变" |
| **T-PLUG-058** | 🔴 冷启动后复用索引随 origin 丢失 ⇒ 首次派发必然重建 N 条（U10） | ✅ **真因修复**（见 A.1/A.2）—— 冷启动 `复用 8 · 新建 0`、宿主会话**净增 0** |
| （U10 连带） | 全复用时 `dsh.director.split` **整键不存在** ⇒ 导图拿不到 `titleOrigin` | ✅ **补登记**「非索引命中」全部 |

**E · 判据（本轮封存值）**

| 项 | 值 |
|:---|:---|
| 真机 | `verify-novel-split` **130/130 · 0 失败 · 0 跳过 · IS_PASS TRUE**（`复用 8 · 新建 0`）· `verify-director-logic` **29/29 · 零兜底 · 连跑 2 次**（`created=0` · `liveEnd=170`）· `link-shots` **8/8**（⏭️ 第二十七轮复跑值）· `verify-novel-e2e-human` TRUE |
| 离线 | 全量 **PASS=56 / FAIL=0 / INVALID=0**（真机套跳过 14 / 总计 70）· 三份索引 `verify-index` **✅ 全部一致** · `test-generators` **4/4** · `test-director-reuse` **35/35** |
| 构建 | **2,008,557 B / 87 模块 / 741 = 741（265 import）/ 指纹 `2085f97cd1b8003c`** · **连跑两次逐字节一致** · `check-stale-build` TRUE · 装机**逐字节复判通过** |
| 基线 | **第十二锁 `3,910,289 B` / 93 文件**（新增 3 / 修改 18），复验 **漂移=0** |
| 新增 | 纪律 **101–102** · 闸门 `test-generators.mjs` · 探针 `_probe-win-drag-geom.mjs` / `_probe-host-titles.mjs` · `waitCdpPage()` |

**F · 未结项**：`T-PLUG-061`（真机 12 套批未完整跑通 —— 本轮按纪律 52 只跑受影响面）· `T-PLUG-059`（U11 间歇同红，本轮两跑未见）· `T-PLUG-063/064`（待用户拍板 / 确认）· `T-PLUG-040`（Harness 幽灵进程 —— 本轮停机时**再次复现**：端口 9222 仍被 pid 32080 占着，`残留进程 0 个` 却 `LISTENING`）。

## 2026-09-17 — 第五轮（**索引体系重构 + 工作区清理** · 结账 0 / 新增 1）

**用户原话**（逐字）：「更新 去处理好所有的文档和脚本的索引 清理多余的内容」

> **一句话**：5 份索引全部对齐，**2 份手工索引从"逐条列全量"瘦身为"索引入口"**（从结构上消除"手工清单必然过期"），
> 新增索引编排器与闸门，清理 **12 项 / 227 文件 / 6.41 MB**。

### A · 索引体系（本轮主线）

| # | 项 | 结果 |
|:-:|:---|:-----|
| 1 | **发现根因** | 不是"忘了更新"，是 **手工清单在结构上必然过期** —— `05-项目大索引.md` 声称 docs **96 篇**（实测 **115 篇**）、编号表只排到 `20-30`；`INDEX.md` 的状态停在 2026-09-11 的「V9.4/V9.5 待重启验证」。**过期的手工索引比没有索引更坏**：它会被当成现状引用 |
| 2 | **手工索引瘦身** | `05` → 「索引入口（四条各司其职）+ 接手三步 + 顶层文件 + 目标包 + **脚本地图（按职责分类，非清单）** + **文档地图（计数 + 人工标注）** + 一致性闸门」；`INDEX.md` → 「新人 5 分钟入口（30 秒上手 / 仓库地图 / 去哪看细节 / 命令链 / 五条铁律）」 |
| 3 | **生成器改造** | `gen-project-index.mjs` **补 `--check`**（口径 = **去掉生成时间行后的正文哈希**，逐字节比会被时间戳必然打断 —— 纪律 63 的反面）+ **排除自指**（文档数 150 → 149）；未知参数 `exit 2` |
| 4 | **新增工具** | `scripts/refresh-index.mjs`（一键重建：无参 / **`--safe`=不碰 src** / `--check`=只对账）· `scripts/verify-index.mjs`（索引闸门，`verify-` 前缀 ⇒ **自动进测试矩阵**） |
| 5 | **验证** | `node scripts/verify-index.mjs` ⇒ **三份全部一致 · IS_PASS: TRUE**；`refresh-index --safe` ⇒ rc=0；幂等性连跑两次 `--check` 均 rc=0 |

**抓到的 3 个真实问题**（详见 `AGENTS.md` 闸门纠错台账**（八）**）：
① **索引自指** —— 索引记录自己的行数/字节 ⇒ 写入的是"上次读到的自己"（**永远滞后一版** + 改一次要跑两次才收敛 + `--check` 假红）；
② **未文档化的副作用** —— `gen-source-map.mjs` **会向 `src/**` 注入 `@map` 血缘块** ⇒ **重建索引会改动源码**（`gen-` 前缀 ≠ 只读；"生成物/输入"边界必须读代码确认，不能按名字猜）；
③ **只有"拒绝"没有"支持"** —— 第 25 轮发现 `gen-project-index.mjs` 不处理 `argv`，当时只做了"拒绝未知参数"，**没补 `--check`** ⇒ 它成为三份索引里唯一没有闸门的。

### B · 工作区清理

| 类 | 明细 | 规模 |
|:--:|:-----|:----:|
| a | 根目录 3 个 **0 字节 + 私用区字符 U+F02A** 垃圾文件名（`任何对话、任何` / `本文件是` / `落死日期：2026-09-12`）—— 典型 **markdown 粘贴 / 重定向事故**（把带 `**加粗**` 的文本当路径） | 0 B |
| a | `_ps_probe.txt`（根级临时探针） | 2.1 KB |
| b | `scripts/dsh_docs_index.txt`（**死产物** / 2026-09-03 / 已被 `assets/docs-index.json` 取代）· `scripts/gen-docs-index.ps1`（**退役生成器** —— 旧链路会把 `const DSH_DOCS_INDEX` **注入宿主** `workspace/.../client.js`，既改不到也不该改） | 1.09 MB |
| c | `_probe/`（根，含 **300 KB** 宿主 `main.js` 副本）· `dsh-director-plugin/_probe/`（8 个改写/探测脚本） | 328 KB |
| d | 4 个旧回收站（`_tmp-*` / `_probe-*` 探针与日志） | 5.09 MB |
| — | **合计 → 根 `.trash-20260917-1330/`（软删除，可回退）** | **6.41 MB / 227 文件** |

> 🔴 **清理判据沉淀**：根目录出现「**0 字节 + 私用区字符（U+E000–U+F8FF）**」的文件即垃圾 —— **可脚本化识别，无需逐个猜名字**。
> 🔴 **为什么先软删不真删**：这批多属 **git 未跟踪** ⇒ `rm` 之后 git 救不回（纪律 83：破坏性动作自带退路）。
> 已登记 `T-PLUG-064`，**用户确认后**才 `rm -rf`。

**清理引出的 1 处行为修正（差点漏网）**：删 `gen-docs-index.ps1` 后复查引用者，
发现 **`scripts/apply.ps1` 第 16 行仍在调用它** —— 首次 `grep` 只搜了 `*.mjs` / `*.md` ⇒ 漏报。
好在有 `if (Test-Path $genScript)` 兜着**不会崩**，但**文件缺失即静默跳过、连 WARN 都不打** ⇒
**无声降级**（纪律 18「跳过比红更危险」/ 纪律 19「降级可以，无声不行」）。
**处置**：该段改为**显式 WARN**（写明"已退役 + 替代物 + 为什么"），并保留历史注释；
`apply.ps1` 语法经 `[Parser]::ParseFile` 复验 **`SYNTAX_OK`**。
⇒ 通则：**删文件前查引用者要覆盖全部脚本类型**（`.ps1`/`.py`/`.json`/`.yml`），
且 **`Test-Path` 包裹的调用 = 天然的静默跳过**，删被调物时必须回头看它。

### C · 同轮产出

- `AGENTS.md`：**闸门纠错台账（八）**（6 条 + **新共性 ⑪「手工维护 ⇒ 必然过期」/ ⑫「重建索引会改源码」**）· **§2.3 标准工作流**重写（补索引对账 / `gen-key-files` / `test-plan` 规划 / `check-stale-build` / `run-live` 批量 / 索引重建，共 13 步）
- `.workbuddy/memory/MEMORY.md`：纪律 **103–105** + 索引体系条 + **垃圾识别判据** + 命令链更新
- `docs/00-统筹入口/05-项目大索引.md` · `INDEX.md`：重写（见 A-2）
- `docs/00-统筹入口/06-工作快照.md`：本轮轮转为当前状态（第四轮降为历史）
- `docs/00-统筹入口/03-待完成任务清单.md`：新增 `T-PLUG-064`；表头计数同步（**2 组 / 25 条**）

### 权威数字（可直接引用）

| 项 | 值 |
|:---|:---|
| 索引份数 | **5** = 自动 **3**（全资源确定 · 源码映射 · 运行时文档）+ 手工 **2**（`05-项目大索引` · `INDEX.md`） |
| 索引规模 | 源码 **89** · 脚本 **234** · 文档 **149**（零遗漏）｜ `docs/` **115 md + 19 html** |
| 索引闸门 | **三份全部一致 · IS_PASS: TRUE** |
| 清理 | **12 项 / 227 文件 / 6.41 MB** → `.trash-20260917-1330/`（软删除） |
| 基线 | 🔴 **仍未重封**（并行会话 `src/**` 在途改动 + 索引注入）—— 待收口后 `baseline-check --write` |

---

## 2026-09-17 — 第四轮（**测试流程与增量测试矩阵** · 弹窗配色修复 · `execution-standards` V3.18.0 · 结账 0 / 新增 3）

**用户原话**（逐字，两条）：
① 「左侧点击总监按钮, 总监的弹窗,背景是黑色的 / 调整下,按照人眼最舒服温馨的风格调整背景和文字的颜色 同时增加自定义背景颜色的选项, 可以上传图片作为背景 / 默认的话可以跟随软件的背景主题 / 还有 harness 有更新 下载回来更新」
② 「测试的时候太消耗token, 有测试测我看着是重复了很多遍, 有些是重复打开关闭软件 / 先写测试流程,确定要测试什么那些能一起测试, 那些测试过没问题只要没有涉及的改动下次就不需要在测试等等 / 把这部分完善掉, 同时逻辑写到技能中审核也别忘了」

> **交付物**：`docs/40-测试质量/22-测试流程与增量测试矩阵-20260917.md`（**三轮审核 9.0 → 10 → 10**）· 技能 `incremental-test-planning`（新建）· `execution-standards` **V3.17.0 → V3.18.0**（新增 §3.14 + 路由索引一行）· 工具三件套 `scripts/_test-ledger.mjs` / `scripts/test-plan.mjs` / `scripts/run-live.mjs` · `scripts/shot-dialog-bg.mjs`。

### A · 测试流程（本轮主线）

| # | 项 | 落地结果 |
|:-:|:---|:---------|
| A1 | **「该跑哪些」可复算** | 三条判据：**A** 产物内容指纹 == 台账全绿基准 ⇒ 离线套件全跳 · **B** 改动集与闸门输入域无交集 ⇒ 全跳 · **C** 按覆盖面取集（结构类一律跑 / 覆盖面 ∩ 改动集 ≠ ∅ / 读产物的只要 `src/**` 动就跑）。`--explain` 逐套件给依据 |
| A2 | **覆盖面是推导的** | `surfaceOf()` 从套件源码（**先剥注释**）提取**真实存在**的 `src/**` 路径；实测 70 套 = 离线 **56**（结构类 **12** / 读产物 **12**）+ 真机 **14**；需平台桩 **49** |
| A3 | **批量化（治"反复启停"）** | 离线：**一条命令跑完全部**；真机：`run-live.mjs` **整批只冷启动一次**（首套自举后后续秒过），前置校验产物新鲜度 + 装机就绪，逐套标 OK/FAIL/INVALID |
| A4 | **免重跑台账** | `logs/test-ledger.json` 记 `passStamp`（**只在绿时推进** · 红不盖绿 · INVALID 不写）；**禁用 mtime/时间作判据**（本仓已被"逐字节一致 ≠ 是新的"咬过） |
| A5 | **判据不许写窄** | 收敛到**共享唯一真相源** `needsPlatformStub()`；删除 `_offline-run.mjs` 里写死的 `if (f === "verify-dialog.mjs")` **手工特例** |
| A6 | **成本红线** | 每套只回显尾部 3 行 · 真机只启动一次（已在跑则不重复启动）· 禁止读回整份日志 · 禁止"重跑到绿" · **没跑的必须说清** |
| A7 | **验收** | 离线批次 `node scripts/test-plan.mjs --run --record` ⇒ **OK=40 / 非 0=0 全绿** |

**红转绿（3 套，两处根因）**：

| 套件 | 旧结果 | 真因 | 处置 |
|:-----|:-------|:-----|:-----|
| `lint-syntax` | exit 1 | `verify-novel-split.mjs` 第 703 行**注释里写了字面 `${}`**，而它在**模板字符串内** ⇒ 开启插值 ⇒ `Unexpected token '}'`（**坑 28 新变种**：不是反引号，是 `${`；报错行号指向该注释本身） | 注释改写为「美元花括号」⇒ `IS_PASS: TRUE` |
| `lint-cdp-templates` | exit 1 | 同上（同一根因连爆两红） | 同上 ⇒ `IS_PASS: TRUE` |
| `verify-dialog` | exit **2 (INVALID)** | **判据写窄**：批量执行器只认 `from "../src/` 一种写法 ⇒ 漏加平台桩钩子；修第 1 次时又写 `suitesByName.get() \|\| {text}` 而 `suites` 在 map 时**丢弃了 `text`** ⇒ 判据**恒 false**（「有兜底」是假象） | 收敛到 `needsPlatformStub()` + 直接用已读到的 text ⇒ **273/273** |

### B · 弹窗配色（用户第一条诉求前半段）

**抓到 5 处真实缺陷**（不是尺子问题）：① `--dp-dlg-bd` **拼写错**（不在变量表 ⇒ 死兜底）· ② 5 处兜底链**漏宿主令牌**（follow 档正文 3.06:1 的主因）· ③ `bg-sunken` 当表面（**实测是深色** `rgb(29,39,57)`）· ④ 浮层用最外层玻璃 `bg-base`（α0.40）⇒ 改 `bg-overlay`（α0.96）· ⑤ 自定义底色**固定比例混色**（中间色必翻车）⇒ 改 **`clampToContrast` 对比度反解**（二分 16 次，最差 t2 **3.34 → 4.50**）。
**默认 warm 档真机验收**：`rgb(247,242,232)` 米白 + 深棕字，对比度 **6.82:1** ⇒ 用户抱怨的「一片黑」已解决。新增 `shot-dialog-bg.mjs`（四档截图 + 实测对比度 + 快照/逐字节还原用户设置）。

### C · Harness 更新（用户第一条诉求后半段）

**调研得一**手证据（Harness 自己打印）：`Update for version 0.1.0-rc.11 is not available (latest version: 0.1.0-rc.5, downgrade is disallowed).` ⇒ 官方桌面源**停在 rc.5 < 本机 rc.11**，点「检查更新」只会得到 up-to-date。**登记 `T-PLUG-063` 待用户拍板**（A 不做 / B 升级内核走 npm）。

**同轮登记**：`T-PLUG-061`（🔴 真机 12 套批未完整跑通 —— 并行会话长期占用同一 Harness）· `T-PLUG-062`（🟡 覆盖面提取器缺**植入缺陷校准** ⇒ 假跳过风险）。



## 2026-09-17 — 第二十五轮（**需求建议 6 条落地** · 跨重启连跑打通 · 🔴 **纪律 97–100** · 结账 4 条）

**用户原话**（逐字）：「可以按照那得需求建议修改 / 修改完成之后对项目收尾」

> **交付物**：`docs/00-统筹入口/18-总监逻辑链四十轮连跑与U6真因纠正-20260917.md` **§十一**（11.1 结论 · 11.2 G1–G8 · 11.3 新套件 36 条 · 11.4 U10/U11 · 11.5 收尾判据 4 步）；锚点 `10-当前基线-落死锚点-V16.md` **§7.20** + 纪律 **97–100** + 纪律 91 就地更正；`AGENTS.md` **闸门纠错台账（六）**。

**A · 「需求建议」6 条全部落地**（源自第二十四轮过程文档 §八「待做」）：

| # | 项 | 落地结果 |
|:-:|:---|:---------|
| U8 | 🟠 `el.click()` 兜底要不要保留 | ✅ 降级为**诊断通道**；`stats{mouse,diag,geomFail}` + 收尾打印「真实鼠标 N ｜ 诊断通道 M ｜ 几何未命中 K」。`verify-novel-split` 实测 **真实鼠标 0 ｜ 诊断通道 2 ｜ 几何未命中 0** |
| U9 | 🟠 谁改变了 `visibilityState` 未分离 | ✅ **受控分离实验完成**：撤销 `setFocusEmulationEnabled`、再单独撤销 `bringToFront`，两步 `visibilityState` **仍为 `visible`** ⇒ 驱动者是**窗口遮挡**，非某个 API。结论写进纪律 90 |
| O1 | 🟠 兜底命中要计数并打印 | ✅ 同上（`diagSummary()` 收尾必打；「高频兜底 = 前提有问题」信号灯） |
| O2 | 🟠 其余套件"固定 `sleep` 后读状态"全扫一遍 | ✅ 两轮扫描：宽口径 **56 处** → 高危 **26 处**；三组探针实测 ⇒ **降级为加固项**（前提成立时延迟 **58–76 ms**，旧记 974–1093 ms 是 `hidden` 态测的）· 纪律 91 **已就地更正** |
| O3 | 🟡 语料池扩到 12–16 条 | ✅ 语料 **8 → 13 条** + 负对照 **8 条**（边界 4 / 校准 4）⇒ `test-director-corpus` **51/51** |
| O4 | 🟡 40 轮加「跨日连跑」 | ✅ **跨重启连跑打通**：**6/6 轮 29/29**（含 **2 次冷启动**、端口 **9222 → 9223**、总 **163s**） |

**B · 结账 4 条**（逐条取证后由 `03` 迁入）：

| 编号 | 任务 | 结账依据 |
|:-----|:-----|:---------|
| **T-PLUG-054** | 🔴 **`el.click()` 兜底应降级为"诊断路径"**（第二十四轮登记） | ✅ **做完**。① 点击层默认路径改回**真实鼠标**；`el.click()` 只在**前提断言已失败**或**单点重试超预算**时作为**诊断通道**，结果句写明"经诊断通道送达"；② 新增 `stats{mouse,diag,geomFail}` + `diagSummary()` **收尾必打印**；③ **判据**（原条要求）成立：前提成立时真机套件**兜底计数恒 0** —— `verify-novel-split` 实测 **「真实鼠标 0 ｜ 诊断通道 2 ｜ 几何未命中 0」**；`DSH_FOCUS_CALIBRATE=hidden` 校准下**诊断通道必被走到**。④ 计量口径落进纪律 96。 |
| **T-PLUG-055** | 🟡 **谁改变了 `visibilityState` 未分离**（第二十四轮登记） | ✅ **做完**。三组受控实验：只 `bringToFront` / 只 `setFocusEmulationEnabled` / 两者都有 —— **撤销任一或全部后仍 `visible`** ⇒ 结论：**驱动者是窗口遮挡**（本机窗口被遮挡时 `visibilityState` 变 `hidden`），不是某次 API 调用"改写"了它。⇒ 纪律 90 现在**写死了归因与处置**（"真实鼠标失效第一查窗口可见性"+ 建立并断言前提），下次"建立失败"有固定排查路径。 |
| **T-PLUG-056** | 🟡 **其余真机套件的"固定 `sleep` 后读状态"要全扫一遍**（第二十四轮登记） | ✅ **做完（并据实降级）**。① 扫描两轮：`grep -n "sleep(" scripts/verify-*.mjs` 宽口径 **56 处** → 逐处审"其后是否紧跟**读状态**" ⇒ 高危 **26 处**；② 三组探针实测派发延迟：**前提成立时 58–76 ms（中位 74）**，而非第二十四轮记的 974–1093 ms（那是 `hidden` 态测的）⇒ **多数 `sleep(150/300)` 实际已覆盖**，故本条从 P2 降为**加固项**，已在锚点 §7.20 §E 与纪律 91 登记；③ 第二十四轮已修的两处（`verify-mindmap` 悬停/toast · `verify-design-studio` 开启动画归位）保留。**未做**（据实登记）：不逐处改写 26 处 —— 因为**判据前提变了，改造的理由已不成立**；若日后再现"固定 sleep 假红"，按纪律 91 就地改该处。 |
| **T-PLUG-057** | 🟡 **语料池扩容 + 跨日（跨重启）连跑**（第二十四轮登记） | ✅ **做完**。① 语料 **8 → 13 条**（补超长输入 / 中英混排 / emoji / 语义合法但与项目无关的需求）+ **负对照 8 条**（`noiseReasonOf` 必须返回 `null`，即**边界不误杀**）⇒ 离线闸门 `test-director-corpus` **51/51**；② **跨重启连跑打通**：`repeat-director-logic.mjs` 支持 `DL_RESTART_EVERY=N`，**每段换端口**（`_harness.mjs` 端口顺移），实测 **6/6 轮 29/29**（含 **2 次冷启动**；`run01` 冷启动 8/29 ⇒ 修起点自举后 **29/29**；端口 9222→9223；总 **163s**）；③ `expectReasonHas` 与负对照均已进断言。 |

**C · 本轮修掉 8 条问题（G1–G8）** —— 全部记入 `AGENTS.md` 闸门纠错台账（六）：**G1** 停机只看 `taskkill` 文案 ⇒ 假成功（真因：幽灵占 9222 ⇒ 新实例**进程活着、界面也起，唯独 CDP 绑不上**；铁证 `bind() returned an error … (0x2740)` + `Cannot start http server for devtools`）· **G2** 连跑段间不换端口（**我自造，306s INVALID**）· **G3** `RU-2/RU-3` 被 INVALID 轮染红 ⇒ `validRows` + 非空守卫 · **G4** `_harness.mjs` 端口顺移条件写反 · **G5** 中文串内嵌 ASCII 双引号 ⇒ `SyntaxError` · **G6** `HT-7` 判据期望写错 · **G7** 🔴 **`RU-4b` 假红实为口径错**（判据隐含"全程同一实例"，而复用索引按 **origin** 隔离 ⇒ 跨重启必然重建，实测 19）· **G8** 冷启动**起点自举缺失**（`run01` **8/29**）。

**D · 新增**：纪律 **97–100** · 闸门 `test-harness-ctrl.mjs`（**33 PASS / 0 FAIL / 3 可分辨跳过**，含**虚拟时钟**驱动的 `ensureDirectorPage` 契约）· 工具 `_cdp-startup.mjs`（统一起点自举）· `_harness.mjs` 重写（验证式停机 + 端口顺移 + stdout 留痕）· backlog `T-PLUG-058` / `T-PLUG-059`。

**E · 判据**：离线全量 **59 套 / PASS 46 / FAIL 0 / 真机跳过 13** · 真机 `verify-novel-split` **108/108** · `verify-mindmap` **103/103 零跳过** · `verify-director-logic` **28/29**（唯一红 = `T-PLUG-058` 现象）· 台账闸门 `verify-ledger-table` **IS_PASS: TRUE**。

## 2026-09-17 — 第二十四轮（连跑 **40 轮** · 语料池化 · 🔴 **U6 真因纠正**：不是"丢事件"，是"窗口不可见" · 结账 1 条）

**用户原话**（逐字）：「继续抛逻，整个的**逻辑测试**，我需要的是总监的**分配机制**也是好用的，**整个调试的机制**也是好用的。回复包括总监对**语言的整理**、对于**文字的描述**、对于**整个项目把控**都是要有的。你可以拿**具体实际项目**，然后去发送，然后可以发送一些**无意义的东西**，然后让总监去**分辨**，然后去整理整个项目。然后在整个过程中，然后**遇到问题就改问题**…整**记下整个流程的情况**…同时判断**哪里还可以优化就优化哪里**。…**重复该流程起码四十次**」

> **交付物**：`docs/00-统筹入口/18-总监逻辑链四十轮连跑与U6真因纠正-20260917.md`（§二 语料池化 · §三 40 轮结果 + 破坏性校准 · §四 **U6 真因纠正** · §五 6 类闸门缺陷 · §八 优化建议 · §九 纪律 90–96）。

| 编号 | 任务 | 结账依据 |
|:-----|:-----|:---------|
| **T-PLUG-051** | 🔴 **其余真机闸门的「点击送达」统一加固**（第二十三轮登记时的结论是「CDP 合成鼠标在 Electron 下静默丢事件」，本轮**全量完成并纠正了归因**） | ✅ **做完**。① **真因纠正**：不是"丢事件"，而是 **`visibilityState === "hidden"` 时 CDP 的 `mousePressed`/`mouseReleased` 被整条吞掉**（`hasFocus()` **仍为 true** —— 骗人的代理指标）；② `_cdp-focus.mjs` 判据换成 `visibilityState`，**11 个真机套件统一加装前提**（`verify-flow` / `verify-mindmap` / `verify-design-studio` / `verify-walk` / `verify-register-gate` / `verify-v21` / `verify-v22` / `verify-orchestrate` / `verify-v17-sync` / `cdp-mouse` / `verify-director-logic`），`verify-novel-split` 单独接入并登记 **`NS-0v`**；③ **破坏性校准**：`DSH_FOCUS_CALIBRATE=hidden` ⇒ **恰红 `NS-0v` 一条 + 早退零消耗**，日志里 `hasFocus: true` 与 `visibility: hidden` **并存**；④ **效果**：`verify-novel-split` **95–99/107 → 108/108**（W 段拖动 947,82 → **605,408** 真动），**兜底依赖 8 → 0**、耗时 **97s → 16s**。 |
| **T-PLUG-052**（部分） | `reload` 后"可交互预热窗口" | ⚠️ **主因已定位（不结账，改挂归因分离）**：本条原写「窗口不可见**已排除**」⇒ **本轮受控对照推翻**，**主因就是它**；仅"是 `setFocusEmulationEnabled` 还是 `bringToFront` 改写了 `visibilityState`"未分离 ⇒ 改挂 **`T-PLUG-055`**。 |

> **本轮修掉 6 类闸门缺陷**（全是"把没等到读成产品坏"）：等待预算只有实测延迟的 **1/3**（实测 974–1093ms/次）· 判据语义反转自造红 · **判据用产品不存在的属性 ⇒ 空真绿** · 前提缺失报 FAIL ⇒ 实体化 · 动画没播完量几何（`.14s` 实测 **1950ms**）· 差额判红。
> **产品侧 3 处读数改动**：`data-reason`（让「分辨」**可断言**）· `data-hit`/`data-noid` · `clearDirectorMessages` 回传 `hit`/`noId`。
> **判据**：真机 `verify-novel-split` **108/108** · `verify-mindmap` **103/103 零跳过** · `verify-flow` **80/80** · `verify-design-studio` **94/94** · `verify-director-logic` **29/29（连跑 40 轮）**；离线全量 **58 套 / PASS 45 / FAIL 0**；基线 **90 文件 / 3,615,328 B / 漂移=0**。

## 2026-09-16 — 第二十三轮（总监逻辑链全链路测试：产品 4 缺口 + **调试机制** 5 缺口 · 真机 **25/25 四连绿**）

**用户原话**（逐字）：「整个的逻辑测试，我需要的是总监的**分配机制**也是好用的，**整个调试的机制**也是好用的。回复包括总监对语言的**整理**，对于**文字的描述**，对于**整个项目把控**都是要有的。你可以**拿具体实际项目**，然后去发送，然后可以发送一些**无意义的东西**，然后让总监去**分辨**，然后去**整理整个项目**。然后在整个过程中，然后遇到问题就改问题…同时判断**哪里还可以优化就优化哪里**。这个才是我们重复测试的意义。」

> **交付物**：`docs/00-统筹入口/17-总监逻辑链全链路测试与优化-20260916.md`（§一 两轮共 9 缺口 · §二 真机取证 · §五 判据回填 · §六 未结项 U1/U5/U6/U7）。

**A 路 · 分配机制经真机验证是好的**：真实项目《墟海》（`D:\workspace\novels\墟海`）⇒ `kind=novel` · `made=8` · `sent=8` · `confirmed=8` · dims **A1–A8 全齐** · 会话净增 **8**；回收链**如实报出** 8 条 `Insufficient Balance（QUOTA 402）`（= `T-PLUG-041` 的降级路径）。

**B 路 · 产品侧 4 个缺口**（用户点名的三块能力 + 一个调试体感）

| # | 缺陷 | 真机证据 | 修复 |
|:-:|:-----|:---------|:-----|
| D7 | 总监**不分辨无意义输入** | 投「今天天气不错 哈哈哈哈」⇒ `made=3`、会话净增 3、导图 3 个空壳框 | F1 `noiseReasonOf()` 七级判据 + `plan()` 短路（`kind` 扩为 `…\|noise`） |
| D8 | **没有"对语言的整理"** | 回收消息 8 条**逐条重复同一句**（822 B / 8 行同构） | F2 `organize()` / `organizeSummary()` + F3 归纳式 `digestMessage()`（合**原因**不合**条目**） |
| D9 | **不做"项目把控"** | `briefOf()` 的 `proj` 段把"先读清单"推给分支 | F4「项目把控 · 先做这四步再动手」（读清单 → 提取回报 → 对分区 → **回报格式「项目现状：…」**） |
| D10 | 审计消息写入后**界面滞后一条** | 库 **3 条** vs 界面页签 **2 条**，缺的正是**最新那条** | F5 `appendDirectorMessage` **提到** `refresh()` 之前（两处） |

**C 路 · 🔴 调试机制自身 5 个缺口**（比产品缺陷更危险 —— **会把自己的失效伪装成产品红**）

| # | 缺陷 | 决定性证据 | 修复 |
|:-:|:-----|:-----------|:-----|
| D11 | 台账 `items[]` **不含 `brief`**（`recordDispatch` 白名单 map 丢弃） | `keys` 无 `brief`；闸门读到 `""` ⇒ D8/D9 **没有观测面** | 台账补 `brief` + 单测 DL-14/14b（**破坏性校准恰红 1 条**） |
| D12 | 起点缺「**宿主已消费注册**」前提（`registered:true` ≠ 可交互） | `reload` 后**立刻**跑 **13/25**；连跑 **25/25** | DL-0 前提断言 + 起点 **40s / 8 轮可重试** |
| D13 | 点击**只认几何命中、不认后果发生** | 同坐标同序列：鼠标**连丢 6 轮**；**同一时刻** `el.click()` 一次生效 | `clickUntil`（点击 → **校验后果** → 重试）+ 第 2 轮起 **JS 保底送达** |
| D14 | 取证日志**被下一次跑覆盖** | FAIL12 的完整日志被紧随其后的绿跑覆盖 ⇒ **白跑一轮** | 带戳副本 `_r23-verify-<ts>-<PASS\|FAILn>.txt` |
| D15 | 消息**只走 DOM**（`dp-dir-msg` 仅消息页签激活时渲染） | `msgRows = 0` 而页签**已是「总监消息 5」** | 双通道：DOM 优先 + IndexedDB 落点兜底 |

**D 路 · 判据**：真机 `verify-director-logic` **25/25 · `IS_PASS TRUE`** 且 **四连绿**（`reload` 后**立刻** ×1 + 连跑 ×3）· 离线全量 **57 套 / PASS 44 / FAIL 0 / 真机跳过 13** · 单测 `test-split-dimensions` **87**（45→87）· `test-dispatch-log` **16**（14→16）· `test-director-collect` **24** · **破坏性校准 3 次全部精确命中** · 构建 **1,854,200 B / 711 = 711（252 import）/ 指纹 `0c43c1d001267747`** · 装机**逐字节一致** · `Page.reload` 后回读 `__dshBuildStamp` 一致。

**E 路 · 环境与未结项**：🔴 `taskkill /F /IM "DeepSeek Harness.exe"` **沙箱内外均拒绝访问**（Harness 以**管理员**权限运行）⇒ 主进程无法自动重启；本轮以 **`Page.reload` + 构建指纹逐字符比对**装载新 bundle（**判据不靠猜**）。新增 backlog `T-PLUG-051`（**其余真机闸门的"点击送达"统一加固 —— 本轮「哪里还可以优化」的首要答案**）· `T-PLUG-052`（`reload` 后"可交互预热窗口"根因）· `T-PLUG-053`（闸门能力抽公共工具）。宿主模型配额不足 ⇒ 回收链"有产出"路径本轮未覆盖（`T-PLUG-041`）。

> **新增锚点 §八 纪律 85–89**（85 已注册≠已可交互 · 86 点了≠生效了 · 87 台账要记"我们说了什么" · 88 取证产物不许被覆盖 · 89 读最新消息走落点）+ 锚点 **§7.18**。

## 2026-09-16 — 第二十二轮（总监消息「一个都没有」诊断与修复：闸门清空用户数据 / OPFS 串桶 / 自带退路）

**用户原话**（逐字）：「总监消息没有持久化么 为什么一个消息都没有」

**先证通道完好，再找真凶（顺序不能反）**

真机往返实测：点一次「统筹」⇒ 页签「总监消息」计数 **0 → 1** 且插件库 `directorConversations` **0 → 1**（**2.5 s 内**）⇒ **写入 / 计数 / 界面刷新三段全通**。⇒ 用户的假设「**没持久化**」被实测**否证**；问题一定在别处。**判据必须跑在真实动作上，不是读代码"看起来写了"**。

**真因三条**

1. **D4（主因）验收闸门把用户数据当耗材** ⇒ `verify-novel-split.mjs` **D 段**测「清除全部总监消息」时调 `clearDirectorMessages()`（**无参 = 清全表**），**只验哨兵消失、跑完不还原**。第二十一轮「**连跑 10 次**验收」= **把库清零 10 次**。实测体检：`directorConversations` **0 条** 而 `directorNodes` **458 条**（库能写，只是消息没了）。**主因不在持久化，在验收**。
2. **D5 OPFS 单文件缓存串桶** ⇒ `store/persist.js` 的 OPFS 通道是**单文件** `director-store.json`（**不含桶名**），而 `loadDirectorStore(sessionId)` **先读这份缓存、命中即返回** ⇒ 多会话互相覆盖。实测 8 个 A1–A8 桶**全空**，唯一有 2 条消息的 `dsh.director.store.director-d1yi2z` 却属**已不在树上**的会话（**孤儿桶**）。
3. **D6 写消息触点只有 4 个**（统筹 / 派发 / 回收 / 运行总监链）⇒「**登记为流转**」只写 flow、**不写消息**；若用户只做"登记"，消息数**恒 0**。（**语义问题，仅记录，待用户裁定** ⇒ `T-PLUG-050`）

**五层落点体检 M1–M10**

| 层 | 落点 | 实测 |
|:--|:--|:--|
| ① | IDB 插件库 `dsh-director-plugin-db@v2` | `directorConversations` **0** / `directorNodes` **458** |
| ② | 宿主库 `dsh-director-db@v3` | `memoryCore: 459` |
| ③ | OPFS 单文件 | **2016 B** |
| ④ | `localStorage` 桶 | 8 个 A1–A8 桶**全空** + 1 个**孤儿桶**（2 条） |
| ⑤ | cookie | **17 个**（**不含 A1–A8 任何一个**） |

桶键 `safeDirectorKey()` 实测映射：A1=`director-hvycae` · A2=`ii41h6` · A3=`c82ren` · A4=`q9td77` · A5=`fxn10m` · A6=`qjknbq` · A7=`8epi23` · A8=`director-f0oyl6` ⇒ **切作用域就是切桶**，用户「逐个点开都是空」是**必然**（不是随机丢失）。

**修复 F1–F4**

| # | 面 | 改动 |
|:--|:--|:--|
| F1 | 闸门 `verify-novel-split.mjs` | D 段改为「**清除前快照**（`{n,realN,sample}`）→ 清除 → **界面真实点击**「↩ 恢复」→ **幂等复点** → 哨兵收尾」；新增 `NS-6l2`/`NS-6p–s` 七条 |
| F2 | `store/plugin-db.js` | 新增 `backupDirectorMessages` / `readMessageBackup` / `pickRestorableRows`（**纯函数**）/ `restoreDirectorMessages` / `clearMessageBackup`；`clearDirectorMessages` 改为**先备份再删除**（顺序不可颠倒）；超 **3 MB** 则 `ok:false` 且**保留旧备份** |
| F3 | `store/persist.js` | payload 记 `sid`；`loadDirectorStore` 校验不符即视为**未命中**并打印 SKIP（**不串桶**） |
| F4 | `components/DirectorPage.js` | 新增「↩ 恢复」按钮 `dp-maint-restore` + 读数 `dp-restore-result`；toast **必须报退路**（「已备份 N 条，可点『↩ 恢复』还原」/「⚠ 备份未成功（原因），本条不可恢复」） |

**判据（每条都问过"反例上会不会也通过"）**

- 离线新增 **`test-message-backup.mjs` 13/13**：`MB-1..5` 挑选规则（含 `MB-5` Set/数组**负对照**）· `MB-6..8` 备份写入（`MB-8` 超限 `ok:false` **且旧备份原样保留**）· `MB-9..11` 读/清（`MB-10` 非法 JSON **不抛**、`MB-10b` `rows` 非数组**不抛**）· `MB-12` **反例校准**。
- 真机 `verify-novel-split` **106/106 `IS_PASS TRUE`**（连跑 2 次），还原链完整读数：`快照 {n:8,realN:7}` → 清除 `8/8` → 备份槽 `{exists:1,count:8}` → **真实点击** `{restored:8,total:8}` → 再点 `{restored:0,skipped:8}` → 哨兵收尾 `{del:true,left:0}` → **消息数 8 → 8（原样）**。

**验收读数**：离线全量 **56 套 / PASS 44 / FAIL 0 / 跳过 12** · 构建 **`1,830,903 B` / 84 模块 / 710 = 710（252 import）/ 指纹 `af7111e0b0064627`** · `check-stale-build` TRUE · 装机 `[4]` 逐字节一致 · 页面 `__dshBuildStamp` 一致 · 基线**第八锁 `3,564,745 B`**（新增 0 / 修改 5）复验**漂移=0**。

**新增**：§八 纪律 **82–84**（82 闸门不许把用户数据当耗材 · 83 破坏性动作必须自带退路且写进结果句 · 84 单文件缓存必须带归属标记） · 脚本 `test-message-backup.mjs` · 交付物 `docs/00-统筹入口/16-总监消息持久化诊断与修复-20260916.md`（§一 结论先行 / §二 五层落点 M1–M10 / §三 修复 F1–F4 / §四 判据 / §五 未结项）。
**新增 3 条 backlog**：`T-PLUG-048`（孤儿桶读数）· `T-PLUG-049`（桶数量配额预检）· `T-PLUG-050`（写消息触点补全，**需用户先确认语义**）。
**未结项（如实告知）**：① 第二十一轮连跑 10 次验收期间被清掉的历史总监消息**找不回**（当时无备份机制），备份机制**从当前时刻起生效**；② `localStorage` 现存 `dsh.director.msgs.backup`（8 条备份）+ 孤儿桶 `director-d1yi2z`（2 条，属已删会话）—— **本轮未主动清理**（按铁律不擅动测试/用户数据）；③ 本轮**未改动** op 的 `debug.log`（639 KB）。

## 2026-09-16 — 第二十一轮（「一百多个会话」真因修复 · 会话档案可见 · 四层落点清理 · 连跑 10 次）

**用户链式任务**（逐字）：「现在会话有问题, 重复创建了一百多个会话 / 我需要的是总监确认完需求之后先考虑目前存在的会话,然后没有才是新建会话 / 不会话都有自己的总监,存在自己的会话总结文档 / 清理无效的多余的文件 和所有会话 / 然后继续反复测试 / 遇到问题就思考怎么处理 / 然后把整个流程记下来,思考如何优化,如何更符合我的想法 / 该流程需要持续重复起码十次」

**修掉 3 个真实产品缺陷**（都在上一轮"已修好"的阴影里）

1. **D1 分支树的摄取点不过滤归档会话** ⇒ `readSessionsFromCtx()` 直接拿宿主快照**全部 126 条**（含 125 条已归档幽灵）⇒ **导图上 126 个框 = 用户说的「一百多个会话」**。第 19/20 批的归档过滤只加在 `director-dispatch.js`（**派发**侧），**漏了树** ⇒ **用户看到的现象与代码里"已修好的东西"完全对不上**。
   - 修复：过滤点前移到**唯一摄取点** + 三处报数；快照非空**一律建树**（不回退 `discover`）。实测 `rows: 126 → 1`，对账 **`rows + archivedExcluded = rawCount`**（`1 + 125 = 126`）。
2. **D2 `applyDossiers` 全仓零调用** ⇒ `store/session-dossier.js` 第 19 批已交付且单测 **17/17 全绿**，但**没人调它** ⇒ 档案写进 `localStorage` 却**从不显示**；`data-dossier-role` 在产物里 **0 处**；用户的「（每个会话）都有自己的总监 / 自己的会话总结文档」**一条都看不见**。**纯函数单测天然看不见"有没有人调它"**。
   - 修复：接入 `refreshBranchTree()`（`diag.dossierApplied` / `withSummary` / `roles`）+ MindMap 挂 **5 个 `data-dossier-*`** + 节点第 2 行**可见**「· 总监 A1 世界观」。
3. **D3「没有总结」与「读不到」不可分** ⇒ `director-collect.js` 两条「读不到」分支不写档案 ⇒ `summary` 恒为 `null`。
   - 修复：补 `{source:"none", ok:false, reason}`；`normSummary` 增 `reason`（**只在无正文时保留**）。

**R4 清理 = 四层落点全清**

| 落点 | 前 | 后 |
|:-----|:---|:---|
| ① 磁盘 `~/.dsh/sessions/**` | 130 | **0** |
| ② 登记 `workspace.json`（`sessionIds` / `archivedSessionIds`） | 130 / 125 | **0 / 0** |
| ③ 投影缓存 `session_projcache.json` 的 `tables.sessions` | **150** | **0** |
| ④ `localStorage`（会话键 + `director.*` 索引孤儿） | 6 项 | **0** |
| ⑤ 宿主下架 `api.workspace.archiveSession` | 侧栏 6 行 | **尝试 134 · 成功 134 · 失败 0** ⇒ 侧栏 **0 行** |

无效文件 **36 个 / 2,729 KB** 移入 `.trash-20260916-2140/`（可回滚）；**未动** `scripts/_probe-*.mjs`（诊断工具）与既有 `.trash-*`（回滚点）。

**R5 连跑 10 次（全部达标）**：run1 `复用 0 · 新建 8 · 宿主 135→143 · 树 1→9`；**run2–run10 恒 `复用 8 · 新建 0 · 净增 0 · 树 9→9`**，每次 `94/94`、`索引孤儿清理 0 条` 恒定 ⇒ 判据 **RU-1 / RU-2** 双向成立（`logs/_repeat-2026-09-16T13-44-37-621Z-run1..10.txt`）。

**闸门加固（94 → 100）**：新增 **L 段 `NS-12a–f`**（8 条各有非空 `data-dossier-role` · **负对照互不相同** · 与分流维度一致 · **可见文本含「总监」** · `data-has-dossier` 双向一致 · `src=none` 必带原因）⇒ **连跑 2 次均 100/100**。**为什么要从界面读**：`applyDossiers` 零调用那次事故证明"函数在不在"不是判据。

**验收读数**：构建 **1,818,225 B / 84 模块 / 708 = 708（252 import）/ 指纹 `9ff70bcd7bdb7a78`** · `check-stale-build` **TRUE** · 装机 `[4]` **逐字节一致** · 页面 `__dshBuildStamp` 一致 · 离线全量 **55 套 PASS=43 FAIL=0 跳过 12**（新增 `test-branch-tree-archive` **13/13**）· lint 四把全绿（`cdp-templates` 231 文件）· 基线**第七锁 `3,540,298 B`**（新增 0 / 修改 6）复验**漂移=0**。

**新增**：§八 纪律 **74–81**（74–77 环境/操作类：宿主管理员权限 ⇒ 非提权杀不动 · 绝不把 `window.close()` 当关闭应用 · 「清盘」≠「清干净」 · PowerShell stdout 恒空；78–81 工程类：存活集单一真相源 · 「模块写好了」≠「接进去了」 · 起点实体化 · 清理一条命令） · 脚本 `test-branch-tree-archive.mjs` · `clean-session-ui-state.mjs` · `repeat-novel-split.mjs` · `clean-all-sessions.mjs` · 交付物 `docs/00-统筹入口/15-会话复用全流程与优化记录-20260916.md`（全流程四段 + 判据 + 七条优化建议）。
**结账 2 条**：`T-PLUG-045`（`localStorage` 第四落点 ⇒ 已由 `clean-session-ui-state` / `clean-all-sessions` 覆盖）· `T-PLUG-046`（"wedged" 根因**坐实** = `blank` 草稿**不挂 `conversation.view` slot** ⇒ **起点实体化**已落地）。**新增 1 条**：`T-PLUG-047`（总监页面板缺「会话总数 / 复用·新建」常驻读数）。
**未结项（留给用户的一步）**：主进程内存仍持 **125 条归档 + 1 条活会话**（磁盘为 0）⇒ **退出时回写磁盘**，**须重启 Harness 才彻底归零**（`clean-all-sessions.mjs --apply` + 重启）；重启后应补跑一次**首跑场景** `reused=0 ⇒ created=8`。

## 2026-09-16 — 第二十轮（清除全部对话 + 继续跑流程：宿主官方「下架」通道 / 修掉第 4 个真实缺陷 / 起点自举）

**用户链式任务**（逐字）：「清除所有的对话，然后继续跑流程。」

**交付 4 件**

1. **R4 清除全部对话（完成）**：磁盘三口径早已 0，但**宿主主进程内存**仍持 115 条（`sessions.list` 快照 `ids` 116 条）。
   - 主进程 `taskkill /F` **拒绝访问**（沙箱内外都拒）；`Page.reload` 刷不掉（渲染进程重新 boot 仍拿主进程那份旧快照）。
   - **找到官方通道**：`api.workspace.archiveSession({ sessionId })` ⇒ 新增工具 `scripts/purge-host-sessions.mjs`（dry-run 默认；`--apply` 逐条下架；**浏览器侧串行循环**，避免 115 次 CDP 往返）。
   - 实测：**尝试 115 · 成功 115 · 失败 0** ｜ 归档全集 115 条 ｜ **侧栏 `sessionRow` 6 → 0 行**。
2. **修掉第 4 个真实产品缺陷**：`snap.ids` 里仍留着已下架会话 ⇒ 「派发前先复用」会复用到用户**看不见**的幽灵会话。新增 `branch-tree.archivedSessionIds()`（**全形态归一**；读不到 ⇒ `null` ⇒ **不过滤**）+ `director-dispatch` 过滤并报数（`archivedExcluded` / `archivedKnown` / `aliveAll`）。一个改动**同时**让「索引孤儿清理」对归档会话生效。
3. **闸门起点自举修正**：原判据只按标题里的时间字样认会话 ⇒ **空壳会话**（标题就写「新会话」）**两段都不点** ⇒ 停在欢迎页、无页签环 ⇒ **20 条红全部由这一个起点问题产生**（闸门报 INVALID）。改为**按 class 认会话行**（`sessionRow`；`projectRow` 是工作区/分组 = 结构）。
4. **继续跑流程（未完成，被环境阻塞）**：真机 `verify-novel-split` 两次 INVALID；排查中坐实两条新环境事实，最终定位 **宿主渲染进程 wedged ⇒ 点击侧栏会话行不切会话视图** ⇒ 需**手动重启 Harness**。

**新增两条环境事实**（已进锚点纪律 72/73）

- 宿主 UI 状态有**第四落点 `localStorage`**：`dsh.workspace.view.v5` 的 `groupExpansion[<wsId>] = false` ⇒ 侧栏**一个会话行都不渲染**（连续把"折叠"读成"没有"是本轮 20 条假红的直接机制）。
- **`blank` 草稿会话不挂 `conversation.view` slot** ⇒ `dp-root` 只在**真实会话**下出现；`FloatDock` 的 `d-open-director` 打开的是 `DirectorDialog` **弹窗**，与 `dp-root` **不可互替**。

**验收读数**：构建 **1,810,801 B / 84 模块 / 706 = 706（251 import）/ 指纹 `8065109aaada40a9`** · 装机 `[4]` 逐字节一致 · 页面 `__dshBuildStamp` 一致 · 离线全量 **54 套 PASS=42 FAIL=0 跳过 12** · `verify-bundle` **97/97** · lint 四把全绿 · 单测 `test-director-reuse` / `test-director-collect` / `test-session-io` 全 `IS_PASS TRUE`。

**新增**：§八 纪律 **69–73** · 工具 `scripts/purge-host-sessions.mjs` · 台账 `T-PLUG-045`（localStorage 第四落点）/ `T-PLUG-046`（wedged 判据与处置）。

**真机取证（同轮完成，无需重启）**：排掉起点问题后**连跑两次 `verify-novel-split`，均 PASS 94 / FAIL 0 / `IS_PASS TRUE`**：

- **run1（首跑场景，起点零会话）**：`复用 0 · 新建 8` ⇒ 宿主会话 **117 → 125（净增 8）** —— 对应 `NS-11c` 的 `reused=0 ⇒ created` 那一路。
- **run2（复用场景）**：`复用 8 · 新建 0` ⇒ **125 → 125（净增 0）** —— 对应 `NS-11c` 的 `reused ⇒ delta=0` 那一路。

⇒ 用户需求「**先考虑目前存在的会话，然后没有才是新建会话**」**双向可证**。

**过程误判已登记**：中途曾判定「渲染进程 wedged、需手动重启」⇒ 真相是**点击有延迟**（点完 **4s** 才出现页签环）；教训：**先给有界等待再复读**，不要在等待不足时下「进程坏了」的结论（与纪律 55 同型）。`T-PLUG-046` 已降级为观察项。

**遗留**：真机测试留下 **8 条宿主会话**（第二轮为复用、未新增）—— 按用户既定纪律「**不主动清理测试环境信息**」**保留原状**；需要时一条命令即可清（`node scripts/purge-host-sessions.mjs --apply`）。

---

## 2026-09-16 — 第十九轮（会话复用 / 会话档案 / 清理 / 产物指纹守卫）

> **动因**（用户原话，本轮唯一指令，含五件事）：「现在会话有问题, 重复创建了一百多个会话,
> 我需要的是总监确认完需求之后**先考虑目前存在的会话,然后没有才是新建会话**；
> （每个）会话都有自己的总监,存在自己的会话总结文档；清理无效的多余的文件 和所有会话；
> 然后继续反复测试；遇到问题就思考怎么处理；然后把整个流程记下来,思考如何优化,如何更符合我的想法」。
> **需求与实施文档**：`docs/00-统筹入口/14-会话复用与会话档案-需求与实施文档-20260916.md`（E1–E10 · §六 三轮自审 **8→9→10** · §八 六条优化建议）。
> **收口**：`baseline-check --write` 重封 → 复验**漂移 = 0**（详见下方"基线"行）。

| 项 | 内容 | 取证 |
|:---|:-----|:-----|
| **R1 复用优先** | 新增 `logic/director-reuse.js`（纯函数 `planReuse`，5 步：建存活集 → 按 `(作品名,维度)` 分组 → 同组按 `at` 降序 → `used` 去重 → `aliveKnown===false` 一律新建）。`director-dispatch.js` 循环体改为**复用优先**，复用与新建走**同一投递通道**；索引**只登记新建** | 单测 `test-director-reuse` **17/17**（RU-1..RU-16，含 6 条负对照）<br>🔴 真机核心验收 `NS-11c`：`{"made":"8","reused":"8","created":"0"}` ｜ **宿主会话总数 131 → 131（净增 0）** |
| **R2 每会话有总监** | `store/session-dossier.js`（key `dsh.director.dossier`）给**每个会话**写 `director: { role:"A2 力量体系 总监", name, at }`；`directorRoleOf(dim)` 复用既有 `label`，**不新造命名表** | 单测 `test-session-dossier` **17/17**；真机 `__dshDossier` 9 键、8 条档案含各维度角色名 |
| **R3 每会话有总结** | 回收链读到助手正文即写该会话**自己的** `summary`（`source:"collect"`）；`SUMMARY_SOURCES` 三态白名单；`ok:true` 但无正文 ⇒ **强制 `ok:false`**（防"读数说有总结、实际是空的"） | `applyDossiers` 沿用 `patch(row, countIt)` 范式（`rows` 与 `byId` **不重复计数** —— 第 18 批 `dispatchApplied:16` 同型坑） |
| **R4 清理** | ① **会话**：新工具 `scripts/clean-sessions.mjs`（dry-run 默认 / 移入 `.trash-<ts>` 可回滚 / **三口径复核**）—— 三处落点 **131 → 0**；② **无效文件**：`scripts/` 下 32 个历史 `_tmp-*` + 9 个一次性 `.expr` 退役（40 件移入 `.trash-20260916-202933`） | 复核原文「① 磁盘 0 ｜ ② sessionIds 0 · archived 0 ｜ ③ 缓存 0」<br>🔴 会话目录**两种命名纪元**（`session-<uuid>` 117 + 裸 `<uuid>` 14）—— 只认前缀会漏一半（写进脚本头注释） |
| **R5 记录 + 优化** | `14-…20260916.md`：§2.3.1 E1 实测结论 · §六 第 2/3 轮自审 · §七 执行序 E1–E10 · **§八 六条优化建议**（每条"现状→痛点→建议→代价"） | 见该文档 |
| **产物指纹守卫**（§八 8.1 当场落地） | 新增 `scripts/build-stamp.mjs`（`src/**` 内容指纹）+ `check-stale-build.mjs`（装机**前置**，未过即拒装）+ `build/build.mjs` 把指纹写进产物并挂 `window.__dshBuildStamp` | 🔴 本轮的**真事故**：`DirectorPage.js` 改动写在 build **之后**，而旧复判是"仓库产物 ↔ 实体包逐字节一致"——**两个旧产物之间当然也一致** ⇒ 装机报 ✅、真机 **5 条假红**。新判据：`IS_PASS: TRUE（产物 = 当前 src 构建出来的）`；负向校准：无指纹的旧产物 ⇒ 装机**写入前中止** |
| **宿主失败原因落地**（真机数据反推的第 3 个产品缺陷） | `extractTurns` 增 `endFailure`（`turn/end.reason.error` 的 `message/code/status`）→ `readSessionOutput` → 台账 `runFailure` → `countStates.hostFailed/hostFailWhy` → 裁定文案 | 实测宿主逐字：`{message:"Insufficient Balance", code:"QUOTA", status:402}`（8/8）⇒ 裁定变为「**简报已送达**，是模型侧没出结果」。**天然正负对照**：run2（旧逻辑）`NS-9f` 红 → run3（新逻辑）绿 |
| **`reload-client.mjs` 升格** | 原 `_tmp-reload.mjs` ⇒ **正式工具**（"主进程不可结束"是长期事实），新增**指纹回读判据** + `Page.reload` 期间的 CDP 挂起应对 | 实测输出：`页面产物指纹 __dshBuildStamp: "8fbca94ba6ae5550"` |

**基线**：`lib/client.js` **1,806,857 B / 84 模块 / 705 = 705（251 条 import）** · 内容指纹 `e0d6c88f7aeb9d2a` · 静态闸门 `TRUE`（阻塞 0）· CDP 模板 `TRUE`（244 文件）· DOM 样式单位 17/17 · 平台桩 9/9 · `verify-bundle` **97/97** · `verify-dialog` **271/271** · `verify-build-stamp` **8/8** · 真机 `verify-novel-split` **94/94 · 零跳过 · `IS_PASS: TRUE`** · 离线全量 **54 套 / PASS=42 / FAIL=0 / 跳过 12** · 新增/改写单测：`test-director-reuse` 17/17 · `test-session-dossier` 17/17 · `test-director-collect` **16→22/22** · `test-session-io` **12→15/15**。

**负向校准**（纪律 32，均**精确命中 + 逐字节复原**）：CAL-A（关掉 `endFailure` 采集）⇒ 恰红 `SIO-13/14`；CAL-B（关掉 `runFailure` 统计）⇒ 恰红 `DC-19/21`；负对照 `SIO-15`/`DC-20`/`DC-22` 全绿；`verify-build-stamp` 的 `BS-4`（改内容）/`BS-5`（只改名）在**临时副本**里证明指纹会变。

**未结项（留给用户的一步）**：清盘后**主进程内存仍持 115 条幻影会话**（`T-PLUG-040`）⇒ 需用户**手动重启 Harness**；重启后应补跑一次真机以取证**首跑场景**（`reused=0 ⇒ created=8`）。

## 2026-09-16 — 第十七·十八轮（总监枢纽闭环 E1–E9：派发 / 回收 / 导图可见 · 闸门 69→**90/90** · 4 个新模块单测）

> **动因**：用户链式任务「① 清理全部对话 ② 按一个流程跑一遍，测试案例 = 写小说（从世界观到剧情）—— 把这些交给总监，总监按不同职能调用不同分支对话处理，**我要在思维导图中看到这些**」
> ＋「小说的世界观架构还有文档，你读取 `D:\workspace\novels\墟海` 的信息作为填充」＋「审核是自己按照审核标准审核，审核一遍文档没有问题执行就行」。
> **交付物**：`docs/00-统筹入口/13-总监枢纽闭环-需求与实施文档-20260916.md`（**E1–E9 全部落地**）。
> **收口**：`baseline-check --write` 重封（**88 文件 / 3,431,150 B**）→ 复验**漂移 = 0**；锚点 §八 追加**纪律 59–62**。

| 项 | 内容 | 取证 |
|:---|:-----|:-----|
| E3 · 派发链 | 新增 `logic/director-dispatch.js`（`projectOf` 提项目根 / `dispatchBranches` 逐维投递）+ `store/dispatch-log.js`（台账，挂 `window.__dshDispatchLog`） | 单测 `test-director-dispatch` **9/9** + `test-dispatch-log` **14/14** |
| E4 · 会话读取通道（方案 P3′ 变更） | 新增 `bridge/session-io.js`：`readSessionOutput` 走 **`history({})` 事件流**、**不切页签**；过滤宿主注入假用户消息；状态集 `blank\|running\|done\|partial\|unknown` | 单测 `test-session-io` **12/12**（含负对照：正文里"提到"关键词不误杀 · `chunk/turn/step` 不产条目 · `turns>0` 无正文 ⇒ `partial` 不是 `done`） |
| E5 · 回收链 | 新增 `logic/director-collect.js`：`collectBranches` / `countStates` / `verdictOf` / `nextActionOf`。**「已产出」口径只认 `say`（有正文），不认 `done`（宿主 status）** | 单测 `test-director-collect` **18/18**（含 `DC-9` 真机读数复现 `done=1/running=7/say=8` ⇒ 必须说「8 条全部有产出（其中 7 条仍在跑）」；`DC-10` 负对照 `done=8/say=0` ⇒ 必须说"尚无产出"） |
| E6 · 导图可见 | `MindMap.js`：节点徽标 + `data-dispatch` / **`data-branch-state`**（派发态；`data-state` 已属血缘态）/ `data-said`；`DirectorPage.js`：`dp-flow-collect[data-total\|read\|unread\|done\|running\|say\|verdict]` + 「📥 回收」按钮 `dp-act-collect` | 真机断言 `NS-9a–g` / `NS-10a–e` 全绿 |
| E7 · 闸门加固 | `verify-novel-split.mjs` **69 → 90**（新增 `NS-4c2`/`NS-4d2`/`NS-6h0`/`NS-6f2`/`NS-10e`；头部段号表与 `t()` 同步）；解掉 **5 类假红 + 1 类假绿** | **真机 90/90 · 0 失败 · 0 跳过 · `IS_PASS: TRUE`**（`logs/hub-e2e-v18g.txt`） |
| E7 · 产品级负向校准 | `GATE_SENTINEL_REWRITE=1`（清除后把哨兵写回）⇒ **`PASS 88 / FAIL 1`，恰红 `NS-6h` 一条**（读数 `{sentIn:1,sentinelLeft:1,countBefore:4}`），相邻断言不误伤；revert 后复跑 **90/90** | `logs/hub-e2e-cal.txt`。**纪律 58** |
| E8 · 真机 e2e + 取证 | 清空起点 → 总监输入《墟海》需求 → 8 分支 → 回收 → 导图截图；两处 `SHOTS=1` 取证钩子（带 `captureBeyondViewport:false` + `pngInkRatio()` 自检） | `logs/acceptance18/H4b-collect-closeup.png`（28,030 B · 510×180 · inkRatio **0.1351**）+ `H5b-mindmap-dispatch-closeup.png`（68,894 B · 386×971 · inkRatio **0.0663**，**8 条派发节点可读**）。**纪律 57** |
| 🔴 真实产品缺陷 ①  | （新单测 `DL-9` 抓到）`applyDispatchLabels` 的 `applied`/`states` 数了 `rows` + `byId` **两遍** ⇒ `diag.dispatchApplied` **翻倍**（真机 `16` 而台账只有 8）；`applySplitLabels` 只数 `rows`（`splitApplied:40` 正确）⇒ 两条"同口径"的**声明**当时并不成立 | 产品修 `patch(row, countIt)`；补 `NS-10e` 防回归（`diag.dispatchApplied === 8`） |
| 🔴 真实产品缺陷 ②  | （新单测 `DD-1` 抓到）`projectOf` 的**终止字符类不含中文标点** ⇒「项目根 `D:\workspace\novels\墟海，先搭世界观`」把「**，先搭世界观**」一起吞进 `root`。真机原话恰好带 `"…墟海"` 引号才没踩到 ⇒ **属运气不是正确性** | 产品修 `STOP` 字符类补中文标点；`DD-1` 断言 `root === "D:\\workspace\\novels\\墟海"` |
| 新模块单测（纪律 52） | 4 个新模块此前**无任何单测** ⇒ 补 **4 套 / 53 条**（`test-director-collect` 18 · `test-dispatch-log` 14 · `test-session-io` 12 · `test-director-dispatch` 9） | **当轮抓出并修掉上表 2 个真实缺陷**；负向校准 `verdictOf` 改一行 ⇒ **恰红 `DC-9`**（`PASS 17 / FAIL 1`），revert 后 md5 **`e6208d475bfd2cfb69660d03db11282c` 逐字节复原**、复跑 18/18 |
| 离线全量 | `node scripts/_offline-run.mjs`：**43 套 → 51 套** | `logs/offline-before.txt` **PASS=35** ⇒ `logs/offline-after.txt` **PASS=39 / FAIL=0 / 跳过 12**（跳过均带可分辨理由） |
| 构建 / 装机 | `gen-source-map` → `gen-key-files`（82 模块 / 1,611,422 B）→ lint 四把（`undefined-symbols` 阻塞 0 · `cdp-templates` 233 文件 · `platform-stub` 9/9 · `dom-style-units` 17/17）→ build → `plugin-install --apply`（`[4]` **逐字节复判通过**） | 产物 **1,762,564 B / 82 模块 / 696 = 696（247 条 import）**；`inject = ["slots","sessions"]` 未变 |
| E9 · 收口 | 锚点 V16 就地更新（§一 产物 · §六 指纹 · §七 读数表 · §八 **纪律 59–62**）· `AGENTS.md` §〇 + **闸门纠错台账（四）** · 台账 03/04 · `gen-docs-index` 刷新 · 临时脚本清理 | `baseline-check --write` → **88 文件 / 3,431,150 B**；复验 **漂移 = 0 · `IS_PASS: TRUE`** |
| ⚠️ 环境结论（非产品缺陷） | 真机循环 ~10 次（每次真建 **8 个宿主会话**，宿主**无 delete 契约** ⇒ 持久累积）⇒ 最终 `read:0 / running:0 / tok:0`，8 条全判 `partial`。**根因 = 模型侧零输出（疑似配额）**，插件无缺陷（投递成功 `turns:1`、状态可读、原因不静默不伪造）；历史轮次 `v18b` 确有 `read:8 / say:8` | 另：Harness 有 **3 只 `taskkill` 杀不掉的进程**（`7412/10312/5656`，一律"拒绝访问"，**纪律 61**）⇒ 需手动结束或重启机器；本会话用 `Page.reload(ignoreCache:true)` 让新产物生效（`NS-10e` 绿 = 可分辨证据，**纪律 62**） |

## 2026-09-16 — 第十六轮（按维度自动分流 · 执行状态窗口三态 · 清除对话消息 + 闸门纠错 ④「读早了」）

> **动因**：用户链式任务「执行状态的窗口需要可以移动最小化,靠边缩进 / 我的描述也有问题, 不需要测试所有的交互, 是每次修改只测试能影响到的部分的交互包含新加新修改部分的交互 / 清除所有的对话消息 / 按照一个流程跑一遍写小说吧,调用小说技能 / 然后按照世界观剧情等应该自动分到不同的对话分支 然后思维导图应该能看出来 / 理解我要做什么 然后完善想法, 列出都要做什么然后审核文档,然后再执行」。
> **交付物**：`docs/00-统筹入口/12-执行状态窗口与分支自动分流-需求与实施文档-20260916.md`。

| 项 | 内容 | 取证 |
|:---|:-----|:-----|
| 执行状态窗口三态 | `expanded ⇄ collapsed ⇄ docked`；纯函数 `nextWinMode()` / `snapWinEdge()` / `clampWinPos()` / `RUNNING_WIN_SNAP_PX=24`；几何从环境读（`usableBox()` 取 `innerWidth/innerHeight + insetRight`） | 离线 `test-running-window` **50/50**；真机 `verify-novel-split` W 段 19 条（**拖动中逐帧采样**判"拖动中就是 expanded"、最小化**真卸载** body、靠边 `docked/left` 换细条且不跑出视口、双击归位复原） |
| 按维度自动分流 | `logic/split-dimensions.js` 8 条 A1–A8（`plan()` / `briefOf()` / `branchTitle()`，按表内原始序）· `store/split-index.js` 标签补偿（宿主**无 rename / 无 delete** ⇒ 空白会话标题全同 ⇒ 在 `refreshBranchTree()` 唯一摄取点覆盖并标 `titleOrigin="plugin:split"`） | 离线 **45/45 + 36/36**；真机 `made=8 / failed=0 / sent=8 / attachFail=0 / via=host-decides`，分支树净增**恰 8**，导图 `[data-split]` **恰 8** 且维度集合相等 |
| 清除全部对话消息 | `plugin-db.clearDirectorMessages` + 待确认撤防（`data-armed` 超时撤防**正负对照**） | 离线 + 真机 `removed===before` + 四项外部读数未被动 + **正对照**插件消息数确实下降 |
| 🔴 主缺陷：分流「**静默半成功**」 | 8 条宿主会话**真的建出来了**，读数却是 `made=0/failed=8` 且**零异常零超时**。真因：宿主 `sessions.create` **注释与源码都写** `{ok:true,value:{sessionId}}`，**真机返回裸字符串** `"session-08b83f34-…"`；并存第二支 `{ok:false,error:{code:"workspace-attach-failed",details:{sessionId}}}`（会话**已建出**、未挂工作区，宿主自己当成功用） | 入参改 `{}`（不猜 workspaceId）+ `resolveCreateId()` **全形态归一** + 失败带**宿主原始返回** `raw` + 读数**先落地**（`data-attachfail` / `data-via`，`attached` **三态**）⇒ 复跑 `made:8/failed:0`、日志 `__directChatSubmit: calling scopedConv.send`（简报**真送出**）。**纪律 53/54** |
| 🔴 闸门纠错 ④：`cdp-click` I16 假红 | 报「summary 覆盖 1 → **51 / 116** ｜ 需汇总的父节点 1 个缺 1 个」，读起来像"逐级汇总坏了"。真因：`await waitIdle();` 默认 **60s** 上限且**返回值被丢弃**，而该任务实测 **140.3 s** ⇒ **恰在 60s 那一刻读数**；`summarizeTree()` 是**后序遍历**，任何时刻恰好有"当前节点 + 其祖先"处于"子级已写、自己未写" ⇒ 探针里 `parentBad` **恒为 1~2**、写完归 0 —— **那条红就是在途节点本身** | `_probe-sum-tree-timeline.mjs` 逐 500ms 采样，收敛于 **t=140264 ms**、终态 `116/116 · parentBad 0 · disabled false`。修：**校验返回值 + 180s 预算 + 超预算判 INVALID(exit 2) + 跑程内收敛判据**（`summaryAt >= 本次点击时间戳`，因为树已总结过时 `withSum` 恒等于 `total` ⇒ 会**假收敛"0 秒"**）。**纪律 55** |
| 🔴 闸门纠错 ④（同轮第二条）：I7「无会话级节点」 | 报 `❌ 无会话级节点`，**而同一轮 I12 读到「会话 121/121 · 文件夹 2/2」**。真因：**app 端口每次启动都变**（实测 32499 / 20401 / 45471），`localStorage`/`IndexedDB` 按**含端口**的 origin 隔离 ⇒ 冷启动时总监树里只有全局根，而"把真实会话落成总监节点"的「同步真实会话」排在 I12（晚于 I7）⇒ **前提不成立，读数伪装成产品缺陷**（上一轮绿只因继承了用户既有实例的 113 条会话） | I7 **自建前提**（走真实界面路径点一次幂等的「同步真实会话」→ 有界重取 → 取不到才**逐条 SKIP 并带可分辨原因**，保证断言总数不随状态变化）· I16 补 1 条**前提断言** |
| 产品改进：把 03 号文 §4.3「跳过」落到实处 | `summarizeNode()` 对**每个**节点都试一次本地模型；本机无 Ollama 时一次 fetch 失败 **2371 ms**（`saveNode` **1 ms** / `getNode` **0 ms** / `dshLog` **0 ms**）⇒ 116 节点 **140 s** | 改成「**本轮首次失败即锁定**，其后 `forceGrade=G0`」，toast 显式说明跳过数（纪律 19）。**同进程 A/B**（`_probe-sum-tree-ab.mjs`，同树背靠背）：**11,757 ms → 293 ms（40.13×）**，模型调用 **9 → 0**，**梯度仍全 G0 ⇒ 结论一致**。**纪律 56** |
| 测试范围收窄（纪律 52） | 用户原话修正「不需要测试所有的交互，是每次修改只测试能影响到的部分的交互包含新加新修改部分的交互」 | 取集方式写死为**读闸门代码**（`grep -rn "<符号>" scripts/*.mjs` 看命中集）；**三条红线仍全量**（删目录 / 改冻结契约 / 动宿主状态）+ 封存交付全量 + 曾假红假绿过必重跑。见 §八 纪律 52 |
| E1 · 闸门加固（→ 结账 `T-PLUG-039`） | `verify-novel-split` D 段：`NS-6m` 由「**读一次**」改为**有界等待**（读一次会在 React 未 flush 时把正确行为判红）；新增 **`NS-6n`**（toast 必须**自述**「已清除总监对话消息 N 条」）与 **`NS-6o`**（该 toast 必须含**宿主侧边界** —— 纪律 19）。📌 台账原描述「从未断言第二击后 armed 复位」**已过期** —— `NS-6m` 本就存在 | **67 → 69/69 零跳过**。**负向校准**（纪律 32）：植入缺陷（toast 文案改坏）后**恰 `NS-6n/6o` 两条红**、相邻 `NS-6e/6m` 仍绿 ⇒ 断言**不恒真、不误伤**；`revert` 后产物 **md5 逐字节复原**（`207e56af11a8c8271a8e144d3ffb904b`）。**纪律 58** |
| E3 · 取证补拍 | 重拍上一轮误删的清除两张图；脚本读数由固定 `sleep(1500)` 改**有界等待**（产品侧 `say()` 排在 `setClearInfo()` **之后** ⇒ 固定等待是"赌"，赌输会把正确行为报成撕裂态 —— 纪律 31） | `07a-clear-armed.png`（按钮「⚠ 确认清除？」+ toast 3 秒倒计时）· `07b-cleared.png`（toast 逐字「已清除总监对话消息 3 条（原有 3 条）· 宿主侧对话消息不在插件权限内（sessions 服务无删除/清空契约）」+ 读数「已清 3/3」）—— **两张与命名逐张核对相符** |
| 第 4 轮审核（用户裁定「审核 AI 自审，通过即执行」） | 前三轮审**纸面**，本轮审**文档 ↔ 实现**的偏离（纪律 14）：抓出 ① §五 `T1–T9` 无状态列 ② `plan(lines,opts)` 与实现 `plan(text,opts)` **语义相反** ③ §4.3 契约表**漏登 12 个闸门锚点** ④ §6.3 裁定表与实际执行差 5 处 ⑤ 台账 `T-PLUG-039` 描述过期 | 评分 **8/10**（完整性 1/2 · 准确性 1/2 · 可验证性 1/2，其余满分）；**4 项整改当场全部落地**（状态列 / 接口按实现校正 / 契约补全 / 影响面偏差登记）。见 `12-…需求与实施文档` §八 第 4 轮 |

| E2 · 结账 `T-PLUG-038`（`dp-root` 间歇不挂载） | 10 次冷启动统计（固定姿势：杀进程 → 等**端口释放** → 启动 → 探针）。**修正前**同样姿势连报 **16 次 FALSE**（E2 十次 + 对照六次，对照只加"启动前 `sleep 8`"一个变量 ⇒ "重启太快"假设**被证伪**）；形态完全一致 `tabs=[]` / `sidebar=7` / `ready=-1` | **真因：冷启动后 app 停在欢迎页（`welcome=Y` 10/10），此时根本没有 `[role="tab"]` 环** —— `verify-novel-split` A 段早有此容错（注释「重启后停在欢迎页 ⇒ 后面全是级联假红」），**新探针少了这一步起点建立**。补「无 tab 环 ⇒ 走真实 UI 路径点开侧栏会话」后：**`dp-root=TRUE` 10/10 · `root=67~114 ms` · `tabBox=inView/hit` 10/10** ⇒ **结账：非产品缺陷，用户侧无风险**（用户自己会点会话）。**纪律 23/51 现场版** |
| 同批修读数缺陷 2 条 | ① `_probe-coldstart-check.mjs` **只等端口、不等 `/json/list` 里有 page target**，且 `js()` 超时**崩栈**（把"没跑成"伪装成"跑了但没挂载"）；② `plugin-install.mjs --apply` 的「⇒ 总判定」打印在**写入之前** | ① 改为脚本**自己轮询 `/json/list`** + 全过程 try/catch + **任何结局都输出一行可分辨结果**（`TRUE`/`FALSE`/`ERROR …`/`NO_PAGE_TARGET`）；② 加"**写入前**快照"标注 + `[4]` **安装后逐字节复判**（`hostClient.equals(srcClient)`）。**纪律 58** |
| 结果 | `cdp-click` **72/73 假红 → 74/74 · 0 失败 · 0 跳过 TRUE** · `verify-novel-split` **69/69 零跳过**（独立冷启动；E1 加固 67→69）· `verify-batch6/7/8` **TRUE / 66/66 / 93/93** · `verify-bundle` **97/97** · `test-orchestrate` TRUE · `verify-walk` **48/0（跳过 37，理由显式）** · lint **阻塞 0 / 提示 17** + `cdp-templates` TRUE（208 文件） | 构建 **1,690,899 B / 78 模块 / 666 = 666（234 import）** ⇒ 装机**逐文件回读校验通过**；新增探针 5 个（`split-trace` / `create-raw` / `sum-tree-timeline` / `sum-tree-ab` / `sum-node-cost`） |

## 2026-09-16 — 第十五轮（「设计 ↔ 实现」差异化清单 + 拖动卡死根治 + 基线重封）

> **动因**：用户链式任务「初始化 → 了解项目实现与现有开发的区别 → 作出差异化清单 → 对清单审核 → 按清单修改 → 多轮测试 → 完成之后再次比对（页面布局 / 实际功能 / 点击交互）」。
> **交付物**：`docs/00-统筹入口/11-设计实现差异化清单-20260916.md`（含三轮审核 + 10 分制评分 · 复比对表 · 多轮测试表 · 负向对照 · 探针清单）。
> **收口**：`baseline-check --write` 重封（**82 文件 / 3,145,708 B**）→ 复验 **漂移=0**；锚点 §7.14 与 §八 纪律 47–51 同步落地。

| 编号 | 任务 | 完成时间 | 关联文档 |
|:-----|:-----|:--------:|:---------|
| T-DIFF-01 | 初始化与现状了解：读锚点 V16 + 跑 `baseline-check`（漂移=0）⇒ 产出「设计 ↔ 实现」差异项 | 2026-09-16 | `11-设计实现差异化清单-20260916.md` §一/§二 |
| T-DIFF-02 | 清单三轮审核 + 10 分制评分（口径审核 / 取证复核 / 可执行性审核） | 2026-09-16 | 同上 §三 |
| T-DIFF-03 | 🔴 产品缺陷 **A2**：`installChatBridgeApi()` 在**渲染体**内每次无条件 `dshLog`，而 `appendLogLine()` 是「读全文→拼一行→重写全文」的 **O(n)** 写 ⇒ 每渲染一次重写整份日志。改：**真幂等**（已装过直接返回同一对象）+ `getFile().size` 元数据 + `createWritable({keepExistingData})` + `seek` + **250ms 合批** + Promise 链**串行** | 2026-09-16 | `src/bridge/chat-bridge.js` · `src/store/file-adapter.js` |
| T-DIFF-04 | 🔴 产品缺陷 **A3**：`MindMap.js` 把「写 store + 弹 toast」放进 `setDragPos(updater)` 的**更新函数体内**（渲染期执行），而组件用 `useSyncExternalStore` 订阅同一 store ⇒ **渲染循环** ⇒ 松手那一下把渲染进程顶死。改：`dragPosRef` 记值 + 副作用移出更新函数 | 2026-09-16 | `src/components/MindMap.js` |
| T-DIFF-05 | 闸门缺陷：`verify-v22` 起点「已开着会话就直接返回」⇒ 会话恰停在 1 条消息时 F9 假红。改 `ensureSessionOpen()`：先切「对话」页签 → 取第一个 `total ≥ 2` 的会话 + 新增 `A0b` 前提断言 | 2026-09-16 | `scripts/verify-v22.mjs` |
| T-DIFF-06 | 闸门缺陷：切页签**重挂对话区** ⇒ 我方标记丢失且异步回补 ⇒ 间歇 INVALID。改：幂等催一次 `applyHostColumnGeometry()` + **有界等待 6s** | 2026-09-16 | 同上 |
| T-DIFF-07 | 闸门缺陷：`verify-design-studio` C14.5 三连偶发 —— **产品侧正控证伪**后登记观察项 | 2026-09-16 | 转入 `03` 台账 **T-PLUG-037** |
| T-DIFF-08 | 多轮测试与复比对：离线 43 套 **PASS=32 / FAIL=0** · `verify-flow` **80/80 ×3** · `verify-mindmap` **101/101** · `verify-design-studio` **94/94** · `verify-v22` **59/59 零跳过** · `cdp-click` 71/71 · `cdp-click-dialog` 102/102 · 三维度（布局 / 功能 / 点击交互）复比对无差异 | 2026-09-16 | 同上 §六/§七 + `logs/*-r*.log` |
| T-DIFF-09 | 基线收口：就地更新锚点 V16（闸门数字 / §7.14 / 纪律 47–51）+ AGENTS.md §〇 基线表同步 ⇒ `baseline-check --write` 重封 **82 文件 / 3,145,708 B** ⇒ 复验 **漂移=0** | 2026-09-16 | `10-当前基线-落死锚点-V16.md` §7.14 · `AGENTS.md` §〇 |

## 2026-09-14 — 第十三轮结账（第 5 批收口 · 5 条任务结账 + 5 条过期状态纠正）

> **动因**：用户「进行评分和是否围绕核心进行」「**还有把所有代办都完成掉**」。
> **结账方式**：**逐条对源码/产物取证**（不采信台账自述）—— 本轮 5 条结账里有 **4 条的台账状态已过期**
> （写着"待做"其实早做完了，或写着"待确认"其实关联物已不存在）。这正是纪律 14「**闸门/台账自己会过期**」的又一次实证。

| 编号 | 任务 | 完成时间 | 关联文档 |
|:-----|:-----|:--------:|:---------|
| T-PLUG-036 | 🔴 **第 5 批界面调整 10 条全量结账 + 2 处 bug 根治**（V19 设计稿 45,684 B · 卫生闸门 5/5 · 审核 4 轮 6→8→9→10 分）。10 条逐条取证：①对话页标题栏去除（`host-panel-trim` 第 3 目标 `host-chat-titlebar`，结构判据 `isHostChatTitleBar`）②「最小化消失」bug（`up:1 → up:0`，**只隐「总监对话」文字、保留宿主那枚有效的 `—` 按钮** —— `verify-v19 [3.5]` 守「源码里 up 必须仍为 0」）③R3 整段删除（`dp-r3` 命中 **0**）④R4/R7 缩回+弹窗+可固定（`dp-rail-*` / `dp-r4-toggle` / `dp-r7-toggle` / `data-rail-*`）⑤「正在使用 智能体/技能」浮窗（**复用既有 `listAgentRuns()`，不新造数据源**）⑥**按消息判定归属对话**（`scopeKeyOf` + `scopeHasConversation` 判目标会话后投递；无对话时**如实告知、不静默改发当前会话**；去掉 `DirectorPage.js` 里本地重复 DFS）⑦R6 并入 R2 后整段删除（`sectionCollapsed.r6` **字段保留**、仅摘 UI 入口）⑧`R4_TABS` 三 Tab 重定义 ⑨`sectionR7()` 双 Tab 接住关键文件（`dp-r4-files` 已退役）⑩R4/R7 接**真实数据源**（`logic/ledger.js` 读 `03`/`04` 台账 + `assets/docs-index.json`；`logic/key-files.js` 生成式；降级带 `reason` 并显示）。<br>**A3 口径定案**：采纳审核推荐的 **①细粒度裁剪**（非"插件侧补最小化"）—— 实测宿主那枚 `—` 按钮调的 `toggleDirectorCollapsed()` 是**宿主自己的** store，**本来就能用**，是被 `up:1` 一起隐掉的。<br>**真机**：`verify-flow 74/74 ×3`（H 段 2→3 目标 + 新增 H5 bug② 回归）· `verify-mindmap 101/101 ×3` · `verify-design-studio 91/91 ×3`；`verify-v19 59/59` + `--selftest` 正负对照成立 | 2026-09-14 | `50-信息中心/V19-界面调整设计稿.html` + `V19-界面调整设计稿-审核记录.md` · `10-…锚点-V16.md` §7.13 |
| T-PLUG-012 | 📄 **`assets/docs-index.json` 与磁盘对账 —— 实况已是 103 = 103**（原记「90 vs 96，缺 6 篇」）。生成器已由旧 PowerShell 通道改为 `dsh-director-plugin/scripts/gen-docs-index.mjs`（不碰宿主、不碰 `src/**`、不碰产物），`--check` 与 `--write` 都走同一口径。<br>**过期状态纠正**：原条目的三条"为何不现在修"理由**均已不成立** —— ①旧生成器已退役；②重新生成已随本轮基线重封一并完成（`baseline-check --write`）；③A14 部署偏差已不再影响本文件 | 2026-09-14 | `dsh-director-plugin/assets/docs-index.json` · `scripts/gen-docs-index.mjs` |
| T-PLUG-030 | 📄 **「文档上下文」死链路 —— 插件侧两半已修，宿主侧并入 B6**。①插件：`src/logic/process.js:104` 已改为**按 map 处理**（原 `docs.length` / `docs.filter` ⇒ 文案恒为「共 undefined 篇文档」且 `TypeError` 被空 `catch` 吞掉），并新增 `logic/ledger.js` 的 **map 优先 + 兼容数组**双通道读取；②资源：`assets/docs-index.json` 已重新生成（103 篇，含 `docCount`）；③**宿主侧** `workspace/.../client.js:6757` 仍是 map 当数组 ⇒ 与 B6 同源、同批处理（宿主文件被 `.gitignore` 排除 ⇒ **不可回滚**，须先授权） | 2026-09-14 | `src/logic/process.js` · `src/logic/ledger.js` · `dsh-director-plugin/docs/07` |
| T-AESTH-002 | 🔍 **27 号文结构项（P0-1~P0-4 / P1-6 / P1-7）—— 判定「已并入并随批次关闭」，不再单列**。取证：①`28-插件化整改执行总清单.md` §三「结构项（27号文 P0 组，并入对应批次）」把这 6 项**逐条指派给 P1 批**；②P1 批即 `T-PLUG-005-B1..B9`，**九个子批已全部结账**（见本文件下方各节）；③该 6 个编号**在任何现行文档里都查不到独立条目**（全库 grep 零命中）⇒ 无"遗留未做"的载体；④原条目的前提（改宿主 `client.js`）已随**插件化架构**失效。**可复验判据**（若日后要重开）：P0-1「主内容区未铺满」改由 `verify-flow` F 段（总监页背景同源）与 C 段几何对账覆盖 | 2026-09-14 | `20-任务文档/27-总监控制台审美与交互审核报告.md` · `20-任务文档/28-插件化整改执行总清单.md` §三 |
| T-PLUG-032 | 🎨 **第 3 批 14 条 —— 全量收口**。原记「剩余 5 条」，逐条取证后实为 **1 条未达标 + 4 条早已落地**：<br>· ②**模型切换器** = 已落地（`dp-model` / `dp-model-menu` / `dp-model-opt`，候选带 `data-ok` ⇒ **取不到的模型置灰**，符合 V18「需求 9 · D1 不编可用项」）<br>· ③**R5 语义统一** = 已落地（`dp-r5` 双 Tab `dp-r5-flow` / `dp-r5-msg`，`data-review-dim` 与 `data-flow-dim` 语义已拆）<br>· ④**R7 执行功能区** = 已落地（`R7_TABS` 双 Tab + `sectionR7()` + 生成式 `key-files.js`）<br>· ⑤**对话框左右清单** = 已落地（`d-left-rail` / `d-right-rail` / `d-right`，`data-same-source`，可各自缩回）<br>· ①**折叠态单箭头** = 🔴 **本轮补齐**：V18 板块 E1 目标是「缩回后**只剩一个箭头**」，而 rail 里还残留 `◆` + 竖排标题文字（`writing-mode: vertical-rl`，属设计稿里的「现状」）⇒ 去掉两处可见文字、**只剩箭头**，可读名改由 `aria-label` / `title` 承载（**无障碍不退化**）。新增真机判据 **A7**（可见文本**恰为 1 个箭头字符**，等值断言 ⇒ 空 rail / 多字 rail / 错字符全红）+ **A8**（缩回栏与 R5 **等高**，含 `>100` 下限防平凡真）⇒ `verify-v17-sync 25 → **27/27 ×3**`。**正负对照**：运行时把标题文字注回 rail ⇒ 判据立刻转假（`textLen 1 → 8`）；移除注入 ⇒ 回真且无残留 | 2026-09-14 | `50-信息中心/V18-界面调整设计稿.html` 板块 A3/E1 · `10-…锚点-V16.md` §7.13 |

## 2026-09-14 — 备份清理（用户确认「推荐档」，释放 115.84 MB）

> **动因**：用户「备份的东西这么多么，清理下备份」。
> 按本项目固化口径执行：**只读扫描 → 列候选（含 git 跟踪状态与真实引用关系）→ 用户确认档位 → 再删**。
> ⚠️ 三个档位（推荐 123 MB / 保守 114 MB / 激进 127 MB）中用户选**推荐档**。

| 编号 | 任务 | 完成时间 | 关联文档 |
|:-----|:-----|:--------:|:---------|
| T-MAINT-001 | 🧹 **备份清理（推荐档）**：删 **101 项 / 115.84 MB** —— ① 58 个 `snapshots/snapshot-*-before-apply`（**历史通道** `apply.ps1` 的 apply 前全量副本，干道已切 `dsh-director-plugin/`，纪律 2）② `dsh-director-plugin/logs/_archive` 中**未被文档引用**的 23 张迭代 png ③ `logs/audit/`（9/13 审计截图，无引用）④ `backups/client.js.bak-20260901-zb2`（9/1 中间副本，md5 与 `original/` 全不同）⑤ 根 `logs/` 18 项旧运行日志。`snapshots/` **115 MB → 1.5 MB**，顶层总量 ≈137 MB → **17 MB**。<br>**硬约束保留**：`snapshots/host-b6-20260911-before-patch`（T-PLUG-005-B6 唯一回滚点）· 锚点文档点名的 5 张取证图 · `visible.png`/`studio.png` · 24 个 verify 日志 · `original/`+`workspace/`（被 `apply.ps1`/`diff.ps1`/`verify-batch1.mjs` **实读**，不可删）· `logs/playwright-tests/test-report.json`（git 跟踪）。<br>**守卫**：清理脚本分 `PROTECT_SELF`（仅本身不可删，子项可删）/ `PROTECT_TREE`（整树含祖先后代）两层断言，dry-run 先列清单，命中即 **exit 2 且零删除**；删后复核「残留 0 + 不可删项完好」 | 2026-09-14 | 本文件 · `06-工作快照.md` 批次 19 |

## 2026-09-14 — 03 清单第二次清账（T-PLUG-033 / 034 / 035 结账）

> **动因**：同日晚真机套件跑完后，`03` 里最后两条「**已做完但未结账**」的条目补迁入本文件；
> 随后把 T-PLUG-034 的**残余项 T-PLUG-035**（Cookie 体积纳入真机常态闸门）**直接做掉**（未流入下一轮排队）。
> **共迁出 3 行**（033 / 034 / 035）⇒ `03` 收敛为 **4 组 / 9 条**
> （计数规则见 `03` 表头：**每个表格行计 1 条**）。
> ⚠️ 下方 §A 表里的 `T-PLUG-033（离线部分）` 是本行的**前一段**（离线闸门 + 构建对账），二者为**同一任务的前后两段**，不是重复条目。

| 编号 | 任务 | 完成时间 | 关联文档 |
|:-----|:-----|:--------:|:---------|
| T-PLUG-033 | 第 4 批界面调整 5 条**全量结账**：代码 `66f654d`（取消 R1 整行 · 去掉两个「全局总管」· 下拉=作用域联动 · 总监数据 per-scope 分桶 · 宿主残留两行只做显示层裁剪）。真机全绿 —— `verify-flow` **73/73 零跳过**（**新增【H】段 5 条**，补上「宿主残留两行裁剪」此前**零闸门覆盖**的真实缺口，含 `restore → apply` 正负对照）· `verify-mindmap` 100/100 ×2 · `verify-design-studio` 91/91 零跳过 ×2 · `cdp-verify` 39/39 · `cdp-mouse sweep ds-top` 零位移 ×3（`量了 12 次 / 检出 0 个`）。构建 **1,021,282 B / 59 模块 / 绑定 498 = 498**，重跑哈希 `844797ee00ac11e2` **逐字节不变**；装机逐文件回读校验通过；基线重封 **65 文件 / 2,000,143 B** | 2026-09-14 | `V18` 板块 L · `10-…锚点-V16.md` §7.11.6 + 第十二轮 |
| T-PLUG-034 | 🔴 **根治「打不开软件」白屏（HTTP 431）**：根因 = Cookie 总量 **16,794 B** 超宿主 Node 默认 `--max-http-header-size` 16,384 ⇒ 宿主回 **HTTP 431 + 空体** ⇒ Chromium 判 `text/plain` ⇒ 渲染文档只剩 **39 B** ⇒ 窗口全白，**而主进程日志一行错都没有**（最难查的形态）。修复：`src/store/cookie.js` 新增**总预算层** `COOKIE_TOTAL_BUDGET = 12288`（=75%，其余 25% 留给 UA / Accept / Sec-Fetch-*）+ 最旧优先淘汰 + `keepKey` 保护 + **每次淘汰必告警** + 回读 `_meta` 自证。闸门 `scripts/test-cookie-budget.mjs` **21/21**（负向校准 17/21 转红）；真机 `document.cookie.length` **16,794 → 6,033** 稳定。附带产出：`probe-window-state.mjs`（窗口三态诊断）· `cdp-shot-raw.mjs`（主线程死 vs 窗口未合成两态分诊）· 三处闸门纠错（C15 安全区判据加 `wco.visible` 门控 + C16.7 改带截止期轮询 + sweep 起点自建/收尾复原） | 2026-09-14 | `10-…锚点-V16.md` 第十二轮 · git `c7b84a0`（15 files, +1223 / -283） |
| T-PLUG-035 | 🔴 **把「Cookie 体积」纳入真机常态闸门**（T-PLUG-034 的残余项，当晚直接做掉）：`verify-data-safe.mjs` 新增第 **[7]** 节 —— **42/42 → 51/51**。判据分三层，缺一层即**平凡真**：① **前提** = 真机确实有 `dsh_director_*` cookie（否则断言的是空集）；② **判据** = 实测总量 ≤ 预算，且**预算与前缀都从源码读**（写死就会与产品脱钩，纪律 14）；③ **正负对照** = 同一测量口径对**合成超预算串**（18321 B，只做字符串**绝不写盘** —— 真写超大 cookie 即复现事故）与**历史事故等效串**（16818 B）必须判「超」。实测 `document.cookie = 6,033 B` · `dsh_director_* = 6,007 B / 14 组` · 预算 12,288 B · 余量 6,281 B。**三向负向校准**（被测源经 `COOKIE_SRC` 覆盖 ⇒ **零改动产品源码**，样本放 `logs/` 用完即删）：预算压到 1000 ⇒ *判据*红（`6007 B ≤ 1000 B 余量 -5007`）· 前缀改名 ⇒ *前提*红而*判据*仍绿（`0 B ≤ 12288 B` —— **正是空真形态，故前提断言不可省**）· 目标不存在 ⇒ **INVALID exit 2** + 可复制命令（不崩栈）。未动 `src/**` ⇒ 指纹不需重封（`baseline-check 65/65 漂移 0`） | 2026-09-14 | `10-…锚点-V16.md` §7.11.6 旧套件行 + 第十二轮 · git `98b945b` 之后 |

## 2026-09-14 — 03 清单第一次清账（历史 ✅ 迁入 + 失效项归档）

> **动因**：用户「03-待完成任务清单怎么有这么多待执行的 你做一下全部清掉」。
> 实况：`03` 158 行里混着大量 ✅ 已完成 / 已失效 / 编号重复的行，长期没迁走，
> 导致「待办」看着像有 40+ 条。本次按 **完成 / 已失效 / 保留** 三类一次分清。

### A. ✅ 已完成（本轮迁入）

| 编号 | 任务 | 完成时间 | 关联文档 |
|:-----|:-----|:--------:|:---------|
| T-V9D-001/002/003 | 批次1 D-01 主题变量体系+设置弹层 / D-02 布局骨架重构 / D-03 docs-index 生成脚本 | 2026-09-01 | 18号文 附录I（随 MOD-B 退坡，UI 由插件承载 · 台账回写 D3/17号） |
| T-P1-001..006 | 阶段1 M1–M5 + 验证（locale 词条 / 总监消息 store / mock 处理 / DirectorView / tab 注册） | ≤2026-09-01 | 04-阶段1详细开发文档（后由 V9 设计取代并实际交付） |
| T-V12-001..007 | 整改 V1.2 清理段：_archived-20260831(28) / scripts 一次性探针(49) / V10 V3 整改脚本(4) / gui_test_v2(2) / backups dsh-director-backup-*(~40MB) / client.js.tmp+client-v2(555KB) / manual-bak-batch1 | 2026-09-06 | 40-测试质量/12 · 20-任务文档/23 |
| T-COG-001 | 插件源码工程：修正为「宿主内联实现 → 迁入 dsh-director-plugin/」 | 2026-09-11 | 07号文 §十 · `01-插件迁移明细清单.md` v2 |
| T-COG-002 | 台账漂移整改 6 处（deploy.py 引用 / README 包表 / INDEX 头 / agents.json / project_overview / 05-大索引） | 2026-09-11 | 07号文 §十 P1-4~P1-10 |
| T-COG-003 | git 工作区纳管（9 改 + 6 未跟踪，含 V11/V12 设计稿 162KB）→ commit `2327912` | 2026-09-11 | 07号文 §十 P1-10 |
| T-PLUG-001/002/003/004 | 插件加载通道勘察 / Cordis 内核溯源 / 通道验证收口 / 插件包骨架 + patch 基线测量 | 2026-09-07~11 | 28号文 §四 · 13号审核 |
| T-PLUG-005-B1 / B1b | 批次1 零依赖层（A14 剥离 + 日志 + 布局/主题 store + 探针 + 模型配置）；宿主 **-914,751 B（-61.8%）** | 2026-09-11 | 01-插件迁移明细清单 |
| T-PLUG-005-B2 / B2b / B2c | 批次2 数据层 6 模块 + 构建安装链路三处安装点 + 打包器「平台模块外置」（React 零打包，ADR-001） | 2026-09-11 | 同上 · `INSTALL.md` |
| T-PLUG-005-B3 / B4 / B5 | 批次3 持久化 5 模块 / 批次4 逻辑层 2 模块 / 批次5 组件层 E1 DirectorFlow（四层验证全绿） | 2026-09-11 | 同上 |
| T-PLUG-005-B7 / B8 / B9 | 批次7 多层级总监结构（三方案评分选 **方案 C 42/50**）/ 批次8 自动同步+覆盖度（**会话 8/8 · 文件夹 2/2 · 全局 1/1**）/ 批次8 总监逻辑完善（五项职责+三级继承+五步执行，真机 66/66） | 2026-09-11~12 | `docs/05` · `docs/06` |
| T-PLUG-005-S1 / S2 / S3 | R3 spike（fs 三法 `ALL_UNREACHABLE`）/ G 区宿主注入点清单（6 写+6 读+5 调用，结论「不迁只保」）/ 启动踩坑登记（`ELECTRON_RUN_AS_NODE`） | 2026-09-11 | `docs/02` · README §启动 |
| T-PLUG-005-S4 / S5 | 宿主缺陷登记（E1 `filteredMessages` 越界引用，插件侧已修）+ 双份实例遗留（已登记为可接受过渡态） | 2026-09-11 | — |
| T-PLUG-008 | apply/restore/deploy 支持插件部署单元：`plugin-install.mjs`（verify/apply/uninstall/purge）+ `INSTALL.md` + `verify-install-clean.mjs` **52/52**；捕获 `PAYLOAD_FILES` 漏 `package.json` 真缺陷；`deploy.ps1` 退役 | 2026-09-12 | `INSTALL.md` |
| T-PLUG-009 | IDB 数据兼容验证：`verify-data-safe.mjs` **42/42**（DB 版本恒 3 / store 恰 6 个 / 宿主 `memoryCore` 零 `getAll`·零 `delete` / 动态反证）；顺带修「约定失效即静默」（`isHierarchyNode` schema 白名单） | 2026-09-12 | `dsh-director-plugin/docs/07` |
| T-PLUG-010 | F-DATA-01 结案：冷启动 store 键「消失」= **假阳性**（观测口径缺陷）⇒ 脚本新增 [0] 就绪等待 | 2026-09-12 | `docs/07` §六 |
| T-PLUG-011 | `Network` 缓存误删缺陷族 3 处（`deploy.ps1`/`clear-cache.ps1`/`restart-harness.ps1` + 坑5 文档）—— 内含 `dsh_director_*` 冻结 cookie，误删即用户数据丢失 | 2026-09-12 | `INSTALL.md` §四 · `50-信息中心/01` 坑5 |
| T-PLUG-014 | 总监与对话架构总纲（业务不变量唯一真相源）+ 逻辑丢失五级归因 + 11 条要求落点 | 2026-09-12 | `docs/10` · `V14.1-弹窗式总监架构设计稿.html` |
| T-PLUG-015 | V14.1 设计稿修订（审美 10 项 + I1–I12）+ T1–T10 实现；修 6 个真缺陷；新增 `lint-cdp-templates`；真机 `cdp-click-dialog` 100/100 ×2 | 2026-09-12 | `docs/11` · `V14.1` |
| T-PLUG-016 | V15.1 画面与交互审核（画面 16 项 / 交互 13 项，6+5 P0 全修） | 2026-09-12 | `V15.1-画面与交互审核版设计稿.html` |
| T-PLUG-017 | V16 三件交付物：设计图（板块 A 5 视图）+ 需求图（板块 B 3 视图）+ 交互逻辑（板块 C 5 视图）；新增 `verify-design-html.mjs` 卫生 5/5 | 2026-09-12 | `V16-设计图·需求图·交互逻辑.html` |
| T-PLUG-018 | 设计图插件 DesignStudio 实现 + 顶栏补齐 / 保存·版本 / 窗口安全区（真机 `inset=138`） | 2026-09-12 | `V16` 板块 D/E · `verify-design-studio` 79/79 |
| T-PLUG-019 | 导图元素库 18 条总账 + ✕ 与原生窗口按钮「同位」修正；修 `inject` 漏声明 `sessions` 致血缘静默降级 | 2026-09-12 | `V16` 板块 F · `verify-mindmap` 80/80 ×3 |
| T-PLUG-020 | 批次11 单框控件/框可拖动/右侧展开对话/四维流转/四处个性化；捕获渲染进程顶死（指针派发记账 + 零超时断言） | 2026-09-12 | `V16` 板块 G · `verify-flow` 46/46 ×3 |
| T-PLUG-021 | 批次12 总监页背景采用原软件背景 · 风格统一（令牌桥接 6 个）；修 CSS `var()` 求值位置陷阱 | 2026-09-12 | `V16` 板块 H · `verify-flow` 53/53 ×3 |
| T-PLUG-022 | 批次13 浮动入口三件套（点击穿透 / 配色随主题 / 定位不漂） | 2026-09-12 | `verify-flow` 57/57 ×3 |
| T-PLUG-023 | 批次14 审核**测试自身**：跨运行污染 / 静态前提 / 平凡真 六缺陷全在闸门侧；新增`C17.1` 落点自检 + `C17.2` 环境复原 | 2026-09-12 | `verify-studio` 90/90 ×3 · `verify-mindmap` 86/86 ×3 |
| T-PLUG-024 | 批次15 总监侧输入 → 五步处理 → 真投递原生对话（**内容与语义分离**，`host-send` 首选通道 + 第三方证据 `__directChatProbe`） | 2026-09-12 | `verify-flow` G 段 · 67/67 ×3 |
| T-PLUG-025 | 批次15 分支链路默认只看本分支 + 「含上一层」+ 下钻 + 导图顶部总览弹窗 | 2026-09-12 | `verify-mindmap` 100/100 零跳过 ×3 |
| T-PLUG-026 | 批次15 右下角两颗按钮「点不中」根因（提示位顶动操作带 23px）⇒ 常驻等高占位槽 + `D5p` 逐像素闸门 | 2026-09-12 | `verify-flow` 67/67 ×3 |
| T-PLUG-027 | 批次15 交互基建：四个「点不中」真因（提示位/rAF 被遮挡暂停/折叠基数取错/视口外点击） | 2026-09-12 | `V16` 板块 J |
| T-PLUG-028 | 批次15 同步审核「审核本身」：5 处闸门过期 + 跳过 5→0（导图 81/0/5 → 100/0/0） | 2026-09-12 | AGENTS.md §闸门纠错台账 |
| T-PLUG-029 | 批次16 闸门纠错 8 处 + 属性语义化（`data-review-dim`/`data-flow-dim`）+ 平台桩唯一真相源 + 弹窗闸门复活（**崩 → 268/268**）；查明「文档上下文」死链路 | 2026-09-13 | `lint-platform-stub` 9/9 |
| T-PLUG-031 | 批次16 续：闸门**持久化写坏产品状态**修复（I0 快照 / I8r 逐节点还原 + 回读自证）+ 判据改「跑程内」+ 用法自诊断与 0/1/2 退出码 | 2026-09-13 | `verify-flow` 68/68 零跳过 · `cdp-click` 71/71 |
| T-CONV-001 / C1 / C2 / C3 / P2 | 双实现收敛：**用户拍板方案①**（收敛到插件）；批 C-1 覆盖核对 95/100 · 批 C-2 MOD-B 退坡 · **批 C-3 保留最小锚点 +《锚点契约》** · P2 DirectorView region 1,519 行移除 | 2026-09-07~11 | 29号文 · 17/21号审核记录 |
| T-PLUG-033（离线部分） | 第4批界面调整 5 条：取消 R1 · 去掉两个「全局总管」· 下拉=作用域联动 · 总监数据 per-scope 分桶 · 宿主残留两行显示层裁剪；4 个 CDP 闸门补 INVALID 自诊断（正负对照双绿） | 2026-09-14 | `V18` 板块 L · `verify-bundle` 97/97 |

### B. ⛔ 已失效 / 被取代（归档备查，不再跟踪）

| 编号 | 原任务 | 失效原因 |
|:-----|:-------|:---------|
| T-FIX-V001 / V002 | V9.4 / V9.5 人工验证清单（9 + 7 项） | 目标 UI 已随 **MOD-B 退坡**移除，验证义务按 N-3.2 转移至插件对应功能 |
| T-V9D-004..010 | V9 设计批次2/3/4（D-04 总览卡片 · D-05 R4 三 Tab · D-06 记忆持久化 · D-07 R3 技能 · D-08 R7 三模式 · D-09 异常降级 X1–X11） | 目标界面已由插件 **R1–R8 重做并交付**（批次 10–15）；旧 R 编号与插件现结构已不对应 |
| T-P2AUDIT-001 | V9.4 审计 P2×13 项 | 目标宿主代码已退坡 / 迁移；条目指向的宿主行号（3574/6329/8892…）在现产物中已无对应 |
| T-P3-001 / P4-001 / P4-002 / P5-001 | 阶段3 自动转发联动 · 阶段4 三面板/设置面板 · 阶段5 样式+思考过程+自动审核 | 已由插件批次 9–15 实际交付（真流转 `host-send` · 智能体/技能面板 · 四处个性化 · 五步含自动审核） |
| T-PLUG-005 | 「MOD-B 迁移 → 插件包」总任务 | 其子批次 B1–B9 均已完成；**唯一未完成的 B6（宿主退坡）已单列保留** |
| T-PLUG-006 | MOD-A 可迁移部分迁移，ChatView 留最小锚点 | 已由 **T-CONV-C3《锚点契约》** 收口（保留最小锚点 + CDP 实证） |
| T-PLUG-007 | 补丁减薄 ≥70% +《锚点契约》文档 | 减薄已由批次 1（宿主 **-61.8%**）+ 收敛批 C 达成；《锚点契约》已随 C-3 落地 |
| T-PLUG-013 | 页面交互重设计：原生 Tab 集成 | 用户 2026-09-12 明示「**两者均为弹窗形式，并允许最小化**」⇒ 本行形态**作废**；其 §五 技术证据链保留为「入口备选通道」 |
| T-AESTH-001 | 审核快赢项 Q2-2（数据字段 hybrid 渲染 + BranchMindMap/DirectorChat + 下拉 items） | 指向的组件已随批次 10–15 重写；原审核记录（15/16/18/19号）已由批次 14/15/16 的闸门审核取代 |
| T-CONV-P2（重复行） | DirectorView 164 引用清理 | **编号重复**且 DirectorView region 已整段移除（见上方 T-CONV-P2 已完成行） |
| T-CONV-V3 | V10.2 第三批（插件 src：Q2 全量图标 / Q4 渐隐 / Q5 卡片结构） | 目标宿主 UI 已退坡；对应观感已由批次 12/13 风格统一 + 浮动组重做取代 |

## 2026-09-13 — 批次 16 续（**闸门持久化污染** + 跑程内判据 + 用法自诊断）

| 编号 | 任务 | 完成时间 | 关联文档 |
|:-----|:-----|:--------:|:---------|
| T-PLUG-031 | 闸门「持久化写坏产品状态」修复：`cdp-click` 新增 **I0 快照 / I8r 逐节点还原 + 回读自证**（清掉快照里没有的 own 配置）⇒ 复位被污染的两级（`__global__` / `ws_*`），全树 23 节点 `own=[]`；**66→71/71** | 2026-09-13 | `10-当前基线-落死锚点-V16.md` §7.11.8 · 纪律 41/42 |
| T-PLUG-031 | 判据改「**跑程内**」：`verify-flow` G3a/G3b 只读 `dp-dir-msg` **最后一条** + 新增 **G3a 前置**；`cdp-click` 轮询要求**行数净增 ≥ 2** ⇒ **68/68 零跳过** | 2026-09-13 | 踩坑索引 坑30 |
| T-PLUG-031 | 用法自诊断 + 退出码约定：`verify-dialog` 缺 `--import` 时打印正确命令并 `exit 2`；钉死 **0 通过 / 1 FAIL / 2 INVALID** | 2026-09-13 | 踩坑索引 坑31 |
| T-PLUG-029 | 批次 16 上半场：闸门纠错 **8 处** + 属性语义化（`data-review-dim` / `data-flow-dim`）+ 平台桩唯一真相源（`_platform-modules.mjs` + 闸门 `lint-platform-stub` **9/9**）+ 弹窗离线闸门复活（**崩 → 268/268**） | 2026-09-13 | `10-…锚点-V16.md` §7.11 |

## 2026-09-12 — 批次 15（总监真流转 + 分支聚焦 + 总览弹窗 + 闸门纠错 5 处）

| 编号 | 任务 | 完成时间 | 关联文档 |
|:-----|:-----|:--------:|:---------|
| T-PLUG-028 | 闸门纠错 **5 处**（batch1 双向一致性 / batch6 版本序比较 / bundle 单调性 / batch8 五步全启用 / mindmap 聚焦退出 + `blankPoint()`）⇒ 导图 **81/0/5 → 100/0/0**（多出 19 条真断言）；跳过 5 → 0 | 2026-09-12 | `AGENTS.md` §闸门纠错台账（一） |
| T-PLUG-015-b | 总监侧输入经五步处理**真正投递到原生对话**（`data-deliver-mode` 四态 · 三通道 · 宿主侧凭据 `__directChatProbe` · G4 空输入反证）；分支链路聚焦；导图总览弹窗 | 2026-09-12 | `10-…锚点-V16.md` §7.10 |


## 2026-09-06 — 整改 V1.2 自动执行段（任务 #1-#10 已落地）

| 编号 | 任务 | 完成时间 | 关联文档 |
|:-----|:-----|:--------:|:---------|
| T-V12-A01 | 顶档配置同步：config.json harness_version rc.5→rc.11 + version 1.1.0→1.2.0 + last_modified | 2026-09-06 | INDEX.md §七 + MEMORY.md §九.2 |
| T-V12-A02 | README.md 版本号同步：V1.0→V1.1 + Harness rc.5→rc.11 | 2026-09-06 | README.md |
| T-V12-A03 | config/ 路径一致性核对：无 mismatch | 2026-09-06 | 整改总览 §三 #2 |
| T-V12-A04 | INDEX.md 大幅更新：V10 V3 完工 + 75 文档 + 46/46 测试 + 整改段 | 2026-09-06 | INDEX.md §一§五§七 |
| T-V12-A05 | MEMORY.md §五 状态快照：2026-09-06 + V10 V3 完工 + 测试门禁 ✅ | 2026-09-06 | _memory/MEMORY.md §五 |
| T-V12-A06 | MEMORY.md §九 整改 V1.2 完整记录：触发/范围/教训/沉淀 | 2026-09-06 | _memory/MEMORY.md §九 |
| T-V12-A07 | 工作快照 06-工作快照.md：补全整改 V1.2 章节 | 2026-09-06 | 00-统筹入口/06-工作快照.md |
| T-V12-A08 | git 仓库初始化：.gitignore (8 类排除规则) + 首次 commit 9a732ca (224 files, 38755 insertions) | 2026-09-06 | 整改总览 §三 #6 |
| T-V12-A09 | _memory/_snapshots 启用：写入 README.md（命名规范+触发条件+归档规则） | 2026-09-06 | _memory/_snapshots/README.md |
| T-V12-A10 | _memory/_archive 启用：写入 README.md（季度蒸馏归档工作流） | 2026-09-06 | _memory/_archive/README.md |
| T-V12-A11 | scripts/ 探针审核报告：91 个文件分类 A-E，候选清单 53 文件 | 2026-09-06 | 40-测试质量/12-scripts探针审核-20260906.md |
| T-V12-A12 | 整改总览文档：12 项任务 + 三级审核 94/100 | 2026-09-06 | 20-任务文档/23-dsh-整改总览-V1.2-20260906.md |
| T-V12-A13 | 待办清单更新：03-待完成任务清单 新增 T-V12-001~008 + T-V12-101~104 | 2026-09-06 | 00-统筹入口/03-待完成任务清单.md |

### 整改 V1.2 教训沉淀（已固化为 §九.4）
1. 方法论锚定：整改任务必先选方法论（execution-unified-standards L3 12 步）
2. 风险分级+显式列出：低/中/高 3 档分风险，中高风险必须用户确认
3. 配置同步盲点：MEMORY.md 早就写了但 config.json 等滞后——暴露「记录先行但执行滞后」
4. AI 自审 ≠ 用户确认：两技能冲突时按用户原始指令优先（execution-unified-standards）
5. git 应早建晚用：项目 2 周多次 apply 但 git 一直没建 — 整改进度变简单

### 待确认清理候选（不擅自执行，等用户勾选）

## 2026-09-06 — 整改 V1.2 清理段（任务 #1-#7 已落地 + git push 待确认）

| 编号 | 任务 | 完成时间 | 关联文档 |
|:-----|:-----|:--------:|:---------|
| T-V12-A14 | T-V12-001：scripts/_archived-20260831/ 删除（28 文件, 90KB），scripts/ 由 91→63 文件 | 2026-09-06 22:35 | 40-测试质量/12-scripts探针审核-20260906.md |
| T-V12-A15 | T-V12-002+003+004：scripts/ 一次性探针批量删（49 文件, ~140KB），含 check-*×8 + activate-*×4 + 截图/操作×27 + V10 V3 辅助×10，scripts/ 由 63→15 文件 | 2026-09-06 22:36 | 同上 §三 |
| T-V12-A16 | T-V12-005：backups/dsh-director-backup-{004910,src-backup-004910} 删除（2 目录 ~40MB），释放 backups/ 大量 node_modules | 2026-09-06 22:38 | 20-任务文档/23-dsh-整改总览-V1.2-20260906.md §五 |
| T-V12-A17 | T-V12-006：backups/client.js.tmp + client-v2.js 删除（2 文件 555KB），仅 client.js.bak-20260901-zb2 保留为版本回滚锚点 | 2026-09-06 22:37 | 同上 |
| T-V12-A18 | T-V12-007：snapshots/manual-bak-batch1/ 删除（1 文件 583KB），snapshots/ 仅剩自动快照目录 | 2026-09-06 22:37 | 同上 |
| T-V12-A20 | MANIFEST 入仓：_memory/_archive/pre-clean-V12-20260906/MANIFEST.md（commit 2d75531，204 insertions）| 2026-09-06 22:30 | _memory/_archive/pre-clean-V12-20260906/MANIFEST.md |
| T-V12-A21 | CP4 后置审核：5 项清理 grep 0 hits 验证 + scripts/ 15 文件对齐审核报告 §六最终建议 | 2026-09-06 22:39 | 本文件 §整改 V1.2 清理段 |

### 清理段总结
- **删除总数**: 80 文件 + 2 目录 ≈ 12873 项（文件+目录）, 释放 ~41MB
- **scripts/**: 91 → 15 文件（-83%），完整对齐审核报告 §六最终建议
- **backups/**: 5 项 → 1 项（client.js.bak-zb2 保留为版本回滚锚点）
- **snapshots/**: 移除 manual-bak-batch1，仅剩 snapshot.ps1 系统化快照
- **风险处理**: 不可逆操作前 MANIFEST.md 入仓（commit 2d75531），万一需恢复有据可查
- **git push 待用户确认**: T-V12-008（🔴 高风险）
- T-V12-001~005：scripts/ + backups/ 临时文件清理（约 91 文件，约 1.5MB）
- T-V12-006：snapshots/manual-bak-batch1 处理决策
- T-V12-007：git push 到 GitHub（凭据 store/manager）
- T-V12-008：.gitattributes 配置（CRLF 处理）

---

## 2026-09-06 — V10 总监控制台 V3 全量整改与测试

| 编号 | 任务 | 完成时间 | 关联文档 |
|:-----|:-----|:--------:|:---------|
| T-V95-001 | A 配色令牌化：z tab b1 小窗+b-1 顶栏 14 处硬编码浅色 → var(--dsh-ac)/--dsh-bg1/--dsh-bg2/--border/dsw-alias-label-*，新增 3 个令牌定义，设置面板改色联动 | 2026-09-01 | 20-任务文档/22-z-tab交互显示规范化调整记录-V9.5.md |
| T-V95-002 | B 显示清理：移除 b1 顶栏「V9 · 驾驶舱」版本徽标 pill + 硬编码项目名 → 动态读取 currentProject | 2026-09-01 | 同上 §三 |
| T-V95-003 | C 操控性强化：拖拽手柄热区 5→9px+hover 主色高亮+title；折叠/展开按钮点击区+title；记忆面板动态 title（共 4 处） | 2026-09-01 | 同上 §四 |
| T-V95-004 | 两批 apply（快照 153741/154732）+ node --check + grep 自证/反证；【未做自动视觉验证】visual-ai 不可用，降级人工 | 2026-09-01 | 同上 §五 |

---

## 2026-09-01 — zb2 滚动失效修复 + 全量审计 P1×7 修复（V9.4）

| 编号 | 任务 | 完成时间 | 关联文档 |
|:-----|:-----|:--------:|:---------|
| T-FIX-001 | zb2（对话 tab 正常对话主窗口）无法滚动根因诊断：V9.1 钉死 viewArea 高度 + 分屏外壳 overflow:hidden 双重破坏 scrollBody 统一滚动机制 | 2026-09-01 | 20-任务文档/21-zb2滚动失效修复与全量审计P1修复记录.md |
| T-FIX-002 | zb2 自足滚动修复（V9.4，4 处）：scrollerOf 前置识别 data-chat-local-scroll + zb2 链补 minHeight:0/overflowY:auto + ChatFlow 补 minHeight:0 | 2026-09-01 | 同上 |
| T-FIX-003 | 全量修改审计（original vs workspace 21 个 js 对比，仅 client.js 有差异）：P0 无 / P1×7 / P2×13，语法校验通过 | 2026-09-01 | 同上 §四 |
| T-FIX-004 | P1×7 全部修复（V9.4-P1）：setState 未定义/轨迹tab发送劫持/降级发错目标/三处无条件滚底/Ollama 无超时无锁/hydrate 竞态丢消息/记忆面板 XSS | 2026-09-01 | 同上 §四 |
| T-FIX-005 | 两批 apply 应用（快照 144304/145719）+ node --check + grep 自证/反证；【未做自动视觉验证】visual-ai 不可用，降级人工确认 | 2026-09-01 | 同上 §五 |

---

## 2026-09-01 — V9 总监驾驶舱 UI 设计（三阶段九审制，全流程完成）

| 编号 | 任务 | 完成时间 | 关联文档 |
|:-----|:-----|:--------:|:---------|
| T-UI-001 | 设计流程方案：三阶段（简图/功能/样式）× 每阶段三轮审核 + 8个补全点（基线冻结/三轮分工/简图三层/HTML原型/元素级映射/收敛标准/交付对接/异常态） | 2026-09-01 | 20-任务文档/18-总监驾驶舱UI设计方案-简图功能样式三阶段.md |
| T-UI-002 | 阶段1简图三轮：R1框架 → R2修订（纵向堆叠+智能体/技能切换+插件边界） → R3内部元素交互态 → R4边界异常四态；收敛，D1=a-2右栏冻结 | 2026-09-01 | 30-开发链路/V9-总监驾驶舱-框架简图-r1.html |
| T-UI-003 | 阶段2功能映射三轮：附录E元素级映射（40+元素/12项能力全覆盖） → 附录F交互细则（13状态字段/数据流时序） → 附录G异常降级（X1-X11）；D2=技能静态打包、D3=docs-index.json注入 冻结 | 2026-09-01 | 同上 附录E/F/G |
| T-UI-004 | 阶段3颜色样式三轮：附录H配色令牌（Harness暗色+设置可调主色4/记忆色3/密度2） → 字体间距组件规范 → 极端内容鲁棒性9场景；产出暗色高保真视觉稿 | 2026-09-01 | 30-开发链路/V9-总监驾驶舱-视觉稿-r1.html |
| T-UI-005 | 阶段4开发拆解：10任务（D-01~D-10）4批次 + 验收总清单；方案定稿V2.0 | 2026-09-01 | 同上 附录I |

### 设计冻结决策
- D1：a-2 右栏（废弃 V9.1 草图的对话区下方方案）
- D2：技能清单静态打包进 client.js（V10+ 再外部化）
- D3：docs/ 读取通道 = apply.ps1 生成 docs-index.json 注入
- 响应式：clamp + 插件区断点 880/640；配色：Harness 暗色令牌 + 设置面板可调

---

## 2026-08-27 — 项目标准整改+行为逻辑框架同步+保障机制集成

| 编号 | 任务 | 完成时间 | 关联文档 |
|:-----|:-----|:--------:|:---------|
| T-STD-001 | 创建项目级AGENTS.md（含项目特殊性Harness修改铁律+10步链路+6条约束+行为逻辑V1.2引用+保障机制四层引用+项目特殊检查点） | 2026-08-27 | AGENTS.md |
| T-STD-002 | 创建_memory/MEMORY.md（含5个ADR+风险注意事项+踩坑记录索引+项目状态快照+后续优化方向） | 2026-08-27 | _memory/MEMORY.md |
| T-STD-003 | 同步最新规范文档：AI对话执行约束规范 V1.0→V1.3 | 2026-08-27 | docs/00-统筹入口/AI对话执行约束规范.md |
| T-STD-004 | 新增AI行为逻辑框架审核文档（V1.0→V1.2全量审核记录） | 2026-08-27 | docs/00-统筹入口/AI行为逻辑框架审核文档.md |
| T-STD-005 | 新增AI规范执行保障机制（四层保障：启动加载协议+执行检查点CP1-CP4+行为逻辑触发表+违规自检） | 2026-08-27 | docs/00-统筹入口/AI规范执行保障机制.md |
| T-STD-006 | 双项目同步：cube-sailor规范文档同步+AGENTS.md更新+保障机制集成 | 2026-08-27 | D:\workspace\cube-sailor\ |
| T-STD-007 | 灵活配置优化补充：保障机制V1.1（简单任务快速通道+渐进式启用Level1/2/3）+执行约束V1.4（适用范围）+workspace AGENTS.md新增§四 | 2026-08-27 | AI规范执行保障机制.md V1.1 |
| T-STD-008 | 文档审核标准+逻辑思维迭代：创建文档审核标准V1.0（11章完整标准）+逻辑迭代V1.0（10个遗漏领域+6个新增机制+三阶段路线图）+执行约束V1.5+同步到本项目 | 2026-08-27 | 文档审核标准与生命周期管理.md V1.0 |
| T-STD-009 | 第一阶段规范同步：风险管理规范V1.0+风险登记册+应急回滚规范V1.0+人机协作规范V1.0同步到本项目 | 2026-08-27 | 项目风险管理规范.md 等6个文档 |
| T-STD-010 | 项目整改：创建INDEX.md+清理3个临时图片文件+补全工作快照 | 2026-08-27 | INDEX.md / 06-工作快照.md |
| T-STD-011 | ns-workspace项目标准整改：创建AGENTS.md（含NetSuite多客户特殊性+代码-文档分离）+INDEX.md+同步9个规范文档+补全工作快照 | 2026-08-27 | ns-workspace/AGENTS.md / INDEX.md |
| T-STD-012 | 第二阶段规范同步：度量改进规范+决策管理规范+决策日志+健康度仪表盘+改进项清单+环境依赖管理规范，共6个文档同步到本项目 | 2026-08-27 | 项目度量与改进规范.md 等6个文档 |

### 整改成果
- ✅ 项目规范体系完整：AGENTS.md+MEMORY.md+12份核心文档+3份AI规范文档
- ✅ 行为逻辑框架V1.2已同步：7维度25条（含示例+例外/豁免+操作步骤+准则关系）
- ✅ 四层保障机制已集成：确保AI"只读该读的、执行该执行的、按标准执行"
- ✅ 项目特殊性已定义：Harness客户端修改5条铁律+高风险操作防护+环境依赖

---

## 2026-08-24

| 编号 | 任务 | 完成时间 | 关联文档 |
|:-----|:-----|:--------:|:---------|
| T-INIT-001 | 工作区目录结构创建（config/docs/scripts/original/workspace/patches/snapshots/logs/_memory） | 2026-08-24 | 10-架构设计/00-架构说明.md |
| T-INIT-002 | config.json配置（8个目标包+路径+规则） | 2026-08-24 | config.json |
| T-INIT-003 | 核心库dsh-mod-lib.ps1（Junction操作/store/日志/状态检查） | 2026-08-24 | scripts/dsh-mod-lib.ps1 |
| T-INIT-004 | 8个自动化脚本（backup/init-workspace/apply/restore/status/diff/snapshot） | 2026-08-24 | scripts/*.ps1 |
| T-INIT-005 | 首次备份（8个包：layout/conversation/trajectory/sidebar/slots/runtime/web-react/web） | 2026-08-24 | original/@deepseek-ai/ |
| T-INIT-006 | 工作区初始化（从original/复制8个包到workspace/） | 2026-08-24 | workspace/@deepseek-ai/ |
| T-INIT-007 | Junction功能验证（创建/读取/删除/复原，修复Remove-Item bug） | 2026-08-24 | 临时测试 |
| T-INIT-008 | 全部脚本语法检查 + diff.ps1验证（0文件差异） | 2026-08-24 | - |
| T-INIT-009 | 架构文档（00-架构说明/01-修改记录模板/02-复原手册） | 2026-08-24 | docs/30-开发链路/ |
| T-DOC-001 | 总监对话模式开发文档（总方案+5阶段） | 2026-08-24 | docs/20-任务文档/03-总监对话模式开发文档.md |
| T-DOC-002 | 阶段1详细开发文档（M1~M5精确到行号+代码） | 2026-08-24 | docs/20-任务文档/04-阶段1详细开发文档.md |
| T-DOC-003 | 自我审核（发现sendToChat链路bug+hooks格式问题，已修正） | 2026-08-24 | - |
| T-STD-001 | 项目标准化配置整改（标准目录结构+config+7份核心文档+文档归类） | 2026-08-24 | docs/00-总统筹入口文档.md |
