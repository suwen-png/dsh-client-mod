# 工作快照 — dsh-client-mod

> 当前任务/中断位置/下一步。断点恢复依据。

## 当前状态（2026-09-14 批次 19：**备份清理 115.84 MB + 批次 18 遗留收口**）

### 状态一句话
**用户「备份的东西这么多么，清理下备份」⇒ 按用户勾选的「推荐档」清掉 101 项 / 115.84 MB 过程产物；
`snapshots/` 由 **115 MB** 收敛到 **1.5 MB**（只剩 B6 专用回滚点）。**
同轮把批次 18 的两项遗留（真机 7 套全绿 · 基线重封）跑完收口。

### 本轮结论（可直接引用的数字）
| 项 | 结果 |
|:--|:--|
| 清理范围（推荐档） | 58 个 `snapshot-*-before-apply`（**107.08 MB**）· `logs/_archive` 未被文档引用的 23 张 png（4.50 MB）· `logs/audit/`（2.76 MB）· `backups/client.js.bak-20260901-zb2`（1.14 MB）· 根 `logs/` 18 项旧运行日志（0.35 MB） |
| 合计释放 | **101 项 / 115.84 MB**（`statSync` 字节数；`du` 报 114 MB 含簇对齐）· 顶层总量 ≈137 MB → **17 MB** |
| 硬约束保留 | `snapshots/host-b6-20260911-before-patch`（**T-PLUG-005-B6 唯一回滚点**）· 锚点文档点名的 5 张取证图（`bg-before/v12/final-director.png` · `zoom-launcher/dock-fixed.png`）· `visible.png`/`studio.png` · 24 个 verify 日志 · `original/`+`workspace/`（**被 `apply.ps1`/`diff.ps1`/`verify-batch1.mjs` 实读**）· `logs/playwright-tests/test-report.json`（git 跟踪） |
| 守卫设计 | 清理脚本分两层断言：`PROTECT_SELF`（**仅本身**不可删 —— `snapshots/`、`_archive/` 容器，子项可删）与 `PROTECT_TREE`（**整树**不可动 —— 命中本身/祖先/后代都算违规）。dry-run 先列清单，任一命中 ⇒ **exit 2 且零删除**。首版曾把「删子项」误判成违规（守卫必须正负对照的现场教材） |
| 批次 18 遗留收口 | ✅ 真机 7 套全绿：`verify-flow` **73/73 零跳过**（含**新增【H】段 5 条**，补上「宿主残留两行裁剪」此前**零闸门覆盖**）· `verify-mindmap` 100/100 ×2 · `verify-design-studio` 91/91 零跳过 ×2 · `cdp-verify` 39/39 · `verify-data-safe` **51/51**（新增 Cookie 体积常态判据）· `cdp-mouse sweep ds-top` 零位移 ×3 · ✅ 基线重封 **65 文件 / 2,000,143 B**（复验漂移 0） |
| 台账 | `03` 三清至 **4 组 / 9 条**（表头声明 = 实际行数，闸门已验）；`verify-ledger-table` 新增「**条数声明 vs 实际行数**」对账（此前表头长期虚报 1 条无人发现） |
| 未动 | 本轮 `src/**` 与 `lib/client.js` **零改动** ⇒ `baseline-check` 漂移 0，**无需重封** |

### 遗留（下一步）
1. **`T-PLUG-032` 第 3 批剩余 9 条** —— 按用户「先出设计图，我看看没问题再做」口径，**开工前需一次确认**。
2. **`T-PLUG-005-B6` 宿主退坡** —— 待授权（改 `workspace/**`，git 不可回滚）。
3. `config/assets.json` 的 `snapshots: 57` 是 `apply.ps1` 的自动产物，与快照同命，**不手改**（下次 apply 会重写）。
4. 候选（待用户点头）：`04-已完成任务清单.md` 是**纯 CRLF**（`git ls-files --eol` = `i/crlf w/crlf`，248/0；同目录 `03`/`06` 均为 LF）⇒ 可归一为 LF 与邻档一致，但会产生整篇重写 diff，须独立 commit（与 `verify-data-safe.mjs` 的 CRLF 归一属同类，可合并一批处理）。

---

## 历史状态（2026-09-14 批次 18：**第 4 批界面调整 5 条 — 代码已落地**）

### 状态一句话
**用户审图后给出 5 条修正，当日全部落地到 `src/**`；离线闸门全绿，真机 7 套按用户要求暂缓。**
核心是把「作用域」收敛成**单一真相源**（下拉选中的节点决定总监数据与对话数据落在哪个桶），
顶栏整行取消、两个控件并入 R2.5，宿主残留两行改为**显示层裁剪**（不动宿主源码）。

### 本轮结论（可直接引用的数字）
| 项 | 结果 |
|:--|:--|
| 5 条修正 | ① 顶栏定稿（取消 R1）② 去掉两个「全局总管」③ 下拉=作用域联动 ④ 总监数据 per-scope 分桶 ⑤ 宿主两行显示层裁剪 |
| 新增文件 | `src/bridge/host-panel-trim.js`（显示层裁剪，可逆 + 降级上报） |
| 改动文件 | `src/components/DirectorPage.js` · `src/store/hierarchy.js`（+ 作用域 API）· `src/components/DirectorWorkbench.js` · `src/client-entry.js` · `scripts/verify-flow.mjs`（哨兵 `dp-r1`→`dp-root`）· `scripts/verify-walk.mjs`（清死模式）· `scripts/verify-data-safe.mjs` / `verify-mindmap.mjs` / `verify-design-studio.mjs` / `shot-studio.mjs`（补 INVALID 自诊断） |
| 离线闸门 | 单测 6 套 **branch-focus/flow/design-logic/design-version 63·63/mindmap/personalize 全绿** · `verify-bundle` **97/97** · lint 阻塞级 **0** · `test-orchestrate` **33/33** · `lint-cdp-templates` / `lint-platform-stub` **9/9** / `prove-dup-gate` 反证成立 |
| 构建 | `lib/client.js` **1,014,691 B** · 语法自检通过 · import 对账 **498 = 498**（189 条） |
| 闸门自诊断 | 5 个连 CDP 的脚本补 **正负对照**：9222 关闭 → 「连不上 CDP」+ exit 2；9222 通但回 `[]` → 「CDP 无 page 目标」+ exit 2、零栈行（后者证明 try 未吞正常路径） |
| 行尾核查 | 本轮 `src/**` 与新建文件**全 LF**（字节级统计 crlf=0）；`verify-data-safe.mjs`(504/0) 与 `lib/client.js`(975/17344 混合) 属**既有事实**，非本轮引入，登记为候选（不擅自改）|
| 设计稿 | `V18-界面调整设计稿.html` 增至 **52,959 B**（新增板块 L）· 卫生闸门 **5/5** |
| 真机 | ⏸ **未跑**（用户「测试需要等等」；Harness 未启动）⇒ `verify-flow` / `verify-mindmap` / `verify-design-studio` / `cdp-*` 全部待补 |

### 遗留（下一步）
1. **真机 7 套**待跑（需先 `restart-harness.ps1`，确认端口号变化）。
2. ✅ ~~`verify-data-safe.mjs` 崩栈~~ **已修**（同批 5 个脚本一起补，正负对照双绿）。
3. 第 3 批其余 9 条未做：对话列浮窗 · R3 去除 + 模型前置 · R4/R7 左右缩回 · R5 语义统一 · R7 执行功能区 · 对话框左右清单。
4. 基线指纹仍**待重封** —— 按命令链顺序，须**真机跑完**再 `baseline-check --write`（提前封会把未验证状态盖上"落死"戳）。
5. 候选（待用户点头）：`scripts/verify-data-safe.mjs` 行尾 CRLF 归一为 LF（消除 `git diff --check` 逐行 trailing-whitespace 噪声与「假重写」陷阱）。

---

## 历史状态（2026-09-14 批次 17：V17 提交 + 第 3 批调整设计稿 V18 · 代码未动）

### 状态一句话
**V17 迭代已提交（仅本地）；该轮只出设计图，`src/**` 零改动。**
设计图 `docs/50-信息中心/V18-界面调整设计稿.html` 覆盖用户 **14 条调整需求**，
卫生闸门 **5/5 一次通过**，三轮审核 **7/10 → 9/10 → 10/10**。

### 该轮结论
| 项 | 结果 |
|:--|:--|
| 提交 | `02a2b36` feat(v17)（23 文件）· `70ac646` fix(gate)（2 文件）· `bdbfbd0` docs(v18) —— **仅本地，未推远程** |
| 版本号纪律 | 全仓 **5 处**修正为「**允许新开版本号，禁止架构重写**」 |
| 该轮 3 个决策点 | 已由用户于批次 18 拍板（去/方案甲/只在页面不显示） |

## 历史状态（2026-09-13 批次 16：闸门纠错 8+3 处 · 平台桩唯一真相源 · **闸门持久化污染修复**）

### 状态一句话
**插件仍为唯一交付物，本轮 `src/**` 零改动 ⇒ 产物逐字节不变、基线指纹不需重封（`漂移=0 · 64/64`）。**
工作全部落在**闸门自身**：修掉 8 处过期/写死断言、抽出**平台桩唯一真相源**（+ 新闸门 9/9）、
把属性命名空间拆开（`data-review-dim` / `data-flow-dim`），并解决一条**最重的** ——
**闸门会持久化改坏产品状态**（`cdp-click` 的写入链把「五项全关」永久写进全局/工作区两级）。

### 本轮结论（可直接引用的数字）
| 项 | 结果 |
|:--|:--|
| 离线闸门 | **22 套全绿**（新增 `lint-platform-stub` **9/9**；`verify-dialog` **崩 → 268/268**；`verify-install` **32/33 NO → 60/60 YES**；`verify-design-doc` **49/49** + 非 V14 族 `INVALID`） |
| 真机 | `cdp-verify 39/39` · `verify-flow **68/68 零跳过**` · `verify-mindmap 100/100 零跳过` · `verify-design-studio 90/90` · `cdp-click **71/71**` · `cdp-click-dialog **102/102**` · `verify-register-gate TRUE` |
| 构建 / 指纹 | **964,872 B / 58 模块 · 绑定 474 = 474（185 import）** · 基线 **漂移=0 · 64/64** · 装机 `IS_PASS: TRUE` |
| 退出码约定 | **0 通过 / 1 FAIL（目标不合格）/ 2 INVALID（用错用法或目标）** |

### 本轮最重的三处（详见 `10-当前基线-落死锚点-V16.md` §7.11 + §八 纪律 41/42）
1. **闸门持久化写坏产品状态**：`cdp-click` 的 I4→I8 是一条真实写入链（翻转开关 → 保存本层 → 向上提交 → 恢复继承只清会话那层）⇒ 「五项全关」被永久写进 `__global__` / `ws_*` **两级**，跨脚本跨运行活着，**连用户真实使用总监都被改坏**。
   修：**I0 快照 → I8r 逐节点还原 → 回读自证**（写回快照里有的 / 清掉快照里没有的 / 对账 23 节点 0 不一致）。已复位。
2. **持久化列表上的页面级判据**：消息 store 按 sessionId 持久化 ⇒ 对整段 `innerText` 做正则**假绿与假红双向**；`verify-flow` 的 G3a/G3b 甚至一红一绿。修：读 `dp-dir-msg` **最后一条** + 轮询要求**行数净增 ≥ 2** + 新增 **G3a 前置**。
3. **退役对象把脚本打哑**：`verify-dialog` 中段 null 解引用 ⇒ 其后 **100+ 断言静默丢失**。修：契约式断言 + 用法自诊断（缺 `--import` 时打印正确命令并 `exit 2`）。

### 下一步（已登记，非本轮范围）
- **T-PLUG-030**「文档上下文」死链路：`docs` 是 **map** 却当数组用（宿主 `client.js:6757` + 插件副本 `logic/process.js:104`）⇒ 文案恒「共 undefined 篇文档」+ TypeError 被 `catch {}` 吞掉；剥离前快照**同样是 map** ⇒ 自诞生即坏；现行五步 `director-run.js` 根本没读文档 ⇒ **随 `T-PLUG-005-B6` 宿主退坡一并处理**。

---

## 历史状态（2026-09-12 T-PLUG-015 V14.1 设计稿修订 + 总监弹窗实现交付）

### 状态一句话
**`docs/11` §六 T1–T10 清单已全部闭环。** 设计稿修订为 **V14.1**（审美修订 10 项 + 交互缺口 I1–I12 全部落实）、
`docs/10` §四 交互规格同步（新增 4.2.1 / 4.6 / 4.7 / 4.8）、`docs/11` 增补反模式 12 条 + 交付记录。
**真机 100/100 ×2 轮 · 离线 9 套 + 设计稿校验 49/49 全绿。**

### 本轮交付
| 文件 | 变更 |
|:--|:--|
| `docs/50-信息中心/V14.1-弹窗式总监架构设计稿.html`（新建 · 91,445 B / 1,198 行） | 落实 §1.1 修订 10 项 + §二 I1–I12；含「本版修订摘要」两张大表；写入真机实测值 |
| `dsh-director-plugin/scripts/verify-design-doc.mjs`（新建 · **49/49**） | 设计稿离线校验：字号底线 / 标签平衡 / 10 项修订 / I1–I12 / 可追溯性 / **反向断言 7 条**；支持 `node scripts/verify-design-doc.mjs V14` 做**反向证伪**（旧稿 **31 项失败**） |
| `dsh-director-plugin/docs/10-总监与对话架构总纲.md`（改 · 498 → 600 行） | §4.1 表格改「非模态覆盖层 + 右栏通道=方案 C」；新增 **4.2.1 焦点判定粒度**、**4.6 键盘路径**、**4.7 折叠态控制可达性**、**4.8 分屏锚点判据（六条 + 真机实测表）**；订正 §五/§九/§十/附录A 中已被否决的 `renderSlot` 通道 |
| `dsh-director-plugin/docs/11-设计稿审核与实现方案.md`（改） | 新增 **§八 反模式 12 条**（E-SPLIT-001~004 / E-TPL-001 / E-DOM-001~002 / E-TEST-001~003 / E-DOC-001）+ **§九 交付记录与测试总账** |
| `docs/00-统筹入口/03-待完成任务清单.md`（改） | T-PLUG-014 收官为 ✅（3 项待裁全部就地裁定）；新增 **T-PLUG-015** 记录本轮交付 |

### 🔴 本轮修复的 6 个真缺陷（全部有断言防线）
| # | 缺陷 | 后果 | 防线 |
|:--:|:--|:--|:--|
| ① | `E-SPLIT-001` 分屏锚点命中**插件自身 DOM** | "右栏完全一致"**全程是假的**（padding 加在自己头上） | `isPluginNode()` + 三条锚点前置 continue + `applySplit` 护栏；离线 C36/C39/H10b–e；真机 D1 |
| ② | `E-SPLIT-002` 兜底锚点用 `input[type=text]` | 命中侧栏搜索框 ⇒ 向上爬到侧栏 | 兜底只用 `textarea` + 宽度下限 40%vw；离线 C32/C35 |
| ③ | `E-SPLIT-003` `padding` 挂到**滚动容器** | 横向溢出 | `isScrollContainer()` 跳过 + 取最深合法祖先；离线 C31/C33/C37/C38 |
| ④ | `E-SPLIT-004` 折叠态下**四个控制按钮全部不可达** | 最小化后无法还原（只剩快捷键） | rail 内置控制簇 + 各自 `stopPropagation`；真机 D8 `railCtl [true,true,true,true]` |
| ⑤ | 拖拽 `down→move→up` 同 task 内位移**静默丢失** | 快速甩动看不见反应 | `calcDragW()` 抽取 + `onDragEnd` 用 pointerup 坐标兜底重算；真机 D9b |
| ⑥ | `listAgentRuns()` 静默截断 | 断言误判「缺记录」 | 无参返回全量 + 渲染层截断 + `data-run-total`；真机 D4 顺序判据 |

### 新增卫生工具
`scripts/lint-cdp-templates.mjs` —— 拦截「`evalExpr` 模板内注释含反引号 ⇒ 截断模板」。
**同一坑本轮连踩 4 次**（报错行指向模板起始行，定位反直觉）⇒ 产品化 + 内置坏/好样本自检（防空转）。

### 测试总账（2026-09-11）
| 类别 | 套件 | 结果 |
|:--|:--|:--|
| 真机 | `cdp-click-dialog.mjs` | **100 / 100**（连续两轮 ⇒ 可反复运行已证明） |
| 真机 | `cdp-click.mjs` / `cdp-verify.mjs` | **70 / 70** ／ **39 / 39** |
| 离线 | `verify-dialog.mjs` / `verify-install.mjs` / `verify-design-doc.mjs` | **269 / 269** ／ **60 / 60** ／ **49 / 49** |
| 离线 | 其余 7 套 | 96 / 65 / 66 / 92 / 63 / 42 / 52 全绿 |
| 卫生 | `lint-cdp-templates.mjs` | `IS_PASS: TRUE` |

### 产物与数据
- `lib/client.js` **413,359 B**（字符 345,123）· 模块图 38 · 平台外置 `react, react/jsx-runtime, react-dom/client`
- `plugin-install.mjs --apply` 逐文件回读校验通过；**重启 3 次**
- 插件库 `dsh-director-plugin-db@v1`：`nodes:12 conversations:36 reviews:3 decisions:3`；宿主库 `dsh-director-db@v3` **未动**；R5 冻结键**零改名/零新增变体**

### 下一步 / 残留
| 项 | 状态 |
|:--|:--|
| `_probe-*.mjs` 临时探针 **9 个**（`_probe-chain/k1/nav/newsession/pad/root/rowclick/sessions/sidebar`）+ `_platform-stub*.mjs` 2 个 + `.tmpcheck/` | 🔴 **untracked ⇒ 删除不可逆，已列候选待用户确认，不自行删除** |
| 上下文压缩（V12 视图 5） | 部分实现（R6 已呈现三段记忆；压缩动作待后续迭代） |
| T-PLUG-012 `docs-index.json` 漂移 / A14 部署偏差 | 待排期（与 A14 同源，一并处理） |
| T-V12-008 GitHub 推送 | 用户明示暂缓（VPN 未开） |
| 已知副作用 | 早期脚本版本为进入对话上下文点过一次「新会话」，在侧栏留下一个空会话「新会话」（现已改为只读，不再新增） |

## 历史状态（2026-09-12 T-PLUG-014 总监与对话架构总纲 + 逻辑丢失归因 · 形态已由 T-PLUG-015 承接）

### 状态一句话
**文档 + 设计图已交付，零实现改动。** 用户 11 条核心要求已逐条落为**业务不变量**并立为**唯一真相源**
（`dsh-director-plugin/docs/10-总监与对话架构总纲.md`）；配套设计图
`docs/50-信息中心/V14-弹窗式总监架构设计稿.html`（暗色对齐 Harness 原生主题）。

### 形态裁定（本轮最大变更）
用户明示「**两者均为弹窗形式，并允许最小化**」＋「左总监 / 右对话数据（与对话 tab 完全一致）」
⇒ **`T-PLUG-013` 的「总监 = 原生 tab」形态作废**；其 §五 技术证据链**保留**（降级为"入口备选通道"）。
新形态 = **覆盖式弹窗**（左＝总监面板，右＝对话数据面板，各自可最小化 ＋ 整窗可最小化）。

### 本轮交付
| 文件 | 内容 |
|:--|:--|
| `dsh-director-plugin/docs/10-总监与对话架构总纲.md`（新建 · 37.6 KB / 498 行） | 11 条要求逐条规格 ＋ 双层数据元 schema ＋ 交互规格 ＋ 缺口表 ＋ **五级丢失归因** ＋ 防复发 6 条 ＋ 分 9 阶段实施清单 |
| `docs/50-信息中心/V14-弹窗式总监架构设计稿.html`（新建 · 39.7 KB / 708 行） | 8 节可视化：11 条对表 / 双层数据元 / 主视图弹窗 / 最小化三态 / 层级联动 / 智能路由 / 双向联动 / 缺口表 |
| `dsh-director-plugin/docs/09-*.md`（改） | 加「〇·补 形态裁定更新」节：逐节处置对照 ＋ 真相源移交 |
| `dsh-director-plugin/docs/04-*.md`（改） | §2.4 订正：方案 A 实现成本 4→8、总分 35→39；新增方案 D/D′（弹窗），选定 D′ |

### 🔴 核心技术判断（用于订正 T-PLUG-009）
**「独立数据元」与「R5 冻结 key」并不冲突。**
T-PLUG-009 的约束是"不能给**宿主的** `dsh-director-db` 升 v4"；而 **IndexedDB 的版本协商只发生在同一个数据库名内**
⇒ **新建插件自有库 `dsh-director-plugin-db` v1 完全不参与宿主的版本协商**，对宿主零影响。
⇒ 要求 ① 可在零风险下达成；T-PLUG-009 的「复用 `memoryCore` + 约定隔离」是在**错误前提**下求得的次优解。

### 丢失归因（五级 · 回答用户「为何全部丢失」）
1. **入口被主动注销**：批 C-2 移除 director tab 注册 ⇒ 宿主 MOD-A 布局 `:6477-7420` **28 处代码活着但不可达**（判据 `client.js:9150` 永假）
2. **迁移拆成两个世界**：A11 布局 store ＋ E1 DirectorFlow **已迁入插件却零渲染消费方**；渲染位置留宿主（F 区「保留现有实现」），"桥"没人修
3. **契约反向固化"共用"**：设计独立库 6 store → 落地共享库 3 store，《锚点契约》再把这层共享冻结为不可动
4. **需求真相源缺失**：11 条散在 7 份文档，**无一份声明自己是业务真相源** ⇒ 被最新文档自动覆盖
5. **版本控制面仅 6.8%**：磁盘 **3593** / git 纳管 **245**；`workspace/`（宿主全部改造）与 `snapshots/`（唯一回滚点）**均 0 纳管**；`fe6e651` 另删 78 文件

⇒ **「此前新建内容未被保留」的直接答案**：不是被谁删了，而是**宿主侧改动从未进入版本控制、其唯一回滚点也未入仓、插件迁移只继承了"数据与组件"而未继承"渲染位置与业务不变量"**。

### 下一步（按优先级）
1. 🟡 **等用户裁定 3 项**：Q1「两者」指左右两面板还是两个视图 ｜ Q2 总监页与对话 tab 的关系 ｜ Q3 右栏渲染通道
2. ⏸ 裁定后按 `docs/10` §九 实施清单推进（阶段 0 补锚点 → 1 独立库 → 2 弹窗形态 → 3 右栏接线 → 4 层级联动 → …）
3. 🟡 **T-PLUG-007 补丁减薄**（《锚点契约》文档部分不需授权）
4. 🔴 **B6 待授权**：改宿主 `client.js:11766`（属 git 忽略目录不可逆操作）
5. ⚠️ **S4** 宿主侧 `filteredMessages` 越界引用（随 B6 处理）；⚠️ **S5** 双份实例硬退坡（独立任务）
6. ⏳ T-PLUG-012 `docs-index.json` 漂移 6 篇（归入 A14 边界）
7. ⏸ T-V12-008 GitHub 推送（待开 VPN）

### 回滚锚点
本轮**未改任何实现**（`src/` · `lib/` · `build/` · `assets/` · 宿主 **零改动**）⇒ 无需回滚。
新增：`docs/10-*.md`、`docs/50-信息中心/V14-*.html`；修改：`docs/09-*.md`、`docs/04-*.md`、台账 `03` / `06`。

---

## 历史状态（2026-09-12 T-PLUG-013 页面交互重设计 · 原生 Tab 集成 · 形态已被 T-PLUG-014 订正）

### 状态一句话
**设计稿已出，待评审；本轮零实现改动**（按用户要求「先输出设计图文档，**不要直接重画实现**」）。
交付两件：`dsh-director-plugin/docs/09-页面交互重设计·原生Tab集成方案.md`（设计文档）+
`docs/50-信息中心/V13-原生Tab集成设计稿.html`（高保真设计图，暗色对齐 Harness 原生主题）。

### 核心结论（回答用户「为什么看起来像完全重画」）
| 层 | 事实 |
|:--|:--|
| 需求 | **一直是原生 tab** —— `03号文 §1.3`：「在 Harness 的每个会话中，**总监 tab** 提供预处理能力」；`18号文` 标题即「**总监 tab 完整界面**」（R1–R8 + E.9）；`V12 设计稿` tab 栏实绘「**总监 ｜ 对话 ｜ 轨迹**」 |
| 现状 | **882×562 独立浮层** —— 真机实测 `#dsh-director-hierarchy-overlay` `x=536,y=190`，右下角另有浮动按钮；而原生 tab 在 `y=48`、高 **27px** ⇒ 差异在**形态层级**，不在配色 |
| 根因 | 三级：①**需求层**（文档写的一直是 tab）②**选型层**（`docs/04` 方案 A 因「slots 实例能否拿到**未经实测**」被打 35 分落选 —— 该前提**已被证伪**）③**实现层**（`mount.js` 探测 `window.__DSH_SLOTS__`，**该全局从来不存在** ⇒ 恒走 DOM 兜底） |

### 技术门已打通（四源交叉，非推断）
1. tab 环**纯 slot 驱动**：宿主 `dsh-client-ui-conversation/lib/client.js:9172` — `renderSlot("conversation.view", props, { only: active.id })`
2. 官方称其为 **tab ring**：`…/lib/invariant.js:13`
3. 插件 `apply(ctx)` **本就收到 ctx**（产物尾部 `void ctx;` 丢弃、`inject=[]` 未声明依赖）
4. 官方契约：`inject:['slots']` + `ctx.slots.register`（`dsh-client-modules/lib/index.js:2146` 示例 + 未声明依赖报错模板）
5. **同族先例**：`dsh-client-ui-trajectory` 正以 `ctx.slots.inject("conversation.view", …)` 注册「轨迹」tab
⇒ 注册 `{id:"director", order:-1, label:()=>"总监"}` 即被接管，**宿主零改动**；`inject(sessionId)` 天然满足「每个对话一个总监」。

### 已有设计图文档的处理方式（文档 §二 · 用户明确要求说明）
- ✅ **沿用**：`03号文`（需求锚点）/ `18号文`（升为设计基线）/ `V12`（视觉基准）/ V10 导图设计图 / 统一方案文档 / `17号文`
- 📦 **降级历史参考（不删）**：`V11` / V10 布局稿 / V10 美化稿 / V9 框架·视觉稿
- ⚠️ **分层适用**：`31号文`「以实现为准」只对 **V10.1 细节层**成立；**信息架构层反转为「以设计稿为准」**
- 🔴 **需修订**：`dsh-director-plugin/docs/04` 方案选型（方案 A 的评分前提被证伪）

### 下一步（按优先级）
1. 🟡 **等设计评审**：`docs/09` §7.2 有一处**待确认口径** —— 「双向联动」= ①同源视图（默认：两 tab 共用一个会话、零搬运）还是 ②跨会话投递
2. ⏸ 评审通过后按 `docs/09` §九 实施清单落地（12 项 · 分 4 阶段；阶段 1 = 最小注册验证）
3. 🔴 **B6 待授权**：改宿主 `client.js:11766`（属 git 忽略目录不可逆操作）
4. ⚠️ **S4** 宿主侧 `filteredMessages` 越界引用（随 B6 处理）；⚠️ **S5** 双份实例硬退坡（独立任务）
5. ⏳ T-PLUG-012 `docs-index.json` 漂移 6 篇（归入 A14 边界）
6. ⏸ T-V12-008 GitHub 推送（待开 VPN）

### 回滚锚点
本轮**未改任何实现**（`src/` · `lib/` · `build/` · 宿主 零改动）⇒ 无需回滚。
新增文件：`docs/09-*.md`、`docs/50-信息中心/V13-*.html`；台账 `03` / `06` 追加登记。

---

## 历史状态（2026-09-12 T-PLUG-008 插件部署单元 + T-PLUG-011 `Network` 误删缺陷族）

### 状态一句话
**T-PLUG-008 已完成**：插件下发从「手工三步」变为**一条命令**，并交付安装权威说明；新增 `scripts/verify-install-clean.mjs`
（**52/52**）在临时沙箱里跑「**双机模拟**」全生命周期。**九层验证离线全绿 536 项**（96/63/60/65/66/92/42/**52**）+ 真机契约 39/39 + 真机逐交互点击 66/66；
产物 `lib/client.js` **279,841 B / 32 模块**（React 零打包），**宿主零改动**。
**T-PLUG-011 已修**（P0 真实数据丢失路径）：3 个脚本 + 1 份「修改前必读」文档的清缓存列表含 `Network`。

### 本轮完成
1. **`dsh-director-plugin/scripts/plugin-install.mjs`**（新建）：三处安装点一把梭。模式 `verify`（**默认零写入**，任何路径都不改）
   / `--apply`（**幂等**，写后**逐文件回读校验**）/ `--uninstall`（**外科式**只删本插件 entry，**绝不删第三方内容**）/ `--uninstall --purge`；
   可重定向 `--harness` / `--profile-root`（供干净目录模拟）
2. **`dsh-director-plugin/INSTALL.md`**（新建）：安装**权威说明** —— 三处安装点为何缺一不可 + 加载链路、命令面与三条硬纪律、
   启动陷阱（`ELECTRON_RUN_AS_NODE`）、缓存清除白名单、验证矩阵、故障排查表、开发者改动清单、回滚。README §安装 收敛为指针（**同一事实只留一份真相源**）
3. **`scripts/verify-install-clean.mjs`**（新建 · 8 节 **52 项**）：`干净 --apply` → `幂等` → `verify 就绪` → `--uninstall` →
   `从已卸载态再 apply` → `--purge` → **真实用户环境零触碰反证** → **三份清单一致性**
4. **`scripts/deploy.ps1` 退役**为硬失败垫片（保留肌肉记忆下的明确指引，全 ASCII 以避开 PowerShell 5.1 无 BOM 编码陷阱）
5. **`clear-cache.ps1` / `restart-harness.ps1` 修正**：移除 `Network` 并把 `Network` 列入 `$dirsToKeep` + 红线注释；
   `50-信息中心/01`（「修改前必读」）三处更正（含**可执行片段**）
6. **README.md 全面刷新**：九层验证表、目录树（补齐 6 个新增脚本 + 34 个源文件层级）、验证命令块（原为陈旧的「四层 66/37/15」）

### 🔴 本轮修掉的缺陷（两条，均含反证）

| # | 缺陷 | 性质 | 反证 |
|:-:|:-----|:-----|:-----|
| A | `plugin-install.mjs` 的 `PAYLOAD_FILES` **漏 `package.json`** | **干净机器上插件永不被装载且不报错**（host Loader 靠 `package.json` 发现本包） | 临时移除后 `verify-install-clean` **8 项转红**（含 `[A10]` ENOENT）；而安装器**自身 `IS_PASS` 仍 TRUE** ⇒ **对自己的缺件是盲的** |
| B | 3 脚本 + 1 文档清缓存列表含 **`Network`** | **真实用户数据丢失路径**：`Network` 是 Chromium 的 **cookie 存储**，内含 `dsh_director_*` 持久化 cookie（**R5 冻结契约**） | 全仓 grep 定位 4 处；`Network` 语义查证 + R5 契约交叉印证 |

> **A 的可复用教训**：本机「一切正常」只因缺件早先被**手工**拷入 —— **环境残留会掩盖缺件**。
> 单机幂等测试**必然漏检**这类缺陷；只有**干净目录 + 独立声明的期望清单**才能暴露它。
> 已固化为常驻测试（`verify-install-clean.mjs` §[H] 静态交叉核对三份清单，防漂移）。
> **B 的可复用教训**：「清缓存」看似无害，但 `Network` 不是缓存而是 **cookie 库**。破坏性列表必须**逐项查证语义**，
> 不能按目录名归类（`Cache`/`Code Cache` 确属缓存，`Network` 不是）。

### 下一步（按优先级 · 2026-09-12 刷新）
1. 🔴 **B6 待授权**：改宿主 `client.js:11766` 一处（F3 改为优先调插件 + 回落），随后真机验证 + 退坡评估。**等待用户批准**（属铁律「git 忽略目录不可逆操作须先确认」）
2. ⚠️ **S4 宿主侧 `filteredMessages` 越界引用**（运行时线上缺陷，行 7047 引用零定义标识符 → 总监流一有消息就 `ReferenceError`）：随 B6 一并处理（改宿主 `client.js:7050`）
3. ⚠️ **S5 双份实例硬退坡**（删宿主内联副本 ≈1,400 行）：建议列为**独立任务**（高风险，需先补快照）
4. ⚠️ **A14 部署偏差**：`workspace/` 版宿主与运行时版**仅差 A14 一段** ⇒ **A14 剥离从未部署到运行时**；且 `apply.ps1` 会调 `gen-docs-index.ps1` 把 541KB 常量**重新注入**，与 A14 互斥。需单独立项
5. ⏳ **T-PLUG-007 补丁减薄 ≥70% + 《锚点契约》文档**：**不依赖授权**，可推进（补丁基线 +13,553/−9,855 行，目标 ≤4,066 增行）
6. 📄 **T-PLUG-012（新登记）**：`assets/docs-index.json` 已漂移 **6 篇**（索引 90 / 实际 96，0 篇多余）—— 归入 A14 边界一并处理（原因见 03 号清单该行）
7. ✅ **T-PLUG-008 已完成**（本轮）：部署单元 + 安装 README（`INSTALL.md`）+ 干净目录验收 52/52
8. ✅ **T-PLUG-011 已完成**（本轮）：`Network` 误删缺陷族 3 脚本 + 1 文档
9. ✅ **F-DATA-01 已结案**（`T-PLUG-010`）：判为**观测口径缺陷**（假阳性）非数据丢失
10. ⏸ T-V12-008 GitHub 推送 — 挂起，待用户开 VPN

### 回滚锚点
- 插件：`dsh-director-plugin/` 在 git 纳管范围内，`git revert` + 重新下发 `lib/client.js` 即可
- **宿主 `workspace/client.js` 本轮零改动**（批次 6 接线仍待授权）
- 运行时安装点：`D:/软件安装/DeepSeek-Harness-Desktop/DeepSeek Harness/resources/host/node_modules/@deepseek-ai/dsh-director-plugin/lib/client.js`（当前 **279,841 B**，与仓库产物 `cmp` 一致）
- 插件卸载：`node dsh-director-plugin/scripts/plugin-install.mjs --uninstall --purge`（完全退出插件通道）
- `~/.dsh/profiles/node_modules/@deepseek-ai/*` → symlink 指向安装目录（**改仓库不改安装目录 = 不生效**）


## 历史状态（2026-09-12 T-PLUG-009 数据兼容与边界安全 + schema 白名单加固）

### 状态一句话
插件迁移主线 T-PLUG-005 **批次 1~9 连续闭环**。**T-PLUG-009 已完成**：新增 `scripts/verify-data-safe.mjs`（**42/42**）
证明宿主 IDB `dsh-director-db` **v3 六 store 条数零增零减**、插件与宿主**双向读取互不可见**；
顺带把「**约定隔离**」硬化成 **schema 白名单**（`isHierarchyNode`），修掉一个「**约定失效即静默**」缺陷
（实测：一条带任意 `level` 字段的宿主记录即可混入总监树，泄露 2 条、树节点虚增 2，**不报错**）。
**八层验证全绿 523 项 + 真机逐交互点击 66/66**；产物 `lib/client.js` **279,841 B / 32 模块**（React 零打包），**宿主零改动**。

### 本轮完成
1. **`scripts/verify-data-safe.mjs`**（新建 · 7 节 42 项）：结构契约 / 宿主访问模式取证 / 记录归属分类 /
   隔离性动态反证 / 硬化反证 / 宿主数据源零损伤 / R5 key 契约
2. **`hierarchy.js` 加固**：`listAllNodes` 过滤由 `n.level` 真值判断 → **`isHierarchyNode` schema 白名单**
   （`id` 非空字符串 **且** `level` 恰为 `global|project|session`，白名单由 `LEVEL` 单一真相源派生）
3. **`docs/07-数据兼容与边界安全验证.md`**（新建）：含反证表、验证矩阵、两条口径修正、开放项登记
4. **重新下发 + 冷启动复验**：产物 278,301 → **279,841 B**，逐字节一致（`cmp`），重启后**八层全绿**

### 🔴 本轮修掉的缺陷（附改前/改后反证，同机实测）

| 注入的宿主形态记录 | 改前（真值判断） | 改后（白名单） |
|:--|:--|:--|
| 无 `level` | 挡住 ✅ | 挡住 ✅ |
| `level: "note"`（伪值） | **泄露 1 条 ❌** | 挡住 ✅ |
| `level: "global"` 但**缺 `id`** | **泄露 1 条 ❌** | 挡住 ✅ |
| 树节点数 | **13**（虚增 2，静默） | **11**（= 真实节点数）✅ |

> 共性教训再次命中：**构建通过 / 约定存在 ≠ 行为正确**。已固化为 [4b] 常驻断言，改回真值判断立刻报红。

### 🔴 两条验证口径修正（踩坑留痕，防复发）
1. **断言「不存在」前，必须先证明判据能命中「存在」**：探针记录刻意不带 `id`，首版却拿 `n.id` 判是否泄露
   → 比对**恒为 0**，**空洞通过**。改用 `projectId`（IDB 真实主键）才有判别力。
2. **契约查「定义处」，不查「环境残留」**：首版断言「localStorage 有 `dsh.director.store.*`」——
   该 key 只在总监 store **有消息且落过盘**后才存在 ⇒ 冷启动必然没有 ⇒ 换个环境就误报。
   改为对 `messages.js` / `persist.js` 做**源码静态断言**。

### 🟢 F-DATA-01 **已结案**：观测口径缺陷（假阳性），**非数据丢失**（任务行 `T-PLUG-010`）
初判「冷启动后 `dsh.director.store.director-e0w2f3` 与 IDB `directorStores` 1 条消失」→ 经 **3 次冷启动只读实验**推翻：
- **宿主核心数据零损伤**：`dsh.workspace.view.v5` **恒为 1118 B**、`memoryCore` **11 条节点完好**、`dsh.sessions.current` 存在
- **无删除来源**：宿主与插件**不存在** `removeItem`(该 key) / `localStorage.clear()` / `deleteDatabase` 任何分支（全仓 grep 取证）
- **根因两条叠加，均在探测侧**：① `dsh.workspace.view.*` **不是** dsh-client-ui-conversation 写的
  （该 bundle 中 `dsh.workspace.view` **0 处命中**），由**另一个 bundle 开机后异步写入** ⇒ **早探测必然读空**；
  ② `dsh.director.store.*` 只在**总监活动**时写入（`create-store.js#notify → saveDirectorStore`）⇒ 冷启动无活动**本就该无**
- **修复**：`verify-data-safe.mjs` 新增 **[0] 就绪等待**（取基线前轮询等 `dsh.workspace.view.*`，超时 60s）+ 第 6 节输出字节数锚点

> 🔴 **可复用教训**：做「重启前后对比」类数据验证，**必须先判定被观测对象是「同步写入」还是「异步/按需写入」**——
> 后者必须**等待就绪**，并区分「未写入」与「被删除」，否则会周期性产出**假阳性**，消耗真阳性该有的注意力。

### 下一步（按优先级 · 2026-09-12 刷新）
1. 🔴 **B6 待授权**：改宿主 `client.js:11766` 一处（F3 改为优先调插件 + 回落），随后真机验证 + 退坡评估。**等待用户批准**（属铁律「git 忽略目录不可逆操作须先确认」）
2. ⚠️ **S4 宿主侧 `filteredMessages` 越界引用**（运行时线上缺陷，行 7047 引用零定义标识符 → 总监流一有消息就 `ReferenceError`）：随 B6 一并处理（改宿主 `client.js:7050`）
3. ⚠️ **S5 双份实例硬退坡**（删宿主内联副本 ≈1,400 行）：建议列为**独立任务**（高风险，需先补快照）
4. ⚠️ **A14 部署偏差**：`workspace/` 版宿主与运行时版**仅差 A14 一段** ⇒ **A14 剥离从未部署到运行时**；且 `apply.ps1` 会调 `gen-docs-index.ps1` 把 541KB 常量**重新注入**，与 A14 互斥。需单独立项
5. ⚠️ **`scripts/deploy.ps1` 已过期失效**：指向旧路径 `D:\hermes-data\dsh-director`，且误删 `Network` 缓存 —— 当前下发**只能手工**。归入 `T-PLUG-008`（插件部署单元 + 安装 README）
6. ✅ **F-DATA-01 已结案**（`T-PLUG-010`）：判为**观测口径缺陷**（假阳性）非数据丢失；`verify-data-safe.mjs` 已加 **[0] 就绪等待**防复发
7. ⏸ T-V12-008 GitHub 推送 — 挂起，待用户开 VPN

### 回滚锚点
- 插件：`dsh-director-plugin/` 在 git 纳管范围内，`git revert` + 重新下发 `lib/client.js` 即可
- **宿主 `workspace/client.js` 本轮零改动**（批次 6 接线仍待授权）
- 运行时安装点：`D:/软件安装/DeepSeek-Harness-Desktop/DeepSeek Harness/resources/host/node_modules/@deepseek-ai/dsh-director-plugin/lib/client.js`（当前 **279,841 B**，与仓库产物 `cmp` 一致）
- `~/.dsh/profiles/node_modules/@deepseek-ai/*` → symlink 指向安装目录（**改仓库不改安装目录 = 不生效**）


## 历史状态（2026-09-12 批次 8 / `T-PLUG-005-B9` 总监逻辑完善 + 真机逐交互验证 66/66）

### 状态一句话
插件迁移主线 T-PLUG-005 **批次 1~8 连续闭环**，插件**已真机装载并在渲染进程内实际运行**。
**批次 8「总监逻辑完善」已完成**：总监从「有个节点」补成「真的按 03号文 §3.1 五项职责干活」，
新增 `logic/duties.js`（五项职责 + prompt）、`store/duty-config.js`（§3.2 三级继承/覆盖/向上提交）、
`logic/director-run.js`（§1.2 五步执行 + 自动转发 + 自动审核）、`components/DirectorWorkbench.js`（执行逻辑面板 + 对话流）。
**七层验证 + 真机逐交互点击全绿：96 / 63 / 60 / 65 / 66 / 92 / 39 + 点击 66**。
产物 `dsh-director-plugin/lib/client.js` **278,301 B / 32 模块**，React 零打包。
**宿主零改动**（批次 6 的 F3 接线仍待授权，见下）。

### 本轮已完成（批次 8）
1. **职责层**：§3.1 五项逐字对齐（默认：整理语言✅/调整模型✅/切换分支✅/上下文筛选⬜/自动审核⬜），每项含可编辑 prompt
2. **继承层**：`resolveDuties` 自底向上回溯（会话→项目→全局→默认）+ 覆盖 + 恢复继承 + 向上提交（写后回读校验）
3. **执行层**：§1.2 五步真实实现；自动转发（§2.3 ④）；第 5 步 `reviewOutput` 规则审核（中文 bigram 分词修正）
4. **交互层**：层级面板右栏加 [概览] [总监] 双页签；工作台 = 执行逻辑面板 + 对话流 + 输入栏 + 自动转发开关
5. **真机逐交互验证** `scripts/cdp-click.mjs`：**22 小节 / 30 个可交互元素 / 66 项断言全绿**，
   每项均为「点击 → 回读实际状态 → 比对预期」；I21 做**全量覆盖审计**（0 个遗漏），I22 清理测试残留

### 🔴 本轮真机实测修复的 5 个缺陷（只有真机能暴露）
| # | 现象 | 根因 | 修复 |
|:--|:--|:--|:--|
| 1 | 面板点击后**完全空白** | `DirectorHierarchy` 用了 `DirectorWorkbench` 却漏写 import → 产物语法合法、构建通过，**运行时渲染才抛错**，React 卸载整棵子树 | 补 import + **打包器加构建期静态检查** `lintUndefinedComponents`（未绑定 JSX 组件 → 构建失败并报组件名+行号） |
| 2 | 点发送后 1.8s **零变化** | `addMessage(user)` 写在 5 步之后；前 3 步调本地模型（单次超时 60s） | 用户消息改为**立即上屏**（§2.3）；`history` 取上屏前快照；补 `catch` 写【总监异常】不静默 |
| 3 | 点树节点**选中态不变** | 测试按文本前缀匹配 `div` → 命中**祖先包裹层**，click 只向上冒泡 | 树行加 `data-node-id` / `data-selected` |
| 4 | 「向上提交」**取错按钮** | 概览区与工作台**同名**，只能靠文档顺序取第一个 | 全部交互元素补 `data-testid`（概览 `h-*` / 工作台 `w-*`） |
| 5 | 后续点击**静默失效** | `guard()` 置 `disabled`，而 `click()` 对禁用按钮是 no-op；固定 sleep 短于实际耗时时必踩 | 测试引入 `waitIdle()` 轮询；I14 判据由「长度增长」（不幂等）改 `summaryAt` 时间戳 |

> 共性教训：**构建通过 / 契约存在 ≠ 功能可用**。已全部固化为防回退断言。

### 下一步（按优先级 · 2026-09-12 刷新）
1. 🔴 **B6 待授权**：改宿主 `client.js:11766` 一处（F3 改为优先调插件 + 回落），随后真机验证 + 退坡评估。**等待用户批准**（属铁律「git 忽略目录不可逆操作须先确认」）
2. ⚠️ **S4 宿主侧 `filteredMessages` 越界引用**（运行时线上缺陷，行 7047 引用零定义标识符 → 总监流一有消息就 `ReferenceError`）：随 B6 一并处理（改宿主 `client.js:7050`）
3. ⚠️ **S5 双份实例硬退坡**（删宿主内联副本 ≈1,400 行）：建议列为**独立任务**（高风险，需先补快照）
4. ⚠️ **A14 部署偏差**（本轮实测发现）：`workspace/` 版宿主与运行时版**仅差 A14 一段**（前 6538 行逐字节一致）⇒ **A14 剥离从未部署到运行时**；且 `apply.ps1` 会调 `gen-docs-index.ps1` 把 541KB 常量**重新注入**，与 A14 互斥。需单独立项处理
5. ✅ 会话标题仍为 id 短码（DOM / fiber / localStorage 三条通道实测均无标题源）；面板内可改名，改名后同步不覆盖
6. ⏸ T-V12-008 GitHub 推送 — 挂起，待用户开 VPN

### 回滚锚点
- 插件：`dsh-director-plugin/` 在 git 纳管范围内，`git revert` + 重新下发 `lib/client.js` 即可
- **宿主 `workspace/client.js` 本轮零改动**（批次 6 接线仍待授权）
- 运行时安装点：`D:/软件安装/DeepSeek-Harness-Desktop/DeepSeek Harness/resources/host/node_modules/@deepseek-ai/dsh-director-plugin/lib/client.js`
- `~/.dsh/profiles/node_modules/@deepseek-ai/*` → symlink 指向安装目录（**改仓库不改安装目录 = 不生效**）


## 历史状态（2026-09-11 插件迁移批次 1-5 全部完成 · 批次 6 方案就绪待授权）

### 状态一句话
插件迁移主线 T-PLUG-005 **批次 1~5 连续闭环**（零依赖层 → 数据层 → 持久化层 → 逻辑层 → 组件层），
插件**已真机装载并在渲染进程内实际运行**；四层验证全绿（源码 96/96 · bundle 63/63 · 安装链路 60/60 · 真机 39/39）。
产物 `dsh-director-plugin/lib/client.js` **162,285 B / 21 模块**。
**批次 6 方案已就绪**（`docs/03-批次6接线与退坡方案.md`）——**最小闭环仅需改宿主 1 行**；因宿主文件位于 `workspace/`（`.gitignore` 排除，git 无法回滚），**依铁律须先取得授权**。

### 本轮已完成（批次 6 方案编制）
1. **F 区按实测重定级**（R8 铁律）：**F1/F2/F4/G1/G4 零改动**（契约解耦）· **F5 ⛔ 已由 MOD-B 退坡**（2026-09-07，`client.js:11966-11967` 仅剩注释）· **F3 需改 1 行**
2. **关键约束澄清**：F3/F4 依赖**宿主私有闭包**（`concreteConversation(ctx)` / `scopedConversation(sessions,...)`）→ **不可迁移**，只能「改为调用插件」（E8 单向调用）
3. **真机实证**：`window.__dshDirectorProcess`（插件）与 `window.__directorSubmit`（宿主）并存，但宿主 F3 调用的是其**内联** `directorProcess` ⇒ 插件 D1 目前是死代码 → 这正是批次 6 要接的线
4. **登记 S5 双份实例**：内存态不共享、持久化 key 共享 → 过渡态可接受（不丢数据）
5. **交付** `docs/03-批次6接线与退坡方案.md`（改动清单 + 风险 + 回滚 + 真机验证方法）
6. **台账同步**：清单 v7 · README · 03 清单 B6 待授权 + S5 · 06 快照 · `_memory/MEMORY.md` §14.16

### 下一步（按优先级 · 2026-09-11 23:15 刷新）
1. 🔴 **B6 待授权**：改宿主 `client.js:11766` 一处（F3 改为优先调插件 + 回落），随后真机验证 + 退坡评估。**等待用户批准**（属铁律「git 忽略目录不可逆操作须先确认」）
2. ⚠️ **S4 宿主侧 `filteredMessages` 缺陷**：随 B6 一并处理（改宿主 `client.js:7050`）
3. ⚠️ **S5 双份实例硬退坡**：建议列为**独立任务**（高风险，需先补快照）
4. 🟡 V9.4/V9.5 修复人工验证（代码已 apply，待重启 Harness 验证 9+7 项）
5. ⏸ T-V12-008 GitHub 推送 — 挂起，待用户开 VPN

> **✅ 本轮重大进展**：
> - **批次 3 持久化层 5 模块** → 四层 66/44/46/24 全绿；修复 `isOpfsAvailable()` 短路、深克隆保真、打包器平台外置。
> - **批次 4 逻辑层 2 模块**（D1/D2）→ 四层 82/54/52/31 全绿；D1/D2 真机实际调用成功。
> - **批次 5 组件层 1 模块**（E1）→ 四层 96/63/60/39 全绿；🔴 修复宿主 `filteredMessages` 越界引用缺陷（S4）。
> - **插件真机装载实证**：`__DSH_BOOT__` 42 entries 含本包；**27 项全局契约全挂载**；`__dshDocsIndex` **docCount=90**。
> - **A14 剥离成果保持**：宿主 client.js `1,479,577 → 564,826 B`（**-914,751 B / -61.8%**）。
> **⛔ 已撤销项**：原「向用户确认插件源码工程缺失」**已解除**（用户澄清：源码在宿主体内内联，无需恢复）。

### 回滚锚点
- mod：git **（本轮批次 5 提交）** = 批次 3/4/5 提交 + fbb0818 + b3aa372 + 43b300b/0cf87c1 + 快照 150715~161023/131412
- director：git 12907c6 + 可重放构建（rev dd46d1352b43）
- **A14 剥离专用回滚**：`python dsh-director-plugin/scripts/restore-a14.py --check`（校验快照 1,479,577 B + 1 处声明）
- **⚠️ 宿主 `workspace/` 不受 git 管理**：改动前必须先备份（见方案 §六 步骤 1）


### 状态一句话
用户对视图5（设计图标注模式）提出增强需求：①增加网格坐标系（横向1,2,3+纵向a,b,c）快速定位 ②增加常见设置框（按钮/输入框/范围区域/图片）+备注框 ③增加设计图独立对话（默认加载设计规范+html skill+execution-standards，节省上下文）④增加技能/智能体/执行规范调用（@技能/@智能体/@区域）。已完成视图5增强版，同时从一次误操作（Write覆盖整个文件）中完整恢复了V12设计稿（8个视图+设计说明区，124KB/1263行）。

视图5增强版核心功能：
- 3×3网格坐标系统（绿色虚线叠加，横向1-3/纵向a-c，标注框自动关联坐标如a1/b2/c2-c3）
- 快速标注工具：🔘按钮/📝输入框/📐范围区域/🖼️图片图标，每种类型有专属颜色和图标
- 备注框：虚线边框，说明区域用途
- 设计图独立对话面板：默认加载设计系统规范（L0令牌+L1组件）+html skill+execution-standards，上下文仅含设计图相关
- 技能/智能体/区域调用：@技能/@智能体/@区域按钮，自动加载对应规范
- 上下文节省效果：用户说"c2-c3按钮颜色不对"→系统定位→识别Button.tsx+--primary令牌→仅加载2个文件约300行（非全项目数万行）

### 下一步
1. 用户审阅视图5增强版，确认或提出修改意见
2. 确认后输出P0阶段详细实现方案（文档先行）
3. 按P0→P1→P2→P3分期执行

### 回滚锚点
- mod：git 43b300b/0cf87c1 + 快照 150715~161023/131412
- director：git 12907c6 + 可重放构建（rev dd46d1352b43）


## 历史状态（2026-09-08 V12设计稿三轮审核通过 · 等待用户确认后进入P0实现）

### 状态一句话
V12多分支流水线协作系统高保真设计稿完成，经过三轮审核（审核→整改→审核→整改→最终审核）全部通过。设计稿包含8个核心视图：①总监调度主视图（分屏+思维导图右键菜单+生成导图入口）②创建分支弹窗（三维定义三步引导）③分支对话+产出物面板（上下文摘要卡片）④节点详情+审核流程（多维度审核）⑤设计图标注模式（区域标注+任务分配+风格统一入口）⑥上下文压缩+模型切换（压缩引擎+降级链+阈值设置）⑦分支管理中心（状态管理+流水线模板+合并汇总）⑧总监仪表盘（统计卡片+流水线进度+告警中心+资源使用+最近活动）。补充完整技术设计说明（8核心函数+8参数流转+10通用组件+10数据模型映射）、交互状态全集（5空状态+6错误+6loading+14hover/focus/active）、设计系统统一机制（L0-L3分层+风格锁定+增量修改工作流，解决"不用每次全重新读取"需求）。等待用户确认设计稿后输出P0阶段详细实现方案。

### 下一步
1. 用户审阅V12设计稿（8视图+技术设计+交互状态+设计系统机制），确认或提出修改意见
2. 确认后输出P0阶段详细实现方案（文档先行）
3. 按P0→P1→P2→P3分期执行

### 回滚锚点
- mod：git 43b300b/0cf87c1 + 快照 150715~161023/131412
- director：git 12907c6 + 可重放构建（rev dd46d1352b43）


## 历史状态（2026-09-08 V12设计稿完成 · 等待用户确认）

### 状态一句话
用户确认v1.2方案方向，要求输出设计图（页面样子/交互/风格样式），调用前端skill和审美skill优化。已完成V12多分支流水线协作系统高保真设计稿（4个核心视图）：①总监调度主视图（分屏模式：左思维导图+右总监对话，含流水线进度总览+总监方案生成+任务分配+上下文传递+审核通过）②创建分支弹窗（三维定义三步引导：处理类型→智能体→模型与配置，含依赖选择+输入输出文件夹+任务描述+上下文预览）③分支对话+产出物面板（上下文摘要卡片+分支状态栏+对话区+右侧产出物面板区分输入/输出文件）④节点详情+审核流程（思维导图节点详情面板+总监多维度审核弹窗：完整性/质量/一致性/规范性评分+审核意见+下一步自动创建下游分支）。遵循html skill（单文件自包含/原生JS/V11设计系统对齐）和creative-design skill（避免AI slop/SVG图标/分支类型色）规范。等待用户确认设计稿后进入P0实现阶段。

### 下一步
1. 用户审阅V12设计稿，确认或提出修改意见
2. 确认后输出P0阶段详细实现方案（文档先行）
3. 按P0→P1→P2→P3分期执行

### 回滚锚点
- mod：git 43b300b/0cf87c1 + 快照 150715~161023/131412
- director：git 12907c6 + 可重放构建（rev dd46d1352b43）


## 历史状态（2026-09-08 思维导图方案v1.2深化完成 · 等待用户确认）

### 状态一句话
用户深化需求：分支不只是不同模型，而是专业化工作单元（处理类型+智能体+模型三维定义），分支间有依赖关系（文档分支产出→开发分支输入），总监作为全局调度者（工作安排+分支切换+上下文传递+质量审核）。已完成方案v1.2深化：新增分支三维定义、数据模型扩展（依赖/文件夹/产出物/调度记录）、流程6（总监全局调度）、流程7（分支间数据流转）、流水线协作示例（需求→设计→前端→后端→测试）、更新实现分期（总监调度和分支依赖列为P0）。等待用户确认后输出P0详细实现方案。

### 下一步
1. 用户审核方案v1.2，确认或提出修改意见
2. 确认后输出P0阶段详细实现方案（文档先行）
3. 按P0→P1→P2→P3分期执行

### 回滚锚点
- mod：git 43b300b/0cf87c1 + 快照 150715~161023/131412
- director：git 12907c6 + 可重放构建（rev dd46d1352b43）


## 历史状态（2026-09-08 思维导图统一方案完成 · 等待用户确认）

### 状态一句话
用户提出5大核心需求（切换模型自动压缩上下文/分支组成思维导图/点击节点进入对话/现有对话生成思维导图/体现思维导图作用）。已完成统一方案文档v1.1：汇总M4分支管理/V10交互/BranchMindMap现有实现，提出"思维导图第一公民"核心理念，设计5大完整流程（模型切换压缩/创建分支自动加节点/点击节点进入对话/现有对话生成思维导图/思维导图4重价值），含统一数据模型+V11设计系统对齐+数据迁移方案+P0-P3分期。等待用户确认后输出详细实现方案并执行。

### 下一步
1. 用户审核统一方案文档，确认或推翻已决策项（分屏模式/模型切换策略）
2. 确认待确认问题（压缩阈值/分支命名/合并策略）
3. 输出P0阶段详细实现方案（文档先行）
4. 按P0→P1→P2分期执行

### 回滚锚点
- mod：git 43b300b/0cf87c1 + 快照 150715~161023/131412
- director：git 12907c6 + 可重放构建（rev dd46d1352b43）


## 历史状态（2026-09-08 V11设计稿完成 · 等待用户确认）

### 状态一句话
用户反馈当前页面交互不好看，要求出新版设计图。已完成 V11 总监控制台高保真设计稿（docs/50-信息中心/V11总监控制台设计稿.html）：基于 V10 现有实现，解决27号文全部P0×4/P1×7问题，统一视觉语言（主色换轴#2563eb/激活态统一下划线+加粗/字阶6档/对比度门禁），功能零删减，含完整页面元素+交互逻辑状态机+设计系统规范。等待用户确认设计稿后进入代码修改阶段。

### 下一步
1. 用户审阅 V11 设计稿，确认或提出修改意见
2. 确认后：输出代码修改方案文档（文档先行），按区域分批修改 dsh-director 插件 TS 源码
3. 修改后：esbuild 重建 → CDP 验证 → 审核记录

### 回滚锚点
- mod：git 43b300b/0cf87c1 + 快照 150715~161023/131412
- director：git 12907c6 + 可重放构建（rev dd46d1352b43）


## 历史状态（2026-09-08 13:30 N-3 完成（除用户输入项）· 清单全闭环）

### 状态一句话
30号文清单全部闭环：N-1（插件收尾）/N-2（宿主退坡）/N-3.1（台账回写 D1-D6+02状态）/N-3.2（验证清单重构）/N-3.3（git 双仓锚定：mod 43b300b+0cf87c1、director 12907c6）。剩余四项均需用户输入：push 凭据、登记表授权、人工验收、远期决策（MOD-A 退役/V11+）。

### 用户侧待办
1. 人工验收：按 03 清单「验证清单重构」段过一遍插件 UI
2. T-V12-008：提供 GitHub 凭据后执行 push
3. 授权：08号登记表补 execution-standards 条目 + 修 3 个断链 junction
4. 远期决策：MOD-A 退役 / V11 嵌套 / 自动任务执行（30号文 N-4）

### 回滚锚点
- mod：git 43b300b/0cf87c1 + 快照 150715~161023/131412
- director：git 12907c6 + 可重放构建（rev dd46d1352b43）


## 历史状态（2026-09-08 13:25 N-1/N-2 完成）

### 状态一句话
插件化整改主体闭环：插件承载全部总监产品能力（M0-M7 + V10 + 快赢 121 + 图标/模型/轨迹增强，rev dd46d1352b43→运行 10afd797e46f 链路）；宿主 conversation 已退坡（MOD-B 注册+组件 1,519 行删除，MOD-A+共享设施按契约保留，numstat +11,996/−9,855）；20/21号审核通过。

### 断点与下一步
1. 远期候选（产品决策后启动）：MOD-A 退役（对话左栏迁移插件）→ 补丁再降；V11 嵌套；自动任务执行
2. 人工验证欠账：V10.1 六项 / V9.4 / V9.5；建议连同收敛后新 UI 一并过一遍
3. T-V12-008 git push（需凭据）；登记表同步（需授权）；dsh-director 已 git 化（基线 12907c6）
4. 全量 46/46 回归脚本面向旧 UI，建议改造为「conversation 核心 + 插件断言」双段（远期）

### 回滚锚点
- conversation：快照 150715/152331/153717/161023/131412 + git revert
- 插件：git 12907c6 基线 + 可重放构建（rev dd46d1352b43）
- profile：package.json.backup（spike 已回滚）


## 历史状态（2026-09-08 10:20 阶段总评审完成 · 30号文）

### 状态一句话
30号文完成：插件完成度（M0-M7 基础版+控制台+快赢 121 处全落地）、目标分层（L0 愿景/L1 形态/L2 功能/L3 质量）、缺口矩阵（P0×2 宿主退坡+人工验证、P1×4）、设计图基准 D1-D7 与漂移提示；下阶段清单 N-1~N-4 待用户确认后连续执行。

### 断点与下一步
1. 用户确认 30号文 §六 清单 → N-1（Q2-2/M6/M7/校准报告）→ N-2（P2 退坡手术+全量回归）→ N-3（台账+git+push）
2. 认知修正：dsh-director 无 git（回滚=99-备份+构建重放）；登记表旧条目失效待同步

### 回滚锚点
- 插件：99-备份 + 构建可重放（rev ce516814c17c）
- conversation：快照 150715~161023 + git


## 历史状态（2026-09-08 09:45 技能切换 execution-standards V2.3.0 · Q2-1 完成）

### 状态一句话
主记忆技能库已重构为 execution-standards V2.3.0（会话已切换，清单驱动模式）；Q2 图标第一批 36 处完成并热更（rev ce516814c17c，19号审核通过）；登记表 08号旧条目失效待同步。

### 断点与下一步
1. Q2-2：数据字段 icon hybrid 渲染器（字母→Icon / emoji→span）+ BranchMindMap(8)/DirectorChat(7) + 下拉 items(7)
2. P2 死代码删除（专门窗口；29号文 §五 契约约束）
3. 主记忆维护（待授权）：08号登记表补 execution-standards 条目 + 修 3 个断链 junction
4. 人工验证欠账；T-V12-008 git push（需凭据）

### 回滚锚点
- 插件：dsh-director git + 可重放构建（rev ce516814c17c）
- conversation：快照 150715/152331/153717/161023 + git


## 历史状态（2026-09-07 19:45 V10.2-3 完成）

### 状态一句话
V10.2 第三批 5 组修改在插件 TS 源码完成并热更（rev 8f964bdca660，18号审核 95/100）：Q4 渐隐上屏、Q5 四卡统一、换轴残留清零；快赢批仅剩 Q2 全量图标（独立大批次）。

### 断点与下一步
1. P2 死代码删除（专门窗口）：区域映射 DirectorView 树+独占辅助 → 逐块删除 → node --check → apply → 全量回归 → numstat 复测（29号文 §五 契约约束）
2. Q2 全量图标：先扩 icons.tsx 清单 → V10Director/BranchMindMap/DirectorChat 逐点替换 → 重建验证
3. 人工验证欠账（V10.1 六项/V9.4/V9.5）；T-V12-008 git push（需凭据）

### 回滚锚点
- 插件：dsh-director git + 可重放构建（rev 8f964bdca660）
- conversation：快照 150715/152331/153717/161023 + git


## 历史状态（2026-09-07 17:05 C-3 完成：MOD-A 锚点保留+契约）

### 状态一句话
C-3 已决策：MOD-A 保留为最小锚点并落《锚点契约》（29号文 §五，CDP 实证插件对话 tab 为透明视口架构、未复刻 ChatView 左栏）；P2 只删 MOD-B 死代码（DirectorView 164 引用），不动契约锚点。

### 断点与下一步
1. P2 死代码删除（建议专门窗口：按区域映射 DirectorView 树+独占辅助 → 逐块删除 → node --check → apply → 全量回归 → numstat 复测）
2. V10.2 第三批（插件 src：Q2 全量图标 / Q4 渐隐 / Q5 卡片结构）
3. 人工验证欠账（V10.1 六项 / V9.4 / V9.5）与 T-V12-008 git push（需凭据）

### 回滚锚点
- conversation：git revert 或 snapshots（150715/152331/153717/161023）
- 插件：dsh-director git + 可重放构建
- profile：package.json.backup（spike 已回滚）


## 历史状态（2026-09-07 16:20 收敛批 C-1/C-2 完成）

### 状态一句话
MOD-B 已从 conversation client.js 退坡（注册块移除 + 标记，served 实证），总监功能由 dsh-director 插件完整承载且顶栏 tab 条本由插件渲染——用户可见交互零变化；17号审核 95/100。

### 断点与下一步
1. 批 C-3：MOD-A 决策（插件 slot/overlay 可达性评估，决定对话左栏去留）
2. P2：DirectorView 死代码删除（164 引用）+ 全量回归 + 补丁行数复测（基线 +13,514/−9,855）
3. V10.2 第三批（插件 src：Q2/Q4/Q5）；人工验证欠账与 T-V12-008 git push 仍挂起

### 回滚锚点
- C-2：git revert conversation client.js 或 snapshots/snapshot-20260907-161023；插件侧 git（dsh-director）


## 历史状态（2026-09-07 16:10 方案①拍板 · V10.2-2 插件侧完成）

### 状态一句话
方案①已拍板（收敛到 dsh-director 插件）；V10.2 第二批 42 处在插件 TS 源码完成并重建热更（16号审核 94/100，DOM 实测旧色清零）；T-PLUG-003 关闭（通道由活样本实证）；29号文退坡明细清单已产出，**等用户确认后执行 client.js 退坡手术（批 C-1/C-2）**。

### 断点与下一步
1. 用户确认 29号文 §四（总监 tab 消失、入口统一左下角按钮的交互变化）→ 批 C-1 覆盖核对 → 批 C-2 退坡手术
2. V10.2 第三批（插件 src）：Q2 全量图标（100+ emoji）/ Q4 列表渐隐 / Q5 卡片结构统一
3. V10.1 六项 + V9.4/V9.5 人工验证欠账；T-V12-008 git push 仍待凭据

### 回滚锚点
- 插件：git（dsh-director 项目）+ 构建可重放；快照 150715/152331/153717；restore.ps1
- profile：package.json.backup（spike 已回滚）


## 历史状态（2026-09-07 15:55 spike×2 失败已回滚 · V10.2-1 已 apply）

### 状态一句话
Harness 已恢复运行（CDP 9222 实证）；conversation bundle 已含 38 处快赢修复且换轴运行时生效；spike 两次注册尝试均致 host 启动卡死、已完整回滚（14号审核 80/100 不通过）；发现屏幕总监 UI 属 dsh-director 插件，双实现收敛待用户拍板（T-CONV-001）。

### 断点与下一步
1. T-CONV-001 用户拍板（推荐收敛到 dsh-director 插件）
2. spike 第三次尝试前先取证：CLI 运行 host runner 捕获启动 stderr（15号 §三 + 14号 §四）
3. V10.2 第二批：Q1/Q2/Q4/Q5 重锚到 dsh-director/src（有 TS 工程与 esbuild，改源码重建即可）
4. V10.1 六项人工验证 + V9.4/V9.5 验证欠账仍挂起

### 回滚锚点
- profile：package.json.backup（原始）+ package.json.spike-attempt1（尝试1快照）；junction 已删
- 快照：snapshot-150715 / 152331 / 153717；restore.ps1 + git 兜底


## 历史状态（2026-09-07 15:00 P0 完成 · 13号审核通过）

### 本次完成（P0 批：T-PLUG-001/002/004）
- ✅ 通道勘察：官方 client 插件通道确认（dsh.client 声明 → 增量扫描 → /plugins/<id>/client.js → __DSH_BOOT__ 注入 → cordis Loader），证据 8 条落 50-信息中心/插件加载通道勘察-20260907.md
- ✅ 开源调研：Cordis = Koishi 开源内核，官方教程 cordis-tutorial 可直接指导插件开发
- ✅ 骨架：dsh-director-plugin/ 6 文件（package.json dsh.client 声明同构官方 + spike bundle + 构建占位 + README），node --check 通过
- ✅ 基线：client.js 补丁 +13,553/−9,855 行（70% 目标 → 迁移后增行 ≤4,066，行数口径 D2 已拍板）
- ✅ 28号文执行总清单（P0-P3 + V10.2 快赢 Q1-Q7 + 审核矩阵）+ 13号三级审核记录（93/100 通过·附条件）
- ✅ 用户决策采纳：D1 P0 启动 / D3 快赢全选 / D4 换轴（品牌蓝 600 级）/ D2 行数口径

### 下一步（按 28号文顺序）
1. T-PLUG-003 spike：复制插件包到 node_modules → 重启 Harness → 查插件中心/__DSH_BOOT__.plugins → 出 14号审核
2. V10.2 快赢批 Q1-Q7（改 client.js 样式/文案 → apply → CDP/人工验证 → 15号审核）
3. P1 迁移批前置：出《迁移明细清单》等确认
4. V10.1 六项人工验证仍挂起（24号文 §8.5）

### 回滚保障
- spike：删 dsh-director-plugin 目录即零残留退出
- V10.2：apply 前自动快照 + restore.ps1 + git


## 历史状态（2026-09-07 下午 需求确认 + 26号文插件化方案 + 27号文审美审核 · 待 P0 确认）

### 本次完成
- ✅ 用户确认需求理解摘要（06 需求画像 6 条）
- ✅ 新指令登记：①总监功能按插件开发标准模型整改 ②当前页面审美/交互调用审美 skill 审核
- ✅ 26号文：插件化标准整改方案（差分实证 + 目标架构 + P0-P3 + 风险回滚 + 验收标准），待确认后启动 P0
- ✅ 27号文：审美与交互审核报告（AutoGLM 识图×5 + 预设库基准 04/10/17 + craft 判据），P0×4 / P1×7 / P2×5，快赢项待勾选
- ✅ 台账同步：03 清单（T-PLUG-001~009 + T-AESTH-001~002）+ 05 大索引（20-23~20-27）+ MEMORY §十一

### 下一步（待用户确认）
1. 确认 26号文 §七 → 启动 P0（T-PLUG-001~004，只读勘察+骨架，不动业务代码）
2. 从 27号文 §五 快赢清单勾选 V10.2 修复项；确认主强调色是否换轴
3. V10.1 六项人工验证仍挂起（24号文 §8.5）
4. 建议：当前 git 工作区未提交修改（MEMORY/工作快照/24/25号文）先 commit 锚定一次

### 回滚保障
- restore.ps1 + snapshots + git；本轮仅新增文档与台账行，无业务代码改动


## 历史状态（2026-09-07 V10.1 UI交互优化已apply · 待人工验证）

### 本次完成（V10.1 UI交互优化）
- ✅ 主记忆加载：AGENTS.md + 必读集合 + MEMORY.md + 总统筹入口文档
- ✅ 方案文档：`docs/20-任务文档/24-V10-UI交互优化方案-5问题修复与插件化架构.md`（V1.1已执行）
- ✅ dModal优化：遮罩透明度0.4→0.75 + 弹窗520px→92vw/92vh + X按钮增加padding和hover
- ✅ D区hover三态：新增dPanelPinned/dPanelHovered状态 + 折叠条/导航列hover事件(400ms延迟) + 📍固定按钮
- ✅ 总监面板折叠宽度不变：26px窄条→保持layout.directorPanelWidth宽度，内容收起为居中展开按钮
- ✅ 语法检查通过 + MD5一致 + 缓存清除(7目录) + Harness启动成功(5进程)
- ✅ 台账更新：MEMORY.md §十 + 方案文档 §八执行记录

### 下一步（待用户验证）
1. 用户验证6项修复点（见24号文§8.5）
2. 问题1（X按钮间距）需用户确认具体场景
3. 问题4（折叠宽度）本次按"保持宽度+内容完全收起"实现，待用户确认
4. 验证通过后：24号文转「使用完成」，更新03/04任务清单

### 回滚保障
- restore.ps1 一键复原
- git仓库可回退
- original/ 保留原始备份

---

## 历史状态（2026-09-06 整改 V1.2 清理段完成 · MANIFEST 入仓 commit 2d75531）

### 本次完成（整改 V1.2）
- ✅ 主记忆加载：AGENTS.md + 6 份必读集合 + project 大索引 + MEMORY.md 全部加载
- ✅ 文档同步：config.json（V1.2.0，rc.11）+ README.md（V1.1）+ INDEX.md（V10 V3 完工 + 整改段）
- ✅ 项目记忆：MEMORY.md §五状态快照重写到 2026-09-06，新增 §九整改 V1.2 完整记录
- ✅ git 初始化：`.git/` + `.gitignore`（8 类排除规则）+ 首次 commit `9a732ca`（224 files, 38755 insertions）
- ✅ _memory/_snapshots + _archive 启用（写入工作流 README）
- ✅ scripts/ 探针审核报告：`docs/40-测试质量/12-scripts探针审核-20260906.md`（候选清单 53 个文件）
- ✅ 整改总览文档：`docs/20-任务文档/23-dsh-整改总览-V1.2-20260906.md`
- ✅ **清理段（用户授权 22:30 「除了 git 其他都同意」）**：
  - MANIFEST 入仓 commit `2d75531`（_memory/_archive/pre-clean-V12-20260906/MANIFEST.md, 204 insertions）
  - T-V12-001：scripts/_archived-20260831/ 删除（28 文件, 90KB）
  - T-V12-002+003+004：scripts/ 一次性探针批量删（49 文件, ~140KB），scripts/ 由 91→15 文件
  - T-V12-005：backups/dsh-director-backup-* 删除（2 目录 ~40MB）
  - T-V12-006：backups/client.js.tmp + client-v2.js 删除（2 文件 555KB）
  - T-V12-007：snapshots/manual-bak-batch1 删除（1 文件 583KB）
  - CP4 后置审核：grep 0 hits 全部通过，scripts/ 完美对齐审核报告 §六最终建议
- 📄 关联：_memory/MEMORY.md §九 + INDEX.md §七（变更记录 2026-09-06）

### 下一步（待用户确认）
1. ~~用户审阅整改总览 §五「待确认事项」 → 给出勾选~~ ✅ 已完成（除 git push 外）
2. ~~AI 按勾选执行清理（一次性执行 + git commit）~~ ✅ 已完成
3. 评估是否 git push 到 GitHub（需凭据）— T-V12-008 🔴 等用户确认
4. 整改 V1.3 候选：每月自动归档脚本 + execution-unified-standards 摘要版 + V10 V3 测试报告合并

### 回滚保障
- git reset HEAD~1 可回退到上次 commit 前
- 工作区修改在 workspace/ 下，原安装目录未动
- restore.ps1 一键复原 Harness
- 整改生成的所有新文档在 docs/ 下，不影响已应用 V10 V3

### 整改 V1.2 关键产出文件清单
| 文件 | 状态 | 大小 |
|:-----|:----:|:----:|
| config.json | ✅ 修改 | +1 行 |
| README.md | ✅ 修改 | +1 行 |
| INDEX.md | ✅ 重写 §一§五§七 | 增 ~30 行 |
| _memory/MEMORY.md | ✅ 修改 §五+§九 | 增 ~80 行 |
| .gitignore | ✅ 新增 | 51 行 |
| _memory/_snapshots/README.md | ✅ 新增 | 31 行 |
| _memory/_archive/README.md | ✅ 新增 | 27 行 |
| docs/40-测试质量/12-scripts探针审核-20260906.md | ✅ 新增 | 195 行 |
| docs/20-任务文档/23-dsh-整改总览-V1.2-20260906.md | ✅ 新增 | 124 行 |

---

## 历史状态（2026-09-01 V9.5 z tab 规范化已 apply，待重启验证）

### 本次完成（V9.5，紧接 V9.4 同窗口期）
- ✅ A 配色令牌化：z tab b1 小窗+b-1 顶栏 14 处硬编码浅色 → 令牌（--dsh-ac/--dsh-bg1/--dsh-bg2/--border/dsw-alias-label-*，新增 3 令牌定义），设置改色联动
- ✅ B 显示清理：移除 b1 顶栏「V9 · 驾驶舱」版本徽标 + 硬编码项目名 → 动态 currentProject
- ✅ C 操控性：拖拽手柄热区 5→9px+hover 主色+title；折叠/展开按钮 title+点击区；记忆面板动态 title
- ✅ 两批 apply（快照 153741/154732），node --check + grep 自证/反证通过
- 📄 记录：`docs/20-任务文档/22-z-tab交互显示规范化调整记录-V9.5.md`
- ⚠ 【未做自动视觉验证】visual-ai 不可用，降级人工确认

### 下一步
1. 用户完全退出 Harness 重启，一次同过两份验证清单：21号文§五（V9.4 9项，T-FIX-V001）+ 22号文§5.2（V9.5 7项，T-FIX-V002）
2. 验证通过后：21/22 号文转「使用完成」，T-FIX-V001/V002 移至 04 清单
3. 继续 V9 驾驶舱批次2（D-04/D-05/D-10）；P2×13 待排期（T-P2AUDIT-001）

### 回滚保障
restore.ps1 + 快照 153741/154732（V9.5）+ 144304/145719（V9.4）+ backups/client.js.bak-20260901-zb2

---

## 历史状态（2026-09-01 V9.4 zb2滚动修复+P1×7 已 apply，待重启验证）

### 本次完成（V9.4）
- ✅ zb2（对话 tab 正常对话主窗口）无法滚动：根因=V9.1 钉死 viewArea 高度 + 分屏外壳 overflow:hidden 双重破坏 scrollBody 统一滚动；修复=zb2 自足滚动（scrollerOf 前置识别 data-chat-local-scroll + 补 minHeight:0/overflowY:auto，共 4 处）
- ✅ 全量审计：original vs workspace 21 个 js 仅 client.js 有差异；P0 无 / P1×7 / P2×13
- ✅ P1×7 全部修复：分支功能 setState 失效 / 轨迹tab发送劫持 / 降级发错目标 / a1a2b1 三处无条件滚底 / Ollama 无超时无锁 / hydrate 竞态丢消息 / 记忆面板 XSS
- ✅ 两批 apply（快照 snapshot-20260901-144304 / 145719），node --check + grep 自证通过
- 📄 修复记录：`docs/20-任务文档/21-zb2滚动失效修复与全量审计P1修复记录.md`
- ⚠ 【未做自动视觉验证】visual-ai 服务不可用，降级人工确认

### 下一步
1. 用户完全退出 Harness 重启，按 21 号文 §五 的 9 项人工验证清单逐项过（T-FIX-V001）
2. 验证通过后：21 号文转「使用完成」，T-FIX-V001 移至 04 清单
3. 继续 V9 驾驶舱批次2（D-04/D-05/D-10）；P2×13 待排期（T-P2AUDIT-001）

### 回滚保障
restore.ps1 + 快照 144304/145719 + backups/client.js.bak-20260901-zb2

---

## 历史状态（2026-09-01 V9 UI设计定稿，待开发）

### 设计阶段全部完成（三阶段九审制）
- ✅ 阶段1 简图三轮收敛：`docs/30-开发链路/V9-总监驾驶舱-框架简图-r1.html`（R4版，纵向堆叠+智能体/技能切换+插件边界+四态模拟）
- ✅ 阶段2 功能映射三轮收敛：附录E元素级映射 / 附录F交互细则（13状态字段） / 附录G异常降级X1-X11
- ✅ 阶段3 颜色样式三轮收敛：`docs/30-开发链路/V9-总监驾驶舱-视觉稿-r1.html`（Harness暗色令牌+设置可调色）
- ✅ 阶段4 开发拆解：10任务4批次（附录I）
- 📄 方案文档：`docs/20-任务文档/18-总监驾驶舱UI设计方案-简图功能样式三阶段.md`（V2.0定稿）

### 冻结决策
D1=a-2右栏 / D2=技能静态打包 / D3=docs-index.json注入 / 响应式clamp+断点880/640 / 暗色+设置可调色

### 关键设计结论
- 布局：纵向堆叠 R1顶栏→R2总览→R3智能体/技能→R4导航（上）→R5对话+R7 a-2（右）→R6记忆（下）→R8输入通栏
- 插件边界：只覆盖对话面主区，Harness原生侧边栏完整保留
- 新增：技能Tab（与智能体切换，总监自动/客户点击两种调用）、设置弹层（颜色可调）

### 下一步（开发，按批次走前置/后置审核）
1. 用户确认 V2.0 定稿方案
2. 批次1：D-01 主题变量+设置弹层 → D-02 布局骨架 → D-03 docs-index脚本
3. 每批次：node --check → diff → 快照 → apply → 清缓存重启 → 验证

---

## 历史状态（2026-08-27 V9第二阶段修复7个问题）

### V9第二阶段修复（用户反馈7个问题）
1. ✅ 智能体区域不显示 → 增加flexShrink:0，简化样式，确保不被DirectorFlow挤压
2. ✅ 记忆面板交互 → 改成hover展开/移开收回，点击锁定（🔒图标），打开文档时取消自动收回（预留）
3. ✅ 顶部固定栏 → 增加蓝色渐变顶部栏（项目名+阶段+V9驾驶舱标签）
4. ✅ Tab切换不工作 → 修复onClick事件，更新__dshSwitchMemoryTab支持新Tab
5. ✅ 记忆不持久化 → 在__directChatSubmit中增加用户消息自动保存到memoryCore.conversationHistory
6. ✅ Tab横排显示 → 改成记忆/索引/数据三个横排Tab，下划线样式
7. ✅ 记忆面板默认收起 → bottomPanelCollapsed默认值改为true

### 新增/修改
- directorLayoutStore增加setBottomPanelCollapsed方法
- __directChatSubmit增加记忆持久化逻辑
- __dshSwitchMemoryTab重写，支持3个新Tab
- 总监面板JSX全面重构（顶部栏+智能体+对话+记忆面板）

### 待用户操作
1. 完全退出Harness（含托盘）
2. 清除缓存（Cache/Code Cache/GPUCache/blob_storage/Network）
3. 重启Harness
4. 验证7个修复点

---

## 历史状态（2026-08-27 V9总监统治架构第一阶段执行）

### V9执行进度
- ✅ 步骤1-7：方案设计+前置审核（7/7维度通过，V9.1方案文档）
- ✅ T1：记忆体系IndexedDB（3个新store：memoryCore/memoryDecisions/memoryRisks + CRUD函数）
- ✅ T5：总监记忆面板UI（可折叠，3个Tab：核心记忆/决策记录/偏差风险）
- ✅ T6：智能体区域UI（5个智能体按钮：代码/文档/调研/测试/审核）
- ✅ T7：分支创建函数（__dshCreateBranch）+ 记忆面板交互（__dshSwitchMemoryTab）+ Toast提示
- ✅ apply.ps1应用成功（1个文件修改）
- ⏳ 待用户：清除缓存+重启Harness+验证

### V9已完成修改内容
1. **数据库升级**：IDB_VERSION 2→3，新增3个store
2. **记忆CRUD**：idbSaveMemoryCore/idbGetMemoryCore/idbSaveDecision/idbListDecisions/idbSaveRisk/idbListRisks
3. **智能体区域**：总监面板顶部增加5个智能体按钮，点击创建分支
4. **记忆面板**：总监面板底部增加可折叠记忆面板，3个Tab切换
5. **分支机制**：__dshCreateBranch创建分支对话（信息存memoryCore），__dshShowToast提示

### 待执行任务（V9剩余）
- T2：项目文档读取与解析（自动提取核心认知）
- T3a/T3b：总监页布局骨架+项目总览卡片
- T4：上面框项目导航（文档树/路线图/关键文件）
- T8：总监对话方向把控（分派任务+决策标记+审核入口）
- T9：a-2三模式切换
- T10：文档自动落实
- T11：扩展点预留

### 关键文件
- 改动文件：`workspace/@deepseek-ai/dsh-client-ui-conversation/lib/client.js`
- 方案文档：`docs/20-任务文档/17-总监统治架构与项目驾驶舱方案-v9.md`
- 审核标准：`docs/00-统筹入口/AI任务审核标准通用模板.md`

### 待用户操作
1. 完全退出Harness（含托盘）
2. 清除缓存（Cache/Code Cache/GPUCache/blob_storage/Network）
3. 重启Harness
4. 验证：智能体区域显示/记忆面板可折叠/点击智能体有Toast提示/切换记忆Tab显示内容

### 下一步
1. 用户验证V9第一阶段功能
2. 继续执行T2-T11（项目文档读取/布局改造/导航/总监对话把控等）
3. 后置审核（6/6维度）
4. 归档更新（任务清单/记忆/索引）

---

## 历史状态（2026-08-27 标准整改完成）

### 标准整改成果
- ✅ 项目级AGENTS.md已创建（含项目特殊性+10步链路+6条约束+行为逻辑V1.2+保障机制四层）
- ✅ _memory/MEMORY.md已创建（含5个ADR+风险注意事项+踩坑索引+状态快照）
- ✅ 规范文档同步：AI对话执行约束规范V1.3 + 行为逻辑审核文档 + 保障机制文档
- ✅ 四层保障机制已集成：启动加载协议+执行检查点CP1-CP4+行为逻辑触发表+违规自检
- ✅ 双项目同步完成：cube-sailor规范文档同步+AGENTS.md更新+保障机制集成

### 项目规范体系状态
| 项 | 状态 |
|----|:----:|
| AGENTS.md | ✅ 已创建 |
| _memory/MEMORY.md | ✅ 已创建 |
| AI对话执行约束规范 | ✅ V1.3 |
| AI行为逻辑框架审核文档 | ✅ 已同步 |
| AI规范执行保障机制 | ✅ 已同步 |
| 总统筹入口文档 | ✅ 存在 |
| 项目大索引 | ✅ 存在 |
| 待完成/已完成任务清单 | ✅ 存在 |
| 工作快照 | ✅ 已更新 |

### 下一步
- 实际使用中验证保障机制的有效性
- 为总监对话模式补充自动化测试
- 完善CI-CD流程
- 建立版本管理机制

---

## 历史状态（2026-08-26 V6）

### 当前任务
对话滚动定位·持久化·A2对话修复（V6方案，按10步链路完整执行）

### 执行进度
- ✅ 步骤1-2：需求解析（持久化+发送滚底+A2对话+log隐藏）
- ✅ 步骤3：提示词库检索（文件缺失，记录缺口）
- ✅ 步骤4：方案推导（根因明确：前九次改错地方——操作内部容器而非scrollBody）
- ✅ 步骤5：项目规范匹配
- ✅ 步骤6：任务清单（9项修改）
- ✅ 步骤7：前置审核（7/7维度通过）
- ✅ 步骤8：落地执行（9项修改全部完成 + 语法检查通过 + apply成功）
- ✅ 步骤9：后置审核（6/6维度通过，任务闭环）
- ✅ 步骤10：归档更新（V6文档+工作快照+项目大索引）

### V6核心修复
1. **持久化**：directorStores Map key改用safeDirectorKey字符串，避免对象引用导致每次渲染创建新store
2. **发送消息滚底**：a-1/a-2最后一条是user消息时无条件滚底（不管atBottomRef）
3. **log标签隐藏**：fontSize:10的probe徽章统一display:none，保留状态标签
4. **A2对话**：机制完整，需用户先点击A2切换focusTarget，再用全局输入框发送
5. **滚动目标**：V5已改为scrollBody（el.closest("[data-conversation-scroll]")），V6确认

### 历史教训（重要）
前九次滚动修改零进展的根因：操作的是内部容器messagesEndRef/scrollRef，但在data-phase=active时实际滚动条在父级scrollBody上。操作不滚动的容器的scrollTop等于什么都没做。V5第十次才改对地方。

### 待用户操作
1. 完全退出Harness（含托盘）→ 清除剩余缓存（Cache/Network被进程锁定）
2. 重启Harness → 按V6文档§七验证清单测试7项

### 关键文件
- 改动文件：`workspace/@deepseek-ai/dsh-client-ui-conversation/lib/client.js`
- 方案文档：`docs/20-任务文档/14-对话滚动持久化A2对话修复方案-v6.md`
- 前置文档：`docs/20-任务文档/13-对话滚动定位与a2消息发送修复方案-v5.md`

### 关键修复内容
1. **b-1 ChatView初始打开**：删除chatScroll.read()恢复位置逻辑，直接toBottom()
2. **a-1/a-2初始挂载**：无条件滚底（V4已实现）
3. **Harness缓存清除**：Cache/Code Cache/GPUCache/blob_storage/Network全部清除（上次apply遗漏，极可能是修改不生效根因）
4. **调试基础设施**：window.__dshDebug全局对象（dumpState/exportLogs/scrollAllToBottom等）

### 待运行时验证
- a-1启动/切回后是否定位底部
- b-1切换对话后是否定位底部
- a-1新消息是否自动跟随滚动
- a-2确认发送/自动转发链路是否正常
- 控制台__dshDebug.dumpState()输出是否完整

### 下一步
1. 用户重启Harness（必须完全退出托盘后重启）
2. 验证上述5项运行时检查点
3. 如a-2发送仍有问题，进入T5追加修复（检查autoForward配置 + sendSession调用）
4. 如滚动仍有问题，用__dshDebug.dumpState()排查

### 关键文件
- 改动文件：`workspace/@deepseek-ai/dsh-client-ui-conversation/lib/client.js`
- 方案文档：`docs/20-任务文档/13-对话滚动定位与a2消息发送修复方案-v5.md`
- 审核标准：`docs/00-统筹入口/AI任务审核标准通用模板.md`
- 执行约束：`docs/00-统筹入口/AI对话执行约束规范.md`

### 注意事项
- React hooks必须使用 `(0, react.useState)()` 格式
- jsx必须使用 `(0, react_jsx_runtime.jsx)()` 格式
- **apply后必须清除Harness缓存**（Cache/Code Cache/GPUCache/blob_storage/Network），否则加载旧版本
- Harness必须完全退出（含托盘）后才能apply和清缓存
- apply.ps1末尾status.ps1相对路径bug（脚本自身问题，不影响文件复制）
