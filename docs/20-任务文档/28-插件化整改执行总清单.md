# 插件化整改执行总清单

> **文档编号**: 28 ｜ **创建**: 2026-09-07 ｜ **依据**: 用户指令「按照你的推荐走 + 把要完成的部分都做成清单 + 完成项目记得审核，审核也需要审核文档」
> **决策记录**: D1 P0 已启动（默认采纳）｜ D2 目录名 dsh-director-plugin 认可、70% 目标**按行数计量** ｜ D3 快赢全选 ｜ D4 主强调色换轴（品牌蓝 600 级）｜ D5 两项事实确认仍开放
> **执行标准**: execution-unified-standards（L3 12步 + 三级审核 §8.8）；每批完成 → 三级审核 → 审核记录文档落 docs/40-测试质量/（编号自 13 起）

---

## 一、状态总览

| 阶段 | 任务 | 状态 |
|---|---|:---:|
| P0 勘察与骨架 | T-PLUG-001 / 002 / 004 | ✅ 已完成（2026-09-07，13号审核通过） |
| P0 spike | T-PLUG-003 | ⏳ 骨架就绪，待复制包 + 重启 Harness 验证 |
| V10.2 快赢 | Q1–Q7（用户已全选） | ⏳ 待执行（小批次：改样式/文案 → apply → 验证） |
| P1 迁移 | T-PLUG-005 / 006 | ⏳ 前置：迁移明细清单再确认一轮 |
| P2 减薄 | T-PLUG-007 | ⏳ |
| P3 部署移植 | T-PLUG-008 / 009 | ⏳ |

## 二、P0 产出与结论（本轮完成）

| 任务 | 产出 | 结论/位置 |
|:---:|------|---------|
| T-PLUG-001 勘察 | 通道七步链 + 8 条源码证据 | ✅ 官方 client 插件通道存在且数据驱动；docs/50-信息中心/插件加载通道勘察-20260907.md |
| T-PLUG-002 调研 | Cordis 内核溯源 | ✅ 开源内核（Koishi 四年）+ 官方教程 deepseek-harness/docs/cordis-tutorial |
| T-PLUG-004 骨架+基线 | 插件包 6 文件 + 基线表 | ✅ dsh-director-plugin/（node --check 通过）；基线见 §四 |

**架构修正确认**：MOD-B（DirectorView 整页）确认可整体迁插件；MOD-A（ChatView 左栏）确认保留宿主锚点（跨插件 value import 被官方构建规则禁止）；双 React 防线由平台冻结模块表原生保证。

## 三、待办清单（按执行顺序）

### P0 收尾

| 编号 | 任务 | 验证 | 审核要求 |
|:---:|------|------|---------|
| T-PLUG-003 | spike：复制 dsh-director-plugin → node_modules → 重启 Harness → 查插件中心/__DSH_BOOT__.plugins → 删目录回滚 | __DSH_BOOT__.plugins 含本包 或 插件中心可见 | 完成后出 14-号审核记录 |

### V10.2 快赢批（用户已全选；可与 spike 并行排期；走快照+apply+重启验证）

| 编号 | 项（27号文映射） | 落点 | 验证 |
|:---:|------|------|------|
| Q1 | P1-1 激活态统一（灰底胶囊/下划线/白底阴影 → 统一「下划线+加粗」） | client.js 页签/分段控件段 | 截图对比 + 交互断言 |
| Q2 | P1-2 emoji → 1.6–1.8px 单线 SVG（🎯⚠️📁✏️📋💗📊） | 各图标位 | 视觉走查 |
| Q3 | P1-3 低对比修复（点击收起/编辑/待办橙字/悬浮条） | 对应样式位 | 对比度 ≥4.5:1 抽测 |
| Q4 | P1-4 列表底部渐隐遮罩 + 滚动提示 | 智能体列表容器 | 截断场景验证 |
| Q5 | P1-5 总览四卡解剖结构统一 | R2 卡片段 | 视觉走查 |
| Q6 | P2-1/P2-4 搜索框间距 8px + 全角半角标点统一 | 工具行/占位符 | 走查 |
| Q7 | P1-7 主强调色换轴（D4：品牌蓝加深至 600 级，accent 同屏 ≤2 处） | 令牌定义位 | 全屏 accent 计数 |

### P1 迁移批（前置 gate：先出《迁移明细清单》确认）

| 编号 | 任务 | 验证 |
|:---:|------|------|
| T-PLUG-005 | MOD-B 迁移：DirectorView/stores/styles/IDB/utils → 插件 src；client.js 留 shim | Playwright CDP 46/46 + 总监全功能人工验证 |
| T-PLUG-006 | MOD-A 可迁移部分（chat-panel 组件/状态）迁移；ChatView 留最小锚点 | 折叠/展开/拖拽/焦点与 V10.1 行为一致 |

### P2 / P3 批

| 编号 | 任务 | 验证 |
|:---:|------|------|
| T-PLUG-007 | 锚点压缩 + 《锚点契约》文档 | 补丁差异较基线下降 ≥70%（行数口径） |
| T-PLUG-008 | apply/restore/deploy 插件部署单元 + 安装 README | 双机模拟：复制→init→apply→重启即用 |
| T-PLUG-009 | IDB 数据兼容验证 | 三 store 条数抽查一致 |

### 结构项（27号文 P0 组，并入对应批次）

| 项 | 并入 |
|---|---|
| P0-1 主内容区未铺满（x≈1017） | P1 批前置探针（随 T-PLUG-005 一起查宽度链） |
| P0-2 悬浮条截断/层叠 | P1 批（先勘察归属：原生 or 我们的修改） |
| P0-3 消息渲染过滤层（JSON/调试徽章泄漏） | P1 批（随 MOD-B 迁移重构渲染入口） |
| P0-4 降级文案矛盾 | P1 批（随迁移落地重写） |
| P1-6 可供性统一 | V10.2 或 P1（视改动面） |

## 四、基线记录（2026-09-07 实测，D2 度量口径 = 行数）

| 口径 | original | workspace | 差异 |
|---|---:|---:|---|
| client.js 字节 | 431,010 B | 1,426,727 B | +995,717 B |
| git numstat | — | — | **+13,553 增 / 9,855 删 行** |
| 70% 目标 | — | — | 迁移后 client.js 增行 ≤ **4,066 行**（相对 original） |

## 五、审核要求矩阵（用户指令：完成必审核 + 审核出文档）

| 批次 | 审核类型 | 审核文档 |
|---|---|---|
| P0（001/002/004） | 三级审核（§8.8） | ✅ 40-测试质量/13-插件化P0批次-审核记录-20260907.md |
| T-PLUG-003 spike | 三级审核 | 14-插件化Spike-审核记录-YYYYMMDD.md |
| V10.2 快赢批 | 前置核对 + 三级审核 + CDP/人工验证清单 | 15-V10.2快赢批-审核记录-YYYYMMDD.md |
| P1 迁移批 | 迁移明细确认 + 每模块三级审核 + 46/46 回归 | 16-P1迁移批-审核记录-YYYYMMDD.md |
| P2/P3 批 | 三级审核 + 双机验证记录 | 17-P2P3批-审核记录-YYYYMMDD.md |

## 六、回滚与安全

- 每批 apply 前自动快照（scripts 既有机制）；git 每批一 commit。
- spike 失败 = 删插件目录即完全退出通道，零残留。
- client.js 主补丁在 P1 完成前保持现状可用；任何时刻 restore.ps1 可全量复原。

---

*清单状态: 【执行中】2026-09-07；本清单为唯一执行台账，状态变更随批同步*

---

## 七、执行状态更新（2026-09-07 15:50）

- **T-PLUG-003 spike：❌ 两次尝试失败已回滚**（14号审核 80/100 不通过·如实记录）。注册机制三要素已由活样本 dsh-director 完全破译（dep link: + bundles 数组 + junction + bundle.patch/host face），失败根因需 host 启动日志取证后第三次尝试。
- **V10.2 快赢第一批：✅ 38 处已 apply 并重启验证**（15号审核 90/100 附条件）。换轴运行时实测生效（--dsh-ac=#2f6feb）；#999 类低对比在 conversation bundle 内清零。
- **重大发现：两套总监实现并存**——屏幕可见 UI 属 dsh-director 插件（09-01 建成，标准模型 + 自有构建链）；conversation bundle 内联版未在当前 DOM。**P1 性质待用户拍板**：收敛到插件（推荐）或维持双轨。
- 待办重锚：Q1/Q2/Q4/Q5 及剩余低对比项 → dsh-director 插件 src/（第二批）；Q7 换轴对插件 UI 的可见性待验证。

---

## 八、执行状态更新②（2026-09-07 16:10 · 方案①已拍板）

- **T-CONV-001 ✅ 用户拍板方案①**：收敛到 dsh-director 插件；29号文产出退坡明细清单（批 C-1/C-2/C-3），**待用户确认后动 client.js**。
- **T-PLUG-003 关闭（重定义）**：通道目标已由活样本 dsh-director 端到端实证（装载/服务/DOM 挂载/构建热更全链路），自建骨架注册不再必要；14号失败记录保留作教训；骨架目录保留为参考样本。
- **V10.2 第二批 ✅ 42 处插件侧修改**（16号审核 94/100 通过）：换轴 #667eea→#2f6feb / #764ba2→#1e40af、#999 清零、提示字号；esbuild 重建 rev a30d47ec0c97，DOM 实测旧色清零、新色上屏。
- **路线验证**：改 TS 源码 + 重建 = 零编码坑（对照 14/15号在编译产物上的反复），插件工程路线效率实证。
- 下一步：用户确认 29号文 → 批 C-1 覆盖核对 → 批 C-2 退坡手术（快照+git+46/46 门禁）。

---

## 九、执行状态更新③（2026-09-07 16:20 · C-1/C-2 完成）

- **批 C-1 ✅**：覆盖核对完成（17号 §一）——插件已覆盖总监入口/控制台/对话集成/记忆/Ollama；记忆存储不同源（IDB vs localStorage+host API），退坡不删 store 代码，数据零风险。
- **批 C-2 ✅ 退坡手术**：slots.register director 注册块（40行/1,524B）已移除并打退坡标记；served bundle 实证注册消失；**用户可见交互零变化**（tab 条本由插件渲染——25号文「原生 header」结论作废）。17号审核 95/100。
- **行数**：numstat +13,514/−9,855（大幅下降在 P2 死代码删除时实现；DirectorView 164 处引用保留 @deprecated-candidate）。
- **待办**：批 C-3（MOD-A 决策）+ P2 死代码删除（届时统一跑全量回归）；V10.2 第三批（插件 src：Q2/Q4/Q5）。

---

## 十、执行状态更新④（2026-09-07 19:45 · V10.2-3 完成）

- **V10.2 第三批 ✅ 5 组修改**（18号审核 95/100）：Q4 列表 mask 渐隐、Q5 四卡解剖统一（首卡去紫渐变 + coreBox 同构）、Q7c 阴影换轴残留清零；rev 8f964bdca660，CDP 双探针实证。
- **V10.2 收敛状态**：Q3/Q7 全清；Q1 激活态插件内已统一（dTab 下划线范式）；**仅剩 Q2 全量图标**（100+ emoji→SVG，独立大批次）。
- **待办**：P2 死代码删除（专门窗口，29号文 §五 契约约束）→ 全量回归 + numstat 复测；人工验证欠账；T-V12-008 git push（需凭据）。

---

## 十一、执行状态更新⑤（2026-09-08 09:45 · 技能切换 + Q2-1 完成）

- **技能基线切换**：主记忆技能库于 09-08 00:04 将技能重构为 execution-standards V2.3.0（整合版，SKILL.md 11.7KB + error-knowledge-base），会话执行逻辑已切换至新版（清单驱动 §6.5 + 5步自审 §五）；**登记表 08号旧条目失效待同步**（新条目未建 + 3 个 WorkBuddy 断链 junction 待修，待用户授权）。
- **Q2 第一批 ✅ 36 处**（19号审核通过）：icons.tsx +6 图标；V10Director chrome 30 处 emoji→Icon；rev ce516814c17c；app 可见 emoji 降至 17（余为数据字段/消息内容 = phase-2）。
- **待办**：Q2-2（数据字段 hybrid 渲染器 + BranchMindMap/DirectorChat + 下拉 items）；P2 死代码删除（专门窗口）；人工验证欠账；T-V12-008 git push。

---

## 十二、执行状态更新⑥（2026-09-08 13:25 · N-1/N-2 完成）

- **N-1 插件收尾 ✅**（20号审核）：Q2-2 hybrid 渲染器 + chrome emoji 清零（仅余数据/内容 17 处）；M6 模型切换 UI（getOllamaModels + 下拉 + 双调用接线 + localStorage）；M7 轨迹筛选/搜索/详情/JSON 导出（TracePanel 重写）；31号校准报告（9 项差异全部「实现为准」，D1-D5 转历史冻结版）。
- **N-2 宿主退坡收官 ✅**（21号审核 95/100 同级）：DirectorView.js region 整段删除（1,519 行）；共享基础设施按契约保留；三段冒烟全绿（rev 10afd797e46f）；numstat **+11,996/−9,855**（净 −1,518）。**70% 目标裁定**：保留 MOD-A 前提下不可达（剩余=契约锚点主体），MOD-A 退役登记为远期候选（产品决策）。
- **N-3 部分**：dsh-director git init + 基线提交 12907c6 ✅；git push（需凭据）、登记表同步（需授权）、人工验证欠账仍挂起。
- **台账回滚锚点修正**：dsh-director 无 git 的旧记录作废（现已 git 化）。
- 下一步：远期候选（MOD-A 退役 / V11 嵌套 / 自动任务执行）；人工验证；push。

---

## 十三、执行状态更新⑦（2026-09-08 13:30 · N-3.1/N-3.2 完成）

- **N-3.1 台账大扫除 ✅**：D1（00号文 rc.11）、D2（README 8 包）、D3（03 清单 V9D 批次1 ×3 回写）、02 需求清单模块 2.1 状态回写（插件承载注记）；INDEX 字数行属日期冻结快照不逐次改（由 05/06 承载现状）。
- **N-3.2 验证清单重构 ✅**：V10.1 六项/V9.4/V9.5 旧欠账随 MOD-B 退坡失效，验证义务转移插件侧；新人工验收清单入 03（总监三切换/四卡/对话流+模型下拉/任务·分支·记忆/轨迹筛选导出/文件浏览器）。
- **本地提交锚定 ✅**：43b300b（收敛批次）+ 0cf87c1（台账回写），工作树清零；dsh-director 基线 12907c6。
- **N-3 剩余（等用户输入）**：T-V12-008 git push（凭据）；登记表同步（授权）；人工验收（用户执行，清单已备）。
- **清单总账**：30号文 N-1 ✅ N-2 ✅ N-3 ✅（除用户输入项）N-4 远期——插件化整改执行清单全部闭环。
